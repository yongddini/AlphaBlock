# WAN-396 — 허수 진입 슬리피지 보정 전후

**판정 (가) 분해만 틀렸다.** 엔진은 후보의 `entry_liquidity`(기본 **메이커**)로 진입을 체결하므로 진입 슬리피지가 **0**인데, 비용 분해는 `BacktestConfig.entry_liquidity`(기본 **테이커**)를 읽어 5bp가 붙었다고 역산했다. `gross_r`와 `slippage_r`가 **같은 크기로 부풀어 상쇄되므로 `net_r`은 처음부터 맞았다** — 손익이 아니라 **진단**이 틀렸다.

근거(코드): 엔진 `backtest/zone_limit_backtest.py::_to_trade` ↔ 분해 `backtest/wan370_cost_decomposition.py::decompose_trade`.

보정: `gross_r`·`slippage_r`·`cost_r`에서 각각 `K × entry_fee_r`을 뺀다(`K = 2.4987506`). `net_r`은 불변.

## 헤드라인 — `oos_warm` 채택 좌표

| 표 | 행 | gross 전 | **gross 후** | slippage 전 | **후** | cost 전 | **후** | net(불변) | 판정 |
| -- | -- | --: | --: | --: | --: | --: | --: | --: | -- |
| wan370_cost_decomposition | taker_tp | +0.0918 | **+0.0069** | 0.1698 | **0.0849** | 0.2716 | **0.1867** | -0.1798 | (나) 시장에선 이겼는데 비용이 먹었다 |
| wan370_cost_decomposition | maker_tp | +0.0920 | **+0.0071** | 0.1350 | **0.0501** | 0.2229 | **0.1380** | -0.1309 | (나) 시장에선 이겼는데 비용이 먹었다 |
| wan388_merge_x_retap_grid | 분리·매탭 | +0.0765 | **+0.0019** | 0.1186 | **0.0440** | 0.1960 | **0.1213** | -0.1194 | (0 근처) 시장에서도 본전 |
| wan389_retap_attribution_grid | 분리·매탭·재진입 켬 · reentry=True | +0.0765 | **+0.0019** | 0.1186 | **0.0440** | 0.1960 | **0.1213** | -0.1194 | (0 근처) 시장에서도 본전 |
| wan394_retap_reentry_tp_grid | 매탭·재진입켬·1.5R · multiple=1.5 · reentry=True | +0.0765 | **+0.0019** | 0.1186 | **0.0440** | 0.1960 | **0.1213** | -0.1194 | (0 근처) 시장에서도 본전 |

## 검산

- **(a) 슬리피지 항등식** 최대 잔차 `3.23e-14` — 진입 몫이 정확히 `K × entry_fee_r`이라는 주장의 근거.
- **(b) 독립 자 대조** 최대 잔차 `4.90e-14` — `gross_r − slippage_r`가 `mean_gross_r_after_slippage`(이 버그에 안 걸리는 자)와 같고, 보정이 그 차를 **안 건드린다**.
- **(c) `net_r` 불변** — 보정 대상 열에 없다(판정 (가)의 정의).

보정한 행 **346개** / 표 6개. 원본 CSV는 그때의 기록으로 **보존한다**(덮어쓰지 않는다).

🚨 **「gross가 사실 0이었다」를 「엣지가 없다」의 새 근거로 인용하지 말 것** — 이 표가 고친 것은 **회계 한 줄**이고, 「엣지 없음」(WAN-84/88/111/114/124/151/201/248/386)은 *진입 규칙이 무작위와 구분되는가*라는 **다른 질문**이다.
