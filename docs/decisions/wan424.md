# WAN-424 — 스토캐스틱 × 오더블록 팔: 복원(§1) · 매칭 대조군(§2)

> 상태: **§1 복원·재현 완료 → 사용자 결정 2026-09-22 「2단계 고고」 → §2 매칭 대조군 완료.**
>
> 재현: §1 `uv run python -m backtest.wan424_stoch_ob_arm --jobs 4` ·
> §2 `uv run python -m backtest.wan424_matched_null --jobs 4` (둘 다 요약만 `--from-csv`)
> 산출: `backtest/reports/wan424_stoch_ob_arm*.{csv,md}` · `backtest/reports/wan424_matched_null*.{csv,md}`

## 판정 한 줄

**시각 널(B)은 앞·뒤 구간 모두 넘는다. %K 선별 널(A)은 앞구간에서 구분되지 않는다(p=0.14).**
즉 「오더블록 + %K로 들어간 시각」은 같은 칸의 아무 시각보다 확실히 낫지만, 「오더블록 탭
**안에서** %K로 고르는 것」은 규칙을 고르는 앞구간에서 무작위 고르기와 구분되지 않는다.
미리 못 박은 규칙(앞구간 유의 → 뒷구간 확인)대로 **A는 통과가 아니다.** 채택 권고 없음.

## §0 왜

WAN-423 §6·§7이 이 저장소 처음으로 **뒷구간 양수**를 냈는데(롱 · ts4 · 손절폭 하한 4% ·
%K<25 → full +0.120 · is +0.106 · **oos_warm +0.165 · 679거래**), 측정 스크립트는 세션
스크래치에서 돌았고 저장소에 **코드가 0줄**이었다. 매칭 대조군을 걸려면 그 팔부터 저장소에
있어야 하고, **재현이 안 되면 대조군을 거는 뜻이 없다** — 그래서 §1은 복원 + 재현 검산이다.

## §1 판정 — 재현된다 (90값 전부)

| 검산 | 결과 |
| -- | -- |
| 이슈가 지목한 기준값 6개(ts4 · 하한4% · %K<25 / %K<15 × full·is·oos_warm) | **6/6 일치** — 거래 수 정확히, net R 소수 셋째 자리 |
| WAN-423 §7 롱 표 전체(보유 5 × 하한 2 × 문턱 3 = 30줄 × 3구간) | **90/90 일치**(스크래치 `hold_ls.log` 파싱 대조) |
| 파일럿 BTC 4h — 팔 후보 **후보 단위** 대조(스크래치 캐시 `hold_ls_payloads`) | 보유 5 × 구간 3 = 15칸 **전부 동일 집합**(진입·청산 시각·가격·사유·손절가) · base 롱 후보 3구간도 동일 |

| 팔(롱 · 31종목 × 9TF · 첫 탭 · 재진입 없음 · `pen_5bp` × 같은 분 익절 금지) | full | is | **oos_warm** |
| -- | --: | --: | --: |
| ts4 · 하한 4% · %K<25 | +0.1199 (2710) | +0.1056 (2031) | **+0.1646 (679)** |
| ts4 · 하한 4% · %K<15 | +0.1539 (861) | +0.1353 (627) | **+0.2032 (234)** |

📌 **모양도 그대로다** — 30줄 전부 세 구간 양수(표는 요약 md). ⚠️ **최선 칸을 고르지 말 것**
(WAN-161) — 이 표는 재현의 증거이지 새 판정이 아니다.

## §2 복원한 것 — 엔진을 안 건드리고 후처리로

스크래치는 `confirmation_arm.derive_arm_candidates`를 **몽키패치**해 `run_cells` 안에서 팔을
만들었다. 그 경로를 저장소에 싣으려면 엔진·러너 소스(`confirmation_arm.py` · `wan169`)를 고쳐야
하고, 그러면 모든 후보 캐시·야간 타임라인 캐시의 리비전이 갈린다(WAN-253/394). 이 팔은 base
후보의 **진입·손절을 그대로** 쓰고 **청산만** 바꾸므로, base 후보를 만든 뒤 같은 1분봉
서브스텝(`build_substeps` — `reentry_window_context`와 같은 식)으로 청산을 다시 푸는 것과 같은
계산이다. 그 등식은 주장이 아니라 **§1의 후보 단위 대조**가 확인했다.

- `backtest/wan424_stoch_ob_arm.py`(신설) — 순수 함수 `stoch_k_line`(20,10,10 %K 선) ·
  `k_before`(**직전 확정봉** %K) · `time_exit`(tsN 청산) · `arm_pool`(롱·첫 탭·재진입 아님·하한) +
  파이프라인(base 후보 → 팔 후보 → 필터 → 채택 북 배치).
- **엔진·기본값·토대 불변**(`ConfluenceParams()`·`OrderBlockParams()`·`LeverageBookParams()` ·
  `ENGINE_SOURCE_FILES`·`RUNNER_SOURCE_FILES` 무변경 → 후보 캐시 리비전 `pay:8ffafaaf5ada` 그대로).
- 🚨 **%K 인과성은 동작으로 고정** — 진입 봉 자신의 %K를 바꿔도 결과가 안 바뀌고, 직전 확정봉을
  바꾸면 바뀐다(`tests/test_wan424_stoch_ob_arm.py`).
- **3h 주기**는 거래소 주기가 아니라(바이낸스 봉 목록에 없다) WAN-423이 1h에서 접어 저장했다 —
  주기 표에 **이 프로세스에서만** 등록한다(`register_3h`, 운영 코드가 3h를 지원한다고 선언하지
  않기 위해).

⚠️ **알려진 성질 둘(스크래치와 같아서 재현에 영향 없음 · 기록)**:
1. **1w 서브스텝 버킷은 epoch 주(목요일 시작)**다 — `build_substeps`가 `floor(t/1w)`로 정렬해
   시간 청산의 「봉 N개」가 저장 주봉(월요일 시작)과 며칠 어긋나고, 같은 이유로 1w의 %K 조회가
   데이터 구멍 경로(한 봉 더 보수적)를 탄다. 9TF 중 1w 한 칸의 문제다.
2. `k_before`는 진입 봉이 저장돼 있지 않으면(구멍) **한 봉 더 오래된** %K를 읽는다(보수 쪽).

## §3 실측 비용 — 이슈 견적(≈5시간)보다 싸다

| 단계 | 시간(`--jobs 4` · M1 · 경합 없음) |
| -- | --: |
| base 후보 279칸(캐시 미스 전 칸) | **3,717초 = 62분** |
| 팔 후보(1분봉 재적재 + 서브스텝 + 시간 청산 + %K) | 4,517초 = 75분 |
| 배치 30조합(북) | **21초 ≈ 0.7초/조합** |
| 합계 | 8,255초 = 2시간 18분 |

📌 **WAN-423의 4시간 57분과 다른 이유**: 그 실행은 **숏까지** 후보로 만들고 **확인 관측**
(`observe_confirmation` — 확인 팔을 요청하면 자동으로 켜지는, 체결 셋업마다 「탭 → 무효화」를
한 번 더 훑는 패스, WAN-394 §0)을 켰다. 이 모듈은 롱만 만들고 팔을 후처리로 푸므로 둘 다 없다.
⚠️ **다른 모듈에 이 값을 옮기지 말 것.** base 후보는 `backtest/cache/wan424_payloads`에
적재됐으므로 **다시 돌리면 base는 캐시 적중**이고 팔 후보(75분)만 다시 든다.

## §4 §2 착수 전 견적(기록) — 구조에서 나왔다

🚨 **대조군이 후보 재생성을 요구하지 않는다** — 이 팔의 거래는 전부 「base 후보(OB 첫 탭 · 하한
통과)」 위의 **선별**(%K) + **청산 규칙**(tsN)이다. 그래서 표준 설계(WAN-70: 존은 같고 진입
시각만 무작위)의 두 갈래가 모두 **이미 만든 팔 후보 위의 후처리**로 선다:

| 대조군 | 묻는 것 | 구성 | 비용 |
| -- | -- | -- | -- |
| (A) **%K 선별 널** | %K가 OB 탭 중 무작위 고르기를 이기나 | 같은 칸·같은 구간의 하한 통과 풀에서 **같은 개수**를 무작위 추출(%K 안 봄) | 표본당 배치 1회 ≈ 0.7초 → **200표본 ≈ 2~3분** |
| (B) **시각 널** | OB+%K 진입이 같은 칸의 무작위 시각을 이기나 | 같은 칸·같은 손절폭·같은 보유 봉, **진입 시각만** 서브스텝 무작위(WAN-423 `nts` 팔과 같은 설계) | 서브스텝 재구성 필요 → 팔 후보 단계와 같은 자릿수(**≈ 75분 1회** + 표본당 초 단위) |

⚠️ 위 비용은 §3 실측에서 **환산**한 값이지 널을 돌린 실측이 아니다. 팔 후보 캐시(`ArmCell`)는
지금 디스크에 남지 않으므로 §2는 75분을 한 번 다시 들이거나, 적재 경로를 먼저 붙인다(선택은 §2).
📌 **어느 쪽이든 좌표(31종목 × 9TF)를 줄일 필요가 없다** — 사용자 코멘트의 「줄이면 +0.165라는
숫자 자체가 달라진다」가 비용 때문에 깨지지 않는다.

📌 **자는 옛 계열에서 물려받는다**(§2 착수 시): 유효 셀 ≥20 · 유의 = `p≤0.05` **이면서** 실제 >
무작위평균 · 부트스트랩 200 · 시드는 `wan151`/`wan248`에서 import(WAN-350/403 방식). 🚨 `pool_k`는
모듈 기본값이 아니라 **공개 CSV가 쓴 값**을 명시한다(WAN-403 함정).

## §5 §2 매칭 대조군 — 결과

좌표는 §1 그대로(롱 · 31종목 × 9TF · 첫 탭 · 재진입 없음 · `pen_5bp` × 같은 분 익절 금지 ·
채택 북 · 복리 끔). 하한 4% × 보유 ts4/ts8 × %K 15/20/25 = 6조건, 판정 칸 = ts4 · %K<25.
반복 200 · 시드 424. 자는 `wan248`에서 import했다(표본 게이트 20 · α 0.05 · 반복 200). p는
WAN-70 식(무작위 중 실제 이상인 비율, 해상도 1/200)이다.

### §5-1 두 대조군

* **A %K 선별 널** — 같은 칸 · 같은 층의 하한 통과 오더블록 탭(%K 안 봄)에서 같은 개수를 무작위로
  고른다. 진입·청산은 그 탭 자기 것이다.
* **B 시각 널** — 실제 거래마다 같은 칸 · 같은 층 · 같은 손절폭 비율 · 같은 보유 봉으로, 진입
  시각만 그 층의 1분봉 중 무작위로 옮긴다.
* **층** — `full`은 따뜻한 경계 앞/뒤를 따로 뽑아 `oos_warm` 개수까지 맞췄다. 실측 무작위 거래 수는
  실제와 같다(A는 정확히 같고, B는 슬롯 경합으로 0~1% 적다).
* **검산** — 실제 팔이 §1 기준값 6개와 전부 일치한다. B의 넘파이 청산은 §1 `time_exit`와 같은
  결과를 낸다(300개 무작위 사례로 테스트가 고정).

### §5-2 판정 칸 (ts4 · 하한 4% · %K<25)

| 널 | is: 실제 / 무작위 [95%] · p | oos_warm: 실제 / 무작위 [95%] · p | 판정 |
| -- | -- | -- | -- |
| A 선별 | +0.1056 / +0.0828 [+0.029, +0.131] · **0.140** | +0.1646 / −0.0007 [−0.076, +0.067] · 0.000 | **(나) 앞구간에서 구분 안 됨** |
| B 시각 | +0.1056 / −0.0178 [−0.048, +0.016] · **0.000** | +0.1646 / −0.0268 [−0.083, +0.032] · **0.000** | **(가) 넘어간다** |

📌 **여섯 조건 전부 같은 모양이다** — B는 is·oos_warm 모두 유의(p ≤ 0.015), A는 is가 전부
비유의(p 0.12~0.32)이고 oos_warm은 전부 유의(p ≤ 0.005). 한 칸의 우연이 아니다.

📌 **두 널의 차이가 무엇을 말하나** — 앞구간(상승장 쪽)에서 A의 풀(오더블록 탭 아무거나)은 이미
**+0.083**을 번다. 같은 구간 B(아무 시각)는 **−0.018**이다. 즉 앞구간에서 값을 만든 것은 주로
**「오더블록 탭에서 산다」**이고 %K는 거기에 거의 더하지 못했다. 뒷구간에서는 탭 아무거나가
**0 언저리**(−0.001)로 떨어지는데 %K로 고른 것은 +0.165를 지킨다. 🚨 **그래서 %K의 기여는 뒷구간
에만 보인다** — 뒷구간은 고르는 축이 아니므로 이것을 「%K가 통했다」로 읽지 않는다(WAN-161).
장세의 몫일 가능성이 열려 있다(WAN-423 §10-3이 스스로 적어 둔 우려).

### §5-3 종목 하나씩 빼기 (판정 칸 · oos_warm · **지갑 재배치** · 널 반복 100)

실제 net R이 **31판 전부 양수**(최저 −CRV +0.128 · 최고 +0.192)이고, **두 널 모두 31판 전부 유의**
(p 전부 0.000)다. 종목 편중이 아니다.

### §5-4 폭락일 통제 (판정 칸 · oos_warm)

| 통제 | 실제 | A 선별 | B 시각 |
| -- | --: | -- | -- |
| WAN-408 최악 10일(KST 진입일) 제외 | +0.2911 (487) | 무작위 +0.1602 · p 0.000 ✅ | 무작위 −0.0235 · p 0.000 ✅ |
| WAN-410 「그날 실현 누계」 버킷 안 차(실제 가중) | — | 차 +0.128 · 하위 5%가 양수 ✅ | 차 +0.081 · **p 0.055 — 경계에서 선을 못 넘음** |

* **최악 10일을 빼면 실제가 오히려 오른다**(+0.165 → +0.291). 이 팔도 그 날들에 잃었다는
  뜻이고, 폭락일이 만든 착시는 아니다. 🚨 같은 제외로 A의 풀도 −0.001 → **+0.160**으로 크게
  오른다 — **뒷구간 오더블록 탭의 부진이 그 10일에 몰려 있었다**는 것이 새 정보다.
* **B는 버킷 안에서 경계에 걸린다**(200반복 중 11번이 차 ≤ 0). 「그날 이미 얼마나 잃었나」를 맞추면
  시각 우위가 절반 남짓(+0.19 → +0.08)으로 줄고 유의 선에 붙는다. **진입 축 여덟 번이 죽은 그
  대리변수가 이번에는 효과를 다 설명하지 못하지만, 일부는 설명한다.**
* 버킷별 실제 net R(oos_warm): 누계 ≥0R 구간 +0.075(333거래) · [−3R, 0R) −0.033(118) ·
  그보다 깊은 네 버킷 +0.21~+0.59(228). **그날 이미 잃은 날에 들어간 거래가 더 벌었다** —
  하락 뒤 반등을 사는 팔의 성질과 정합적이다. ⚠️ 관측이지 규칙 제안이 아니다.

### §5-5 칸 단위 보조 열 (판정 칸)

oos_warm에서 20거래를 넘는 칸이 **하나도 없다**(679거래를 279칸이 나눠 가진다). is는 6칸 중
A 1 · B 3 유의. **칸 단위 자로는 이 팔을 판정할 수 없다** — 판정은 북(한 지갑)에서 냈다(WAN-341).

### §5-6 실측 비용

**7,719초 = 2시간 9분**(`--jobs 4` · M1): base 후보 14초(캐시 적중) · 팔 + 널 B 4,495초(75분) ·
6조건 × 2널 × 200 배치 1,745초(29분 · 한 판 111~204초) · LOO 31 × (1 + 2 × 100) 1,470초(25분).
§4 견적과 비교하면 「A 200표본 2~3분」은 맞았다. 「B ≈ 75분 1회」는 틀렸는데, 좋은 쪽으로
틀렸다. B는 팔 후보와 **같은 패스**에서 풀려서 §1의 팔 단계(4,517초)에 **거의 더하지 않았다**.
대신 B의 배치가 A와 같은 자릿수로 붙었다. ⚠️ **진행 로그가 없어서** 생성 단계의 진행률이 안
보였다. 다음 모듈은 칸마다 로그를 남길 것. 널 작업물은 `backtest/cache/wan424_null_work.pkl.gz`(180MB · gitignore)에
남아 다시 돌리면 그 75분을 건너뛴다(모듈 소스 + 후보 캐시 리비전이 키).

### §5-7 읽는 법 — 무엇이 섰고 무엇이 안 섰나

* ✅ **섰다**: 이 팔의 진입(오더블록 첫 탭 × 직전 확정봉 %K × 손절폭 하한 4%)은 **같은 칸 · 같은
  기하 · 같은 회계의 무작위 시각**을 앞·뒤 구간 모두 넘고, 31판 LOO와 최악 10일 제외를 버틴다.
  ⚠️ 「이 저장소 첫 사례」로 인용하지 말 것 — WAN-231(재진입 시각 널)·WAN-350(per-cell 널)도
  시각 계열 널에서 유의를 냈다(좌표·팔·자가 달라 이 표와 섞지 않는다).
* ❌ **안 섰다**: %K가 오더블록 탭 **안에서** 고르는 힘은 앞구간에서 무작위와 구분되지 않는다.
  뒷구간의 큰 격차는 고르는 축이 아니라 확인 축이라 근거로 쓰지 않는다.
* ⚠️ **경계**: 그날 실현 누계로 층화하면 B의 우위가 절반으로 줄고 p=0.055다.
* ⚠️ **안 잰 것**: `pen_5bp` 밖의 체결 보수화 · 같은 분 축(이 팔은 익절이 없어 같은 분 익절은
  없지만 **같은 분 손절**은 있다) · 틱 체결(WAN-98 Canceled) · 1w 버킷 어긋남의 크기.
* 📌 **다음 질문(개발자 임의 착수 금지)**: A의 「앞구간 비유의」가 장세 탓인지 가르려면 **구간을
  장세로 나눈 대조군**이 필요하다. 채택은 **재-베이스라인 = 사용자 결정**이다.

## §6 안 한 것 · 경고

- ❌ **채택 권고 없음 · 기본값 전환 없음** · 숏 범위 밖(`short_enabled=False` 그대로) · 최선 칸 고르기 금지(WAN-161).
- ⚠️ 이 팔은 **채택 좌표가 아니다**(31종목 × 9TF · 첫 탭 · 재진입 없음 · 익절선 없음 · 시간
  청산) — 채택 북(−0.12R)과 나란히 놓지 말 것. 복리 MDD **30~95%**.
- ⚠️ 전부 `pen_5bp`(＋ 같은 분 익절 금지) 위의 값 · 틱 체결 미측정(WAN-98 Canceled).
- ⚠️ **「엣지 없음」(WAN-84/88/111/114/124/151/201/248/386)을 뒤집었다고 쓰지 않는다** — 그
  계열은 **다른 팔**(채택 엔진 · 익절 1.5R · 재진입 · 볼린저 무력화 축)이다. 이 표는 **이 팔**
  (익절 없음 · 시간 청산 · 손절폭 하한 4% · %K)이 시각 널을 넘는다는 것까지이고, 그 계열 판정과
  한 표에 섞지 않는다.
- 실거래 보류 유지(`ALPHABLOCK_LIVE_TRADING=false`).
