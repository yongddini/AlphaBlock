# WAN-395 — 익절 배수의 꺾임을 찾는다 (0.4·0.5R로 격자를 늘린다)

**측정 전용 · 팔 하나(오늘 페이퍼가 돌리는 그 규칙) · 흔든 축은 익절 배수 하나.**
기본값·토대 불변(`ConfluenceParams()`·`LeverageBookParams()` 그대로 ·
`take_profit_r=1.5`(WAN-81/90)·`min_stop_distance_fraction=0.003`(WAN-76/79) **안 건드렸다** ·
핀 없음(WAN-305) · 실거래 보류 `ALPHABLOCK_LIVE_TRADING=false` 유지).

재현:

```bash
uv run python -m backtest.wan395_exit_multiple_inflection --pilot                    # 한 칸 견적
uv run python -m backtest.wan395_exit_multiple_inflection --jobs 4                   # §1 격자
uv run python -m backtest.wan395_exit_multiple_inflection --lens pen_5bp --jobs 4 --append  # §2
uv run python -m backtest.wan395_exit_multiple_inflection --from-csv                 # 요약만
```

원본: [`wan395_exit_multiple_grid.csv`](../../backtest/reports/wan395_exit_multiple_grid.csv) ·
[`wan395_leave_one_out.csv`](../../backtest/reports/wan395_leave_one_out.csv) ·
[`wan395_checksum.csv`](../../backtest/reports/wan395_checksum.csv) ·
[`wan395_exit_multiple_summary.md`](../../backtest/reports/wan395_exit_multiple_summary.md).

관련: [`wan381.md`](wan381.md)(**부모** · 배수 4점에서 멈춘 격자 · 검산 (d)의 상대) ·
[`wan394.md`](wan394.md)(세 축 격자 · payload 캐시 · 부호 관문) ·
[`wan386.md`](wan386.md)(§0 기계를 빌려온 곳) · [`wan387.md`](wan387.md)(재무장 일정 = 알려진
한계) · [`wan90.md`](wan90.md)(1.5R을 고른 옛 판) · [`wan161.md`](wan161.md)(배수 argmax 금지) ·
[`wan370.md`](wan370.md)·[`wan396.md`](wan396.md)(비용 천장 · gross 자 둘) ·
[`wan336.md`](wan336.md)/[`wan348.md`](wan348.md)/[`wan359.md`](wan359.md)(같은 분 익절 낙관) ·
[`wan341.md`](wan341.md)(판단은 북에서) · [`wan305.md`](wan305.md)(핀 없이 채택 좌표) ·
[`wan367.md`](wan367.md)(하드 제로는 경보다) · [`wan115.md`](wan115.md)(잔존율 함정).

## 0. 묻는 것

WAN-381이 가드 5점 × 배수 4점을 돌아 **가드 축은 닫고**(gross 진폭 0.0117R · 다섯 점 전부
음수) **배수 축을 열어 뒀다**(거래당 net R 진폭 ＋0.1130R · gross가 0.6·0.8R에서 0을 넘는다).
그런데 그 결정문이 스스로 적었다:

> **꺾임이 격자 안에 없다** — 0.6R이 최선인 것은 「거기가 최적」이 아니라 **「격자가 거기서
> 끝났다」**이다. 그 아래(0.4·0.5R)는 **아무도 안 쟀다.**

WAN-394도 같은 자리에서 멈췄다 — 세 축을 함께 흔든 실측 최선이 **0.6R 점**이었는데 그 역시
격자의 **끝값**이다. 이 표가 그 아래 두 점을 연다.

**답은 셋 중 하나이고 다음 행동이 다 다르다**(이슈가 착수 전에 못 박았고, 이 저장소에서
드물게 **코드가 고른다** — `inflection_verdict()`):

| 결과 | 뜻 | 다음 행동 |
| -- | -- | -- |
| 0.4~0.5R에서 **더 좋아진다** | 꺾임이 아직 더 아래 | 더 당기는 축을 계속 판다 |
| 0.6R **근처에 꺾임** | 최적을 찾았다 | 그 지점이 재-베이스라인 **후보**(사용자 결정) |
| 0.4~0.5R에서 **나빠진다** | 0.6R이 진짜 정점 | **배수 축도 닫힌다** |

### 🚨 산수가 「곧 소진된다」고 예고했다

손익분기 승률 = `(1 + 비용R) / (1 + 목표R)`. 비용을 0으로 두면 0.4R은 **71.4%**, 0.5R은
**66.7%**를 요구하는데 WAN-381 실측 0.6R 승률이 **67.75%**였다. **그래서 이 표가 「닫힌다」로
끝날 확률이 높았고, 그것도 충분히 값진 답이다.**

⚠️ **이슈 본문의 손익분기 표는 「비용 0」 판이다**(`1/(1+R)`). 실제로 넘어야 하는 선은
**비용R을 넣은 판**이고 이 표는 **둘 다** 낸다.

## 1. 격자

| 축 | 값 | 점 | 비고 |
| -- | -- | --: | -- |
| 익절 배수 | `0.4` · `0.5` | 2 | ❌ **신규 — 아무도 안 쟀다** |
| | `0.6` · `0.8` · `1.0` · `1.5`(채택) | 4 | ✅ **검산 (d)** — WAN-381과 비트 일치 |
| 손절폭 가드 | `0.30%`(채택) | 1 | **축이 아니다**(WAN-381 §3이 닫았다) |
| 체결 렌즈 | `baseline`(§1) · `pen_5bp`(§2, 판정 점 둘) | — | §2는 후보를 **다시 만든다** |

좌표는 채택 그대로: 12종목 × 4TF **한 지갑** · 못 박은 6년(2020-09-15~2026-07-22) · 존폭 필터
끔(WAN-384) · 인과 취소(WAN-365) · 재진입 ON(band, WAN-273) · cap_only 5배(WAN-213) ·
**핀 하나도 없다**(WAN-305) · 판단은 북에서(WAN-341).

📌 **가드를 축으로 두지 않은 것은 절약이 아니라 판정이다** — WAN-381 §4가 *「다섯 가드 전부
최적 배수가 0.6R로 같다」*를 냈다. 두 눈금은 산수로 얽혀 있지만 **이 폭에서는 안 움직인다.**

### 왜 모듈이 따로인가 (이슈는 "새로 만들지 말라"고 했다)

축·기계는 **하나도 새로 만들지 않았다** — `wan381_exit_scales`(→ `wan386_confirmation_pnl`)의
`build_payloads`·`arm_payloads`·`guard_census`·`_row_kwargs`·`GridRow`를 그대로 쓴다. 파일이
갈린 이유는 둘뿐이고 **둘 다 완료기준이 강제한다**:

1. **검산 (d)의 상대가 `wan381_exit_scales_grid.csv`다.** 그 CSV를 덮어쓰면 비트 일치를
   대조할 상대가 사라진다(옛 표는 그때의 기록으로 보존한다 — 이 저장소의 관행).
2. **격자 모양이 다르다** — 가드가 축이 아니라 고정값이라 이 표는 2차원이 아니라 배수 하나의
   **공선**이다.

🚨 그 대신 **스파이 테스트**가 두 모듈의 `run_cells` 인자를 실제로 캡처해 대조한다 — 후보가
갈라지면 검산 (d)가 성립할 리 없는데, 그것을 5시간짜리 격자로만 확인할 수는 없다(WAN-330
패턴).

