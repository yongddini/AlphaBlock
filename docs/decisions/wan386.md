# WAN-386 §3 — 확인 진입 3팔 × 손절폭 가드 3점 × 익절 배수 5점 (손익 격자)

**측정 전용 · WAN-383 §3 스핀아웃 · 사용자 사양 2026-08-27 + 가드 축 결정 2026-08-28.**
이 문서는 §0(엔진 배선)과 §3(손익 격자)의 기록이다. WAN-383이 §1(Phase 1 관문)에서
*「§3은 이 PR에 없다 — 무엇이 남았고 왜인가」*로 남긴 그 잔여를 받는다.

재현:

```bash
uv run python -m backtest.wan386_confirmation_pnl --pilot            # 한 칸 견적
uv run python -m backtest.wan386_confirmation_pnl --jobs 4           # 48칸 격자
uv run python -m backtest.wan386_confirmation_pnl --jobs 4 --checksum-book   # + 독립 경로 검산
uv run python -m backtest.wan386_confirmation_pnl --from-csv         # 요약만
```

원본: [`wan386_confirmation_grid.csv`](../../backtest/reports/wan386_confirmation_grid.csv) ·
[`wan386_leave_one_out.csv`](../../backtest/reports/wan386_leave_one_out.csv) ·
[`wan386_checksum.csv`](../../backtest/reports/wan386_checksum.csv) ·
[`wan386_confirmation_pnl_summary.md`](../../backtest/reports/wan386_confirmation_pnl_summary.md).

관련: [`wan383.md`](wan383.md)(**부모** — §0 관측·§1 관문) · [`wan372.md`](wan372.md)(MACD 색) ·
[`wan131.md`](wan131.md)(「선별 대 가격」 통제 — 팔 `C`가 그 자리) ·
[`wan114.md`](wan114.md)/[`wan123.md`](wan123.md)(모멘텀 확인이 이미 한 번 실패한 선례) ·
[`wan370.md`](wan370.md)(비용을 싸게 잡는 실패 · 비용 천장) · [`wan378.md`](wan378.md)(가드 축의
선례 · 「거래 수 병기」) · [`wan161.md`](wan161.md)(배수 argmax 금지) ·
[`wan341.md`](wan341.md)(판단은 북에서) · [`wan305.md`](wan305.md)(핀 없음) ·
[`wan346.md`](wan346.md)(복리 착시 · `compound_sizing=False`) ·
[`wan166.md`](wan166.md)/[`wan377.md`](wan377.md)(미래 절단 불변) ·
[`wan345.md`](wan345.md)(배선을 빠뜨리는 실패) · [`wan154.md`](wan154.md)(가드가 자르는 부류).

## 0. 묻는 것 — 그리고 §1이 왜 못 닫았나

**존에 닿자마자 들어가지 말고 「떨어지다 멈췄다」가 확인된 뒤에 들어가면 나은가?**

§1이 답한 것은 *「확인 규칙이 버는 돈의 대부분을 버리는가」* 하나이고 답은 **아니다**
(이익 비중 15~34%)였다. 🚨 **그것은 「확인 진입이 낫다」를 아무것도 말하지 않는다.** §1이
**원리적으로** 못 재는 채널이 셋이고 전부 확인 팔에 **불리한** 방향이다:

1. 늦게 사면 **손절폭이 1.39~2.12배**가 되어 고정 배수 목표가 멀어진다.
2. 거래 수가 줄어 **공유 자본·슬롯이 재배치**된다(WAN-323이 per-cell과 북 판정이 갈리는
   것을 겪은 그 채널 — 그래서 판단은 북에서, WAN-341).
3. 트리거는 전부 **위쪽**이라 지정가로 걸면 즉시 시장가 체결이다 — **테이커 4bp + 슬리피지
   5bp**를 문다(기준 팔은 메이커 2bp).

이 표가 그 셋을 손익으로 잰다.

## 1. §0 엔진 — 관측에서 팔로

WAN-383 §0이 만든 것은 **관측**(`ConfirmationProbe` — 트리거가 언제 오나)이지 **팔**(그때
실제로 진입하는 경로)이 아니었다. 이 PR이 그 자리를 채운다.

| 새 것 | 무엇 |
| -- | -- |
| `substep.simulate_fixed_entry_exits` | 진입이 **확정된** 포지션 하나를 **여러 익절 목표로 동시에** 청산 판정하는 순수 함수 |
| `_Candidate.entry_liquidity` | 후보가 들고 오는 **진입** 체결 유동성(기본 메이커 = 예전과 비트 동일) |
| `backtest/confirmation_arm.py` | 트리거 판독(`arm_trigger`) + 후보 변환(`derive_arm_candidates`) |
| `run_cells(confirmation_arms=, confirmation_multiples=)` | `CellPayload.arm_candidates`(`팔\|배수` → 구간 → 후보) |

### 1-1. 왜 「셋업은 그대로」인가 — 그리고 그 대가

이슈 설계가 *「셋업·나머지 전부 동일 · 진입 시점만 다르다」*다. 그래서 변환은 **후보를 새로
만들지 않는다** — 같은 탭·같은 존·같은 손절 참조가를 쓰고 바뀌는 것은 `entry_time`·
`entry_price`·`entry_liquidity`·청산뿐이다. 두 팔이 다른 셋업을 매매하면 그 차이가 「진입
시점의 값어치」인지 「다른 셋업을 골랐다」인지 못 가른다.

📌 **그 대가로 재무장 일정(재진입)은 기준 팔의 것을 쓴다**(알려진 한계). 재진입 후보는 base
후보의 per-cell 시퀀싱에서 나오므로(WAN-261) 팔마다 다시 파생하면 재무장 **시점**까지 팔에
따라 움직이고, 그러면 이 표가 재는 축이 둘이 된다. 🔁 **그 잠금을 푸는 것은 WAN-387이다** — 각주로만 두면 잔여가 유실되므로 별도 이슈로 넘겼다(WAN-379 → WAN-386이 겪은 패턴). 변환 자체는 base 후보와 재진입 후보
**양쪽에** 걸린다(WAN-345 부류 방지 — 한쪽만 걸면 표가 거래의 상당 부분을 조용히 놓친다).

### 1-2. 한 순회로 배수 다섯 — 후보 생성이 **한 번**인 이유

익절 배수는 **청산만** 바꾸고(WAN-137/143 훅) 손절선·진입가는 공유하므로, 목표마다 서브스텝을
다시 훑는 것은 같은 일을 다섯 번 하는 것이다. `simulate_fixed_entry_exits`가 한 순회에서
목표별 첫 도달을 함께 기록한다. 그래서 이 실행의 무거운 값은 **후보 생성 한 패스**이고,
가드는 **배치 축**이라(WAN-197) 후보를 안 바꾼다 — 60조합이 배치 반복만으로 나온다.

⚠️ **회귀 테스트가 「한 순회 = 목표마다 따로 훑기」를 값으로 고정한다** — 빨라졌는데 숫자가
달라지면 그건 버그다(WAN-203 선례).

### 1-3. 비용을 싸게 잡지 않는다

`기준`은 존 근단에 지정가를 걸어 두고 가격이 **내려오면** 체결이라 메이커(2bp · 슬리피지 0)
지만, 확인 팔의 트리거는 전부 **위쪽**이라 지정가로 걸면 즉시 시장가로 체결된다. 그래서 확인
팔 후보는 `entry_liquidity=TAKER`(4bp + 슬리피지 5bp)를 달고 나가고, 검산 (c)가 그것을
**라벨이 아니라 후보의 값으로** 센다. 🚨 이 이슈가 지는 가장 흔한 방식이 「비용을 실제보다
싸게 잡는 것」이다(WAN-370).

같은 이유로 팔 `2`의 진입가는 `P*`가 아니라 **`max(P*, 그 순간 현재가)`** 다 — `P*`가 이미
현재가 아래인 「시그널선 따라잡기」 부류(§1 실측 25.3%)에 **없는 가격 이점을 지어내지 않는다**.

### 1-4. 팔 `C`의 오프셋은 자유 파라미터가 아니다

`ARM_C_OFFSET = 1.0259806%` — 팔 `2`가 실제로 기다린 **평균 상대 거리**이고 WAN-383 §1이 채택
북 위에서 실측해 `wan383_confirmation_reach.csv`에 실려 있는 값이다. 🚨 **여기서 다시 재면
모집단이 달라져**(§1은 배치 층 · 이 표는 후보 층) `2 − C` 통제가 **다른 오프셋 두 개를 비교**
하게 된다. 그래서 §1이 낸 값을 **입력으로 못 박는다**(WAN-131이 볼린저에서 쓴 통제의 규약:
통제 팔은 실험 팔의 평균에 맞춰야 뜻이 있다).

## 2. 격자와 읽는 법

| 축 | 값 | 점 |
| -- | -- | -- |
| 진입 시점 | `기준` · `1_봉마감` · `2_교차` · `C_고정오프셋` | 4 |
| 손절폭 가드 | `0.30%`(채택) · `0.40%` · `0.50%` | 3 |
| 익절 배수 | `1.0 · 1.5`(채택) `· 2.0 · 2.5 · 3.0R` | 5 |

= **60조합 × 구간 4개**(`full`/`is`/`oos_warm`/`oos`). 좌표는 채택 그대로다 — 12종목 ×
4TF(15m·1h·2h·4h) **한 지갑** · 못 박은 6년 창 · 존폭 필터 끔(WAN-384) · cap_only 5배 ·
재진입 ON(band) · 유동성 한도 채택값 · 익절 메이커 2bp(WAN-370) · 인과 취소(WAN-365) ·
`baseline` 렌즈 · **핀 하나도 없다**(WAN-305).

* **판정 자는 거래당 net R**이다. 총수익 %는 이 좌표에서 복리로 −100%에 포화해 팔을 구분하지
  못하므로(WAN-169/213) **사이징 기준을 초기 자본에 못 박은**(`compound_sizing=False`) 판을
  낸다(WAN-346 §2가 만든 노브).
* 🚨 **그래서 이 표의 MDD는 채택 북이 보고하는 MDD와 비교 불가다** — 복리를 끄면 베팅은
  고정인데 지갑이 커져 **분모만 커진다**(WAN-346). 이 표 **안에서** 팔끼리만 비교한다.
* **거래 수를 net R 옆에 병기**한다 — 「덜 매매해서 좋아 보이는 것」과 구분(WAN-378 판정 2).
* **팔끼리는 같은 배수에서** 비교하고 **argmax는 채택 근거가 아니다**(WAN-161).
* ±0.005R 안은 「0과 구분되지 않는다」(WAN-366/370 규약).

### 2-1. 가드 축은 강건성 확인이 아니라 **공정성** 문제였다

확인 진입은 손절폭을 1.39~2.12배로 넓히는데 가드는 *「손절폭이 문턱보다 좁으면 그 거래를 안
한다」*이다. ⇒ **손절폭이 넓어진 팔에서는 가드가 자를 게 거의 없다. 기준 팔만 깎이고 확인 팔은
통과한다.** 그래서 가드를 **각 팔의 자기 진입가로** 판정하되(실전 규칙 · WAN-305) 그 대가를
`guard_cut`/`guard_kept` 열로 낸다 — 두 팔이 다른 셋업 집합을 매매하는 정도가 그 차이다.

🚨 **이 축의 용도는 하나다**: 같은 가드에서 팔끼리 비교하고 그 순위가 가드를 바꿔도 유지되는지
본다. **팔마다 최적 가드를 고르는 것은 앞구간 승자를 찾는 기계다**(WAN-366 §0).

## 3. 결과 — 판정 (b): 확인을 기다리면 나빠진다. 그리고 MACD는 값을 안 더한다

> **채택 배수·채택 가드에서 기준 팔이 세 확인 팔을 전부 이긴다. `2 − C`(MACD가 실제로 더한
> 값)는 **음수**다. 채택 권고 없음 · 기본값 불변.**

주 구간 `oos_warm` · 가드 0.30% · **거래당 net R**(괄호는 거래 수):

| 팔 | 1.0R | **1.5R(채택)** | 2.0R | 2.5R | 3.0R |
| -- | --: | --: | --: | --: | --: |
| `기준` | **−0.0726** (16,834) | **−0.1194** (14,825) | −0.1492 (13,206) | −0.1785 (12,180) | −0.1891 (11,398) |
| `1_봉마감` | −0.1399 (7,686) | −0.1374 (6,701) | **−0.1482** (5,921) | **−0.1618** (5,260) | **−0.1685** (4,771) |
| `2_교차` | −0.2222 (10,568) | −0.2352 (9,233) | −0.2421 (8,333) | −0.2392 (7,457) | −0.2493 (6,864) |
| `C_고정오프셋` | −0.1985 (7,532) | −0.2106 (6,826) | −0.2287 (6,318) | −0.2405 (5,822) | −0.2544 (5,466) |

### 3-1. 판정 줄 — `2 − C`가 이 이슈의 진짜 답이다

| 줄 | 1.0R | **1.5R** | 2.0R | 2.5R | 3.0R |
| -- | --: | --: | --: | --: | --: |
| `1 − 기준` | −0.0673 | −0.0180 | +0.0010 (≈0) | +0.0166 | +0.0206 |
| `2 − 기준` | −0.1496 | −0.1158 | −0.0929 | −0.0607 | −0.0602 |
| 🚨 `2 − C` | **−0.0237** | **−0.0246** | **−0.0134** | +0.0014 (≈0) | +0.0051 |

🚨 **MACD는 「그냥 정해진 만큼 오르면 산다」를 못 이긴다.** 팔 `C`는 팔 `2`의 평균 거리를
그대로 고정 오프셋으로 쓴 통제군인데(§1-4), 채택 배수에서 **팔 `2`가 그 통제군보다
0.0246R 나쁘다**. 높은 배수(2.5·3.0R)에서 부호가 바뀌지만 **크기가 잡음선(±0.005R) 언저리**라
읽을 것이 없다. [WAN-131](wan131.md)이 볼린저에서 낸 *「기여의 84%가 선별이 아니라 그냥
가격」*과 **같은 방향**이고, [WAN-114](wan114.md)/[WAN-123](wan123.md)의 「모멘텀 확인이
쳐낸 쪽이 오히려 좋았다」 계열에 한 사례가 더 붙는다.

⚠️ **`1 − 기준`이 높은 배수에서 양수인 것을 채택 근거로 쓰지 말 것** — 그 부호는 `기준` 팔이
높은 배수에서 더 빨리 나빠져서 생기는 상대값이고(둘 다 음수다), 팔 `1`의 배수 argmax는
**IS→OOS에서 뒤집힌다**(§3-4). WAN-161이 못 박은 자리다.

### 3-2. 🚨 §1이 예측한 「손실 회피」는 손익으로 갚아지지 않았다

WAN-383 §1의 가장 큰 발견은 *「확인이 건너뛰는 거래가 정확히 지는 거래다」*였다(트리거 안 온
뭉치가 거래당 −0.84~−0.95R · 기준 팔 손실의 51~74%). **그런데 그 회피가 손익에서 이기지
못한다.** §1이 원리적으로 못 재던 세 채널이 그것을 다 먹었다:

* 손절폭이 1.39~2.12배가 되어 고정 배수 목표가 멀어지고,
* 거래 수가 절반 이하로 줄어 공유 자본·슬롯 배치가 달라지며,
* **테이커 4bp + 슬리피지 5bp**를 문다.

📌 §1이 *「상당 부분이 정의상 그렇다 — 가격이 안 올랐으면 ① 트리거가 안 오고 ② 그 거래는
손절로 끝난다. 한 사건의 두 얼굴」*이라 경고한 그 상관이, 실제로 늦게 사 보니 **값으로
바뀌지 않았다**는 것이 §3의 답이다.

### 3-3. 🚨 비용만의 이야기가 아니다 — gross에서도 진다

수수료·펀딩 **전**(채택 배수 · `oos_warm`):

| 팔 | net R | gross R | 거래 수 |
| -- | --: | --: | --: |
| `기준` | −0.1194 | **−0.0421** | 14,825 |
| `1_봉마감` | −0.1374 | −0.0729 | 6,701 |
| `2_교차` | −0.2352 | −0.1580 | 9,233 |
| `C_고정오프셋` | −0.2106 | −0.1736 | 6,826 |

**수수료를 0으로 만들어도 순위가 그대로다.** 확인 진입이 진 것은 테이커 비용 때문이 아니라
**진입가가 나빠졌기 때문**이다(⚠️ 슬리피지는 체결가에 녹아 있어 이 열에서 안 빠진다).
[WAN-370](wan370.md)의 *「비용을 0으로 만들어도 천장이 ＋0.09R」*과 같은 자리다.

### 3-4. 강건성 — 네 구간·세 가드·열두 종목이 같은 말을 한다

| 구간 | `기준` | `1_봉마감` | `2_교차` | `C_고정오프셋` |
| -- | --: | --: | --: | --: |
| `full` | **−0.0981** | −0.1343 | −0.2235 | −0.2192 |
| `is` | **−0.0890** | −0.1326 | −0.2177 | −0.2225 |
| **`oos_warm`** | **−0.1194** | −0.1374 | −0.2352 | −0.2106 |
| `oos` | **−0.1242** | −0.1435 | −0.2422 | −0.2138 |

* **네 구간 전부 `기준` > `1` > (`2`·`C`)** — IS에서 고른 결과가 아니다.
* **가드 3점에서 순위 유지**(0.30%·0.40%: `기준 > 1 > C > 2` · 0.50%: `기준 > 1 > 2 > C`) —
  뒤집히는 것은 **꼴찌 둘의 자리**뿐이고 **`기준`이 1등인 것은 안 흔들린다**.
* **leave-one-out 4팔 전부 부호 유지** — 어느 종목을 빼도(신규 3종목을 묶어 빼도) 판정이
  그대로다. 이 저장소가 반복해 겪은 ETH·SOL 편중(WAN-111/119/124/151)이 여기서는 없다.
* **IS→OOS 배수 뒤집힘 2/4팔**(`1_봉마감` 3R→1.5R · `2_교차` 3R→1R) — **argmax는 채택 근거가
  아니다**(WAN-161).

### 3-5. 🚨 가드 교란은 실측으로 거대했다 — 그런데도 기준 팔이 이긴다

`oos_warm` · 1.5R · **가드에 잘린 셋업**(0.30% / 0.40% / 0.50%):

| 팔 | 0.30% | 0.40% | 0.50% | 잘린 셋업 |
| -- | --: | --: | --: | --: |
| `기준` | −0.1194 (14,825) | −0.1018 (12,825) | −0.0931 (11,089) | **17,803 / 27,605 / 37,148** |
| `1_봉마감` | −0.1374 (6,701) | −0.1283 (6,535) | −0.1294 (6,293) | 727 / 1,787 / 3,341 |
| `2_교차` | −0.2352 (9,233) | −0.2152 (8,846) | −0.2048 (8,303) | 1,936 / 4,249 / 7,301 |
| `C_고정오프셋` | −0.2106 (6,826) | −0.2106 (6,826) | −0.2106 (6,826) | **0 / 0 / 0** |

이슈가 착수 전에 지목한 불공정이 **그대로 나왔다** — 기준 팔은 가드가 1.8만~3.7만 셋업을
자르는데 팔 `C`는 **한 건도 안 자른다**(손절폭이 넓어 가드가 할 일이 없다). 📌 **그 교란은
확인 팔에 유리한 방향인데도 기준 팔이 이긴다** — 즉 판정이 가드 배치의 산물이 아니다.

⚠️ **「그러니 가드를 0.50%로 올려라」로 읽지 말 것** — 기준 팔이 가드를 조일수록 좋아지는 것은
[WAN-197](wan197.md)/[WAN-154](wan154.md)가 이미 낸 성질이고, 이 표는 그 축을 **통제**로 썼지
고르지 않았다. 가드 전환은 **재-베이스라인 = 사용자 결정**(WAN-76/79 소관).

### 3-6. 🚨 지갑 층 열은 이 좌표에서 **뜻을 잃었다**

복리를 껐는데도(사이징은 초기 자본 고정) **잔고가 0을 뚫는다** — `oos_warm` 기준 팔이
−1,106%, 최악 팔이 −1,554%다. 자본이 음수를 지나면 「자본 대비 비율」(MDD·수익/MDD·동시
리스크·실효 리스크·청산 건수)은 분모가 부호를 바꾸며 무의미해지므로 **요약이 비율을 내지
않고 「정의 상실」로 찍는다**([WAN-115](wan115.md)가 증분 부호 함정에서 세운 관행의 이 축
판이고, 회귀 테스트가 「오해를 부르는 비율이 본문에 안 나온다」로 고정한다).

📌 **읽을 수 있는 것은 넷뿐이다** — 거래당 net R · 승률 · 거래 수 · 최대 동시 칸(그리고 상한
발동률). ⚠️ **이것은 팔의 성질이 아니라 좌표의 성질이다**: 존폭 필터를 끈 오늘 엔진
([WAN-384](wan384.md))의 채택 북은 6년 창에서 **모든 팔이** 초기 자본을 넘겨 잃는다
([WAN-378](wan378.md) 「108팔 전부 음수」와 같은 자리). 확인 진입이 만든 결과가 아니다.

### 3-7. 실측 비용 — 이슈 견적의 3분의 1

**17,386초 = 4시간 50분**(48칸 · `--jobs 4` · M1 · 경합 없음). 그중 **후보 생성이
16,833초(4시간 41분)**이고 **배치 60조합 + leave-one-out 52판 + 검산은 553초**다.

📌 이슈 견적은 **≈15시간**이었다(「팔마다 후보를 다시 만들어야 한다」는 전제). §1-2가 그
전제를 없앴다 — 팔·배수는 후보를 새로 만들지 않고 가드는 배치 축이다.
⚠️ **이 값을 다른 모듈에 옮기지 말 것**(WAN-203 → WAN-312 · WAN-383이 세 번 데인 자리).
⚠️ **파일럿 값(BTC 4h 단칸 273초)도 격자 견적으로 인용 금지.**

## 4. 검산 — 전부 비트 일치 (`0.00e+00`)

| 검산 | 무엇 | 결과 |
| -- | -- | -- |
| **(a-1)** | 기준 팔 후보 ≡ 엔진 base+재진입 (48칸 × 4구간 · 진입·청산·손절가·사유까지) | 불일치 칸 **0** |
| **(a-2)** | 기준 팔 지갑 ≡ 채택 북 지갑(복리 켬) · 4구간 × 6지표 | **0.00e+00** |
| **(b)** | 같은 팔의 다섯 배수는 진입 집합 비트 일치 (4팔) | **0.00e+00** |
| **(c)** | 확인 팔 전부 테이커 · 기준 팔 전부 메이커(라벨이 아니라 **값**으로) | 어긋난 후보 **0** |

📌 **(a-1)이 완료기준 6①의 정본이다** — 그 기준이 요구한 것은 *「기준 팔 ≡ 인자 없는 채택
북과 **같은 셋업 집합**」*이고, (a-1)은 그것을 칸·구간마다 **청산까지** 대조한다. (a-2)는 그
후보로 만든 **지갑**까지 같음을 더한다.

⚠️ **완전히 독립한 경로(후보를 처음부터 다시 생성)와의 대조는 옵트인이다**
(`--checksum-book`) — 그 판은 이 모듈의 무거운 값을 **한 번 더** 쓰기 때문이고(후보 생성이
비용의 97%), 이 실행에서는 **켜지 않았다**. 요약이 그 사실을 밝힌다(「검산했다」는 라벨만
남기지 않는다).

📌 **검산 ③(미래 절단 불변, WAN-166/377)은 회귀 테스트가 값으로 고정한다** — 청산이 이미
끝난 거래는 뒤 봉을 통째로 잘라도 비트 동일하다(엔진 층·팔 변환 층 양쪽).

## 5. 결정 — 채택 권고 없음 · 기본값 불변

**확인 진입을 채택하지 않는다.** 판정이 (a)가 아니고, 세 팔 모두 채택 배수·채택 가드에서
기준 팔보다 나쁘며, `2 − C`가 *「MACD가 그냥 반등을 사는 것보다도 못하다」*를 말한다.

* `ConfluenceParams()`·`LeverageBookParams()` **불변** · `min_stop_distance_fraction=0.003`
  **불변** · 확인 팔은 전부 옵트인(안 켜면 비트 재현) · **실거래 보류 유지**.
* 🚨 **「그럼 기준 팔이 좋다」로도 읽지 말 것** — 기준 팔도 **−0.1194R**이다. 이 표가 고른 것은
  **가장 덜 나쁜 팔**이지 흑자가 아니다.
* 잔여는 **WAN-387**(재무장 일정을 팔 자신의 것으로)이다.

## 6. 범위 밖 · 경고 (전부 유효)

* ⚠️ **가드 기본값 전환 제안이 아니다** — `min_stop_distance_fraction=0.003`은 불변이고 변경은
  **재-베이스라인 = 사용자 결정**(WAN-76/79 소관).
* ❌ **청산 규칙을 안 건드린다**(*「진입하고 또 진한 빨강이 나오면 나온다」*는 별도 이슈) ·
  ❌ MACD 파라미터(12/26/9) 스윕 금지 · ❌ 대기 길이를 파라미터로 열지 않는다(「존 무효화까지」
  하나) · ❌ 존폭 필터(끔)·오프셋(2bp)·TF·유니버스 안 건드린다.
* 🚨 **모멘텀 확인은 이미 한 번 실패했다** — RSI 게이트가 거래를 13~14% 쳐냈는데 그 쳐냄이
  순손해라 [WAN-123](wan123.md)에서 제거됐다.
* 🚨 **「싸게 사는 것」을 포기하는 방향이다** — [WAN-131](wan131.md): 볼린저 기여의 **84%가
  「선별」이 아니라 「가격」**.
* 🚨 **「흑자」로 기대하지 말 것** — [WAN-370](wan370.md): 비용을 0으로 만들어도 시장에서 얻는
  것의 천장이 **＋0.09R**. [WAN-378](wan378.md) 격자는 **108팔 전부 음수**.
* ⚠️ **재무장 일정은 기준 팔의 것**이다(§1-1의 알려진 한계 · 잔여는 **WAN-387**) · **스코프는 채택 좌표
  `both`(4TF 한 지갑) 하나**다 — 북은 이어붙일 수 없고(WAN-316) TF별 스코프는 배치를 5배로
  늘리는데 완료 기준이 요구하지 않는다.
* ⚠️ 판단은 북에서([WAN-341](wan341.md)) · 핀 없이([WAN-305](wan305.md)) · 전부 `baseline`
  (닿으면 체결) 낙관 렌즈 위 값이고 **체결 보수화(`pen_5bp`)는 범위 밖** · 6년 MDD는 폭락
  미포함 **바닥선**.
* ⚠️ **「엣지 없음」(WAN-84/88/111/114/124/151/201/248) 불변** — 이 표는 *어느 시점에
  들어가나*를 묻지 *진입 규칙이 무작위와 구분되는가*를 묻지 않는다. **다른 질문이다.**
* **기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()` 그대로 · 확인 팔은 전부
  옵트인 · 안 켜면 비트 재현) · **실거래 보류 유지**(`ALPHABLOCK_LIVE_TRADING=false`).
