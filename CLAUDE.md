# AlphaBlock — Claude Code 개발 가이드

오더블록(Order Block) 기반 암호화폐 자동매매 프로젝트. **이 저장소에서 Claude Code의 역할은 "개발자"다.** 완료 판단·상태 관리·다음 작업 제안 등 PM 업무는 별도 자동화(Cowork PM 러너)가 담당하므로 여기서 신경 쓰지 않는다.

## 역할 분담 (중요)

- **Claude Code (여기, 로컬)** = 개발. 아래 "개발 워크플로우"만 수행한다.
- **PM 러너 (Cowork, 매시간)** = 완료 판단(In Review→Done), 상태 관리, 다음 Todo 제안, 보고. 코드는 건드리지 않는다.
- 승인은 사용자가 이슈를 **Approved** 상태로 옮기는 것으로 표현된다.

## 토대 확정 (Foundation Freeze) — 2026-07-15

📌 **아래 3개 토대는 확정이며, 그 위에서만 전진한다.** 모든 측정·최적화·전략 이슈는 이
토대를 **고정 입력**으로 받는다. 토대를 바꾸면 그 위에 쌓인 모든 숫자·결정이 무효화되므로,
**변경은 반드시 명시적 「재-베이스라인」 이슈로만** 한다(지나가는 김에 바꾸지 않는다).

1. **진입 방식 = 존 근단 지정가 + 오프셋 2bp**(`entry_mode="zone_limit"`, WAN-95 /
   `zone_limit_offset_bps=2.0`, **WAN-112**). 사용자의 실매매. 진입가를 재산정하는 **볼린저
   밴드의 표본 시점은 `intrabar_live`(봉내 라이브)** 다(**WAN-132** — 아래 📌 문단).
2. **공식 체결 렌즈 = `baseline` 단독**("닿으면 체결", WAN-104 / **🔁 3렌즈 병기 폐지 WAN-128**).
   모든 채택 판단·리포트의 주 수치는 `baseline`으로 내고, **신규 리포트는 `baseline` 단독**이다
   — `pen_5bp`·`pen_5bp_drop_50` 병기 요구는 WAN-128이 폐지했다(사용자 결정, 아래 📌 WAN-128
   문단). 두 민감도 렌즈는 **삭제가 아니라 옵트인**이다(`--fill`로 언제든 수동 확인).
3. **비용 = 현재 값 확정**(테이커 4bp / 메이커 2bp / 슬리피지 5bp, WAN-95 배선 기준).
   비용 현실화(WAN-92)는 **명시적 재-베이스라인**으로만 반영하고, 그 전까지 현재 값이 정본이다.

🚨 **채택 성과를 인용하기 전에 WAN-364를 읽을 것(2026-08-23)** — 백테스트가 「존이 깨진 봉」의
대기 지정가를 **봉 시작으로 소급 취소**하는 룩어헤드가 확인됐고, 그것을 인과로 바꾸면 채택 북
`oos_warm`의 거래당 net R이 **+0.1985R → −0.1798R로 부호가 뒤집힌다**(아래 🚨 WAN-364 문단).
**기본값은 안 바꿨다**(전환은 재-베이스라인 = 사용자 결정) — 즉 **아래의 모든 채택 성과 수치는
그 룩어헤드 위의 값**이고, 그 사실을 밝히지 않은 채 인용하지 말 것.

⚠️ **작업 층 순서(아래층이 굳은 뒤 위층)**: (1) 엔진 정확성/버그 → (2) 토대(위 3개) →
(3) 구조 결정(채택 TF·숏 온오프·오프셋·다중 포지션) → (4) 측정·최적화(익절 R·MFE 등).
**하위층 이슈는 자기 상위 입력이 열려 있는 동안 Approved로 올리지 않는다**(PM 러너는 이
관계를 `blockedBy`로 강제한다). 마지막 구조 잠금이던 **채택 TF는 WAN-107이 닫았다**
(15m·1h 공동 작업 TF — 아래 📌 TF 문단). 그 재판정이 15m을 되살리자 **WAN-103의 다중
포지션 판정도 WAN-108이 15m 축에서 다시 냈고**(결론: 기본값 유지 + 사이징 2안 기각 —
아래 ⚠️ 다중 포지션 문단), 이로써 구조 결정 층은 **닫혔다**. 🔁 측정·최적화 이슈는
**15m·1h·4h 세 축을 병기**해야 한다(WAN-182가 4h를 작업 TF로 승격 — 아래 📌 WAN-182 문단.
옛 「두 축 병기」 요구는 WAN-181까지의 규칙이다).

📌 **채택 좌표 = 9종목 × 못 박은 6년 창(2020-09-15~2026-07-22) × 작업 TF 15m·1h·4h
(WAN-182 = WAN-179 사용자 결정 실행, 2026-07-23)**:
[`docs/decisions/wan182.md`](docs/decisions/wan182.md). **엔진이 아니라 좌표의 재-베이스라인**
이다(`ConfluenceParams()` 불변) — 근거는 WAN-176(전략 판정)·WAN-180(레버리지)이 이미 낸
표이고 새 격자는 없다. 유니버스 6→**9종목**(+DOGE·LINK·LTC), 창 `--years 3`(미끄러짐)→
**못 박은 6년**(SOL 상장 하한 · 균일 창, 실효 5.85년), 4h **작업 TF 승격**(1d는 표본 미달로
제외 유지).
🔁 **작업 TF는 WAN-252가 2h를 승격해 15m·1h·2h·4h 네 축이 됐다**(사용자 결정 2026-08-05,
[`docs/decisions/wan252.md`](docs/decisions/wan252.md)) — WAN-182의 4h 승격과 **같은 패턴의
좌표 재-베이스라인**(엔진 불변, 근거는 WAN-236 2h 성능·WAN-243 2h 재진입 널이 이미 낸 표).
2h는 저장된 1h에서 무손실 리샘플로 파생된다(사전 적재 불필요, WAN-24). 후속 측정·리포트는
**15m·1h·2h·4h 네 축을 병기**한다(옛 「세 축」은 WAN-251까지의 규칙).
🔁 **유니버스는 WAN-307이 12종목으로 확장했다**(사용자 결정 2026-08-14 「12로 하자」,
[`docs/decisions/wan307.md`](docs/decisions/wan307.md)) — 같은 패턴의 좌표 재-베이스라인
(엔진 불변, 근거는 WAN-304 사다리가 이미 낸 표: 12종목은 렌즈에 MDD가 +0.1%p로 거의 안
흔들리는데 15종목은 +4.3%p 튄다 · 밀림율 4.24% < 5% 포화선). 합류 3종목(ADA·DOT·BCH)은
**ADV 동결 순서 규칙**(`wan300_universe_size.CANDIDATE_SYMBOLS` 상위 3)이지 자유 파라미터가
아니다. 9종목 시절 결론 리포트 27개는 **`harness.LEGACY_NINE_SYMBOLS`(신설)로 명시 고정**
했다(wan104/wan108의 3심볼 `LEGACY_SYMBOLS`와 다른 세대의 스냅샷). **채택 성과(wan95)만
재산출**(12종목 × 4TF × 채택 창 — ⚠️ 9종목 판과 셀 비교 금지). 펀딩은 12종목 전부
자기 확정 데이터가 창을 덮는다(DOGE·LINK·LTC는 WAN-292 백필, ADA·DOT·BCH는 상장 초기부터
수집분 — 대리 규칙은 안전망으로 존치·무동작).
- 📌 **채택 좌표가 코드 기본값이 됐다** — `harness.DEFAULT_SYMBOLS`(9종목)·
  `DEFAULT_TIMEFRAMES`(**15m·1h·2h·4h**, WAN-252)·`DEFAULT_START/END`(신설). **인자 없는
  `backtest.run`은 이제 못 박은 채택 창을 돈다**(`--years N` 명시만 옛 미끄러지는 창). 정본
  리포트 = `uv run python -m backtest.run --oos-warm`(인자 없이).
- 📌 **파급은 예외적으로 작다** — 옛 리포트 대부분이 좌표를 자기 모듈 상수로 들고 있어
  무변이고, harness 기본값을 직접 읽던 **wan104/wan108만 `harness.LEGACY_SYMBOLS/
  LEGACY_TIMEFRAMES/LEGACY_YEARS`(3심볼 × 1h × 3년)로 핀**했다. **채택 성과(wan95)만
  재산출**(9종목 × **4TF**(15m·1h·2h·4h, WAN-252) × 채택 창 — ⚠️ 옛 판과 셀 비교 금지,
  좌표가 통째로 다르다).
- ⚠️ **신규 3종목(DOGE·LINK·LTC)은 펀딩 데이터가 0행**(WAN-178 백필 전) — wan95는
  WAN-180과 같은 **대리 규칙**(기존 종목 중 확정 펀딩 평균 최고 = 롱에게 가장 비싼 종목의
  시계열)로 보정하고, 자기 데이터가 생기면 대리는 저절로 꺼진다. **범용 CLI는 대리를 얹지
  않으므로** 신규 3종목 행은 `funding_coverage` 0%다(읽을 때 확인).
- ⚠️ **엣지 판정 계열(WAN-84/88/111/114/124/151)은 옛 창 기록으로 얼어붙는다** — 새 창
  판정의 정본 근거는 **WAN-176 표**(`wan176_summary.md`)이고, 옛 판정과 한 표에 섞지 말
  것. 안 낸 축(부품 분해·숏 널·체결 보수화)의 새 창 재검은 별도 이슈다(임의 착수 금지).
- **실거래/시그널 대상 불변**(`live_signal_symbols` BTC 단독 · `ALPHABLOCK_LIVE_TRADING
  =false` 유지) — 유니버스 확장은 측정·수집 대상이지 실거래 승인이 아니다(WAN-111 원칙).
  **「엣지 없음」은 뒤집힌 게 아니다**(유니버스·창을 넓혀 재측정했을 뿐, WAN-176).

## Linear 연결

이 워크스페이스는 Linear로 이슈를 관리한다. Linear MCP를 연결해 사용:

```
claude mcp add --transport http linear-server https://mcp.linear.app/mcp
```

세션에서 `/mcp`로 인증. 팀은 **Alphablock** (key: `WAN`).

## GitHub 연동

원격: `github.com/yongddini/AlphaBlock` (기본 브랜치 `main`). Linear ↔ GitHub 기본 연동을 사용해 **GitHub 동작이 Linear 상태를 자동 동기화**한다: 브랜치/커밋 → In Progress, PR 열림 → In Review, PR 머지 → Done. 따라서 개발자는 상태를 손으로 바꾸기보다 브랜치·PR·머지 행위로 상태가 흐르게 한다. 이슈 연결은 브랜치명 또는 PR 제목/본문의 이슈 식별자(`WAN-x`)로 이뤄진다.

`main`은 브랜치 보호로 **CI(WAN-10) green + PR** 없이는 머지되지 않는다(설정 스크립트: `scripts/setup-branch-protection.sh`, 적용은 저장소 소유자 1회 실행). 머지 버튼은 사람이 누른다 — 자세한 규칙은 README "브랜치 보호 · 머지 정책" 참고.

## 개발 워크플로우

1. **Approved 이슈만 개발한다.** Linear에서 team=Alphablock, state=`Approved` 이슈를 우선순위 순으로 가져온다. Backlog/Todo/Rejected 는 절대 개발하지 않는다.
2. **착수 = 브랜치 생성.** 이슈의 Linear 제안 브랜치명(이슈 식별자 포함, 예 `yu04038/wan-7-...`)으로 `main`에서 브랜치를 만든다. 브랜치·첫 커밋이 올라가면 Linear GitHub 연동이 이슈를 자동으로 **In Progress**로 옮긴다(안 되면 수동 전환).
3. 이슈의 완료 기준(Acceptance Criteria)을 충족하도록 이 저장소에서 개발한다.
4. 커밋 메시지 앞에 이슈 식별자를 붙인다. 예: `WAN-7: 오더블록 탐지 로직 추가`.
5. **완료 = 브랜치 push + PR 생성.** 브랜치를 `origin`에 push하고 `main`으로 향하는 GitHub **Pull Request**를 연다.
   - PR 제목/본문에 이슈 식별자(`WAN-7`)를 넣어 이슈에 자동 연결한다. PR 본문에는 변경 파일·완료기준 체크리스트·테스트/품질게이트 결과를 적는다.
   - PR이 열리면 Linear 연동이 이슈를 자동으로 **In Review**로 옮긴다(연동이 상태를 관리하므로 직접 Done으로 옮기지 않는다).
   - CI(WAN-10)가 PR에서 자동 실행된다. **CI 초록불이 아니면 리뷰 대상이 아니다** — 실패를 고쳐 다시 push한다.
6. PM 러너가 미흡하다고 판단해 **Approved로 되돌리고** 변경요청 코멘트를 달면, 그 피드백을 반영해 **같은 브랜치/PR에 추가 커밋**하고 다시 3~5를 수행한다.
7. **머지는 사람이 한다.** PM 검증 + CI green 확인 후, 사용자가 PR을 `main`에 머지한다. 머지되면 Linear 연동이 이슈를 자동으로 **Done**으로 옮긴다. **Claude Code는 PR을 자동 머지하지 않는다.**

## 품질 게이트 (커밋/리뷰요청 전 필수 통과)

```bash
uv run ruff check .
uv run ruff format --check .
uv run mypy .            # strict
uv run pytest
```

처음이라면 `uv sync --dev` 로 의존성 설치, `uv run pre-commit install` 로 훅 설치.

## 안전 규칙

- **실거래 자동 활성화 금지.** `ALPHABLOCK_LIVE_TRADING` 기본값은 `false`. 실주문/자금 이동 코드는 사용자가 명시적으로 승인·수행하기 전까지 활성화하지 않는다.
- API 키·시크릿은 코드에 하드코딩하지 않는다. `.env`(커밋 금지, `.gitignore` 포함)나 환경변수로만 주입하고, 새 설정은 `.env.example`에 예시를 추가한다.
- 테스트가 실패하거나 완료기준을 못 채운 상태로 PR을 열지 않는다(= In Review로 넘기지 않는다).
- **PR을 자동으로 머지하지 않는다.** main 반영(머지)은 CI green + PM 검증 후 사용자가 수행한다.
- `.github/`(CI·릴리즈 워크플로우)는 승인된 이슈 범위 내에서만 수정한다.

## 프로젝트 구조

```
AlphaBlock/
├── data/        # 시세 수집·저장 (OHLCV, 웹소켓) — WAN-6
├── strategy/    # 오더블록 탐지·시그널 생성 — WAN-7
├── execution/   # 주문 실행·포지션·리스크 관리 — WAN-9
├── backtest/    # 백테스팅 엔진 — WAN-8
├── config/      # 설정 로딩 (pydantic-settings)
├── tests/       # 테스트
└── pyproject.toml
```

## 전략 규칙 요약 (WAN-81 메인 엔진)

컨플루언스 전략(`strategy/confluence.py`)의 확정 규칙. **WAN-23/WAN-66 규칙(EMA 60 +
VWMA 100 익절, 롱 온리, 존당 첫 탭 1회)은 WAN-81로 전면 교체됐다** — 아래가 현재
기본값(`ConfluenceParams()`)이다.

📌 **공식 체결 기준선 = `baseline`(WAN-104 확정, WAN-97 결정 1 번복)**:
[`docs/decisions/wan104.md`](docs/decisions/wan104.md) §1. **모든 채택 판단과 리포트의
주 수치는 `baseline`**("닿으면 체결")로 낸다. 근거(사용자 지시): 지정가 **오프셋**이
"가격을 미리 밀어 관통을 주문 가격으로 만족시키는" 수단인데 그 위에 `pen_5bp`(관통 벌점)를
또 얹으면 **같은 마찰을 두 번 친다**. 사용자의 실매매 = 지정가 걸고 닿으면 체결 =
`baseline`이다. 🔁 **세 값 병기 요구는 WAN-128이 폐지했다 — 재인용 금지**(아래 📌 WAN-128
문단이 정본). 원문은 "세 값을 **`baseline`(공식)** · `pen_5bp`(민감도) · `pen_5bp_drop_50`
(스트레스) 로 병기한다"였으나, **신규 리포트는 `baseline` 단독**이다. 두 민감도 렌즈는
**폐기가 아니라 옵트인**이다(파라미터·`--fill` 존치, 수동 확인 가능).
⚠️ **`baseline`이 낙관이라는 성질은 사라지지 않았다** — 큐 우선순위를 모델링하지 않으므로
**공식 수치는 상한으로 읽는다**(민감도 병기가 없어졌으니 그 상한이 얼마나 부풀려졌는지
자동으로 보이지 않는다 — 아래 📌 WAN-128 「잃는 것」 1·2). 실제 해소는 틱·호가 데이터
(WAN-98) 소관이다.

📌 **체결 민감도 렌즈 병기 폐지(WAN-128 = 사용자 재-베이스라인): 신규 리포트는 `baseline`
단독이다.** [`docs/decisions/wan128.md`](docs/decisions/wan128.md). 사용자 결정("둘 다 버리고
baseline만"). **리포트 층위의 변경이고 엔진은 안 건드린다** — `fill_penetration_bps`(기본
`0.0`)·`fill_dropout_rate`(기본 `0.0`)·`fill_dropout_seed`와 `backtest.run --fill` 옵션은 전부
**존치**한다(파라미터를 지우면 과거 리포트 재현이 깨지고 되돌리기 어렵다). CLI 기본값은 이미
`baseline` 단독이라(`harness.BASELINE_FILL`) 바꿀 코드 경로가 없었고, 3렌즈는 각 기존 리포트
모듈의 `LENS_NAMES` 상수일 뿐이라 **기존 md/csv는 무수정 보존**한다(당시 기록). 신규 리포트만
`baseline` 단독으로 낸다.
- ⚠️ **이 결정으로 잃는 것(기록 — 나중에 "왜 안 보이지"를 막기 위해, PM이 반대 근거로 제시했고
  사용자가 알고 선택했다)**: (1) `baseline`의 **낙관 성질은 그대로**인데 그 상한이 얼마나
  부풀려졌는지 자동으로 볼 장치가 없어진다. (2) **채택 엔진 체결 취약성 실측**(WAN-124 3단)이
  리포트에서 사라진다 — 15m OOS에서 관통을 요구하면 **거래 94.29%가 남는데 수익은 3.30%만
  남는다**(1h 95.46%/35.10%). 즉 수익 대부분이 "스치듯 닿고 되돌아선 체결"에 실려 있다는 사실이
  안 보인다. (3) **결론을 가른 선례**가 끊긴다 — WAN-88의 `baseline` 유의 4셀은 `pen_5bp` 한
  번에 전멸했고 WAN-124는 11→10셀로 살아남아 "성질이 다르다"는 판정이 가능했다(WAN-120은
  `pen_5bp` 부호 반전으로 WAN-119의 잘못된 주장 하나를 걷어냈다). (4) **체결률 81%가 전부
  `baseline` 위의 값**이라(WAN-123 게이트 제거 후) 낙관 가정의 비중은 준 게 아니라 늘었다.
  (5) 기존 3렌즈 md/csv와 신규 `baseline` 단독 리포트는 **열이 안 맞는다**(대조 축 단절).
- ⚠️ **「엣지 없음」(WAN-84/88/111/114/124)이 뒤집혔다는 뜻이 아니다** — 렌즈 병기를 그만둔
  것이지 새 수치를 낸 게 아니다. 큐 우선순위 걱정(`baseline`은 상한)은 그대로이고, 실제 해소는
  틱·호가 데이터(WAN-98) 소관이다.
- **「선택 사항」(리포트마다 `pen_5bp` 잔존율 한 줄)은 넣지 않았다** — 이슈가 **사용자 결정
  없이 개발자가 임의로 넣지 말라**고 명시했고, 자율 루틴에는 그 결정 입력이 없어 보수적으로
  **미구현**했다(추가는 언제든 별도 이슈로 가능). wan128.md §선택-사항 참고.
- **기본값·토대는 바꾸지 않았다**(`ConfluenceParams()` 불변, 실거래 보류 `ALPHABLOCK_LIVE_TRADING
  =false` 유지). 파급: WAN-126(다중TF 겹침)·WAN-127(익절 Phase 2)의 "3렌즈 병기" 사양이
  `baseline` 단독으로 축소된다(격자가 줄어 오히려 유리).
📌 **채택 TF = 15m·1h 공동 「작업 TF」 · 4h 보류 · 1d 제외 (WAN-107 확정, WAN-97 결정 2
부분 개정)**: [`docs/decisions/wan107.md`](docs/decisions/wan107.md). 「작업 TF」 = **후속
최적화·검증을 이 TF 위에서 돌린다**는 뜻이지 실거래 승인이 아니다. **후속 이슈는 15m·1h를
병기한다**(1h는 MDD가 가장 낮고 1분봉 서브스텝 비용이 15m의 ~1/10이라 반복 실험의 기본 축,
15m은 OOS 수익·표본이 가장 큰 축). 4h는 **OOS 심볼당 15.7거래**로 WAN-84 유효 기준 20건
미달이라 **판정 불가**(대조군으로 병기), 1d는 표본(10거래)·수익(+1.02%) 모두 의사결정
가치가 없다.
🔁 **이 중 「4h 보류」는 WAN-182가 개정했다 — 4h는 작업 TF로 승격됐고 후속 이슈는 세 축
(15m·1h·4h)을 병기한다**(위 📌 WAN-182 문단이 정본). 근거: 6년 창에서 4h 표본이 유효
기준을 넘는다(WAN-176). 당시 「판정 불가」는 3년 창의 표본 기준으로 옳았다 — 1d 제외는
유지된다.

⚠️ **WAN-97의 「15m 제외」는 번복됐다 — 그 근거(관통 민감도 10.3× · OOS 심볼 편중)를
재인용하지 말 것.** 두 근거 모두 **15m을 1h와 가르지 못한다**(wan107.md §2·§3):
(1) **관통 민감도**의 10.3× vs 5.6×는 **전 구간** 값이고 **OOS에서는 9.6× vs 8.2×로 붙는다**
— 게다가 그 벌점을 다 물려도 `pen_5bp` OOS에서 15m(+11.38%)이 1h(+5.71%)를 이긴다.
(2) **OOS 심볼 편중**은 `baseline`에서 **사실이 아니다** — "15m BTC 마이너스(−3.81%)"는
`pen_5bp` 수치이고 `baseline`에선 **+4.14%**, "어느 TF도 OOS 3/3이 아니다"도 **15m이 3/3**
이라 거짓이다. ETH 편중·IS→OOS 순위 뒤집힘은 참이지만 **1h·4h에 똑같이 적용된다**(ETH를
빼면 15m +9.16%(2/2) > 1h +4.01%(1/2)).
⚠️ **단, 관통 민감도의 진단 자체는 살아 있다** — 수익이 "스치듯 닿은 체결"에 몰린다는
성질은 여전하고, 다만 **15m만의 것이 아니다**(1h도 OOS에서 수익의 30%가 관통 요구 한 번에
사라진다). 실제 해소는 틱·호가 데이터(WAN-98) 소관이다.
⚠️ **이 재판정은 렌즈 교체의 산물이 아니다** — 15m은 **OOS에서 세 렌즈 전부** 1h를 이긴다
(`baseline` +15.09%p, `pen_5bp` +5.68%p, `pen_5bp_drop_50` +3.88%p). 즉 **렌즈를 안 바꿨어도
15m이 앞섰다**. WAN-104의 오프셋 판정이 렌즈에 통째로 의존한 것과 **정반대 성격**이니
둘을 같은 부류로 묶지 말 것.
⚠️ **어느 TF도 "엣지가 있다"는 뜻이 아니다** — 공식 렌즈(`baseline`) OOS는 15m +23.26%(3/3),
1h +8.18%(2/3), 4h +2.85%(2/3)이고, 스트레스(`pen_5bp_drop_50`)에선 15m +3.94%(1/3) ·
1h +0.05%(1/3)로 사실상 0이다. **가장 덜 나쁜 후보**일 뿐이며 실거래 승인이 아니다
(`ALPHABLOCK_LIVE_TRADING=false` 유지, [WAN-86 결정 2](docs/decisions/wan86.md)).
⚠️ **`--years 3`은 마지막 봉 기준으로 창을 자르므로 새 캔들이 쌓이면 창이 미끄러진다**
(`backtest/harness.py:load_recent`) — 옛 리포트 CSV는 비트 단위로 재현되지 않는다(WAN-107
실행에서 WAN-97 대비 +8~12시간 이동, IS만 최대 0.68%p 변동·OOS 무변). 창을 못 박으려면
`--start`/`--end`를 쓸 것.

📌 **엣지 판정(WAN-88 확정): 채택 기본값에 통계적 엣지는 확인되지 않았다.**
🔁 ⚠️ **이 판정의 범위를 정확히 읽을 것 — 무력화 축이 「RSI 게이트」다.** WAN-124가 같은 자로
**볼린저**를 무력화 축에 놓고 다시 재자 **15m에서는 무작위와 구분됐다**(9/12 셀, 아래 📌
WAN-124 문단). **모순이 아니라 다른 질문**이고, 그쪽에도 「엣지 찾았다」로 읽지 말라는 경고가
세 겹 붙는다. 📌 **그리고 이 모듈의 널은 게이트가 없는 새 기본값에서는 돌릴 수 없다** — 이
표의 **대조군 풀이 곧 WAN-123 채택 기본값**이기 때문이다(같은 문단).
`backtest/reports/wan88_long_only_summary.md`(재현: `uv run python -m
backtest.wan88_long_only_validation`). `pen_5bp`에서 **유효 셀 16개 중
p≤0.05 & 실제>무작위평균인 셀은 0개**(OOS만 보면 7셀 중 0개)다. 즉 WAN-84(숏 포함)의
「엣지 없음」 결론은 **롱 온리 + 지정가 + 첫 탭 면제 교정 엔진에서도 유지된다** —
WAN-87의 숏 제거는 절대 수익률을 크게 끌어올렸지만(음수 셀 대거 양전환) **판정은
바꾸지 못했다**. 숏이 롱의 엣지를 가리고 있던 게 아니라 드러날 엣지가 없었다는 뜻이다.
⚠️ **대조표의 "유의 셀 0→4개"를 단독 인용하지 말 것** — 그 4개는 `baseline`(낙관) 축에서만
유의하고 **전부 15m이며, 관통 요구(`pen_5bp`) 한 번에 4개 전부 유의성을 잃는다**(거래는
평균 6.7%만 감소). 유의성이 "지정가를 스치듯 닿고 되돌아선 체결"에 통째로 기대고 있다는
뜻이고, 이는 WAN-97 §2-2가 수익률로 관찰한 관통 민감도를 **유의성 지표로 재확인**한 것이다.
⚠️ **이것을 「15m 제외」의 보강 근거로 읽지 말 것 — 그 제외는 번복됐다**(WAN-107, 위 📌 TF
문단). 관통 민감도 **진단**은 살아 있으나 그것이 **15m만의 성질이 아님**이 확인됐으므로,
이 관찰은 "15m을 빼라"가 아니라 **"공식 렌즈(`baseline`) 수치는 어느 TF든 상한으로 읽어라"**
로 읽는다. **이 판정은 "전략이 손실"이라는 뜻은 아니다** —
`pen_5bp` 1h OOS는 3/3 심볼 플러스(BTC +7.14%, ETH +17.43%, SOL +4.08%)지만, 그 수익이
**같은 오더블록에 무작위로 진입해도 나오는 수준**이라 값이 매겨지는 대상은 RSI·타이밍
규칙이 아니라 오더블록 존 자체이거나 시장 베타일 수 있다.

📌 **심볼 유니버스 = 12종목(BTC·ETH·SOL + BNB·XRP·TRX(WAN-111) + DOGE·LINK·LTC(WAN-182) +
ADA·DOT·BCH(WAN-307))**. 수집 대상 기본값(`config.settings._default_symbols`)이 12종목이다 —
**수집 대상일 뿐 실거래 대상이 아니다**(실시간 페이퍼 감시 `live_signal_symbols`는 이 목록을
상속해 12종목 — WAN-191 설계 · 여전히 페이퍼, 실거래 게이트는 별개 불변). 신규 심볼을
수집기에 올리지 않으면 그 심볼만 낡아 다음 격자에서 12종목이 서로 다른 창을 보게 된다.
펀딩은 12종목 전부 자기 확정 데이터가 채택 창을 덮는다(위 🔁 WAN-307 문단 — 옛 「신규
3종목 펀딩 0행」 경고는 WAN-292 백필·ADA·DOT·BCH 수집분 실측으로 해소됐다).

⚠️ **채택 수치는 3심볼 표본에 크게 기대고 있었다 — 심볼을 6개로 넓히면 사실상 0이 된다**
(WAN-111): `backtest/reports/wan111_symbol_universe_summary.md`(재현: `uv run python -m
backtest.archive.wan111_symbol_universe_report`, 요약만 재생성은 `--from-csv`). 공식 렌즈
(`baseline`) **OOS 심볼평균이 15m +19.01%→+3.91%(−15.10%p) · 1h +8.53%→+1.22%(−7.32%p)**
로 내려앉고, **`pen_5bp` 한 단계에 둘 다 마이너스**(−0.76% · −1.44%)가 된다. 창을
**2023-07-14~2026-07-15로 못 박고** 6심볼이 동일한 봉 수를 보도록 맞춘 격자다.
- **편중은 사실이다** — 15m OOS는 **ETH를 빼면 6심볼 평균이 −4.86%로 내려가고**(delta
  −8.77%p), 전 구간에서는 SOL이 같은 역할이다(delta −14.56%p). 1h OOS의 +1.22%도 BTC나
  ETH 하나만 빼면 마이너스다(−0.20% · −1.70%).
- ⚠️ **「신규 심볼이 나쁘다」로 인용 금지 — IS에서는 정반대였다.** 신규 3심볼의 1h IS는
  **+17.62%(3/3)로 기존 3심볼(+15.79%)보다 좋았고**, 그 표본이 OOS에서 −6.10%(0/3)로
  뒤집혔다. 「어느 심볼이 좋은가」가 IS→OOS로 넘어가지 않는다는 뜻이다(CLAUDE.md가 이미
  적어 둔 IS→OOS 순위 뒤집힘의 심볼 축 재확인).
- ⚠️ **유동성 가설로 설명되지 않는다** — 신규 심볼의 **체결률은 안 무너진다**(15m OOS
  51.96%→50.93%, 1h OOS 48.48%→**56.33%로 오히려 높다**). 주문은 잘 채워졌고 **그 채워진
  거래가 돈을 잃었다**. 손상은 체결이 아니라 손익 쪽이다(MDD는 1.6~2배로 악화).
- **이것은 WAN-88의 「엣지 없음」을 6심볼로 넓혀 재확인한 것**이지 새 판정이 아니다. 채택
  TF·기본값은 **바꾸지 않았다**(실거래 보류 `ALPHABLOCK_LIVE_TRADING=false` 유지).
- ⚠️ **이 표의 3심볼 수치를 `wan95_zone_limit_summary.md`와 비교하지 말 것** — 그쪽은
  `--years 3`(미끄러지는 창)이라 15m 전 구간이 67.04%인데 이 표는 못 박은 창이라 68.0%다.
  3 vs 6 대조는 **WAN-111 표 안에서만** 성립한다(그래서 3심볼도 같은 창에서 다시 돌렸다).

📌 **부품 분해(WAN-114): 진입 규칙 셋 중 값을 더하는 건 볼린저 하나뿐이고, 그것도 15m에서만이다.**
[`docs/decisions/wan114.md`](docs/decisions/wan114.md)(재현: `uv run python -m
backtest.wan114_entry_rule_ablation`, 요약만 재생성은 `--from-csv`). WAN-84/88/111의 매칭 널이
"규칙 진입이 무작위와 구분되지 않는다"까지만 말했다면, 이 표는 **어느 부품이 죽은 무게인지**를
지목한다. 존-단독(`L0`: 첫 탭 무조건 진입 · RSI 게이트 off · 볼린저 off)에서 채택 기본값
(`L2` = `ConfluenceParams()`)까지 부품을 하나씩 켠 사다리이고, 창은 WAN-111과 같은 못 박은
창(2023-07-14~2026-07-15)이다. **`L2`의 168셀이 `wan111_symbol_universe.csv`의 `baseline`
IS/OOS 행과 비트 단위로 일치**하므로 새 수치가 아니라 **같은 수치의 분해**다.
공식 렌즈(`baseline`) OOS 심볼평균 증분:
- **RSI 게이트는 마이너스다** — `−2.76%p`(15m) · `−2.28%p`(1h). 12셀(2TF × 2구간 × 3렌즈) 중
  **8셀 음수**, **1h는 6셀 전부 음수**. 유일한 일관된 플러스인 15m IS(+2.78%p)는 **OOS에서
  −2.76%p로 뒤집힌다**(IS→OOS 뒤집힘의 부품 축 재현). 게이트는 거래를 13~14% 쳐내는데
  **그 쳐냄이 손해**다 = 고르는 방향이 틀렸다.
- **재탭 노출도 마이너스다** — 12셀 중 **9셀 음수**(15m OOS −3.85%p, **0/6심볼 상승**, MDD +3.96%p).
- **볼린저만 플러스** — 15m OOS **+16.20%p**(5/6, 세 렌즈 전부 생존) / 1h OOS −1.18%p.
- **1h는 규칙 층 전체가 마이너스라 존-단독이 채택 기본값을 이긴다** — IS(+17.95% vs +16.70%) ·
  OOS(+2.48% vs +1.22%). 크기는 작지만(−1.2%p) **두 구간이 같은 방향**이다.
- ⚠️ **「그럼 존 자체가 값이다」로 인용 금지** — 15m OOS 존-단독은 **−5.68%(2/6심볼)** 다.
  이 표는 "존은 좋은데 규칙이 망친다"가 아니라 **"둘 다 신호가 없다"** 로 읽는다(WAN-88/111
  「엣지 없음」의 부품 단위 분해이지 반박이 아니다). 15m의 플러스(+3.91%)는 **볼린저 한
  부품**에서 온다(`L0`·`L0r`·`L1`이 OOS에서 전부 마이너스).
- ⚠️ **볼린저의 기여는 「선별」이 아니라 「가격」일 가능성이 크다** — 진입가를 낮춰 1R을 줄이면
  고정 1.5R 익절이 가까워진다(15m OOS 승률 +3.23%p · MDD −10.99%p로 실제 그렇게 움직인다).
  WAN-96의 "수익은 셋업이 좋아서가 아니라 더 좋은 가격에 들어가서 나온다"와 **같은 결론**이다.
  선별 대 가격은 이 표가 못 가른다(규칙 3만 켜고 진입가는 존 근단인 단이 없다).
  🔁 **WAN-131이 그 「선별 대 가격」을 직접 갈랐다 — 판정 (b) 가격이 대부분**(아래 📌 문단).
- ⚠️ **하필 그 부품에 알려진 룩어헤드가 있었고, 🔁 WAN-115/119가 재검했다 — 결론: 룩어헤드가
  아니었다.** 밴드가 탭 봉 SMA20(그 봉 종가 포함)인데 체결은 그 봉 내부다. WAN-115는 밴드를
  직전 확정봉으로 바꿔 증분이 +16.20%p → +11.32%p(−4.88%p)로 깎이는 것을 보고 그것을
  "룩어헤드의 몫"이라 불렀지만, **WAN-119가 그 이름을 개정했다**: 미래 정보만 빼고 현재 봉은
  쓰는 `intrabar_live`에서 증분은 **+16.70%p로 오히려 늘고** 채택 기본값도 **+4.41%**로
  살아 있다. WAN-114의 "재검 전까지 확정하지 말 것"은 **답을 얻었다**: 볼린저가 유일한
  플러스 부품이라는 결론은 **유지**되고, **그 기여는 룩어헤드가 아니다**(아래 📌 WAN-119
  문단이 정본).
- ⚠️ **체결률 ~51%는 시장이 아니라 규칙 층이 만든 것이다** — 존-단독은 **체결률 100%**(탭이 곧
  존 근단 터치라 첫 탭 무조건 진입은 정의상 항상 체결)이고, RSI 게이트가 100%→62%, 볼린저
  재산정이 62%→51%로 깎는다. WAN-95/96이 "체결률 ~50%가 낙관"이라 걱정한 그 수치의 **출처**다
  (큐 우선순위 걱정 자체는 유효).
- **기본값은 바꾸지 않았다** — 측정 이슈다(실거래 보류 유지). 권고는 wan114.md §5: (1) 볼린저
  룩어헤드 재검(엔진 정확성 1층) → (2) RSI 게이트 제거 여부(**사용자 결정** 필요 — WAN-81
  확정 규칙이다) → 그 뒤에 (3) WAN-90/113 익절 최적화(**기각이 아니라 순서** — 익절 층은 이
  사다리가 고정 입력으로 잡아 둔 층이라 가치가 부정되지 않았다). 🔁 **(1)은 WAN-115 →
  WAN-119 → WAN-120으로 닫혔고, (2)도 닫혔다** — 사용자가 **B(제거)** 를 골라
  **WAN-123이 기본값을 `rsi_gate_mode="unconditional"`로 옮겼다**(아래 📌 WAN-123 문단).
  다음은 (3)이다.
  ⚠️ **이 사다리의 「`L2` = 채택 기본값」은 WAN-123 이후 거짓이다** — 정확히는 **WAN-122까지의
  채택 기본값**이고, 사다리는 게이트를 `first_tap_free`로 **고정**한다(그래야 `L0r→L1`이라는
  단이 존재한다). CSV는 비트 단위로 재현된다.

📌 **볼린저 「선별 대 가격」 분리(WAN-131): 판정 (b) 가격이 대부분 — 볼린저의 +17.65%p 중
가격이 84% · 선별이 16%다. 볼린저는 「좋은 존을 고르는 것」이 아니라 대부분 「그냥 싸게 사는
것」이다.** [`docs/decisions/wan131.md`](docs/decisions/wan131.md)(재현: `uv run python -m
backtest.wan131_selection_vs_price --tf 1h --jobs 6` → `--tf 15m --jobs 6 --append`, 요약만은
`--from-csv`). WAN-114/145/151이 세 번 "볼린저가 유일한 플러스 부품"이라 했지만 그 기여가
「선별」인지 「가격」인지 못 갈랐다(WAN-124 §7-2가 후속으로 남김). 이 이슈가 옵트인 파라미터
`DeviationFilterParams.select_only`(밴드는 규칙 3 기각만 하고 진입가는 존 근단 유지)로 3팔
사다리(A 볼린저 off / B 선별만 / C 선별+가격 = 현행)를 세워 **선별 = B−A · 가격 = C−B**로
쪼갰다. 창·심볼·렌즈는 wan151과 같고 **존폭 필터는 껐다**(사용자 결정 · 필터도 「좁은 존
선별」이라 볼린저 가격 효과와 겹친다).
- 📌 **공식 렌즈 OOS 심볼평균**: 15m 선별 **+2.83%p**(5/6) · 가격 **+14.82%p**(6/6) · 총합
  **+17.65%p**(가격 84%) / 1h 선별 +0.49%p(IS 뒤집힘) · 가격 −4.40%p · 총합 −3.92%p(가격
  90%). **두 작업 TF 모두 (b)** — 1h에서 볼린저가 해로운 것도 재산정 탓이다.
- 📌 **가격 지문이 교과서적이다** — 15m OOS 가격 효과(C−B) +14.82%p에 **승률 +3.14%p · MDD
  −10.01%p**가 딸린다(진입가↓ → 1R↓ → 고정 1.5R 목표 가까워짐). 선별 효과는 승률을 거의
  안 바꾼다(+0.36%p). WAN-114가 "가격의 지문"이라 부른 그것이다.
- 📌 **드물게 ETH 편중이 아니다** — 15m OOS 총합 +17.65%p → ETH 제외 **+16.40%p**(5/5), 가격은
  6/6 전 심볼 플러스, 선별도 5/6. 「15m 플러스는 전부 ETH」(WAN-111/119/120/124/151)가 여기선
  성립 안 한다.
- 🚨 **검산**: A≡`L1`·C≡`L2`·Cadopt≡`L2u`(wan151) 72행 차이 0 · 볼린저 총합(C−A)이 wan151
  `L1→L2`와 정확히 일치(새 수가 아니라 그 수를 쪼갠 것) · B·C의 `eligible_setups` 24셀 차이 0
  (봉내 라이브에서 선별은 A→B에서 끝나고 C−B는 순수 재산정 — 회귀 테스트가 동작으로 고정).
- 🚨 **「엣지 찾았다」로 인용 금지** — 전부 `baseline`(낙관) 렌즈 위의 값이고, 하필 「가격」이
  가장 취약하다(C−B의 체결률 성분 = 밴드가에 닿는 체결 = 큐 우선순위상 가장 안 될 체결).
  볼린저 축 체결 보수화(`pen_5bp`)는 안 쟀다. **「엣지 없음」(WAN-84/88/111/114/124/151)은
  다른 질문**(*진입 규칙이 무작위와 구분되는가*)이라 뒤집힌 게 아니다. 선별 +2.83%p도 매칭
  널을 안 거쳐 「가격 하나만은 아니다」까지만 읽는다. **WAN-152의 존폭 축 (a) 선별과 섞지 말
  것** — 볼린저 축은 (b) 가격이다.
- **기본값·토대 불변**(측정 전용, 실거래 보류 유지). 다음: 볼린저 축 체결 보수화 · 선별
  +2.83%p 매칭 널 · 틱·호가(WAN-98).
- 🔁 **이 갈래는 WAN-352가 판정 관문에서 뺐다**(사용자 결정 2026-08-21 「싸게 사는 것도
  실력이다」) — **이 표는 그때의 기록으로 유효**하되 **후속 이슈가 이것을 「남은 관문」으로
  인용하지 않는다**. 📌 붙인 이름도 교정됐다: C는 B가 하는 거래의 **76%만 한다**(위 검산의
  「C−B는 순수 재산정」 옆에 wan131.md가 스스로 적어 둔 사실)이라 「가격」 칸에 이미 선별이
  섞여 있다 — 아래 📌 WAN-352 문단이 정본.

📌 **룩어헤드 재검(WAN-115): 밴드를 직전 확정봉으로 바꾸면 15m 플러스가 사라진다 — 판정 (b).**
🔁 **⚠️ 단 이 이슈의 「−4.88%p = 룩어헤드의 몫」이라는 이름과 「15m 플러스는 룩어헤드 없이는
남지 않는다」는 결론은 WAN-119가 개정했다 — 재인용 금지**(아래 📌 WAN-119 문단이 정본).
**숫자는 전부 유효하고 해석이 틀렸다**: `prev_closed`는 미래 정보를 빼는 동시에 **현재 봉을
통째로 버리는데**, 그 −4.88%p는 전자가 아니라 **후자의 대가**였다(WAN-119가 둘을 갈랐다).
[`docs/decisions/wan115.md`](docs/decisions/wan115.md)(재현: `uv run python -m
backtest.archive.wan115_bollinger_lookahead_recheck`, 요약만 재생성은 `--from-csv`). 밴드를 탭 봉
SMA20 대신 **직전 확정봉 SMA20**으로 바꾼 옵트인 모드(`DeviationFilterParams.band_bar`)로
채택 기본값을 재산출했다. 창·격자는 WAN-111/114와 같고, **`L1`·`L2` 행이
`wan114_entry_ablation.csv`와 비트 단위로 일치**하므로 같은 엔진의 재검이다.
- **직전 확정봉 밴드는 볼린저 증분의 약 1/3을 깎는다** — 공식 렌즈(`baseline`) OOS 15m
  **+16.20%p → +11.32%p**(69.9% 잔존, **−4.88%p**), IS +31.96%p → +21.31%p(66.7%). 잔존
  비율이 네 셀 중 셋에서 **66~70%로 붙어 있다**(TF·구간을 안 가린다). WAN-95의 "실무 영향은
  SMA20 한 틱 차이 수준"이라는 각주는 **틀렸다**(이 반박은 WAN-119 이후에도 유효하다 —
  밴드 봉 기준은 각주 크기가 아니다).
  ⚠️ **그 −4.88%p를 「룩어헤드의 몫」이라 부르지 말 것**(WAN-119: 미래 정보만 빼는
  `intrabar_live`는 오히려 **+0.50%p 번다** — 잃은 건 현재 봉의 실재하는 정보였다).
- ⚠️ **「70% 남았으니 괜찮다」로 인용 금지 — `prev_closed`에서는 성적표의 부호가 뒤집힌다.**
  증분이 아니라 절대 수익률을 보면 **15m OOS가 +3.91% → −0.97%**(플러스 심볼 3/6 → 2/6)다.
  이유는 단순하다: 볼린저 없는 바닥(`L1`)이 −12.29%라 **플러스의 여유가 4%p도 없었다**.
  편중도 더 깊어진다 — ETH(+40.44%)를 빼면 5심볼 평균 **−9.25%**(`L2`의 −4.86%에서 하락).
  ⚠️ **이것을 「15m 플러스는 룩어헤드의 산물」로 읽지 말 것** — 그 플러스는 사용자가 실제로
  보는 밴드(`intrabar_live`)에서 **+4.41%로 살아 있다**(WAN-119). `prev_closed`가 유독
  나쁜 밴드일 뿐이다.
- **손상은 「선별」이 아니라 「가격」이다** — 밴드 봉 기준을 바꿔도 거래는 거의 안 바뀐다
  (15m OOS 231.3 → 238.2건 **+3.0%** · 체결률 51.44%→52.19%). **같은 셋업에 같은 빈도로
  들어가는데 수익만 −4.88%p 빠진다.** WAN-114가 "가르지 못한다"고 남긴 선별 대 가격
  질문에서, **밴드 정의의 몫은 순수하게 가격**임이 확인됐다(WAN-96 진단과 같은 결론).
  이 관찰은 **WAN-119 이후에도 유효하다**(그쪽도 거래 +1.9%인데 수익만 움직인다).
- **렌즈에 의존하지 않는다** — 15m OOS `prev_closed`의 손실은 세 렌즈 전부 같은 방향
  (`baseline` −4.88%p · `pen_5bp` −5.67%p · `pen_5bp_drop_50` −3.13%p). **WAN-104의 오프셋
  판정과 같은 부류로 묶지 말 것**(그건 렌즈에 통째로 의존했다).
- ⚠️ **1h OOS의 「잔존 비율」을 계산하지 말 것** — 기준 증분이 음수(−1.18%p)라 비율이 뜻을
  잃는다(교정이 −2.03%p로 **더 깎는데도** 172%가 나와 "유지"로 읽힌다). 그 셀은 **증분의
  부호**만 본다. 리포트 코드가 이 부호 함정을 가드+테스트로 막는다.
- ⚠️ **「그럼 볼린저를 빼라」로 인용 금지** — 교정 후에도 볼린저는 15m에서 **+11.32%p를 더한다**
  (5/6심볼 · 세 렌즈 전부 생존). 유일한 플러스 부품이라는 WAN-114 결론은 **유지**다.
- ⚠️ **「그럼 15m을 빼라」로도 점프 금지** — 교정 후에도 15m 증분(+11.32%p)이 1h(−2.03%p)를
  이긴다(WAN-107 번복 근거는 유효).
- **이 이슈는 기본값을 바꾸지 않았다**(당시 `band_bar="tap"` 유지) — 밴드 정의를 기본값에서 바꾸는 것은
  WAN-70/84/88/95/111/114의 엔진 정의를 흔드는 **명시적 재-베이스라인**이고 **사용자 결정**
  이다. ⚠️ 단 **「엣지 없음」 판정(WAN-84/88/111)은 바뀌지 않는다.**
  🔁 **권고 대상은 이제 `prev_closed`가 아니라 `intrabar_live`다**(WAN-119 §6).

📌 **봉내 라이브 밴드(WAN-119): 15m 플러스는 룩어헤드 없이도 남는다 — 판정 (a) 되살아남.**
[`docs/decisions/wan119.md`](docs/decisions/wan119.md)(재현: `uv run python -m
backtest.archive.wan119_intrabar_live_band`, 요약만 재생성은 `--from-csv`). 사용자 관찰("트레이딩뷰는
봉 시작과 동시에 띠가 생기고 봉이 움직이는 대로 값이 달라진다")대로, SMA20의 20번째 표본에
**체결 순간의 현재가**를 넣는 셋째 밴드 정의(`band_bar="intrabar_live"`)를 만들어 3자 비교했다.
창·격자는 WAN-111/114/115와 같고 **`L1`·`L2`·`L2p` 행이 `wan115_bollinger_recheck.csv`와
비트 단위로 일치**한다(`L2→L2p` 거래 수 +3.0%가 그대로 재현되는 것이 검산).
- 📌 **WAN-115의 −4.88%p는 「룩어헤드의 몫」이 아니었다 — 이것이 이 이슈의 핵심이다.**
  `prev_closed`는 **두 가지**를 한다: 미래 정보를 빼고(원한 것) + **현재 봉을 통째로
  버린다**(부작용). 전자가 원인이면 **미래만 빼는** `intrabar_live`도 같이 잃어야 하는데
  **오히려 +0.50%p 번다**. 즉 그 손실은 **현재 봉의 실재하는 정보를 버린 대가**이고, 그
  정보는 실거래에서 **화면에 떠 있다**. `prev_closed`는 룩어헤드 없는 반사실이 아니라 그냥
  **더 나쁜 밴드**다(눈을 하나 감은 밴드).
- **공식 렌즈(`baseline`) 15m OOS 채택 기본값**: `tap` +3.91%(3/6) · `prev_closed`
  **−0.97%**(2/6) · **`intrabar_live` +4.41%(3/6)**. 증분은 `L1→L2i` **+16.70%p**로 `tap`
  (+16.20%p)을 넘어 **110.2% 회복**한다(IS는 80.7%).
- 🔁 **~~렌즈에도 더 강하다~~ — WAN-120이 이 주장을 뺐다. 재인용 금지**(아래 📌 WAN-120 문단이
  정본). 원문은 "15m OOS `pen_5bp`에서 **L2i +1.30% vs L2 −0.76%** — 관통을 요구하면 채택
  기본값은 마이너스로 넘어가는데 라이브 밴드는 버틴다"였는데, **그 버팀이 잔여 1분 룩어헤드의
  산물**이었다(엄격 인과 `L2c`에서 **−0.37%로 부호가 뒤집힌다**). 세 렌즈 전부 L2i > L2 > L2p
  라는 **사실 자체는 유효**하나 그것을 `intrabar_live`의 **강건성 근거로 쓸 수 없다**.
- ⚠️ **1h는 정반대다 — 라이브 밴드가 제일 나쁘다**(OOS `baseline` L2i **−0.41%** < L2 +1.22%
  < **L1(존 단독) +2.40%**, 세 렌즈·두 구간 동일 방향). 단 **밴드 정의로 1h를 판단하지 말 것**
  — 그 축은 존-단독이 이미 채택 기본값을 이기므로 문제는 **규칙 층 전체**다(WAN-114 결론).
- ⚠️ **「엣지 없음」(WAN-84/88/111)은 바뀌지 않는다 · 「15m이 좋다」로 인용 금지** — +4.41%는
  **ETH 하나가 만든다**(ETH +37.71%, 빼면 5심볼 **−2.25%**). 플러스 심볼 3/6이고 스트레스
  렌즈에선 −0.29%다. 이 표는 **밴드 정의를 바로잡았을 뿐 신호를 찾은 게 아니다.**
- ⚠️ **1분봉 근사이지 틱이 아니다 — 잔여 1분 룩어헤드가 남는다**(밴드는 close(t)로 계산되는데
  체결 판정은 같은 1분봉 안이다. `RealtimeRsi`가 체결 스텝에서 쓰는 관행과 같지만, RSI는
  게이트이고 밴드는 **가격**이라 성격이 같지 않다). §5가 2심볼로 "작고 방향이 갈린다"고 봤고
  (BTC 손익 +1.9% · ETH −8.5%), 🔁 **WAN-120이 그 격자 재측정을 끝냈다 — 판정은 안 흔들린다**
  (아래 📌 문단). 그 2심볼 진단은 **격자에서 거의 그대로 재현됐다**(BTC +1.52%p · ETH −6.90%p).
- **이 이슈는 기본값을 바꾸지 않았다**(당시 `band_bar="tap"` 유지, 실거래 보류 유지) — 권고는
  wan119.md §6: **`intrabar_live`를 정본으로 올리는 재-베이스라인(사용자 결정)**.
  🔁 **그 권고는 WAN-132가 수락해 실행했다 — 지금 기본값은 `intrabar_live`다**(아래 📌 문단). 최적화가 아니라 **정확성**
  이다(`tap`은 그 봉 종가를 알아야 나오는 재현 불가능한 가격 · `intrabar_live`는 사용자가
  보고 거는 그 가격). 비용은 15m OOS +0.50%p·IS −2.05% / **1h OOS −1.62%p·IS −2.89%p**.

📌 **엄격 인과 밴드(WAN-120): 잔여 1분은 판정을 흔들지 않는다 — 단 렌즈 주장 하나는 빠진다.**
[`docs/decisions/wan120.md`](docs/decisions/wan120.md)(재현: `uv run python -m
backtest.archive.wan120_strict_causal_band`, 요약만 재생성은 `--from-csv`). WAN-119 §6-3이 남긴 격자
재측정이다 — 밴드 표본을 **직전 1분봉 종가**로 미뤄 잔여 룩어헤드를 **0**으로 만든 넷째 정의
(`band_bar="intrabar_causal"`, 옵트인)로 4단 사다리(`L1`·`L2`·`L2i`·`L2c`)를 돌렸다. 창·격자는
WAN-111/114/115/119와 같고, **`L1`·`L2`·`L2i`의 504행이 `wan119_intrabar_live_band.csv`와
비트 단위로 일치**한다(10개 지표 전부 차이 0).
- **판정 유지** — 공식 렌즈(`baseline`) 15m OOS 채택 기본값 `intrabar_live` +4.41% →
  **`intrabar_causal` +2.49%(3/6)**, 증분 +16.70%p → **+14.77%p(88.5% 잔존)**. IS도 같다
  (+21.80% → +17.63% · 86.0%). **WAN-119의 판정 (a)는 잔여 1분에 기대고 있지 않다.**
- 🔁 **단 WAN-119의 「렌즈에도 더 강하다」는 개정됐다**(위 문단) — 15m OOS `pen_5bp`에서
  **L2i +1.30% → L2c −0.37%로 부호가 뒤집힌다**. "관통을 요구해도 라이브 밴드는 버틴다"는
  성질이 **그 1분이 만들던 것**이었다(WAN-96 비대칭과 같은 자리 — `pen_5bp`가 지우는 "스치듯
  닿은 체결"을 잡아내는 능력이 그 분을 조금 아는 데서 나왔다). **핵심 판정은 유지되고 렌즈
  우위 주장만 빠진다.**
- 📌 **`intrabar_causal`는 정본 후보가 아니다 — 감사 계기다.** A안(봉 단위)에는 "직전 1분봉
  종가"가 **존재하지 않아 ValueError로 거부한다**(`deviation_band_at`) — 조용히 `tap`으로
  접으면 "인과 라벨을 달고 룩어헤드 값을 돌리는" WAN-95/100 부류의 사고가 된다. 정본으로
  올리면 대시보드·`live.runner`·스윕이 전부 깨진다. **실질 결정 공간은 `tap` vs
  `intrabar_live`** 이고 이 표는 그 선택의 **감사**다(`intrabar_live`는 A안에서 `tap`과 같은
  값이라 통과한다).
- ⚠️ **1h IS의 증분 부호 반전(+0.07%p → −1.55%p)을 「흔들림」으로 인용 금지** — 리포트 코드는
  부호만 보므로 기계적으로 그렇게 찍지만 **+0.07%p는 0과 구분되지 않는다**(뒤집힌 게 아니라
  원래 0이었다). 게다가 **1h는 밴드 정의로 판단할 축이 아니다**(WAN-114/119).
- **손상은 또 「가격」이다** — 거래 +1.7%인데 수익만 −1.93%p(15m OOS, `mean_r` 0.1798→0.1607).
  ⚠️ **「엣지 없음」(WAN-84/88/111)은 그대로** — `L2c` +2.49%도 **ETH가 만든다**(ETH +30.81%,
  빼면 5심볼 **−3.18%**).
- ⚠️ **전환 시 파급이 WAN-112와 다르다 — `band_bar`는 아무 리포트도 고정하지 않았다**(오프셋은
  WAN-88/96/103/110이 0bp로 명시 고정했다). 기본값을 바꾸면 B안 리포트(wan84/88/95/96/99/104/
  110/111/114)가 **조용히 새 밴드로 돈다** — 재산출 또는 `band_bar="tap"` 명시 고정이 필요하다
  (A안 고정 리포트 wan87/91은 무관 — A안에선 `intrabar_live` = `tap`). 목록은 wan120.md §6-2.
  🔁 **WAN-132가 그 파급을 실제로 처리했다**(고정 28개 모듈 + wan95만 재산출, 아래 📌 문단).
- **이 이슈는 기본값을 바꾸지 않았다**(당시 `band_bar="tap"` 유지, 실거래 보류 유지) —
  **엔진 정확성(1층)은 이 이슈로 닫힌다**. 다음은 WAN-116(RSI 게이트, 사용자 결정) →
  WAN-90/113(익절 측정).
  🔁 **WAN-116은 닫혔다 — 사용자가 B(제거)를 골랐고 WAN-123이 실행했다**(아래 📌 문단).
  🔁 **밴드 정본 결정도 닫혔다 — 사용자가 `intrabar_live`를 골랐고 WAN-132가 실행했다.**

📌 **진입가 정본 = 봉내 라이브 밴드(WAN-132 = 사용자 결정, WAN-119/120 권고 수락):
`band_bar` 기본값이 `"tap"` → `"intrabar_live"`다.**
[`docs/decisions/wan132.md`](docs/decisions/wan132.md). 사용자 결정("132는 tap 말고
intrabar_live로 갈거야"). **최적화가 아니라 정확성이다** — `tap`은 **그 봉이 어떻게 끝날지
알아야 나오는 가격**(탭 봉 최종 종가를 포함한 SMA20)인데 체결은 그 봉 **내부**라 실거래에서
재현할 수 없었고, `intrabar_live`는 사용자가 트레이딩뷰에서 보고 지정가를 거는 **바로 그
값**이다. **새 격자는 돌리지 않았다** — 근거는 WAN-119/120이 이미 낸 표다.
- ⚠️ **「수익이 올라서 바꿨다」로 인용 금지** — 전환 비용은 **15m OOS +0.50%p이지만 1h OOS
  −1.62%p**다(IS는 15m −2.05% · 1h −2.89%p). **1h는 알고 받는 손해**이고, 다만 **1h를 밴드
  정의로 판단하지 말 것**(그 축은 존-단독이 채택 기본값을 이미 이겨 문제가 규칙 층 전체다).
- ⚠️ **「엣지 없음」(WAN-84/88/111/114/124)은 그대로다** — 15m OOS +4.41%는 **ETH 하나가
  만든다**(빼면 5심볼 −2.25%, 스트레스 렌즈 −0.29%). 밴드 정의를 바로잡았을 뿐이다.
- 📌 **「틱 데이터가 오면 해결된다」는 틀렸다**(사용자 질문에 대한 기록) — 틱 해상도는
  `intrabar_live`의 **잔여 1분**만 없애고(WAN-120: 증분 88.5% 잔존, 판정 불변), **`tap`의
  룩어헤드는 정의상 데이터로 해소되지 않는다**(15분 뒤 종가는 어떤 해상도에서도 미래).
  게다가 WAN-98(틱 수집)은 **Canceled**라 기다리면 무기한이다.
- 📌 **파급 처리(WAN-112/123과 같은 패턴이되 더 넓다)**: `band_bar`는 **아무 리포트도 고정해
  두지 않았으므로**, 옛 수치를 결론에 박아 둔 **28개 모듈을 `harness.LEGACY_BAND_BAR`로 명시
  고정**하고 **채택 성과(`wan95_zone_limit_summary.md`)만 재산출**했다. A안 전용 리포트
  (wan81/87/91 · wan95 종가 파트)는 **무관**이다(봉 단위에서 `intrabar_live` = `tap`이라
  고정할 것도 움직일 것도 없다). ⚠️ **wan123_\*(= WAN-124 판정)도 예외적으로 고정했다** —
  두 모듈의 상호 검산(48셀 차이 0)과 wan111 CSV 비트 일치가 `tap`을 전제하기 때문이다.
  새 밴드에서의 널 재검은 **별도 이슈**다. 표는 wan132.md §4.
- 🚨 **이 밴드는 일부 엔진 기능을 `ValueError`로 거부한다 — 후속 이슈가 먼저 부딪힐 자리다**:
  `min_rr` 게이트 · `take_profit_override` · `stop_loss_override` · `width_kind="atr"` ·
  `source != "close"`(진입가가 봉내에 정해져 탭 봉 시점에 그 값들을 알 수 없다). 특히
  **WAN-127(익절 Phase 2)은 익절 오버라이드 경로를 쓸 가능성이 큰데 그 경로가 채택
  기본값에서 막혔다** — 봉내 경로에 배선할지, `tap` 고정으로 측정하고 한계를 명시할지를
  **먼저 정해야 한다**(wan132.md §5).
- **다른 기본값·토대는 불변**(오프셋 2bp · `unconditional` 게이트 · 고정 1.5R · 롱 온리),
  **실거래 보류 유지**(`ALPHABLOCK_LIVE_TRADING=false`). 이 결정이 막고 있던 WAN-131/127/
  135/136의 차단이 풀린다 — 넷 다 **새 밴드 위에서** 돌아야 한다.

📌 **존 병합 폐지(WAN-149 = 사용자 결정 · 구 WAN-141 흡수): `combine_obs` 기본값이 `True` →
`False`(원본 존 단위 분리)다.** [`docs/decisions/wan149.md`](docs/decisions/wan149.md). 사용자
결정("나는 모델에서도 분리를 반드시 해야할 것 같고" → "그냥 기본값을 그걸로 하고 웹페이지 다시
만들자"). 겹치는 오더블록을 하나로 접지 않는다. `True` 옵트인 경로는 **존치**한다.
- 🚨 **「데이터가 분리를 골랐다」로 인용 금지** — WAN-134 부검의 공식 판정은 **(c)「병합은
  손절률에 중립」 · 채택 권고 없음 · 기본값 불변**이었고 **그 판정은 뒤집히지 않았다**.
- 📌 **§3 4TF 격자 판정: 두 작업 TF 모두 (c) 「구간에 갈린다」이고 방향이 서로 반대다**
  (`wan149_merge_grid_summary.md`, 6심볼 · 못 박은 창 · `baseline` 단독 · 오늘의 채택
  기본값 위). 심볼평균 `total_return` ON→OFF: **15m IS Δ−0.35%p / OOS Δ+2.19%p** ·
  **1h IS Δ+5.82%p / OOS Δ−0.72%p**. 4h(심볼당 19.7거래)·1d(2.0거래)는 **표본 게이트가
  코드에서** 「⚠️ 판정 불가(대조군)」를 찍는다(WAN-143 게이트 재사용, WAN-109 소관).
- 🚨 **15m OOS의 「수익 상승」 단독 인용 금지 — 플러스 심볼은 오히려 줄었다**(OOS 3/6 → 2/6,
  IS 4/6 → 3/6). 평균을 올린 것은 소수 심볼이고 「더 많은 심볼에서 통한다」가 아니다.
- ⚠️ **편중 그대로** — ETH를 빼면 15m OOS +7.84% → **+0.01%** · 1h OOS +2.25% → **−4.93%**.
  **체결률은 거의 불변**(77.7→78.0%)이라 손상/개선은 체결이 아니라 손익 쪽이다.
- ⚠️ **두 팔은 아예 다른 거래를 한다**(거래 +4~8%) — 수익 변화가 규칙 때문인지 베팅을 더 해서인지
  이 표는 **못 가른다**. 그 차이는 「병합이 존폭의 대리변수」(wan134.md:45)라 **존폭 효과일 수
  있고**, 거래 수를 맞춘 재검은 **WAN-142** 소관이다(이 결정은 그 답을 기다리지 않았다).
- 📌 **§4 존 수명(구 WAN-141): 두 작업 TF 모두 (b) — 「같은 존에 여러 번 진입하면 더 잘 깨진다」는
  성립하지 않는다.** `wan149_zone_lifetime_summary.md`(존 1행 23,832행 · Kaplan-Meier + 위험률).
  **진입 축** 기울기 15m **+0.056**(p=0.190) · 1h **+0.009**(p=0.798), 위험률은 회차 1 이후
  71~74%로 평평하다. WAN-138 진단이 **생존 편향을 걷어낸 뒤에도 유지**된다.
  📌 **leave-one-out 6/6 같은 부호**라 손익 축과 달리 **ETH가 만드는 결과가 아니다**.
- 🚨 **§4의 「전곡선 기울기」를 인용 금지 — `h(0)`은 다른 모집단이다.** 회차 0을 넣으면 1h가
  **+0.138(p=0.001)로 (a) 성립**처럼 보이는데, 회차 ≥1만 보면 **+0.009**(회차 1~4는 −0.004)로
  사라진다. `h(0)`의 위험집합은 탐지된 **모든** 존이고 그 대부분은 **진입조차 못 한 존**이라
  (무효화 존의 **15m 77.7% · 1h 72.4%**가 진입 0회) `h(1)` 이상과 같은 모집단이 아니다 —
  그 **모집단 교체가 기울기로 둔갑**한다. 판정은 `MIN_LEVEL_ENTERED=1`에서 내고 회귀 테스트가
  이 함정을 동작으로 막는다.
- ⚠️ **「위험률이 안 오른다」를 「존이 안전하다」로 읽지 말 것** — 수준 자체가 67~75%이고
  **무효화 비율이 15m 96.6% · 1h 93.1%** 다. 거의 모든 존이 결국 깨지되 **그 시점이 진입
  회차와 무관**할 뿐이다.
- 📌 **세 카운트 간극**(탭 → 체결 가능 → 진입): 볼린저가 탭의 **~62%를 걸러내고**(체결 가능
  37~38%), 동시 1포지션 제약이 그 중 **또 절반 안팎을 깎는다**(진입/체결 가능 15m 43.3% <
  1h 52.2% < 4h 63.1% — TF가 짧을수록 더 깎인다). ⚠️ **다중 포지션 채택 근거로 쓰지 말 것**
  (WAN-108/130 소관, WAN-110이 15m 다중 우위가 낙관 렌즈에 기댐을 보였다) — 자료로만 기록.
- 📌 **파급 처리(WAN-132와 같은 부류이되 더 넓다)**: `combine_obs`는 **탐지 파라미터**라 옛
  리포트 대부분이 기본값에 맡겼다(`wan*.py` 39개 중 명시한 것은 3개뿐). 고정하지 않으면
  **조용히 분리 엔진으로 다시 돌아** 본문과 어긋난다 — 「바꿨다고 믿으면서 안 바뀐 것」의
  **거울상**이다. **34개 모듈을 `harness.LEGACY_COMBINE_OBS`로 명시 고정**하고 **채택 성과
  (wan95)만 재산출**했다. wan134/wan77/wan81은 **이미 명시**해서 무변경(특히 wan134는 이
  축이 실험 변수라 고정하면 실험이 깨진다). 목록은 wan149.md §2-3.
- 📌 **`--combine-obs` 축이 생겼다 — 다른 축과 성격이 다르다.** 유일한 **탐지** 파라미터 축이라
  값마다 오더블록을 **다시 탐지**한다(`_run_cell`이 값별 캐시를 둔다). 두 팔이 같은
  `OrderBlockResult`를 공유하면 **라벨만 다른 같은 숫자**가 나온다. `RunRow.combine_obs`는
  실제로 탐지에 넘어간 객체에서 읽는다.
- 📌 **차트 버그가 부수적으로 해소됐다**(사용자 발견: *"OB도 없는데 어떻게 진입한거야"*).
  병합 시그널은 합쳐진 존(`top=max`·`bottom=min`)을 싣는데 차트 목록은 **원본 단위 아카이브**라
  `zone_key`가 어느 원본과도 안 맞아 근거 박스가 **어떤 필터로도 안 보였다**. 분리하면 맞아떨어진다.
  ⚠️ **버그 자체가 고쳐진 건 아니다 — 옵트인 경로에는 그대로 남는다**(개발자 판단: 근본 수정 대신
  문서화 + 테스트로 한계 고정. `zone_key`를 느슨하게 하면 서로 다른 존이 같은 키를 갖는다).
- ⚠️ **엣지 판정 계열이 병합 시절 기록으로 얼어붙는다** — WAN-84/88/111/114/124/145는 전부
  고정됐다. 새 기본값 위의 널·부품 분해는 **별도 이슈**다(WAN-132 → WAN-145와 같은 패턴).
- **다른 토대는 불변**(오프셋 2bp · `intrabar_live` · `unconditional` · 고정 1.5R · 롱 온리),
  **실거래 보류 유지**(`ALPHABLOCK_LIVE_TRADING=false`).

📌 **재탭 RSI 게이트 제거(WAN-123 = WAN-116 결정 B): 기본값이 `rsi_gate_mode="unconditional"`
(게이트 없음)이다.** [`docs/decisions/wan123.md`](docs/decisions/wan123.md). 사용자 결정
("그래 빼자"). 근거는 WAN-114 ablation의 `L0r→L1`(게이트만의 순수 기여)이 **12셀 중 8셀 음수 ·
1h는 6셀 전부 음수**이고, 유일한 일관 플러스인 15m IS(+2.78%p)가 **OOS에서 −2.76%p로
뒤집힌다**는 것 — 게이트는 거래를 13~14% 쳐내는데 **그 쳐냄이 순손해**였다.
- ⚠️ **`"none"`이 아니라 `"unconditional"`이다 — 둘은 동의어가 아니다.** `"none"`은 게이트
  **판정**만 없앨 뿐 두 진입 경로의 워밍업 요구(`rsi is not None`)를 남겨 **RSI 워밍업 구간의
  탭을 계속 막는다**(WAN-114 `L0r`이 그 의미로 CSV에 고정돼 있다). 게이트를 끄려던 자리에
  `"none"`을 넣으면 **껐다고 믿으면서 워밍업 필터만 남는다** — WAN-91/95/112와 같은 부류의
  조용한 실패다. 회귀 테스트가 라벨이 아니라 **동작**으로 막는다.
- **되돌릴 여지**: `rsi_gate_mode="first_tap_free"` 옵트인 = 옛 규칙(WAN-81) 그대로.
- ⚠️ **「엣지 없음」(WAN-84/88/111)이 뒤집혔다는 뜻이 아니다 — 아직 안 쟀다.** 게이트 제거는
  드래그를 뗀 것이지 신호를 더한 게 아니다. **매칭 널·체결 보수화 재검은 미실행**이고
  (WAN-123의 2·3단, 새 모듈 `wan123_*` 소관), 그전까지 새 엔진의 엣지 판정은 **없다**.
- 📌 **체결률이 ~51% → ~81%로 뛴다 — 그리고 이건 WAN-114가 예고한 값이다**(재산출된
  `wan95_zone_limit_summary.md`). WAN-114 분해가 *"체결률 ~51%는 시장이 아니라 규칙 층이
  만든 것 — 존-단독은 100%, RSI 게이트가 100%→62%, 볼린저가 62%→51%"*라 했고, 게이트를 빼자
  그 몫이 돌아왔다. **두 리포트가 서로를 검산한다**(게이트가 실제로 꺼졌다는 독립 증거).
  ⚠️ 단 **큐 우선순위 걱정은 그대로다** — 81%도 `baseline`("닿으면 체결") 위의 값이다.
- ⚠️ **파급이 WAN-112보다 크다 — 거래 집합 자체가 넓어진다**(오프셋은 같은 거래의 가격만
  옮겼다). 재산출된 wan95 기준 지정가 거래 **+19~27%**. ⚠️ WAN-114의 "13~14% 감소"는 **창을
  못 박은 6심볼 격자** 값이라 이 수치와 직접 비교 금지. 게이트가 켜진 수치를 결론에 박아 둔
  리포트는 전부 `rsi_gate_mode="first_tap_free"` **명시 고정**했다
  (`harness.LEGACY_RSI_GATE_MODE`): wan81/83/84/87/88/91/96/99/103/104/110/111.
  **채택 성과(wan95)만 재산출**했다. 목록·이유는 wan123.md §4.
  ablation 사다리(wan114/115/119/120)는 단마다 게이트를 이미 명시해 **CSV가 비트 단위로
  재현**되지만, 그 표들의 **"`L2` == 채택 기본값" 라벨은 이제 거짓**이다(정확히는
  "WAN-122까지의 채택 기본값").
- ⚠️ **재산출 전후 델타를 「게이트 효과」로 읽지 말 것** — `--years 3` 창이 하루 미끄러진 것이
  섞여 있다. 게이트만의 격리된 기여는 창을 못 박은 WAN-114 `L0r→L1`이 정본이다.
- **실거래 보류 유지**(`ALPHABLOCK_LIVE_TRADING=false`). 🔁 **2·3단은 WAN-124가 닫았다**
  (아래 📌 문단) → 다음은 **WAN-90**(익절 측정).

📌 **게이트 제거 엔진 재검(WAN-124 = WAN-123의 2·3단): 「엣지 없음」은 게이트 off에서 재검정이
아니라 전제였다 — 그래서 널의 축을 옮겼다.** [`docs/decisions/wan124.md`](docs/decisions/wan124.md)
(재현: `uv run python -m backtest.wan123_matched_null` · `uv run python -m
backtest.wan123_fill_conservatism`, 요약만 재생성은 `--from-csv`).
- 📌 **WAN-88식 널을 「그대로 다시 돌린다」는 것은 불가능하다 — 그 널의 대조군이 곧 지금의 채택
  기본값이다.** 게이트가 없으면 시뮬레이터가 RSI를 **읽지도 않으므로**(`backtest/substep.py`
  단락 평가) WAN-70/88의 무력화 오버라이드(`rsi_oversold=100`)가 **아무것도 하지 않고**, 풀이
  실제 후보 집합과 **글자 그대로 같아진다**(BTC 1h 실측: `first_tap_free` 33 vs 52 →
  `unconditional` **52 vs 52**). 그대로 돌리면 널이 **자기 자신을 검정**하면서 p값을 멀쩡히
  뱉고 **이슈가 예상한 답**("엣지 없음 유지")을 낸다 — **틀린 이유로 맞는 답**(WAN-91/95/100/112
  부류). 뒤집어 읽으면 **WAN-88이 대조군으로 쓰던 52건 유니버스를 지금 매매하고 있는 것**이라,
  「엣지 없음」은 게이트 off에서 **전제**다. `run_random_control_b_segment`가 풀 == 실제 파라미터를
  **ValueError로 거부**해 이 퇴화를 동작으로 막는다(옵트인 `pool_params`라 WAN-70/84/88 CSV는
  비트 단위로 재현).
- **그래서 무력화 축을 볼린저**(게이트 제거 후 남은 유일한 선별 규칙)로 옮겼다. 판정: 공식
  렌즈(`baseline`) 유효 24셀 중 **11셀 유의** — **15m 9/12 vs 1h 2/12**. **WAN-114를 완전히
  다른 통계량으로 재확인**한 것이다(그쪽 볼린저 증분 15m OOS **+16.20%p** vs 1h OOS
  **−1.18%p** — 순위가 정확히 일치). 15m은 **무작위 평균이 깊은 마이너스**(−10~−26%)라 볼린저가
  손실을 피하고, 1h는 무작위가 **플러스**(+3~+14%)라 실제가 더 벌지 못한다.
- ⚠️ **WAN-88과 결정적으로 다르다 — 이 유의성은 체결 가정의 산물이 아니다.** WAN-88의
  `baseline` 유의 4셀은 `pen_5bp` 한 번에 **전멸**했는데 이 표는 **11셀 → 10셀**로 살아남는다.
- 🚨 **그래도 「엣지 찾았다」로 인용 금지 — 경고가 세 겹이다**: (1) **유의 ≠ 수익** — 15m OOS
  유의 4셀 중 **BNB는 −17.20%로 잃으면서** 무작위(−26.07%)를 이긴다(플러스는 3/4, **TRX는
  무작위보다 나쁘다**). (2) **ETH 편중 그대로** — 15m OOS 심볼평균 +8.63% → **ETH 빼면
  −1.81%**(1h는 +5.28% → −0.25%). (3) **「선별」과 「가격」을 못 가른다** — 풀은 존 근단가,
  실제는 밴드가다. WAN-96/114/115/120이 전부 **가격** 쪽을 가리키므로 **「가격 효과의 재확인」
  으로 먼저 읽는다**. 가르는 단은 이 표에도 WAN-114 사다리에도 없다(볼린저의 선별과 가격은
  같은 연산이라 코드에서 분리되지 않는다 — **후속 이슈 소관**).
- ⚠️ **「엣지 없음」(WAN-84/88/111)은 뒤집히지 않았다 — 다른 질문이다.** 그쪽은 **RSI 타이밍**을
  물어 "아니오"를 얻었고 이 표는 **볼린저**를 물었다. 같은 자(거래 20건 · p≤0.05 &
  실제>무작위평균)지만 **무력화 대상이 다르다**. WAN-114가 이미 부품 단위로 같은 말을 했다.
- 📌 **3단(체결 보수화): 수익은 「스치듯 닿은 체결」에 실려 있다 — 다만 「늘어난 거래」가 아니라
  엔진 전체가 그렇다.** 채택 팔 15m OOS는 관통을 요구하면 **거래의 94.29%가 남는데 수익은
  3.30%만 남는다**(+8.63% → +0.28%; 1h는 95.46% / 35.10%). ⚠️ **그런데 증분은 기존 거래보다
  덜 민감하다** — 옛 팔은 부호가 뒤집히는데(+3.91% → −0.76% = 잔존 −19.4%) 증분은 **22.3%(15m)
  · 81.0%(1h)** 를 지킨다. 게이트가 쳐내던 거래는 드래그였을 뿐 아니라 **유독 마진 체결에 기댄
  것도 아니었다**. ⚠️ 델타는 복리 수익률 차이이지 「새 거래만의 손익」이 아니므로 **크기가 아니라
  방향**으로만 읽을 것.
- 📌 **체결률 51.44% → 81.39%(15m OOS)로 WAN-114 예고값이 그대로 나왔고, 그래서 큐 우선순위
  걱정은 준 게 아니라 커졌다** — 81%는 전부 `baseline`(낙관) 위의 값이라 **채택 팔 15m OOS
  수익의 96.7%가 관통 없는 체결에 실려 있다**. 실제 해소는 틱·호가(WAN-98) 소관.
- **게이트 제거는 OOS 6셀(2TF × 3렌즈) 전부 플러스**(15m +4.71/+1.05/+0.31%p · 1h
  +4.06/+3.29/+2.57%p)지만 ⚠️ **IS는 조이면 마이너스로 뒤집힌다**(15m IS `pen_5bp` −3.39%p ·
  1h IS 스트레스 −4.18%p) — **IS→OOS 뒤집힘**의 또 한 사례라 어느 쪽도 단독 인용 금지.
  스트레스 렌즈에선 **두 팔 다 마이너스**다(15m OOS −1.46% · 1h OOS −0.43%).
- ⚠️ **거래 증가 +19~21%를 WAN-114의 「13~14% 감소」·wan95의 「+19~27%」와 같은 수치로 인용하지
  말 것** — 전자는 볼린저 없는 단(`L0r→L1`)의 격리값, 후자는 `--years 3` 3심볼이다.
- **검산 2개 통과**: `first_tap_free` 팔 ↔ `wan111_symbol_universe.csv` **252행 × 6지표 비트
  일치**(이동이 게이트의 몫임을 보증) · 2단 널의 실제 ↔ 3단 채택 팔 **48셀 차이 0.00e+00**
  (두 모듈이 서로를 검산).
- **기본값·토대는 바꾸지 않았다**(검증 전용, 실거래 보류 유지). 다음은 **WAN-90**(익절 측정) ·
  「선별 대 가격」을 가르는 후속 이슈(wan124.md §7).

📌 **익절 R 측정(WAN-90 Phase 1): `take_profit_r=1.5`는 검증됐고 유지다 — 두 작업 TF 모두 (b).**
[`docs/decisions/wan90.md`](docs/decisions/wan90.md)(재현: `uv run python -m
backtest.archive.wan90_mfe_distribution`, 요약만 재생성은 `--from-csv`). WAN-81이 익절을 선 도달에서
고정 1.5R로 당겼지만 그 값이 다른 값과 비교된 적이 없었다. 이 표가 **거래가 실제로 어디까지
가는가**(MFE 분포)와 **사용자 제안(1.5R 부분익절+본절스탑+R재산정)의 손익 부호**(= E[러너]가
1.5R보다 큰지, 이슈 §2 부호식)를 한 번에 잰다.
- 📌 **MFE/MAE를 R 단위로 거래 단위 계측하는 엔진 필드를 새로 넣었다**(`backtest.substep` →
  `ZoneLimitOutcome`·`Trade.mfe_r`/`mae_r`). **측정 전용**이라 손익·사이징에 안 쓰이고 기존
  리포트는 비트 단위로 재현된다(회귀 테스트가 고정). 분포는 **익절만 끈(no-TP) 변형**을
  **동시 1포지션 시퀀싱 이전 · 셋업 단위**로 잰다(승자가 1.5R에서 잘리면 MFE가 검열되므로).
- **E[러너]는 1.5R을 강건하게 못 이긴다 — ~0R이다.** 공식 렌즈(`baseline`) **OOS 중앙값
  E[러너]가 6셀(3심볼×2TF) 전부 0.000R**, 본절 복귀율 **97~99%**. 큰 MFE(5R+ 버킷 ~23%)는
  존재하지만 **왕복**이라(무효화 경계 −1R로 가며 진입가 관통 → 본절 러너 0R) 홀드로 못 챙긴다
  = **평균회귀**(사전 가설 확인). ⚠️ **E[러너] 평균의 IS 폭주(15~16R)를 인용 금지** — 통째로
  `openEnd%`(창 경계 미실현)·`top3%`(소수 대박) 산물이라 **OOS에서 0.09~0.25R로 붕괴**한다.
- **R 스윕은 OOS에서 1.5R을 지지한다** — 공식 렌즈 **OOS 심볼평균 최적 R이 15m·1h 둘 다
  1.5R**(15m +28.01% · 1h +14.93%). IS는 다른 값(15m 2R · 1h 1R)을 고르지만 **OOS에서
  무너진다**(과최적화). R−수익 곡선은 OOS에서 1.5R 정점의 **고원**(뾰족 아님).
- ⚠️ **"1.5R이 좋다"가 아니라 "가장 덜 나쁜 값"이다** — 「엣지 없음」(WAN-84/88/111/114/124)은
  그대로다. 익절 R은 **알파가 아니라 위험의 모양만 바꾼다**. 부분익절은 기대값을 깎으므로
  (E[러너]<1.5R), MDD 축소를 사는 타협으로만 정당화되고 그 계량은 **Phase 2**(래더, 별도 PR).
- **기본값은 바꾸지 않았다**(`take_profit_r=1.5` 유지, 실거래 보류 유지). 다음은 Phase 2
  (반익절·본절 래더)·변형 C(저항-OB 익절)·「선별 대 가격」 후속 이슈.

📌 **저항-OB 익절(WAN-137 = WAN-90 변형 C): Phase 1(도달률) + Phase 2(손익) — 판정 (c) TF·
위험조정에 갈린다, 기본값(`take_profit_r=1.5`) 유지.** [`docs/decisions/wan137.md`](docs/decisions/wan137.md)
(재현: `uv run python -m backtest.wan137_resistance_distance` · `uv run python -m
backtest.archive.wan137_phase2_pnl`, 요약만 재생성은 `--from-csv`). 사용자 제안("가장 가까운 저항
오더블록에 판다")을 익절 목표로 검정했다. 저항 탐지·룩어헤드 클리핑·하위TF 아카이브 캐시는
WAN-126 재사용.
- 🔁 **Phase 1만의 판정 (b)「도달 불가」는 PM 변경요청이 철회했다 — 도달률은 손익을 예측하지
  못한다.** Phase 1은 채택 엔진 체결 셋업마다 가장 가까운 상방 저항(약세 OB) 근단까지 거리를
  무검열 MFE와 대조해 **모든 (진입TF × 저항TF)가 MFE 너머**(자기-TF 15m 3.49R·MFE내 30% ·
  1h 3.70R·29%)라 (b)로 닫으려 했다. 🚨 **그 게이트는 설계 결함**: 도달률이 낮아도 닿은 거래가
  크면(1h 4.5R) 총손익은 이길 수 있다 — 승률↓·크기↑ 교환은 **손익으로 재야** 안다.
- 📌 **Phase 2 손익 격자(정본): (c) 갈린다.** 정본 팔 `resistance_self`(자기-TF, 무게 규칙 B)
  OOS 심볼평균 total_return — **15m: fixed +8.63% vs 저항 +2.52%(−6.11%p, 위험조정 0.58→0.12)
  = fixed 승(b)** · **1h: fixed +5.28% vs 저항 +7.32%(+2.04%p) = 저항이 raw만 승(a), 단
  MDD 9.98→14.26%라 수익/MDD 0.53→0.51로 위험조정 우위는 증발**. 세 구간 방향 일치·1h
  leave-one-out 6/6 플러스(강건)지만 **위험조정하면 사라진다** — 익절 R은 알파가 아니라 위험의
  모양만 바꾼다(WAN-90) 재확인.
- 🚨 **PM 핵심 질문 답 — 못 닿은 거래는 홀딩으로 안 남고 손절로 끝난다**: 8개 (TF×구간) 셀
  **전부 EOD=0**. 저항 팔 = "더 많은 손절 + 더 적지만 큰 익절"(15m OOS 손절 866→969, 1h
  262→306). "안 닿으면 무한 홀딩"이라던 Phase 2 미실행 근거가 손익으로 반증됐다.
- ⚠️ **참고 팔 `resistance_combined`(전TF 최근접)는 쓸 수 없다** — 15m OOS +0.34%(BTC 개별
  −16.95%) · full→OOS 진폭 극단(15m full +70.20%→OOS +0.34%). Phase 1 퇴화 경고(최근접 67%가
  1m)가 손익에서 그대로 드러난다.
- ⚠️ **「엣지 없음」(WAN-84/88/111/114/124) 불변** — TRX/XRP 두 팔 다 마이너스, ETH가 fixed를
  떠받친다(fixed OOS −ETH: 15m −1.81%·1h −0.25%). 위험의 모양 안에서 TF마다 유불리가 갈린다까지만.
- 📌 **결론: 기본값 유지, 재-베이스라인 결정 이슈 권고 안 함**(판정 (c)+위험조정+낙관렌즈). ⚠️ 단
  **1h raw 우위는 기록** — 사용자가 경고를 알고도 1h 한정 재-베이스라인을 원하면 별도 결정 이슈로
  (개발자 임의 착수 금지, 렌즈 보수화·틱 체결 WAN-98 선행). 익절 후속은 WAN-127(래더 B·트레일링 D).
- **엔진 훅(옵트인·비트 재현)**: `build_zone_limit_candidates(take_profit_override=)`는 익절
  목표만 갈아끼워 청산만 바꾸고 진입·체결 집합은 기본과 **비트 동일**(팔별 진입시각 대조로 검산,
  None이면 예전과 같아 기존 CSV 재현). ①(뚫린 저항 제외)·②(확정 시점)는 순수 함수(Phase 1)와
  손익 오버라이드 경로(Phase 2) 양쪽에서 회귀 테스트가 동작으로 고정.

📌 **존높이 익절(WAN-143 = WAN-133 스핀아웃 · WAN-144 흡수): §0 훅을 새 밴드에 배선 + §1 판정
(c) 두 작업 TF가 정반대 — 기본값(`take_profit_r=1.5`, 진입가 기준 1R) 유지.**
[`docs/decisions/wan143.md`](docs/decisions/wan143.md)(재현: `uv run python -m
backtest.wan143_zone_height_tp --tf 1h` → `--tf 15m --append`, 요약만 재생성은 `--from-csv`).
창·유니버스는 WAN-111/114/137과 같은 못 박은 창 · 6심볼 · `baseline` 단독 · **채택 기본값 그대로**
(오프셋 2bp × `intrabar_live` × `unconditional`).
- 📌 **§0(엔진): `take_profit_override`·`stop_loss_override`가 봉내 라이브 밴드에서 동작한다**
  — WAN-132가 진입가 정본을 `intrabar_live`로 옮긴 뒤 두 훅은 `ValueError`로 막혀 있었다(구
  WAN-144). 사용자 결정 `배선-새밴드`대로 훅을 **탭 봉 → 체결 순간**으로 옮겼다
  (`LiveLimitProvider.resolve_exits(limit) -> (손절, 익절) | None`). **오버라이드를 안 주면
  기존 CSV가 비트 단위로 재현**되고, 회귀 테스트가 라벨이 아니라 동작으로 고정한다.
  ⚠️ 정적 경로와 딱 하나 다르다 — `stop_loss_override`가 `None`일 때 정적은 셋업을 분모에서
  빼지만 봉내는 이미 주문이 걸린 뒤라 **미체결**로 끝난다(두 밴드의 체결률 직접 비교 금지).
  ⚠️ **나머지 제약은 그대로 막혀 있다**: `min_rr` 게이트 · `width_kind="atr"` ·
  `source != "close"`(wan143.md §0-5 표). **WAN-127(익절 Phase 2)의 차단은 이로써 풀린다** —
  단 **부분 청산**은 이 훅으로 표현되지 않는다(훅은 목표가 하나만 낸다).
- **§1(측정) 공식 렌즈 OOS 심볼평균**: **15m은 현행이 이기고**(① `entry_r+guard_on` +5.64% vs
  ④ `zone_height+guard_off` +0.41%) **1h는 존높이가 이긴다**(① +2.98% vs ④ **+5.48%**,
  수익/MDD **0.27→0.51**로 위험조정도 개선 — WAN-137의 "raw만 승"과 다른 모양).
  **하나의 기본값으로 둘 다 좋게 할 수 없어** 기본값을 유지한다(TF마다 다른 규칙 = IS에서
  고르는 새 자유 파라미터, WAN-108이 경계한 자리). 채택은 **사용자 결정**이다.
- 📌 **1h 개선분은 거의 전부 「익절의 몫」이다** — 가드만 풀면 +0.02%p인데 익절까지 바꾸면
  +2.48%p다. ⚠️ **반대로 15m에서 가드를 푸는 것은 −4.09%p로 명백히 해롭다** — 「가드를 풀자」는
  이 표의 결론이 **아니고**, WAN-79 가드가 15m에서 보호 장치라는 근거가 손익으로 재확인됐다.
- **R 배수 스윕(1h·`zone_height`)**: **IS 최적 1.0R → OOS 최적 1.5R**(3.0R은 OOS −1.48%).
  **자(1R 정의)를 바꿔도 배수 1.5는 살아남는다**(WAN-90 진입가 축의 재현). ⚠️ 15m·4h 스윕은
  **비용 때문에 안 돌렸다**(후속).
- ⚠️ **「1h에서 이겼다」 단독 인용 금지** — ④ +5.48%는 **ETH를 빼면 −0.51%**(플러스 3/6)이고
  **IS에서는 현행이 이긴다**(+18.26% vs +16.38%). **「엣지 없음」(WAN-84/88/111/114/124) 불변** ·
  `baseline` 낙관 렌즈 위의 값이며 **체결 보수화 재검 미실행**(존높이 팔은 목표가 멀어 슬롯을
  더 오래 잠근다).
- **4h는 대조군이다** — 사용자 제안으로 열을 추가했으나 **OOS 심볼당 19.7거래로 WAN-84 유효
  기준(20건) 미달**이라 판정하지 않는다(방향은 1h와 같은 쪽). 📌 그 게이트는 주의문이 아니라
  **코드**다: `verdict()`가 표본 미달이면 (a)/(b)/(c) 대신 「⚠️ 판정 불가(대조군)」를 낸다.
  **1d는 안 돌렸다**(OOS 10거래). 🗣️ 사용자가 *"4h·1d도 매매 대상으로 넣자"*고 했다가
  **"추후 재고"로 보류** — 개발자 임의 착수 금지이며, 착수 시 첫 질문은 손익비가 아니라
  **표본**(창·심볼 확대)이다.
- **기본값·토대 불변**(§1은 측정 전용, 실거래 보류 `ALPHABLOCK_LIVE_TRADING=false` 유지).

📌 **존높이 익절 오늘-엔진 재측정(WAN-206 = WAN-143을 필터 1.28·9종목·6년으로): 세 TF 모두
baseline에선 존높이가 근소 우세(a)이나 채택 근거 아님 — 15m 우위는 `pen_5bp`에서 뒤집힌다.
기본값(`take_profit_r=1.5`·진입가 기준 1R) 유지.**
[`docs/decisions/wan206.md`](docs/decisions/wan206.md)(재현: `uv run python -m
backtest.wan206_zone_height_tp_today --tf 4h` → `--tf 1h --r-sweep --append` → `--tf 15m
--r-sweep --append`, 요약만 `--from-csv`, 검산 `--checksum`). WAN-143(6종목3년·필터끔·판정 (c))
이후 필터 1.28(WAN-159)·9종목6년(WAN-182)이 채택됐는데 그 위에서 존높이 익절을 잰 적이
없었다. WAN-143 모듈의 익절 오버라이드·산식을 그대로 import하고 좌표·엔진만 오늘 것으로 바꿨다
(핀 없음, `OrderBlockParams()`). 익절 2팔(`entry_r` 현행 vs `zone_height` 제안) × 렌즈
(`baseline`+`pen_5bp`) · 가드 0.003 고정(축 아님) · OOS는 WAN-166 정본.
- 📌 **공식 렌즈 따뜻 OOS 심볼평균 Δ(존높이−현행)**: 4h **+0.52%p**(pen_5bp +0.54%p 유지) ·
  1h **+0.30%p**(pen_5bp +0.80%p 유지) · 15m **+1.28%p**(pen_5bp **−0.30%p 뒤집힘**). 판정
  문자는 세 TF 다 (a)지만 **채택 권고 없음**.
- 🚨 **15m 우위는 낙관 렌즈의 산물** — `pen_5bp`(체결 보수화)에서 부호가 뒤집힌다. 존높이는
  목표가 멀어 슬롯을 더 오래 잠그고 마진 체결에 더 기댄다(WAN-96/124 15m 체결 민감성 재현).
  4h·1h는 pen_5bp에서도 유지되나 폭이 미미하다.
- 📌 **위험조정하면 무승부~손해** — 승률↓(존높이 4h −3.4%p·1h −1.7%p)·MDD↑라 수익/MDD는
  평평(4h·1h)하거나 나빠진다(15m 6.16→5.87). 익절 자는 알파가 아니라 **위험의 모양만
  바꾼다**(WAN-90) 재확인. 승률↓·크기↑(meanR↑) 교환이라 총수익만 근소 우세.
- 📌 **WAN-143 대비(방향만 · 셀 직접 비교 금지)**: **15m 방향이 뒤집혔다**(옛 판은 15m만 현행
  승) · **ETH 편중이 풀렸다**(4h·1h는 leave-one-out 9/9 존높이 우세 — 필터 1.28이 좁은 존만
  남겨 편중을 푼 것). ⚠️ 단 **15m은 DOGE(신규·대리 펀딩) 한 종목에 기댄다**(−DOGE 빼면 현행 승).
- 📌 **R 배수 스윕**: 1h는 **OOS 최적 1.5R**(자를 존높이로 바꿔도 1.5 정점 살아남음, WAN-90
  재현) · 15m은 OOS 3.0R까지 단조 상승(단 baseline 위 값 + pen_5bp 취약이라 "3.0R로 가라"로
  읽지 말 것). 4h 스윕은 비용으로 생략.
- 📌 **검산**: `entry_r` 팔(override=None) ≡ `harness.run_once` **비트 일치**(따뜻·차가움,
  `--checksum` + 실데이터 게이트 테스트) · 익절이 진입 안 바꿈(두 팔 체결 셋업 집합 동일)도
  동작 고정 · 신규 3종목 펀딩 BTC 대리(wan95 `apply_funding_proxy`).
- ⚠️ **「엣지 없음」(WAN-84/88/111/114/124/151/201) 불변**(다른 질문) · 전부 `baseline`(낙관)
  위 값. **기본값·토대 불변**(측정 전용, 실거래 보류 유지). 존높이 전환은 **재-베이스라인 =
  사용자 결정** · 개발자 임의 착수 금지.

🚨 **시점 배너(WAN-365, 2026-08-23) — 아래 채택 성과 계열 문단(WAN-323 · WAN-330 · WAN-336 ·
WAN-346 · WAN-359 · WAN-312/316)의 수치는 전부 「소급 취소」 엔진에서 나왔다.**
[`docs/decisions/wan365.md`](docs/decisions/wan365.md)이 그 기본값을 인과(`bar_close`)로 옮겼다 —
같은 좌표를 인과로 다시 돌리면 채택 북 `oos_warm` 거래당 net R이 **+0.1985 → −0.1798로 부호가
뒤집힌다**(WAN-364 §4). **지우지 않고 시점 표시만 단다** — 재산출은 별건이고(순서는 wan365.md
§6), 그 표들은 「그때는 그랬다」로 읽는다. ⚠️ **특히 WAN-346의 「거래당 실력 0.109R ~ 0.199R
사이」라는 폭 판정은 통째로 무효다**(두 끝값이 다 소급 취소 위의 값). ⚠️ **「엣지 판정 계열」의
방향은 안 뒤집힌다** — 결함이 실제 팔·무작위 팔 **양쪽에 대칭으로** 걸리므로 얼어붙는 것은
**절대 수준**이다.

🚨 **시점 배너(WAN-345, 2026-08-21) — 아래 두 문단(WAN-323 · WAN-330)의 래더 팔 수치는
「재진입 거래가 래더를 못 받은」 엔진에서 나왔다.** `wan228_reentry_census.reentry_candidates()`
가 반익절 인자 셋(`partial_take_profit_r`·`partial_take_profit_fraction`·
`breakeven_after_partial`)을 **받기만 하고 `_iter_reentries`로 안 넘겨**(WAN-323 커밋
`af1a550`이 시그니처만 넓혔다) 래더를 켠 북 팔에서도 **재진입 거래는 전량 익절**로 돌았다 —
**채택 북은 재진입 ON**(WAN-273)이라 영향받는 것이 하필 **헤드라인 팔**이다.
[`docs/decisions/wan345.md`](docs/decisions/wan345.md)이 배선을 고쳤고 **재산출은 하지 않았다**
(팔당 66분 × 6팔 = 7시간+ · 재산출 여부는 **사용자 결정** · 두 리포트 결론이 원래 둘 다
「채택 권고 없음」이라 숫자가 움직여도 결정이 안 바뀔 가능성이 크다). 📌 **방향까지 추측 금지**
— 커질 수도 작을 수도 있다(WAN-323이 밝힌 채널 *「본절 청산이 재진입을 앞당긴다」* 가 정확히
이 자리다). ⚠️ **래더를 안 켠 팔(`A0` 등)은 무관**하고 비트 재현되며, **WAN-323 §0c**
(`exit_at_breakeven` 재진입 게이트)는 **별개로 정상**이다 — 두 개를 섞지 말 것.

📌 **반익절 래더(WAN-323 = WAN-90 Phase 2 · 구 WAN-127 B팔 · 사용자 사양 2026-08-18): 엔진에
부분 청산 + 본절 스탑을 옵트인으로 배선 + 14팔 격자 — per-cell은 (c) TF에 갈리는데 채택 북에선
세 축이 같은 방향이다. 기본값 불변 · 채택 권고 없음.**
[`docs/decisions/wan323.md`](docs/decisions/wan323.md)(재현: `uv run python -m
backtest.wan323_partial_tp_ladder --tf 4h --jobs 4` → `--tf 2h --append` → `--tf 1h --append` ·
북은 `--book --book-arms A0,A1_be_off,A1_be_on --tf 4h,2h,1h`, 요약만 `--from-csv`).
- 📌 **§0 엔진(옵트인, 안 켜면 비트 재현)** — WAN-143 §0-5가 「부분 청산은 이 훅으로 표현되지
  않는다」로 남긴 자리다. `simulate_zone_limit_trade(partial_take_profit_r=,
  partial_take_profit_fraction=, breakeven_after_partial=)`가 진입 순간 확정된 1R의 배수 지점에서
  수량 일부를 청산하고 잔량을 목표까지 끈다. **룩어헤드 없음**(분할 지점은 체결 순간 계산) ·
  **손절 우선**(같은 스텝 동시 도달 시) · **본절은 그다음 스텝부터**(부분 체결 관측 후에야 주문을
  옮길 수 있다) · **자(1R)는 진입 시점 값 고정**(본절 이동이 MFE/MAE·사이징 자를 안 바꾼다) ·
  래더 없이 본절만 켜면 **거부**(라벨만 붙는 조용한 실패 방지).
- 📌 **폐쇄형 항등식이 이 표의 정본이다 — 「이기면 버그」라던 초안 전제를 실측이 교정했다.**
  본절을 끄면 청산 시각이 안 바뀌어 그로스 R이 딱 두 가지로만 움직인다: `T`R 익절 `−f·(T−k)` ·
  `k`R 찍고 되돌아 손절 `+f·(k+1)` · 분할 미도달 손절 `0`. **손익분기 구제 비율 `r*=(T−k)/(T+1)`**
  (A1은 20.0%)이고 **부호는 두 부류의 개수 비가 정한다**. WAN-90의 부호식은 **익절선 그 자리**에서
  반익절하는 다른 제안의 것이라 여기 적용되지 않는다. 실측이 세 TF 모두 부호를 맞힌다(구제율
  4h 19.16% · 2h 20.17% · 1h 17.56% ↔ Δ수익 −0.14 · +0.43 · −1.70%p). `run_cell`이 **매 셀에서
  실제 거래로** 이 항등식을 검산한다(`_check_ladder_identity`).
- 📌 **per-cell 판정 (c)** — 위험 축은 3/3 TF 일관(MDD −0.5~−1.1%p · 승률 +11.8~14.4%p ·
  **종목 단위 승률 34/34 전부 개선**, ETH 편중 아님)인데 수익은 갈린다(2h +0.93%p · 4h −0.41%p ·
  **1h −2.80%p**). 6년 `full`에서는 세 TF 다 −11~19% 반납한다(상승장 비용). **분할 지점은 1.0R이
  1.2/1.3/1.5R을 모든 축에서 이긴다** — 깊게 하면 도달률·구제가 더 빨리 무너진다.
- 📌 **승률 ≈ 분할 지점 도달률** — 본절을 켜면 「분할 지점에 닿으면 절반이 +1R 확정 + 잔량 최악이
  본절」이라 그 거래가 순플러스가 된다. 🚨 **단 그 보장에는 하한이 있다**: 확정 이익 `f·k·R`이
  비용을 넘어야 하는데 임계 손절폭이 **0.22%**이고 가드가 0.3%라 **1.36배 여유뿐**이다 —
  **가드가 래더의 승률 효과를 떠받치고 있어 둘은 독립이 아니다**(가드를 낮추면 래더가 먼저 깨진다).
- 📌 **§2 채택 북(재진입 켬 · 사용자 결정 2026-08-18 「앞으로 재진입은 무조건」)에서는 세 축이
  같은 방향** — `A1_be_on` oos_warm **MDD 19.58→14.73%(−4.84%p) · 승률 +10.5%p · 수익/MDD
  +92.2% · 청산 0**(차가운 oos도 유지). per-cell에 **원리적으로 없는 채널** 둘 때문이다:
  부분 청산이 **공유 자본의 명목·리스크 자리를 비워** 다른 칸이 쓰고(거래 +12.6% · `be_off`만으로
  최대 동시 리스크 11.76→10.66%), 본절 청산이 **재진입을 앞당긴다**. ⚠️ **`full`에서는 MDD가
  오른다**(+0.89%p) — 상승장엔 이득 없이 노출만 는다.
- 🚨 **§0c 재진입 게이트 수리(사용자 지적)** — 재진입은 「익절이면 재무장, 손절이면 존 종료」로
  갈리는데 **본절 청산은 존 무효화 경계를 건드리지 않는다**. 그대로 두면 래더가 **멀쩡한 존을
  죽여** 재진입을 18~20% 없앴다. `ZoneLimitOutcome.exit_at_breakeven`(신설)을 두 게이트가 함께
  본다. 📌 **원래 손절은 게이트가 옳다** — 손절 참조가 = 존 원단이고 `zone_invalidation="wick"`
  이라 저가가 그 선을 뚫는 순간 손절과 `break_time`이 **동시**에 찍힌다(둘이 정의상 겹친다).
- 🚨 **채택 권고 없음 — 이유 셋이 다 크다**: (1) **북 결과는 채택 북 거래의 34%만 본 값**이다 🔁 (**WAN-330이 4TF로 닫았다** — 위 📌 문단)
  (채택 지갑은 4TF, 이 표는 3TF — `oos_warm` 6,336 vs 2,132으로 **15m이 66.4%**). 🚨 **북 지갑은
  이어붙일 수 없어**(한 프로세스·한 지갑) 4TF를 통째로 다시 돌려야 한다 — 사용자 결정으로 후속
  분리. 하필 **15m이 래더에 가장 불리할 이유가 넷**이다(손절폭이 가장 좁아 0.22% 임계에 근접 ·
  `cost_r` 최대 · 구제율이 TF가 짧을수록 하락 · WAN-316 손절 스트레스 최민감). (2) 전부
  `baseline`(닿으면 체결) 위 값이고 **부분 익절은 체결을 하나 더 요구**하는데 `pen_5bp` 미측정
  (WAN-124: 관통 요구 시 15m OOS 수익의 96.7% 소멸). (3) `full` MDD 상승.
- ⚠️ **분할 비율은 절반 고정**(사용자 사양)이고 스윕은 후속이다 — 📌 **`be_off`는 스윕이
  불필요함이 증명됐다**(`f`가 항등식 밖으로 빠져 부호를 못 바꾸고 최적이 끝값이며 `f=1`은 R 스윕
  으로 퇴화). **`be_on`만 재면 된다**(확정 이익 `f·k`와 본절 구제 `(1−f)·1`이 반대로 움직여
  폐쇄형이 안 닫힌다).
- **기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()` 그대로 · 래더는 전부 옵트인 ·
  실거래 보류 `ALPHABLOCK_LIVE_TRADING=false` 유지) · **「엣지 없음」(WAN-84/88/111/114/124/151/
  201/248) 불변**(익절 구조는 알파가 아니라 **위험의 모양**만 바꾼다, WAN-90) · 채택은
  **재-베이스라인 = 사용자 결정** · 개발자 임의 착수 금지.

🚨 **위 WAN-345 시점 배너가 이 문단에도 그대로 적용된다** — 아래 표의 래더 팔은 재진입
거래가 전량 익절로 돌던 엔진의 값이다([`docs/decisions/wan345.md`](docs/decisions/wan345.md)).

📌 **반익절 래더 · 채택 좌표 완성(WAN-330 = WAN-323 §7-(1) 스핀아웃 · 구 WAN-331 흡수): 15m을
넣고 체결까지 보수화하면 WAN-323 헤드라인 −4.84%p가 **−0.90%p**로 81% 사라진다 — 단
**본절을 끈 반익절 팔은 버틴다**(−2.60%p). 기본값 불변 · 채택 권고 없음.**
[`docs/decisions/wan330.md`](docs/decisions/wan330.md)(재현: `uv run python -m
backtest.wan330_partial_tp_ladder_4tf --lens baseline --jobs 4` → `--lens pen_5bp --append`,
검산 `--check-legacy-grid`, 요약만 `--from-csv`). WAN-323의 북 판은 **4h·2h·1h 3TF 지갑**인데
채택 북은 **15m 포함 4TF**이고 `oos_warm` 거래의 **66.4%가 15m**이라(WAN-312), 그 표는 채택
지갑이 실제로 하는 매매의 **3분의 1만** 본 값이었다.
- 📌 **북은 이어붙일 수 없지만(WAN-316) 떼어낼 수는 있다** — 지갑은 한 프로세스의 **칸 집합**
  이므로 4TF 실행의 칸 후보에서 15m만 빼고 배치하면 3TF 실행과 같은 값이 나온다. WAN-312→316과
  같은 **스코프 패턴**(`both` · `both_no15m`)으로 새 판과 옛 판을 **한 표에서** 낸다 — 그래서
  옛 판 보존이 라벨이 아니라 **같은 숫자**이고, 그 등식이 검산 (c)다.
- 📌 **§1(`baseline`) 판정: 축소** — `A1_be_on` `oos_warm` ΔMDD **−4.84%p → −2.67%p**(55%
  잔존, 부호 유지). 승률 이득은 거의 그대로다(+10.51 → **+9.88%p**).
- 🚨 **§1의 더 큰 발견 — 팔 순위가 뒤집힌다(3TF 판에서는 안 보이던 사실)**: 4TF에서
  `A1_be_off`(**−3.81%p**)가 `A1_be_on`(−2.67%p)을 이긴다(3TF는 −3.33 vs −4.84%p로 정반대).
  **본절 스탑의 순기여가 −1.52%p → +1.14%p로 부호가 바뀐다.** 이슈가 예측한 자리 그대로다 —
  본절 보장의 임계 손절폭이 **0.22%**인데 15m 손절폭 중앙값이 **0.458%**(WAN-328)라 여유가
  1.36배뿐인데 그 15m이 지갑의 66.4%를 차지한다. ⚠️ **「본절이 아무 일도 안 한다」가 아니다**
  — 승률은 여전히 +9.88%p 오른다(`be_off`는 +0.14%p). **승률과 낙폭이 갈라지는** 것이 4TF의
  새 정보다.
- 📌 **§2(`pen_5bp`, 구 WAN-331) 판정: 본절 팔은 대부분 소멸 · 반익절 팔은 버틴다** —
  4TF `oos_warm` ΔMDD가 `be_on` −2.67 → **−0.90%p(34% 잔존)** · `be_off` −3.81 → **−2.60%p
  (68% 잔존)**. 3TF는 둘 다 90% 잔존이라, **15m과 체결 보수화 두 축이 같은 방향으로 본절
  스탑만 깎는다**. 수익/MDD도 `be_on`은 +42.4% → **+4.1%**로 소멸하는데 `be_off`는 +15.0%를
  지킨다. **순위 역전이 렌즈에서 3배로 벌어진다**(−2.60 vs −0.90%p). 이유는 구조적이다 —
  본절은 **분할 청산이 체결돼야** 작동하는데 부분 익절은 체결을 하나 더 요구한다.
- 📌 **거래 수 감소율 병기(완료기준 6)**: `A0` 4TF `oos_warm` **6,336 → 5,622건(−11.27%)**.
  거래는 11%만 주는데 `be_on` 이득은 66%가 날아간다 — WAN-96 비대칭이 **수익 수준이 아니라
  래더 효과에** 걸린 사례다(수익 수준도 따로 0.28x로 무너지고, 3TF 0.545x와 견주면 **15m이
  낙관 체결 의존을 두 배로 깊게** 한다 — WAN-124 15m 진단과 같은 방향).
- ⚠️ **`full`·`is`의 부호 반전을 「보수화하니 좋아졌다」로 읽지 말 것** — 4TF `full`에서
  `be_on` ΔMDD가 +0.46 → −4.37%p가 되는 것은 **기준선이 더 망가져서**다(`A0` MDD 17.45 →
  **25.62%**). 🚨 그런 셀의 **잔존율은 뜻이 없다**(WAN-115가 문서화한 함정) — `_residual_ratio`가
  기준 증분 0.5%p 미만이거나 부호가 갈리면 비율을 **내지 않고** 「부호만 읽으라」고 찍는다
  (회귀 테스트가 동작으로 고정).
- 📌 **검산 3종 전부 비트 일치** — (a) 펀딩 대리 무동작 `0.00e+00`(이 표가 WAN-323과 같은
  자로 잰 값) · (c) `baseline × both_no15m` **12행 `0.00e+00`**(「보존」이 같은 숫자이고 15m
  칸이 다른 스코프로 새지 않았다는 증거) · **계획에 없던 셋째** — `A0` 4TF 행이 **WAN-312/316의
  채택 북 `k=1.00`과 비트 일치**한다(다른 모듈·다른 실행이 같은 숫자 → 이슈 검산 1 「`A0` =
  인자 없는 채택 북」이 못 박힌다).
- 📌 **실측 비용(다음 이슈가 인용할 값)**: 팔당 **66분**(48칸 · `--jobs 4` · M1 · 경합 없음) ·
  6팔 총 **7시간 15분**. 🚨 첫 팔 88분은 **테스트 스위트와 CPU를 나눠 쓴 값**이라 섞어 인용
  금지.
- **채택 권고 없음** — 판정이 (a)가 아니고, 상승장(`is`·`full`)에서는 두 팔 다 이득이 없거나
  해로우며, **승률·낙폭·수익이 서로 다른 팔을 가리킨다**(승률 `be_on` 압도 · 낙폭 `be_off`
  우세). **기본값·토대 불변**(래더는 전부 옵트인 · `ConfluenceParams()`·`LeverageBookParams()`
  그대로) · **「엣지 없음」(WAN-84/88/111/114/124/151/201/248) 불변**(익절 구조는 알파가 아니라
  **위험의 모양**만 바꾼다, WAN-90) · 총수익 %는 복리 착시(WAN-169/213) · 6년 MDD는 폭락 미포함
  **바닥선** · `pen_5bp`도 진값이 아니다(큐 우선순위는 틱·호가 WAN-98 Canceled 소관 — **민감도**
  이지 실측이 아니다) · 채택은 **재-베이스라인 = 사용자 결정** · 개발자 임의 착수 금지 ·
  실거래 보류 유지(`ALPHABLOCK_LIVE_TRADING=false`).

📌 **같은 분 익절(WAN-336 = WAN-330 범위 밖 발견): 채택 북 `oos_warm` 순손익의 **약 48%**가
「진입한 그 1분 안에서 익절」인 거래에 실려 있다 — 손절 쪽에만 있던 보수성을 익절 쪽에서
처음 쟀다. 기본값 불변 · 채택 권고 없음.**
[`docs/decisions/wan336.md`](docs/decisions/wan336.md)(재현: `uv run python -m
backtest.wan336_same_step_tp --jobs 4`, 요약만 `--from-csv`). 1분봉은 그 1분의 **시·고·저·종
네 숫자만** 알려 주고 **안의 순서는 모른다**. 롱 지정가 진입은 가격이 **내려와야** 체결되고
고정 1.5R 익절은 **올라가야** 닿으니 「같은 1분에 진입 + 익절」은 **저가 먼저 · 고가 나중**을
가정한 것이다. 손절 쪽에는 그 가정을 누르는 장치가 둘 있었는데(`stop_before_tp` 동시 도달 시
손절 우선 · WAN-46 관통 카운터) **익절 쪽에는 장치도 카운터도 없었다** — 이 저장소는 그
낙관을 **관측조차 한 적이 없다**.
- 📌 **§1 관측(`oos_warm` · 6,336거래)**: 같은 분 익절 **467건(7.37%)**이 순손익의
  **48.44%(USD 자) · 48.18%(net R 자)**를 만든다. 📌 **두 자가 일치하는 것이 요점** — 복리·
  시점 편중의 산물이 아니다(`full`은 60.75% vs 41.52%로 갈리는데 **그 셀의 USD 비중은 단독
  인용 금지**, 뒤쪽 거래가 지배한다는 신호다). TF별(USD/R) **15m 76.08%/60.05%** · 4h
  43.20%/36.97% · 1h 33.95%/33.28% · 2h 25.51%/29.71%.
- 📌 **드물게 편중이 아니다** — 종목을 하나씩 빼고 **지갑을 다시 배치해도**(라벨 필터가 아니라
  WAN-316 스코프 패턴) net R 비중이 **46.15%(−TRX)~48.90%(−LINK)**다. ETH·SOL 편중 계열
  (WAN-111/119/124/151)이 여기서는 성립하지 않는다.
- 🚨 **가장 말이 많은 숫자 — 같은 분 익절 467건 대 같은 분 손절 7건(67배).** 익절은 1R의
  1.5배 거리, 손절은 1배 거리라 **순진하게는 손절이 더 흔해야 한다**. 정합적인 설명: 봉내
  라이브 밴드(WAN-132)가 봉 **안에서** 가격을 따라 내려가며 지정가를 재산정해 **체결가가 그
  봉의 저가 근처**에 놓인다 → `고가 − 진입`은 봉 범위에 가깝고 `진입 − 저가`는 거의 0이다
  (존폭 필터 1.28이 1R을 작게 만들어 더 키운다). ⚠️ **이 표가 증명한 게 아니라 정합적인
  설명까지다** — 체결가가 봉 범위 어디에 놓이는지를 직접 재는 것은 별도 이슈(WAN-328
  `path_fill_price`가 인접한 자).
- 📌 **§2 반사실(옵트인 팔 `no_same_step_tp` = 「진입 스텝에서는 익절을 판정하지 않는다」)**:
  `oos_warm` MDD **22.90% → 25.22%(+2.33%p)** · 승률 **55.33% → 53.32%(−2.01%p)** · 거래
  **−4.5%** · **청산 두 팔 다 0건**. ⚠️ 거래는 4.5%만 주는데 낙폭·승률이 그만큼 움직인다
  (WAN-96 비대칭의 이 축 재현). 🚨 **수익/MDD는 배율(×)로만 읽는다** — 분자가 6년 복리
  총수익이라 절댓값이 천문학적이다(WAN-169/213).
- ⚠️ **「수익의 48%가 가짜다」로 인용 금지** — 순서가 반대였다면 그 거래는 **손실이 아니라 더
  오래 보유**이고 그 뒤는 미지다. §1은 **노출된 표본의 크기**이지 손익 보정이 아니고, §2는 그
  미지를 **반대쪽 극단**으로 눌러 본 값이다. **진값은 두 극단 사이**이고 좁히는 것은 틱·호가
  (WAN-98, **Canceled**) 소관이다. 두 숫자가 함께 있어야 「크게 기대고 있지만 무너지지는
  않는다」가 읽힌다 — 어느 하나만 인용하면 정반대 인상을 준다.
- 🚨 **`pen_5bp`가 이 축을 대신하지 않는다** — 그쪽은 *「주문이 채워지느냐」*(큐 우선순위),
  이건 *「채워진 뒤 그 1분 안의 순서」*다. **다른 질문이라 이 저장소의 모든 체결 보수화 관문이
  이 낙관을 통과시켜 왔다.** (이 이슈도 `pen_5bp` 축은 안 쟀다 — 직교하고 격자가 두 배가 된다.)
- 📌 **검산 넷**: (a) `base` 팔 ≡ 인자 없는 채택 북 **0.00e+00** · (b) 후보 층 카운터와 북
  거래 층 귀속이 **같은 술어**(`is_same_step_take_profit`) · (c) 거래·배치 짝 계약(길이+손익
  이중 대조로 어긋나면 죽는다) · (d) **반사실 팔의 후보 층 카운터가 전 구간 0**(기준선은
  6,787건) = 팔이 라벨이 아니라 실제로 동작했다는 직접 증거.
- 🚨 **범위 밖에서 찾은 결함** 🔁 **— WAN-345가 고쳤다**
  ([`docs/decisions/wan345.md`](docs/decisions/wan345.md)):
  `wan228_reentry_census.reentry_candidates`가 WAN-323 래더 인자 셋을 **받기만 하고
  `_iter_reentries`로 넘기지 않았다**(커밋 `af1a550`) — 즉 **래더를 켠 북 팔에서도 재진입
  거래는 전량 익절로 돌았다**(채택 북은 재진입 ON이라 영향받는 팔이 헤드라인 팔이다).
  ⚠️ **결함은 없어졌지만 wan323/wan330의 공개 CSV는 그 위에서 산출된 그대로다** — WAN-345는
  재산출 대신 **시점 배너**를 달았고 재산출은 여전히 **사용자 결정**이다.
- **측정 전용 · 기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()` 그대로 · 팔은
  전부 옵트인 · 끄면 비트 재현) · **「엣지 없음」(WAN-84/88/111/114/124/151/201/248) 불변**
  (이 축은 *진입 규칙이 무작위와 구분되는가*가 아니라 *이미 잰 숫자가 얼마나 낙관인가*를
  묻는다 — **다른 질문**) · 전부 `baseline` 위 값 · 6년 MDD는 폭락 미포함 **바닥선** ·
  기본으로 켜는 것은 **재-베이스라인 = 사용자 결정**(WAN-132/149/159급 파급) · 개발자 임의
  착수 금지 · 실거래 보류 유지(`ALPHABLOCK_LIVE_TRADING=false`).

🚨 **그 가정을 틱으로 직접 쟀다(WAN-348 = WAN-336 후속): 「같은 분 익절」 중 틱이 지지하는
것은 **약 41%뿐**이고 엔진이 가정한 「저가 먼저·고가 나중」 그대로였던 것은 **약 16%**다 —
진값은 WAN-336의 두 극단 사이에서 **반사실 쪽으로 59% 기운 자리**다. 기본값 불변 · 채택
권고 없음.** [`docs/decisions/wan348.md`](docs/decisions/wan348.md)(재현: `uv run python -m
backtest.wan348_same_minute_tp`, 요약만 `--from-csv`). WAN-336이 낸 것은 노출의 **크기**이고
(그 467건이 `oos_warm` 순손익 net R의 48.18%) 진값은 **두 극단 사이**로 남았는데, 이 표가 그
폭 안의 **위치**를 낸다. 자료는 일자별 체결내역 아카이브(WAN-347 §0이 「유일한 길」로 실측한
경로) — **문제의 그 분들만**, TF 층화 무작위 **100건**(시드 348 고정 · 15m 53·1h 19·2h 15·4h 13).
- 📌 **두 질문을 갈라 센다 — 크기가 다르고 뜻도 다르다**: **`순서`**(익절가보다 체결가에
  **먼저** 닿았나 = 엔진의 가정 그대로) **16.0%** · **`성립`**(체결 뒤 그 분 안에 익절가에
  닿았나 = **그 손익이 실제로 났나**) **41.1%**(층 가중 · 95% 구간 31~50%). 📌 **`성립`이
  헤드라인 p다** — 순서가 뒤집혔어도 체결 뒤 다시 닿았으면 손익은 그대로 나므로(표본의 23%가
  그 부류) **`순서`만 인용하면 할인을 과대평가한다**. 판정 분포: 인공물 60 · 순서역전-성립 23 ·
  진짜 17.
- 📌 **§4 할인 한 줄**: `oos_warm` **MDD ≈ 24.3%**(현행 22.90 ↔ 반사실 25.22) · **승률 ≈
  54.2%**(55.33 ↔ 53.32) · 그 구간 net R의 48.18% 중 **약 28%p가 재판정 대상**. 🚨 **재판정
  대상 ≠ 손실이다** — 순서가 반대였다면 그 거래는 손실이 아니라 **더 오래 보유**이고 그 뒤는
  미지다(대가의 상한이 반사실 열). ⚠️ **복리 총수익에 이 혼합을 쓰지 말 것**(비선형, WAN-169/213).
- 📌 **왜 그런가가 실측으로 나왔다 — WAN-336이 「정합적인 설명」으로만 남긴 사슬이다.** 체결
  시각 중앙값이 그 분의 **33초**이고 성립률이 체결 시각에 **단조**로 무너진다(0~15초 **89%** ·
  15~30초 35% · 30~45초 23% · 45~60초 **12%**). 봉내 라이브 밴드(WAN-132)가 떨어지는 가격을
  따라 지정가를 재산정해 체결가가 그 분 **저가 근처**에 놓이고, 그러면 1.5R을 올라갈 시간이
  없다. 같은 사슬이 WAN-336의 「같은 분 익절 467 대 손절 7(67배)」도 설명한다. 🚨 **즉 인공물의
  대부분은 「그 분에 익절이 안 났다」가 아니라 「그 분의 고가가 우리가 사기 전에 지나갔다」다.**
- 📌 **흔들리지 않는다**: **종목 편중 아님**(leave-one-out 36.7%(−SOL)~42.1%(−DOT) — ETH·SOL
  편중 계열 WAN-111/119/124/151이 여기서 성립 안 함) · **크기 가중도 같음**(net R 가중 39.9%
  vs 건수 40.0%) · **두 팔 97.6% 일치**(83/85 — §3-8이 걱정한 「틱 위 밴드 재산정」이 결론을
  안 흔든다) · **재구성 100/100 성공**(오더블록+봉내 라이브 밴드+오프셋으로 되살려 **기록된
  진입가를 재현**해야만 판정 — 재현 못 하면 판정하지 않는다) · **틱 고·저가가 저장 1분봉과
  100/100건 일치**(출처가 다른 두 자료 — 어긋나면 엉뚱한 파일을 펼친 것이라 판정 전체가
  무효다) · 팔 `band` 미체결 13건이 **WAN-328의 11.4%와 같은 자릿수**(독립 교차 확인).
- 📌 **§1 비용 실측**: 파일 **77개**(= (종목,날짜) 쌍) · **1,028MB** · 받는 데 **38초** ·
  실패 0. 모집단 467건 전수라도 **257파일**이라 WAN-347 §0의 권고(*「비싼 것은 「전부 계속」
  이지 「문제의 그 분들」이 아니다」*)가 확인됐다. ⚠️ **날짜를 겹치도록 몰아 뽑지 않았다**
  (§2-6이 허용한 절감) — 비용이 그럴 만큼 크지 않아 **시점 편중을 사지 않았다**.
- 🚨 **범위 밖에서 찾은 것 — WAN-346 §0 거래별 CSV의 `익절가` 열이 재진입 행 987건 전부에서
  비어 있다**(관측 열이 재진입 후보 경로에 미배선 — WAN-345가 고친 자리의 **남은 조각**).
  **순수 관측이라 손익·판정·후보 집합 어디에도 안 쓰여** WAN-346의 결론은 안 흔들린다. 이
  이슈는 고쳐서 재산출하지 않고(팔당 66분) 고정 R 규칙으로 되살렸고 **값이 적힌 330건 전부가
  그 되살림을 재현**한다(최대 상대오차 1.78e-15). 배선 수리는 별도.
- ⚠️ **답하지 못하는 것**: 「우리 주문이 채워졌을까」(호가 깊이·큐 우선순위 — 별개 축 ·
  `pen_5bp`가 그 민감도 · **WAN-98 Canceled 유지**) · **더 이른 분에서 체결했을 가능성**
  (팔 `band`는 그 1분 안만 다시 푼다 — WAN-328 `path_fill_price`와 같은 한계) · TF 순위
  (층별 표본이 얇아 구간이 넓다). ⚠️ **「41%」를 소수점까지 인용 금지**(표본 100건 · 구간
  31~50%) — 이 표가 가른 것은 **「대부분 진짜」와 「대부분 인공물」 중 후자**라는 것이다.
- 🔁 **모집단 전수는 WAN-359가 냈다 — 표본은 편향되지 않았고(전수 독립 판정 36.2%가 이 표의
  95% 구간 안) 다만 「사슬 중복 계상」이 드러나 헤드라인 p는 29.8%가 됐다**(아래 📌 WAN-359
  문단이 정본). 🚨 **그리고 §4의 「할인 한 줄」(MDD ≈ 24.3% · 승률 ≈ 54.2%)은 재인용 금지** —
  그 자리를 WAN-359가 **실측**(24.66% / 53.66%)으로 바꿨고, 더 중요하게 **그 보간 방법 자체가
  `full` 구간에서 두 극단 밖으로 나갔다**. 이 문단의 나머지(측정법 · 왜 그런가 · 흔들리지
  않는다)는 전부 유효하다.
- **측정 전용 · 엔진·기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()` 그대로 ·
  손절폭 가드 0.3%·존폭 필터 1.28 **안 건드렸다**) · **DB 불변**(받은 zip은 `data/cache/`
  캐시 파일 · WAN-194 원칙) · **「엣지 없음」(WAN-84/88/111/114/124/151/201/248) 불변**(다른
  질문) · 전부 `baseline` 위 값 · `no_same_step_tp`를 기본으로 켜는 것은 **재-베이스라인 =
  사용자 결정**(WAN-132/149/159급 파급) · **개발자 임의 착수 금지** · 실거래 보류 유지.

🚨 **위 WAN-365 시점 배너가 이 문단에도 적용된다** — 아래 수치는 「소급 취소」
엔진의 값이다([`docs/decisions/wan365.md`](docs/decisions/wan365.md)).

📌 **표적 반사실(WAN-359 = WAN-348 §7-3 후속): 「틱이 지지하지 않는 익절」만 골라 끄고
채택 북을 다시 돌렸다 — 보간은 주 구간에서 대체로 맞았지만(24.27% → 실측 **24.66%**) 그 옆
구간에서는 **두 극단 밖으로 나갔다**. 값이 아니라 **방법**이 틀렸다. 기본값 불변 · 채택 권고
없음.** [`docs/decisions/wan359.md`](docs/decisions/wan359.md)(재현: `uv run python -m
backtest.wan359_tick_targeted_tp --part verdicts` → `--part book --jobs 4`, 요약만 `--from-csv`).
WAN-348의 41%는 **표본 100건의 비율**이고 그 뒤 쓰인 할인 수치는 그 비율로 두 극단 사이를
**직선 보간**한 값이었다 — 이 이슈가 그 보간을 **실측**으로 바꿨다.
- 📌 **§1 전수 판정: 사슬 인지 29.8% · WAN-348과 같은 자(행마다 독립) 36.2%.** 후자가 표본
  판정의 95% 구간(30.9~49.8%) **안**이라 **표본 추출은 편향되지 않았다**. 🚨 **두 자를 가른
  것이 이 이슈가 WAN-348 기계에 더한 것**이다 — 같은 분에 여러 번 체결하는 재진입 사슬
  (467건 중 **156건** · 67개 분 · 그중 **60개 분은 사슬 전체가 같은 지정가**)을 행마다 독립으로
  재면 **같은 틱 왕복을 사슬 길이만큼 센다**. 사슬 판정은 그 순서를 한 번만 쓰고(체결 → 익절 →
  그 뒤부터 다음 체결) 끊긴 지점부터는 뒤 거래가 애초에 일어나지 않는 것으로 본다(`사슬끊김`).
  **6.4%p가 그 중복 계상의 크기**다. 📌 **크기로 가중해도 같다**(net R 가중 30.0% vs 건수 29.8%).
- 📌 **§2 `oos_warm` 3팔**: `base` MDD **22.90%**(승률 55.33% · 거래당 net R 0.1985) ·
  **`tick_off` 24.66%**(53.66% · 0.1567) · `all_off` 25.22%(53.32% · 0.1482). 청산 **12행 전부 0**.
- 🚨 **핵심 — `full`에서 `tick_off`가 두 극단 밖으로 나간다**(MDD **22.22%** vs `base` 17.45% ·
  `all_off` 18.70%). **선형 보간은 진값이 두 끝 사이에 있다고 가정하는데 그 가정이 성립하지
  않는다** — 북은 한 지갑이라 거래를 **일부만** 지우면 남은 거래의 순서와 자본 자리가 통째로
  달라지고(WAN-213/323) 그 재배치가 어느 쪽으로 갈지는 미리 알 수 없다. ⚠️ **「부분이 전체보다
  나쁘다」를 규칙으로 읽지 말 것** — 보인 것은 **어느 방향으로든 벗어날 수 있다**는 것이다.
  📌 **보간의 전제 셋 중 둘은 실제로 성립했고**(인공물이 큰 거래에 안 몰림 · 주 구간 오차
  0.4%p) **깨진 것은 셋째(비선형) 하나인데 그것으로 충분했다.**
- 📌 **§3 인공물 거래는 손실이 아니다** — 막힌 분에 진입한 **237건이 net R 합 +214.13R**로
  끝난다(익절 200건 +259.30R · 손절 37건 −45.17R · 보유 중앙값 0분 → **2분**). WAN-348 §4가
  *「재판정 대상 ≠ 손실」*이라 남긴 자리의 답은 **「대부분 그냥 몇 분 뒤에 익절한다」**이고
  손절로 끝나는 것은 6분의 1이다. ⚠️ `base`의 같은 분 거래(310건 +389.10R)와 **직접 빼지 말
  것**(팔이 다르면 뒤에 놓인 거래도 다르다) — 읽을 수 있는 건 **그 손익의 절반 남짓이 남는다**까지다.
- 📌 **검산 다섯이 전부 0.00e+00** — (a) `base` ≡ 인자 없는 채택 북 · (a′) `base` ≡ WAN-336
  `base` 4구간 · (b) `all_off` ≡ WAN-336 `no_same_step_tp` 4구간 · (d) **`is` 구간에서
  `tick_off` ≡ `base`** · 팔 동작(`all_off` 후보 카운터 전 구간 0 · `tick_off` 같은 분 익절
  467 → 131). 📌 **(d)는 계획에 없던 검산이고 공짜로 얻어졌다** — 표적 목록이 `oos_warm`
  거래에서 왔으므로 `is`에는 막을 분이 **하나도 없고**, 그러면 그 구간은 비트 단위로 같아야
  한다. **표적 팔이 시킨 것 말고는 아무것도 안 건드렸다는 직접 증거**다. (c) 틱 고·저가 ↔ 저장
  1분봉 **465/467**(어긋난 2건은 종목·시각까지 밝히고 빼지 않았다 — 판정은 극값이 아니라
  **순서**를 본다).
- 📌 **엔진(옵트인 · 안 주면 비트 재현)**: `simulate_zone_limit_trade(no_same_step_tp_minutes=)`
  가 **1분 스텝 `open_time` 집합**을 받아 그 분에 체결된 셋업에서만 같은 스텝 익절을 막는다.
  `no_same_step_tp`(전부 끔)와 **같은 축의 두 값**이라 함께 주면 **거부**하고, 🚨 **아무 칸과도
  안 맞는 표적 키도 거부한다**(안 걸린 채 라벨만 붙으면 기준선과 같은 수가 나오고 「보간이
  맞았다」가 근거 없이 만들어진다). 표적 단위가 **(칸, 1분)** 인 이유는 **증거의 단위가 그것**
  이기 때문이다.
- ⚠️ **`tick_off`는 하한이다** — 「첫 거래는 지지받고 뒤 거래만 못 받는」 23개 분은 분 단위
  스위치로 표현되지 않아 **안 막았다**. 실제 대가는 조금 더 클 수 있다.
- 🚨 **방법론 파급 — 이 저장소에서 「두 극단 사이를 보간」한 다른 자리도 같은 의심을 받는다**
  (북 위에서는 그 가정이 보장되지 않음이 `full`에서 실측으로 나왔다). 훑는 것은 별도 이슈다.
- **측정 전용 · 엔진·기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()` 그대로 ·
  팔은 전부 옵트인 · 손절폭 가드 0.3%·존폭 필터 1.28 **안 건드렸다**) · **DB 불변**(받은 zip은
  `data/cache/` 캐시 · WAN-194 원칙 · **WAN-98 Canceled 유지**) · **「엣지 없음」(WAN-84/88/111/
  114/124/151/201/248) 불변**(다른 질문) · 전부 `baseline` 위 값 · 6년 MDD는 **바닥선** ·
  `no_same_step_tp`를 기본으로 켜는 것은 **재-베이스라인 = 사용자 결정** · **개발자 임의 착수
  금지** · 실거래 보류 유지(`ALPHABLOCK_LIVE_TRADING=false`).

📌 **소급 취소를 인과로(WAN-365 = 사용자 결정 2026-08-23 「일단 고쳐야지」 · WAN-364 실행):
`invalidation_cancel` 기본값이 `"bar_open"` → `"bar_close"`다 — 존이 깨지는 봉의 대기 지정가를
봉 시작으로 소급 취소하지 않고 체결시킨 뒤 손절에 맡긴다. 엔진 정확성 1층 재-베이스라인.**
[`docs/decisions/wan365.md`](docs/decisions/wan365.md). 새 격자(스윕)는 안 돌렸다 — 근거는
WAN-364가 이미 낸 표다. 📌 **검산: 인자 없는 `backtest.run --oos-warm`(48칸 · 2시간 45분)이
WAN-364 팔 B와 16개 값 전부 `0.00e+00`으로 비트 일치**했다 — 「기본값이 실제로 옮겨졌고 다른
경로가 조용히 갈라지지 않았다」의 직접 증거다(wan330/336/346의 `A0 ≡ 인자 없는 채택 북`과 같은
형식). 순서 논증 그대로다: 롱 지정가는 존 **안**, 손절선은 존 **아랫변**이라
`체결 → 무효화` 순서가 강제되고, 무효화 시점엔 이미 포지션이 있어 **취소할 주문이 없다**
(사용자 지적 *"손절을 해야지 뭘 취소한다는 거냐"*).
- 🚨 **알고 받는 것 — 성과가 나빠진다. 그게 맞는 숫자다.** 채택 좌표 북 `oos_warm`(WAN-364 §4):
  거래 6,336 → **9,514** · 승률 55.33% → **40.28%** · 거래당 net R **+0.1985 → −0.1798**(부호
  반전) · MDD 22.90% → **100.00%**. 되살아나는 3,309건은 **손절 2,922 · 익절 387**.
  ⚠️ **「그래서 전략이 손실이다」로 확정 금지** — `baseline`(낙관) 위 값이고 `pen_5bp`·**인과 팔
  위의 대조군 실험은 아직 안 쟀다**. 확정된 것은 **「옛 플러스가 지워진 손절 덕이었다」** 하나다.
  🚨 **「청산 0건」이 아무것도 보증하지 않는다**(MDD 100%인데 청산 0 — WAN-312 §4 · WAN-367).
- 📌 **축을 파라미터로 올렸다** — `ConfluenceParams.invalidation_cancel`(신설)이 정본이고,
  WAN-364의 kwarg는 **오버라이드로 남는다**(`None` = 파라미터를 읽는다 · `rsi_gate_mode` 규약).
  자리가 맞는 이유: `limit_valid_bars`·`cancel_limit_on_condition_fail`이 이미 같은 **「주문 취소
  규약」 가족**이다. `ADOPTED_INVALIDATION_CANCEL`은 **리터럴을 다시 적지 않고** 파라미터에서
  읽는다(채택 기본값이 두 곳에 살면 갈라진다). 부수 효과: 취소 시점이 params를 타므로
  `run_cells`의 표준 경로 검산(`engine_check`)이 **두 팔 모두에서** 성립한다.
- 📌 **CLI `--invalidation-cancel`** — **미지정 = 채택(인과)** · `bar_open` = 옛 동작 명시(둘은
  다르다, WAN-159 `none`·WAN-273 `off`와 같은 규약) · per-cell은 콤마 격자, **북은 단일값만**
  (한 실행 = 한 지갑, WAN-316과 같은 이유).
- 📌 **파급이 WAN-149와 글자 그대로 같은 부류다** — `invalidation_cancel`은 **WAN-364가 처음
  만든 축이라 기존 리포트가 하나도 명시하지 않았다**. 고정하지 않으면 옛 결론 리포트가 전부
  조용히 인과 엔진으로 다시 돌아 본문과 어긋난다(「바꿨다고 믿으면서 안 바뀐 것」의 거울상인
  **「안 바꿨다고 믿으면서 바뀐 것」**). 엔진에 닿는 `wan*.py` **72개 중 66개를 명시 고정**했다
  — 43개는 `harness.pin_invalidation_cancel`(파라미터 핀), 22개는
  `run_cells(invalidation_cancel=harness.LEGACY_INVALIDATION_CANCEL)`(북 계열 호출부 핀),
  3개는 자기 파라미터 빌더 핀(겹침 둘: wan323·wan327이 per-cell·북 양쪽). 목록·제외 사유
  표는 wan365.md §4. ⚠️ `grep -li invalidation_cancel`로 세면 68이 나오는데 **오탐 둘**을
  포함한 값이다(wan364는 축이 실험 변수 · wan362는 **모듈 이름**을 인용할 뿐이다).
  🚨 **고정 대상을 손으로 적었으면 빠뜨렸을 자리가 있다** — 호출부가 부르는 이름이 엔진에서
  몇 단계 떨어진 자리다(`wan142.build_cell`을 wan154·wan176·wan203이 빌려 쓰고
  `wan70.run_random_control_b_evals`를 wan350이 쓴다). 목록은 **전이 폐포**로 구했고, 회귀
  테스트가 매번 다시 구해 「언급조차 안 한 모듈」을 잡는다(돌연변이 확인 완료).
  🚨 **반대로 「고정하면 안 되는 부류」도 게이트가 골라냈다** — `wan197`·`wan204`·`wan248`·
  `wan350`은 **「이 팔 == 인자 없는 채택 실행」을 검산으로 못 박아 둔** 모듈이라 옛 엔진을
  박으면 **그 라벨이 거짓**이 된다. 표가 낡는 건 맞는 결과(wan95 규약)이고 검산이 깨지는 건
  틀린 결과다. 처음에 넷을 고정했다가 그 검산들이 실패해 드러났다(wan365.md §4-2).
  - ⚠️ **공유 러너에는 핀을 박지 않았다**(`wan169.run_cells` · `wan228._iter_reentries` ·
    `wan70.run_random_control_b_segment` 내부) — 배선에 핀을 박으면 채택 규칙을 따라야 할 새
    호출부까지 조용히 옛 엔진으로 끌려간다(WAN-305가 이름 붙인 실패의 이 축 판). **호출부가
    명시**한다. **wan95(채택 성과)와 wan364(이 축이 실험 변수)는 고정 대상이 아니다.**
  - 📌 **대조 도구는 인과를 따른다**(`live/live_vs_backtest.py` 등, WAN-305) — 이건 **정합성이
    좋아진 자리**다. 페이퍼 러너는 이미 실질적으로 인과 쪽인데(한 봉 늦게 취소) 대조 백테만
    소급 취소로 돌고 있었다.
  - 🚨 **파급 누락을 테스트가 잡는다** — `tests/test_wan365_invalidation_rebaseline.py`가
    `backtest/wan*.py`를 AST로 훑어 **엔진을 부르는데 취소 시점을 명시하지 않은 모듈**이 있으면
    실패한다(예외는 이유와 함께 선언). 핀이 라벨이 아님도 **실데이터 후보 집합**으로 건다.
- ⚠️ **얼어붙는 것** — **엣지 판정 계열**(WAN-84/88/111/114/124/151/201/248/350)은 **방향은 안
  뒤집힌다**(결함이 실제 팔·무작위 팔 **양쪽에 대칭**으로 걸린다) · 얼어붙는 것은 **절대 수준**
  이다. **채택 성과 계열**(WAN-312/316/323/330/336/346/359)은 통째로 「그때는 그랬다」이고, 특히
  **WAN-346의 「거래당 실력 0.109R ~ 0.199R 사이」라는 폭 판정이 무효**다(두 끝값이 다 소급 취소
  위의 값). **파라미터 채택 근거**(WAN-159/197/90/143/206/112/182/252/307/213/273)는 **고른 값을
  안 건드리되 고른 근거가 된 표가 얼어붙는다** — 그래서 재탐색이 필요하다(WAN-366부터, 순서는
  wan365.md §6. 🚨 **가장 큰 위험은 계산 시간이 아니라 과최적화다**).
- **다른 기본값·토대는 전부 불변**(존폭 1.28 · 가드 0.3% · 익절 1.5R · 오프셋 2bp · cap_only
  5배 · 재진입 band · 롱 온리 · `combine_obs=False` · `intrabar_live`) · **라이브 러너의 취소
  시점은 안 건드렸다**(이미 인과 쪽 · WAN-364가 「취소를 앞당기는 것은 답이 아니다」로 닫았다) ·
  **WAN-361(같은 분 익절 A/B/C)은 뒤로 미룬다**(두 재-베이스라인을 한꺼번에 움직이면 「어느 쪽이
  얼마를 움직였나」를 못 가른다) · 실거래 보류 유지(`ALPHABLOCK_LIVE_TRADING=false`).

📌 **인과 엔진 부품 사다리(WAN-366 = 「이제 다시 해봐야지」 1단계): 값을 더하는 부품은
손절폭 가드 하나뿐 — 볼린저는 세 번의 「유일한 플러스」가 **부호가 뒤집혔고**, 어느 단도
양수가 아니다. 기본값 불변 · 채택 권고 없음.**
[`docs/decisions/wan366.md`](docs/decisions/wan366.md)(재현: `uv run python -m
backtest.wan366_causal_ablation --census-only` → `--rungs L2,L3,L4 --jobs 4 --skip-census` →
`--rungs L1 --jobs 4 --append --skip-census` → `--rungs L0 --jobs 4 --append --skip-census`,
요약만 `--from-csv`). WAN-365(인과 전환) 뒤 채택 파라미터들이 전부 결함 엔진의 표로 골라진
것이라, 후보 집합 층(볼린저·존폭 필터·가드·재진입)을 5단 사다리(`L0`~`L4`)로 채택 북
(12종목 × 4TF 한 지갑 · 핀 없음)에서 다시 쟀다. 생성은 3회뿐이다 — 가드는 **사이징** 축
(`build_book_rows(min_stop_distance_fraction=)`, wan76 §3·WAN-197 성질), 재진입은 payload의
별도 dict라 후보를 안 바꾼다(검산 (b)가 매 실행 확인).
- 📌 **§0(탐지 층 인구조사, 310초): 지워진 탭은 좁은 존에 단조로 몰려 있다** — 무효화 봉
  탭 비율이 존폭÷ATR14에 단조(가장 좁은 버킷 **50.79%** → 가장 넓은 **2.79%**), 좁은 존
  (≤1.28) 25.86% vs 넓은 존 6.75% = **+19.11%p**, 4TF 전부 같은 방향. 존폭 필터의 「4관문
  통과」(WAN-142/152/154/203)가 전부 그 오염된 엔진 위였고 오염이 하필 필터가 사는 쪽에
  4배로 몰려 있었다. ⚠️ 탐지 층이라 손익을 안 잰다 — 채택 근거 아님.
- 📌 **§1 사다리(`oos_warm` 거래당 net R)**: `L0` 존 단독 **−0.2367** → `L1` ＋볼린저
  **−0.3571**(증분 **−0.1204**) → `L2` ＋필터 **−0.4267**(**−0.0696**) → `L3` ＋가드
  **−0.1652**(**+0.2615**) → `L4` ＋재진입 = 채택 북 **−0.1798**(**−0.0145**). **네 증분
  전부 4구간 같은 부호**(IS→OOS 뒤집힘 없음) · LOO 지갑 재배치 13판 전부 같은 방향(편중
  아님) · 다섯 단 전부 총수익 ≈ −100%·MDD ≈ 100%·청산 0(포화된 자는 단을 못 가른다 —
  판정은 net R로만, WAN-312 §4).
- 🚨 **볼린저 부호 반전이 가장 큰 발견** — WAN-114/145/151의 「유일한 플러스 부품」이 인과
  엔진에서 **최대 손해 부품**이다. WAN-131(기여의 84%가 「가격」 = 싸게 사기)과 합치면
  메커니즘이 닫힌다: 싸게 산 진입일수록 존 아랫변에 가까워 소급 취소가 그 손절을 지워
  주고 있었다 — **볼린저의 옛 성적표는 버그였다**. ⚠️ per-cell 격리에서는 볼린저 켠 쪽이
  전 칸 나아 보였는데 북에서 뒤집혔다(WAN-341 「판단은 북에서만」의 실증).
- 📌 **검산 셋 통과**: (a) `L4` ≡ 인자 없는 채택 북 0.00e+00 · (a′) `L4` ≡ **WAN-364 팔 B**
  4구간×6지표 전부 차이 0(다른 모듈·다른 날의 독립 기록) · (b) `L2`·`L3` 후보 수 일치
  (가드가 후보 생성에 안 샌다).
- 📌 **다음 이슈에 넘길 한 줄**: 쓸 축은 **가드뿐**이고 후보 집합 층의 답은 **「이 규칙
  집합에는 없다」**에 가깝다. 최선의 단은 `L3`(재진입 없는 북 · −0.165R)이라 **WAN-273
  (재진입)·WAN-159(필터)·볼린저 채택의 근거가 소멸하는 방향**이되, 전환은 전부
  **재-베이스라인 = 사용자 결정**이다. 🔁 ⚠️ **「존 단독+가드」는 WAN-368이 쟀다 — `L3`보다
  나쁘다**(아래 📌 문단이 정본). ⚠️ 익절 배수·오프셋(2단계)·cap_only·배수·TF·유니버스
  (3단계)는 이 사다리 범위 밖.
- **측정 전용 · 기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()` 그대로 ·
  사다리 축 전부 옵트인 · 끄면 비트 재현) · 전부 `baseline` 위 값(`pen_5bp` 미측정) ·
  **「엣지 없음」(WAN-84/88/111/114/124/151/201/248) 불변**(다른 질문) · 실거래 보류 유지
  (`ALPHABLOCK_LIVE_TRADING=false`).

📌 **분기 단 「존 단독 ＋ 가드」(WAN-368 = WAN-366 결정문 §2 명시 잔여): `L0g`는 사다리
최선 단 `L3`보다 **나쁘다**(네 구간 다) — 가드는 볼린저·필터와 **상호작용해** 혼자서는
기여의 78~85%를 잃는다. 후보 집합 층의 마지막 희망이 닫혔다. 기본값 불변 · 채택 권고 없음.**
[`docs/decisions/wan368.md`](docs/decisions/wan368.md)(재현: `uv run python -m
backtest.wan366_causal_ablation --rungs L0,L0g --jobs 4 --append --skip-census`, 요약만
`--from-csv` — **모듈은 wan366을 그대로 쓴다**). WAN-366 사다리는 **누적**이라 가드가
`L2`→`L3`, 즉 **볼린저·필터가 이미 켜진 위에서만** 측정됐다. 그 둘은 「손절폭이 좁은 거래」를
**만드는** 부품이고 가드는 정확히 그 부류를 쳐내니 셋이 독립이 아닐 수 있었다 — 그래서 손해
부품 셋을 다 끄고 가드만 켠 **분기 단 `L0g`**를 한 번 배치했다.
- 📌 **판정(거래당 net R · `L0g` vs `L3`)**: `full` −0.1562 vs −0.1288(**−0.0274**) ·
  `is`(선택) −0.1461 vs −0.1114(**−0.0347**) · `oos_warm`(확인) −0.1782 vs −0.1652
  (**−0.0130**) · `oos` −0.1800 vs −0.1664(−0.0136). **네 구간 전부 `L3` 승**이고 격차가
  ±0.005R 노이즈 밖이다. 🚨 **여섯 단 중 양수가 하나도 없다** — 최선은 여전히 `L3` −0.1652R.
- 🚨 **상호작용 확인 — 단순 덧셈은 성립하지 않는다**: 가드 기여가 맨몸 `L0`→`L0g`
  **＋0.0311R**(IS) · **＋0.0585R**(`oos_warm`)인데 볼린저·필터 위 `L2`→`L3`는 **＋0.2103R** ·
  **＋0.2615R**이다(**잔존 14.8% · 22.4%**). 거래 수가 이유를 보여 준다 — 가드가 지우는 거래가
  맨몸에선 **−11.5%**인데 쌓으면 **−31.1%**다(`oos_warm`). ⚠️ 산수 예측(＋0.0248R)은 물론
  **PM 시나리오 표의 가장 보수적인 값(−0.0627R)보다도 나쁘다** — 지운 건수가 더 적었고
  (2,203 vs 3,534) 지운 거래의 평균도 −1.00R이 아니라 **약 −0.69R**이었다(역산값 · 크기가
  아니라 「−1.00R보다 얕다」는 성질만 읽을 것).
- 📌 **승률은 거의 안 움직인다**(`oos_warm` 38.67 → 39.04%, ＋0.37%p) — 가드가 쳐내는 것이
  「지는 거래」가 아니라 **「1R 대비 비용이 말이 안 되는 거래」**라는 WAN-154 §3과 정합적이고,
  맨몸에선 그 부류가 적어 효과도 작다.
- 📌 **드물게 편중이 아니고, 판정이 12판 전부에서 선다** — LOO **지갑 재배치**(WAN-316) 12판이
  −0.1704R(−BNB) ~ −0.1827R(−ETH)로 좁게 모이고, **최선인 −BNB조차 `L3`(−0.1652R)보다 나쁘다**.
- 🚨 **재현 검산 비트 일치** — 이번 실행이 `L0`을 **다시** 돌렸는데 WAN-366 CSV와 4구간 ×
  거래당 net R·거래 수·후보 수 **전부 차 0.00e+00**이고, 48칸 per-cell 후보 합계가 옛 `L0`
  북 행과 **113,612 = 113,612**다. 모듈을 고쳤는데 기존 단이 안 움직였다는 직접 증거이자,
  `L0`·`L0g`가 같은 후보를 본다는 컴퓨트 설계의 전제다(검산 (b)가 매 실행 확인 —
  **짝의 한쪽만 돌리면 적재된 CSV의 짝 행과 대조**하므로 조용히 건너뛰지 않는다).
- 📌 **`L0g`는 누적 사다리 위의 단이 아니라 분기다** — 증분은 `L0`→`L0g` 하나뿐이고 표시
  순서상 이웃한 `L0g`→`L1` 같은 차는 **뜻이 없다**(`STEPS` = `CHAIN` + `BRANCHES`가 그 관계를
  명시적으로 들고 있고 요약 표가 `🔀`로 찍는다). 이웃 zip으로 되돌리면 「볼린저의 순기여」
  (`L0`→`L1`)가 조용히 다른 양으로 바뀐다.
- 📌 **다음 행동: 2단계를 시작하지 않는다** — 이슈가 착수 전에 정해 둔 갈림 그대로다.
  「손해 부품을 끄면 가드만으로 0을 넘을 것」이 후보 집합 층에 남아 있던 **유일한 희망**이었고
  그것이 닫혔으므로, WAN-366의 **「이 규칙 집합에는 없다」**가 한 겹 더 단단해진다 — 실패가
  아니라 **결론**이다. ⚠️ **후보 집합 층이 닫힌 것이지 모든 축이 닫힌 게 아니다**(익절 배수·
  오프셋 = 2단계 · 회계·사이징·유니버스 = 3단계). 다만 WAN-90이 이미 적어 뒀다 — **거래당
  net R이 음수면 어떤 배수도 그 부호를 못 바꾼다.**
- ⚠️ **다른 조합은 안 뒤졌다**(존＋볼린저＋가드 …) — 축이 넷이면 조합이 16개이고 **기댓값이
  음수인 엔진에서 조합을 뒤지면 앞구간에서 좋아 보이는 것은 반드시 나온다**(WAN-161: 배수
  argmax가 8셀 중 7에서 뒤집혔다 · WAN-90 · WAN-111). 결정문이 지목한 **한 단만** 쟀다.
- 📌 **실측 비용**: 후보 생성 48칸 + 배치 2단 **4,060초(≈68분)** · `--jobs 4` · M1(15m 12칸이
  대부분 · 칸당 ~19분).
- **측정 전용 · 기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()`·손절폭 가드
  0.3%(WAN-76/79)·존폭 필터 1.28(WAN-159) 전부 안 건드렸다 · 분기 단은 옵트인) · 전부
  `baseline` 위 값(`pen_5bp` 미측정) · 총수익 %는 6년 복리라 **여섯 단 전부 ≈ −100%로 포화돼
  단을 못 가른다**(청산 0건도 판정 열이 아니다 — WAN-312 §4 · WAN-367) · **「엣지 없음」
  (WAN-84/88/111/114/124/151/201/248) 불변**(다른 질문) · 전환은 **재-베이스라인 = 사용자
  결정** · 실거래 보류 유지(`ALPHABLOCK_LIVE_TRADING=false`).

📌 **존의 두께 층 재측정 §0·§1a(WAN-376 = 사용자 제안 2026-08-25 「여러 기준으로 한번
측정해보는게 좋을거 같긴한데 / 물론 가드도 0.3, 0.4 이렇게」): §0 — 손절폭 가드는
`proximal` 진입가에서 **사실상 15m 전용 축**이고 진입가를 존 중앙으로 내리면 그 성질이
바뀐다 · §1a — **지름길이 비트 단위로 성립해** §1b 후보 생성이 3N패스 → **3패스**가 된다.
기본값 불변 · §1b 격자 점은 ★사용자 결정 대기.**
[`docs/decisions/wan376.md`](docs/decisions/wan376.md)(재현: `uv run python -m
backtest.wan376_zone_thickness --part census` — **1분봉을 안 읽어** 탐지 층에서 끝난다 ·
§1a는 `--part shortcut`, 요약만 `--from-csv`). 존폭 필터 **1.28**(WAN-159)과 손절폭 가드
**0.3%**(WAN-76/79)는 둘 다 **소급 취소 버그가 있던 엔진**의 표를 비교해 골라졌는데,
WAN-365가 그 버그를 고친 뒤 인과 엔진에서 잰 것은 **그 값에서의 켬/끔뿐**이었다
(WAN-366/368) — **눈금을 흔들어 본 적이 없다.** 이 문단은 그 재측정의 **앞 두 단**이다.
- 🚨 **이 이슈는 단계별이다** — §0(지도) → §1a(지름길 검산) → **★결정(§1b 격자 점 확정 =
  사용자 결정)** → §1b(격자) → §2(MFE). §0의 답이 점을 바꾸고 §1a의 답이 비용을 N배 바꾼다.
  **§1b·§2는 별도 PR이고 개발자 임의 착수 금지다.**
- 📌 **§0 발견 1 — 가드는 `proximal`에서 15m 전용 축이다**: 가드 0.3%가 자르는 탭이 **4h
  0.0%(4,815 → 4,815) · 2h 0.2% · 1h 1.0% · 15m 10.1%**다. WAN-197이 손익으로 본 *「1h는
  무영향」*의 정체가 **「효과가 상쇄됐다」가 아니라 「걸리는 거래가 없다」**임이 보인다(긴 TF는
  손절폭 중앙값이 4h 3.36% vs 15m 0.77%로 넓다).
- 🚨 **§0 발견 2 — 진입가와 가드는 독립이 아니다**: 진입가를 존 중앙(`mid`, 사용자 제안
  *"OB Zone의 딱 반 되는 가격"*)으로 내리면 손절폭이 절반이 되어 같은 가드 0.3%가 **1h를
  99.0% → 92.4%**까지 문다(전 칸 합계 생존 105,309 → 83,477). **「가드만 스윕하면 「그
  진입가에서의 가드 최적값」이 나온다」**는 이슈의 경고가 탐지 층에서 확인된다(WAN-368이
  손익으로 본 **자리 의존 4.5배**의 같은 자리). `mid`의 대가는 손익표에 나타나기 **전에**
  가드 축에서 **표본**으로 먼저 나온다.
- 📌 **§0 발견 3 — 채택 문턱 1.28은 분포의 41~45% 지점이고 그 분포가 TF-불변이다**: 존폭÷ATR
  분위가 네 TF에서 소수점 두 자리까지 겹친다(p33 1.07~1.12 · p50 1.37~1.42). WAN-159가
  「TF마다 다른 값은 자유 파라미터가 는다」는 이유로 15m 1.24 / 1h 1.32의 중간을 고른 것의
  **사후 근거**다. ⚠️ **「그러니 1.28이 옳다」로 읽지 말 것** — 이 표는 *분포의 어디인가*만
  말하고 *어디가 버는가*는 §1b가 답한다.
- 📌 **§0 발견 4 — 붕괴는 좁은 존이 아니라 4h의 표본 희소성에서 온다**: 격자 60점 중 20 탭
  미만으로 떨어지는 칸은 **4개뿐이고 전부 4h**(BTC·ADA·ETH·DOT)이며 죽는 점은 전부 **문턱
  0.60·0.80 × 높은 가드**다. 🚨 **WAN-154 §3의 TRX 15m 붕괴(후보의 92.6% 절단)는 이 격자에서
  재현되지 않는다** — 그쪽은 **분위 1/3 필터**(칸마다 다른 절대값)였고 여기는 **절대 문턱**
  이다(WAN-197의 「가드가 TRX를 죽이지 않고 오히려 구한다」와 정합적).
- 🚨 **§0는 「탭」을 세지 「거래」를 안 센다** — 볼린저 기각·체결 실패·북 용량이 뒤에서 또
  깎으므로 **상한이고 분류(triage)이지 측정이 아니다**. 한 방향으로만 읽는다: 여기서 20 탭
  미만이면 그 칸은 **확실히** 판정 불가다(반대는 성립하지 않는다). ⚠️ **볼린저 팔은 지도에
  안 나온다**(진입가가 봉 안에서 정해진다, WAN-119/132).
- 📌 **검산 — §0 전체 탭 113,612 ≡ WAN-366 `L0` full 후보 113,612**(다른 모듈·다른
  파이프라인의 같은 숫자). `L0`은 부품을 전부 끈 단이라 **탭이 곧 체결**이고(WAN-114 「존-단독
  체결률 100%」) 그 per-cell 후보 합계가 탐지 층 탭 수와 같아야 한다 — 이 등식이 §0가 엔진과
  **같은 탭 집합**(무효화 봉 탭 포함 = 인과 엔진, WAN-365)을 셌다는 직접 증거다.
- 📌 **엔진 추가는 순수 관측 필드 하나다** — `_Candidate.zone_width_atr`·
  `SetupDiagnostic.zone_width_atr`(`observe_zone_width_atr` 옵트인, WAN-376). **엔진이 필터에
  실제로 쓰는 그 비율**을 그대로 싣고, 안 켜면 전부 `None`이라 예전과 **비트 단위로 같다**
  (`mfe_r` WAN-90 · `exit_extreme` WAN-276 · `path_fill_price` WAN-328과 같은 부류).
  🚨 **밖에서 ATR을 다시 계산하지 말라고 있는 필드다**(WAN-77의 사본이 실제로 갈라졌다).
- 📌 **§1a 판정 — 지름길이 성립한다**: *「필터 **끔**으로 후보를 1번 만들고 「존폭÷ATR > f」인
  후보를 **빼고 재배치**한 것 ≡ 처음부터 **필터 켜고** 만든 것」*이 **비트 단위로** 참이다.
  **서로 다른 지갑 둘**(`4h+2h` 12종목 24칸 · `15m+1h+2h+4h` 4종목 16칸 — 북은 이어붙일 수
  없어 각자 자기 지갑이다, WAN-316)에서 (칸 × 구간) **120개** 대조에 **base 후보 · 재진입
  후보 · 기록된 비율 전부 어긋난 곳 0**이고 북 집계(거래 수·승률·거래당 net R·MDD·최대
  동시·청산) 16행도 **최대 절대차 0.00e+00**이다. **합치면 네 TF 전부가 §1a를 지나간다**
  (2h·4h는 두 지갑 모두에서). → **§1b의 후보 생성이 3N패스 → 3패스**가 되고 **문턱 점을
  늘리는 것이 공짜**다.
- 📌 **실측 비용(§1b가 인용할 값)**: 15m 칸당 **정공법 ~16분 · 지름길 ~25분**(`--jobs 4` ·
  M1) · 1h ~5분 · 2h ~2.5분 · 4h ~1.2분. 🚨 **지름길 15m은 후보가 2.26배인데 시간은 1.6배**다
  (로딩·탐지·밴드 시딩 같은 고정 비용이 후보 수에 안 비례한다) — **그래서 손익분기가 후보 수
  기준 `N ≈ 2.3점`에서 실측 시간 기준 `N ≈ 1.6점`으로 내려간다**(문턱을 두 점만 재도 지름길이
  이긴다). ⚠️ **이 값을 다른 모듈로 옮기지 말 것**(WAN-316이 지적한 2배 과대 추정 부류).
- 📌 **한 패스만 보면 지름길이 더 비싸다** — 필터가 시뮬레이션 **전에** 걸러 주던 걸 안 하니
  탭의 100%를 돈다(정공법 1.28은 44.3% = **2.26배**). 이득은 그 한 패스가 **모든 문턱**을
  덮는 데서 온다(문턱 5점 정공법 2.14 vs 지름길 1.00 · 「필터 끔」까지 3.14 vs 1.00 · 9점＋끔
  4.65 vs 1.00). ⚠️ **손익분기는 N ≈ 2.3점**이라 두 점만 잴 거면 지름길이 손해다.
- 🚨 **성립하는 이유가 코드 성질이라 칸을 늘려도 같은 시험의 반복이다** — 셋업 사이에 걸친
  상태가 셋뿐이고(`candidates` 리스트는 append만 · `_IncrementalRsiSeeder`는 **설계상 경로
  독립**(WAN-78 독스트링) · `dropout_rng`는 **깨지지만** `baseline`이 난수를 뽑지도 않는다),
  후보 집합이 같으면 북은 같은 입력을 받아 자동으로 같다. 🚨 **탈락 렌즈**
  (`fill.dropout_rate > 0`)와 지름길을 함께 주면 **거부한다** — 추첨 순서가 후보 집합에
  의존해 원리적으로 깨지는데 조용히 돌면 표가 거짓이 된다(회귀 테스트가 동작으로 고정).
- ⚠️ **가장 의심스러운 자리는 재진입이었다** — 재진입 후보는 base에서 파생되므로(WAN-261)
  컷을 파생 **뒤에** 걸면 「빠진 셋업의 재진입만 살아남는」 잡종이 되고 **개수만 세면 안
  보인다**. 엔진 훅은 컷을 **base 직후 · 재진입 파생 앞**에 건다. 🚨 **선례(WAN-316)는 칸
  단위지 셋업 단위가 아니다** — 그쪽은 *「4TF 후보에서 15m **칸**만 빼면 3TF와 비트 일치」*라
  이 주장이 **더 강하다**. 그래서 §1a는 형식이 아니라 진짜 관문이었다.
- ⚠️ **§1a 표의 손익 수치를 인용하지 말 것** — *두 팔이 같은가*만 묻는 표이고 그 지갑들은
  채택 좌표가 아니다(4TF 12종목이 아니다). **검산 (a)「정공법 팔 ≡ 인자 없는 채택 북」은
  §1b가 건다**(채택 좌표를 도는 쪽에서만 성립한다).
- ⚠️ **측정 전용 · 기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()`·존폭 필터
  1.28·손절폭 가드 0.3% 전부 안 건드렸다 · 핀 하나도 없다(WAN-305)) · 전부 `baseline` 위 값 ·
  **「엣지 없음」(WAN-84/88/111/114/124/151/201/248) 불변**(이 표는 *두 자의 눈금이 맞나*를
  묻는다 — 다른 질문) · 전환은 **재-베이스라인 = 사용자 결정** · 실거래 보류 유지
  (`ALPHABLOCK_LIVE_TRADING=false`).

📌 **「존의 두께」 §1b 격자(WAN-378 = WAN-376 스핀아웃 · ★결정 확정 2026-08-26 「1. 9점 /
2. 4점 / 3. 3팔 다」): 108팔 중 거래당 net R 양수는 0개이고, 존폭 문턱의 답은 「눈금을
옮겨라」가 아니라 **「끄는 쪽이 낫다」**다. 기본값 불변 · 채택 권고 없음.**
[`docs/decisions/wan378.md`](docs/decisions/wan378.md)(재현: `uv run python -m
backtest.wan378_zone_thickness_grid --part grid --arms bollinger --jobs 4` → `--arms proximal`
→ `--arms mid` → `--part checksum`, 요약만 `--part summary`). 존폭 필터 **1.28**(WAN-159)과
손절폭 가드 **0.3%**(WAN-76/79)는 둘 다 **소급 취소 버그가 있던 엔진**에서 골라졌고, WAN-365가
그 버그를 고친 뒤 인과 엔진에서 잰 것은 **그 값에서의 켬/끔뿐**(WAN-366/368)이었다 — 이 표가
처음으로 **눈금을 흔든다**. 12종목 × 4TF **한 지갑** · 못 박은 6년 · 문턱 9 × 가드 4 × 진입가 3
× 재진입 2 × 구간 4 · **핀 하나도 없다**(WAN-305).
- 🚨 **판정 자는 거래당 net R이다** — 이 좌표에서 총수익 %·MDD는 **−100%/100%로 포화**한다
  (채택 좌표 `total_return −1.000000`). 그런데 **청산은 전 격자 0건**이다 — 사이징이 현재
  자본의 %라 연쇄 손실로는 청산 조건이 구조적으로 안 걸린다(**WAN-312 §4 함정** · 청산 0을
  안전 신호로 읽지 말 것).
- 📌 **108팔 전부 음수**(`oos_warm` · 재진입 ON) — 최선 `mid` × 2.60 × 가드 0.40%
  **−0.0986R** · 채택 좌표(볼린저 × 1.28 × 0.30%) **−0.1309R** · 최악 −0.3715R. **표본 게이트
  미달 0행** — ★결정이 문턱 `0.60`을 뺀 것이 이 뜻이었다. ⚠️ **여유는 팔마다 다르다**(채택
  좌표는 종목당 339거래인데 격자 최소는 **30거래**, `mid` × 0.80 × 0.40% — 가장 조인 팔들은
  20건 게이트 바로 위다).
  📌 **이슈가 명시한 조건이 충족돼 WAN-379(§2 MFE)는 돌릴 필요가 없다.**
- 📌 **문턱은 세 팔 · 네 구간 전부 「조일수록 나빠짐」**. ⚠️ **「단조」로 인용하지 말 것** —
  느슨한 끝(`끔`·2.60)이 언제나 최선이되 둘의 순서는 칸마다 갈리고, 더 조인 점이 `끔`을
  이기는 칸도 있다. **다만 그 폭이 전부 +0.0035R 이하**(= ±0.005R 「0과 구분되지 않는다」
  규약 안)이고 예외는 `is` × 가드 0.40% 두 칸뿐이다(+0.0078·+0.0100R). 현행 1.28이 「필터 끔」에 대해
  치르는 값은 볼린저 **−0.0101R**(거래 14,843 → 9,515) · `proximal` −0.0167R · `mid` −0.0159R.
  🚨 **per-cell 표는 정반대로 보였다** — `full` per-cell 총수익 %로는 「조일수록 손실이 준다」
  (15m −76.8% → −22.6%)인데, 그건 **거래를 74% 줄여 복리 손실 총액이 준 것**이지 남은 거래의
  질이 좋아진 게 아니다. **판정 자를 바꾸면 부호가 뒤집히는 자리**이고 WAN-341(「판단은 북에서」)이
  손익으로 재확인된 셈이다.
- 📌 **IS→OOS 뒤집힘 12칸 중 3칸**이고 **셋 다 가드 0.40% 열**이다 — 나머지 9칸은 앞뒤 구간이
  같은 문턱을 고른다(「느슨한 쪽이 낫다」가 구간을 넘어간다). ⚠️ **그래도 argmax를 채택 권고로
  쓰지 않는다**(WAN-161) — 모든 점이 음수라 「어느 쪽이 덜 나쁜가」의 순위일 뿐이다.
- 📌 **가드는 유일한 플러스이고 격자 끝에서 아직 개선 중** — 문턱 1.28 `oos_warm`에서 끔 →
  0.40%가 볼린저 **+0.2140R** · `mid` +0.1496R · `proximal` +0.0839R(WAN-366의 **+0.2615R**과
  같은 방향·자릿수 = 독립 재확인). 🚨 세 팔 전부 0.40%에서 꺾이지 않아 **어디가 최적인지 이
  격자로는 알 수 없다**(★결정이 0.50%를 뺐다) — **「가드를 올려라」는 이 표가 낼 수 있는 결론이
  아니다**(가드 기본값은 WAN-76/79 소관).
- 📌 **진입가: 가드 0.30%에서 9/9 문턱 전부 `bollinger` > `mid` > `proximal`**(볼린저 기여
  **+0.0325R**). 🚨 **WAN-366과 부호가 반대다**(그 사다리는 볼린저를 −0.1204R로 쟀다) — 그쪽은
  **필터·가드·재진입을 전부 끈 맨몸** 위의 단이고 이 표는 **가드·재진입이 켜진** 위에서 진입가만
  바꿨다. WAN-368이 「가드는 볼린저·필터와 상호작용한다」고 못 박은 자리인데 **부호가 뒤집힐
  만큼 크다**는 것이 새 정보다. ⚠️ **두 표를 「어느 쪽이 맞다」로 다투게 하지 말 것** — 함의는
  **「볼린저의 값은 가드가 켜져 있느냐에 달려 있다」**이고, 순위도 축을 옮기면 바뀐다(가드
  0.40%에서는 격자 최선이 `mid` × 2.60).
  📌 **WAN-131(선별 대 가격)에 주는 것**: `mid`(더 싸게 사기)는 `proximal`보다 낫지만 볼린저를
  **못 이긴다** — 가격만으로는 볼린저가 재현되지 않는다. ⚠️ 이 표는 그 둘을 **분리하지 않는다**
  (`select_only` 축의 인과 엔진 재검은 미실행).
- 📌 **재진입은 세 팔 전부 9/9 문턱에서 마이너스**(채택 좌표 −0.0144R · WAN-366의 **−0.0146R**과
  소수점 셋째 자리까지 일치 = 독립 재확인). ⚠️ **「재진입을 끄자」가 아니다** — 채택 규칙이고
  (WAN-273) 이 열은 **최적화 축이 아니라 대조**이며, 판정 자가 거래당 net R이라 「거래를 더
  하는 것」이 불리하게 보이는 자다.
- 📌 **leave-one-out 9행 전부 「부호 유지」** — 종목 하나씩 빼고 **지갑을 재배치**해도 음수다
  (채택 좌표 주변 ±0.01R). 🚨 편중 계열(WAN-111/119/124/151 「플러스는 전부 ETH」)이 여기서는
  **방향이 반대로** 성립한다 — 한 종목이 만든 플러스가 아니라 **한 종목을 빼도 안 사라지는
  마이너스**다.
- ✅ **검산 24개 값 전부 `0.00e+00`** — 격자의 채택 좌표 팔(지름길: 필터 끔으로 만들고 밖에서
  컷) ≡ **인자 없는 채택 북**(`backtest.run --oos-warm`, 엔진 필터). **다른 코드 경로**가 같은
  숫자를 냈고, WAN-376 §1a가 채택 좌표에서 성립시킬 수 없던 등식을 여기서 채운다.
- 📌 **§0(도구) — 문턱 9점을 한 패스로**: `wan169.run_cell_variants`(한 칸을 한 번만 무겁게
  돌고 문턱마다 **컷 → 재진입 파생**만 다시) · `run_cells_multi` · `ReentryWindowContext`(창
  전처리 캐시 · 성능 인자라 안 넘기면 비트 동일) · `_Task.zone_limit_ref`(옵트인 `mid` 축) ·
  `strategy.models.ZoneLimitRef`. `run_cell`은 **문턱 하나짜리 특수 경우**로 위임한다(두 경로로
  갈라 두면 조용히 다른 숫자를 낸다 — WAN-95/112/123).
  🚨 **루프 순서는 반드시 컷 → 파생**이다(WAN-376 §1a 급소 — 뒤에 걸면 「빠진 셋업의 재진입이
  살아남는」 잡종).
- 🚨 **WAN-376 §1a의 「3N패스 → 3패스」는 절약의 크기를 과대평가한다(정정)** — 실측 BTC 4h는
  재진입 **켜고 216초 / 끄고 35초**라 **재진입 파생이 셀 비용의 84%**이고, 문턱 9점의 생존
  비율 합이 ~4.6배라 격자 셀은 `35 + 4.6×181 ≈ 868초`(**재진입이 96%**)다. **지름길이 아끼는
  것은 후보 생성인데 그게 청구서의 4%** — 정공법 대비 실제 절약은 **~13%**(창 전처리 캐시를
  더해 ~26%). ⚠️ §1a를 무효로 만드는 게 아니라 **절약의 크기만 다시 매긴다**. 📌 후속 아이디어:
  문턱이 달라도 **부모의 청산 시각이 같으면 재무장 결과가 같다** → `(부모, 청산 시각)` 메모
  (겹치는 비율 **미실측**).
- 🚨 **팔마다 재무장 규칙이 다르다(개발자 판단 · 표에 밝힘)** — 채택 `band`는 **볼린저가 꺼진
  팔에서 정의되지 않아**(`wan228_reentry_census`가 밴드 없으면 아무것도 안 내고 return) 그대로
  두면 `proximal`·`mid`가 **재진입 0개**로 돈다(라벨만 「ON」). 그래서 두 팔은 `zone`으로
  재무장한다 — `zone_limit_price`가 `zone_limit_ref`를 따라 **각 팔이 자기가 들어간 자리**에
  다시 건다. ⚠️ 팔 사이 규칙 차이는 남지만, 대안(두 팔을 재진입 없이)은 「축을 하나만 흔든다」를
  더 크게 위반한다.
- 📌 **실측 비용**: `bollinger` **7시간 31분** · `proximal` **3시간 28분** · `mid` **2시간 38분**
  · 검산 **4시간 4분** = **약 18시간**(12종목 × 4TF · 6년 · `--jobs 4` · M1). 볼린저 팔이
  2~3배 비싼 것은 **봉내에서 매 서브스텝 밴드를 재산정**하기 때문이다(나머지 둘은 후보가 **더
  많은데도** 빨랐다). ⚠️ **다른 모듈의 셀 비용과 섞지 말 것**(WAN-203 「15m ~37분」·WAN-376 §1a
  「~25분」은 다른 격자 — WAN-316이 못 박은 사고 부류).
- **채택 권고 없음 · 측정 전용 · 기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()`
  그대로 · **존폭 필터 1.28과 손절폭 가드 0.3%는 손대지 않았다**) · 전부 `baseline`(낙관) 렌즈
  위 값이고 **체결 보수화(`pen_5bp`) 미측정** · 총수익 %는 복리 착시이자 이 좌표에서 **포화** ·
  6년 MDD는 폭락 미포함 **바닥선** · **「엣지 없음」(WAN-84/88/111/114/124/151/201/248) 불변**
  (이 표는 *두 자의 눈금이 맞나*를 묻지 *진입 규칙이 무작위와 구분되나*를 묻지 않는다 — 다른
  질문) · 눈금 변경(**필터 끄기 포함**)은 **재-베이스라인 = 사용자 결정** · 개발자 임의 착수
  금지 · 실거래 보류 유지(`ALPHABLOCK_LIVE_TRADING=false`).
- 📌 **이 표가 가리키는 다음 지렛대는 눈금이 아니라 비용이다** — WAN-370이 *「시장은 +0.09R
  버는데 비용이 0.27R을 먹는다」*를 냈고, 이 격자의 최선(−0.0986R)도 그 구멍 안에 있다.
  🔁 그 「+0.09R」은 **WAN-396이 +0.007R로 정정**했다(허수 진입 슬리피지 · 필터 끔 좌표는
  +0.002R). 즉 **그 구멍의 대부분은 비용이 아니라 그냥 지고 있는 것**이다.

📌 **출구 눈금 둘을 한 격자로(WAN-381 = WAN-378 §1b 판정 4 후속 · 사용자 결정 2026-08-31):
**가드 축은 닫혔고 배수 축이 가장 큰 지렛대다** — 채택 1.5R에서 격자 끝 0.6R까지 거래당
net R이 **+0.1171R** 좋아지고 **0.6~0.8R에서는 수수료 전(gross)이 0을 넘는다**. 그래도 20조합
전부 음수 · 꺾임이 격자 밖 · 채택 권고 없음. 기본값·토대 불변.**
[`docs/decisions/wan381.md`](docs/decisions/wan381.md)(재현: `uv run python -m
backtest.wan381_exit_scales --jobs 4`, 요약만 `--from-csv`, 파일럿 `--pilot`). 팔은 **하나**
(오늘 페이퍼가 돌리는 규칙 · 볼린저 지정가 · 존폭 끔 WAN-384 · 인과 취소 WAN-365 · 재진입 ON
WAN-273 · cap_only 5배)이고 흔든 것은 **출구 눈금 둘**뿐이다. 12종목 × 4TF 한 지갑 · 못 박은
6년 · **핀 없음**(WAN-305) · 판정은 북에서(WAN-341).
- 📌 **두 눈금을 함께 흔든 이유는 산수다** — `손익분기 승률 = (1 + 비용R) / (1 + 목표R)`이라
  가드가 비용R을 내리면 최적 목표가 따라 움직인다. **따로 돌리면 서로를 무효화한다.**
  실측 답: **이 격자 폭에서는 안 움직인다**(다섯 가드 전부 최적 배수 `0.6R`) — 「움직이지
  않는다」도 곱해야 나오는 답이고, 다음 이슈는 두 축을 **분리해도 된다**는 뜻이다.
- 📌 **판정(`oos_warm` · 거래당 net R)**: 채택 좌표(0.30% × 1.5R) **−0.1194**(14,825거래) →
  격자 최선(0.40% × **0.6R**) **−0.0023**(17,305거래). 가드 축 끔→0.80%는 1.5R에서 −0.1194 →
  −0.0692(+0.0502R)인데 **배수 축은 같은 가드에서 +0.1130R**을 움직인다.
- 🚨 **주 판정 열은 gross이고 그것이 두 축을 갈랐다** — 가드 축 gross는 진폭 **0.0117R**로
  평평하고 **다섯 점 전부 음수**(−0.0421 → −0.0370 → −0.0372 → −0.0400 → −0.0304)다. 즉
  **가드가 하는 일은 「좋은 거래 선별」이 아니라 「비싼 거래 버리기」**이고, WAN-386이 세 점에서
  본 그 평평함이 0.60·0.80%에서도 유지된다 → **이 축은 닫혔다.** 반면 배수 축 gross는 진폭
  **0.1038R**이고 **0.6R +0.0617 · 0.8R +0.0314로 0을 넘는다.**
- 📌 **메커니즘이 손익분기 승률로 그대로 보인다**(가드 0.30% · `oos_warm`): 0.6R 실제 승률
  **67.75%** vs 손익분기 62.50%(여유 **+5.25%p**) · 0.8R 58.93% vs 55.56%(+3.37%p) · 1.0R
  51.78% vs 50.00%(+1.78%p) · **1.5R 40.08% vs 40.00%(+0.08%p)**. 📌 **채택 배수에서 우리는
  수수료 전 정확히 본전이다** — 이슈가 승률 역산으로 짚은 자리가 gross 열로 확인됐고, 목표를
  당길수록 그 여유가 **단조로 커진다**(비용R은 목표와 무관하게 고정).
- 📌 **드물게 거래가 늘면서 좋아진다** — 1.5R 14,825 → 0.6R **20,055(+35.3%)**. 목표가 가까워
  포지션이 일찍 나가고 칸 슬롯·공유 자본이 풀린다. ⚠️ **반대로 가드 축은 거래를 절반으로
  줄여서**(14,825 → 7,371 = −50.3%) 좋아 보이는데 그 개선이 gross에 안 실린다 — **두 축의
  성질이 정반대다**(WAN-374/378이 경계한 그 구분).
- 📌 **IS→OOS 뒤집힘 2/9줄**이고 **배수 축은 다섯 가드 전부 앞뒤 구간이 같은 값(0.6R)을
  고른다**(뒤집힘 0 · 네 구간 같은 방향). 🚨 WAN-161이 *「배수 argmax가 8칸 중 7칸 뒤집혔다」*고
  못 박은 자리인데 여기서는 안 뒤집힌다 — **그래도 argmax는 채택 근거가 아니다**(최선 자체가
  음수라 「덜 나쁜 쪽」의 순위일 뿐).
- 🚨 **채택 권고 없음 — 이유 넷**: (1) **20조합 전부 음수**이고 최선 −0.0023R은 **BNB 하나를
  빼면 부호가 바뀐다**(leave-one-out · 지갑 재배치). (2) **꺾임이 격자 안에 없다** — 0.6R이
  최선인 것은 「거기가 최적」이 아니라 **「격자가 거기서 끝났다」**이고 그 아래는 미측정이다.
  (3) 아래 재진입 교란이 하필 배수 축에 물려 있다. (4) 전부 `baseline` 낙관 렌즈 위 값이고
  **체결 보수화(`pen_5bp`)·같은 분 익절 축은 안 쟀다**(목표를 당기면 그 낙관에 더 기댈 수 있다).
- 🚨 **알려진 한계 — 재진입 재무장 일정이 채택 배수(1.5R)에 고정돼 있다.** 재진입은 「익절 →
  재무장」이라 **익절 시각이 곧 재무장 시각**인데, 배선이 재진입 후보를 1.5R 시퀀싱에서 한 번만
  만들고 배수 오버라이드를 얹는다(WAN-386에서 물려받은 배선). 크기는 `oos_warm` 14,825거래 중
  **1,591건(10.7%)**(WAN-389). ⚠️ **WAN-386과 성격이 반대다** — 거기선 진입 시점이 축이라 이
  고정이 **통제**였는데 여기선 **익절 시각 자체가 축**이라 **교란**이다. 🚨 **방향은 단정 못
  한다**(WAN-389: 재진입은 몫을 **−0.0288R 깎는** 세금 → 과대평가일 수도, 슬롯을 채우는 채널
  → 과소평가일 수도). 고치려면 배수마다 재진입을 다시 파생해야 하고 **재진입 파생이 셀 비용의
  84%**라 4시간 55분이 **16시간대**가 된다 — 별도 이슈.
- 📌 **표본은 안 깨졌다** — ★결정이 붕괴를 보려고 넣은 0.80%에서도 종목당 **224거래**다(20건
  게이트). **그것도 답의 일부다.**
- 🚨 **지갑 층 열은 이 좌표에서 뜻을 잃는다** — 복리를 꺼도 잔고가 0을 뚫어 MDD·수익/MDD·청산이
  전부 「정의 상실」이다(`wallet_defined` · WAN-115 관행 · WAN-386과 같은 술어). 실행 로그의
  「마진콜 사거리」 경고도 그 구간의 산물이다(WAN-367). **읽을 수 있는 건 거래당 net R·gross·
  승률·거래 수·최대 동시 칸뿐**이고, 그래서 판정 자가 처음부터 그것이다.
- ✅ **검산 다섯 통과** — (a-1) 기준 팔 후보 ≡ 엔진 base+재진입 `0.00e+00` · (a-2) 그 지갑 ≡
  채택 북 지갑 24행 `0.00e+00` · (b) 배수 불변 진입 집합 · (c) 진입 유동성 메이커(라벨이 아니라
  후보의 값) · **(d) 겹치는 6조합 ≡ `wan386_confirmation_grid.csv` 168행 `1.78e-15`**(정수 열은
  **정확히 0**, 부동소수만 CSV 왕복 끝자리 = **비트 일치** · 다른 모듈·다른 날의 독립 실행이 같은
  숫자 → WAN-386 이후 머지된 WAN-388/389/392/393이 채택 북을 안 움직였다는 증거이기도 하다).
  🚨 **(d)는 채택 좌표를 돌 때만 성립한다** — 좁혀 돈 판을 대조하면 좌표 차이가 **배선 오류처럼
  보인다**(파일럿에서 `5.63e+04`). `on_adopted_coordinates()`가 그때는 **「건너뜀」으로 찍는다.**
  후보 **생성** 인자가 인자 없는 채택 북과 같은지는 **스파이 테스트**가 고정한다(WAN-330 패턴).
- 📌 **실측 비용 17,729초 = 4시간 55분**(48칸 · `--jobs 4` · M1). 15m 칸당 **약 81분** × 12칸이
  전체의 **74%**이고, 배치 20조합 + 손절폭 분포 + leave-one-out 26판 + 검산은 **분 단위**다
  (파일럿 한 칸 238초). ⚠️ **다른 모듈의 칸 비용을 옮기지 말 것**(WAN-203 → WAN-312 · WAN-383).
  📌 **후보는 저장되지 않는다** — `CellPayload`를 디스크에 남기는 경로가 저장소에 없어 같은 채택
  좌표를 훑는 이슈(WAN-312/330/336/346/359/364/366/378/386/388/389 + 이 이슈)가 **매번 후보를
  새로 만든다.** 캐시를 안 만드는 것은 **stale 서빙 위험**(WAN-106/253/325) 때문이고, 대신
  **축을 착수 전에 확정해 한 번에 훑는 것**이 이 이슈가 쓴 방법이다(사용자 결정 2026-08-31).
- 📌 **다음으로 넘기는 것**: 배수 **0.4·0.5R**(⚠️ 0.4R이면 손익분기 승률이 **71.4%**라 여유가
  곧 소진될 것이나 **안 쟀다**) · 재진입 배수별 재파생 · 배수 축 `pen_5bp` · 배수 × 같은 분
  익절(WAN-336/348/359). 📌 **가드 축은 안 넘긴다 — 닫혔다.**
- **측정 전용 · 기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()` 그대로 ·
  `min_stop_distance_fraction=0.003`(WAN-76/79)·`take_profit_r=1.5`(WAN-81/90) **안 건드렸다**) ·
  **「엣지 없음」(WAN-84/88/111/114/124/151/201/248) 불변**(이 표는 *어디서 손절을 접고 어디서
  이익을 챙기나*를 묻는다 — **다른 질문**) · 전환은 **재-베이스라인 = 사용자 결정** · 개발자
  임의 착수 금지 · 실거래 보류 유지(`ALPHABLOCK_LIVE_TRADING=false`).

📌 **가장 보수적인 가정 위의 채택 북(WAN-346 = 사용자 요청 2026-08-21): 보수 축 둘을
쌓으면 거래당 실력이 **0.1985R → 0.1089R로 절반**이 되지만 **부호는 남는다** — 진값은 그
폭 사이다. 기본값 불변 · 채택 권고 없음.**
[`docs/decisions/wan346.md`](docs/decisions/wan346.md)(재현: `uv run python -m
backtest.wan346_conservative_book --arms A,D,B,C --jobs 4`, 요약만 `--from-csv`). 사용자
요청: *"1분만에 익절된 그것마저 가장 보수적으로 쟀을 때 전체 거래내역을 보고싶어 … 복리도
그렇게 하지 말고."* 축 둘은 **직교한다** — 가로 `pen_5bp`는 *「주문이 채워지느냐」*(큐
우선순위, WAN-96/124), 세로 `no_same_step_tp`는 *「채워진 뒤 그 1분 안의 순서」*(WAN-336).
그래서 **팔 D(둘 다)는 이 저장소가 낸 적 없는 팔**이다.
- 📌 **주 구간 `oos_warm` 판정**: 거래 6,336 → 5,388(85%) · 승률 55.33 → **51.58%** ·
  **거래당 net R 0.1985 → 0.1089(54.9%)** · 손익비 1.487 → 1.153 · 청산 두 팔 다 **0**.
  **산출물은 점이 아니라 폭**이다 — **거래당 진짜 실력은 0.109R ~ 0.199R 사이**이고 0을
  포함하지 않는다(팔 D = 바닥 · 팔 A = 천장 · 좁히는 건 틱·호가 WAN-98, Canceled).
- ⚠️ **WAN-124의 붕괴와 같은 부류로 인용 금지** — 그때는 관통 요구에 15m OOS 수익의
  **96.7%가 사라져 부호가 뒤집혔다**. 이번엔 **크기가 절반이 되되 부호는 남는다.**
- 🚨 **리스크는 오히려 나빠진다 — 「보수적으로 재니 더 안전」이 아니다**: 거래를 15% 덜
  하는데 **MDD가 22.90% → 25.99%(+3.09%p)**. 보수화가 지운 467건이 **전부 이익 거래**(같은
  분 익절)라 **승자만 빠지고 패자는 남기** 때문이다.
- 📌 **§2 복리 착시의 크기가 처음 숫자로 나왔다**(사이징 기준을 초기 자본에 못 박은 옵트인
  팔 `compound_sizing=False`): `oos_warm` 팔 A **151,980% → 854.2%**(CAGR 4,220% →
  **218.8%**) · 팔 D 5,664% → **454.5%**(704% → **141.2%**) · `full`은 **684,130,600% →
  4,024%**. 📌 **요점은 거래당 net R이 거의 안 움직인다는 것**이다(0.1985 → 0.1952 ·
  0.1089 → 0.1073) — **실력은 그대로인데 총수익만 178배 차이**나고 그 차이는 전부 「돈이
  불면 베팅도 커진다」에서 온다. 🚨 **복리 끈 판의 MDD를 「더 안전」으로 읽지 말 것**(22.90%
  → 9.22%) — 베팅은 고정인데 지갑이 8.5배가 되어 **분모만 커진 것**이라 두 MDD는 비교 불가다.
- 📌 **실측 사이징 — 「거래당 1%」는 실제로 중앙값 0.529%다**: 거래의 90%가 상한에 걸린다
  (**거래당 명목 천장 46.4% · ADV 0.5% 유동성 한도 43.7%**). 명목 천장은 자본에 비례해 같이
  크지만 **ADV 상한은 절대값**이라 후반부를 지배해 시드 대비 리스크가 **0.74% → 0.18%**로
  내려간다(WAN-279가 「복리 착시를 깬다」고 적은 것의 실측).
- 📌 **드물게 종목 편중이 아니다** — 팔 D `oos_warm`을 **종목 하나씩 빼고 지갑을 재배치**해도
  거래당 net R이 **12종목 전부 양수**(−ETH 0.0973 최악 ~ −LTC 0.1216 최선, 기준 0.1089).
  ETH·SOL 편중 계열(WAN-111/119/124/151)이 여기서는 성립하지 않는다.
- 📌 **부수 산출물 — 설명 둘이 죽었다**: **베타**(같은 창 12종목 buy&hold 평균 −6.6% ·
  **중앙값 −26.6%**인데 롱 온리로 +854% — DOT −83%·ADA −53%인 **하락장**이었다) · **복리
  착시**(끄고도 +854%/+454%). 🚨 **그래도 「엣지 있음」이 아니다** — 「엣지 없음」이 실제로
  말한 것은 *"같은 존에 **무작위 시각**으로 들어가도 비슷하다"*이고 **그 널은 팔 D 위에서
  돌린 적이 없다**(WAN-350이 그걸 묻는다).
  - 🔁 🚨 **그중 「베타」 갈래에 시점 배너를 단다(WAN-393, 2026-08-31)** — 이 표는
    **2026-08-21**에 돌았고 소급 취소 룩어헤드를 고친 **WAN-365는 2026-08-23**이다. 즉 위
    「롱 온리로 +854%」는 **지는 거래를 골라 지우던 엔진** 위의 값이고, 인과로 고치면 같은
    좌표의 거래당 실력이 **+0.1985R → −0.1798R로 부호가 뒤집힌다**(WAN-364 §4). **인과
    엔진에서 베타 대조를 재확인한 적이 없다.** 게다가 **2026-08-31 페이퍼 실측이 반대
    방향을 가리킨다** — 페이퍼가 돈 창(2026-07-31~08-31)에 12종목 buy&hold가 **+22.55%
    (12/12 상승)**인데 페이퍼 계좌는 비용을 제대로 세면 **−9.4%**였다(WAN-392 · 아래 📌
    WAN-393 문단). ⚠️ **이 문단의 다른 결론까지 무효라는 뜻이 아니다** — 위 🚨 WAN-365
    시점 배너가 이미 「폭 판정」을 무효로 했고, 이 줄은 **「베타」 갈래에만** 한 줄을
    더한다(**복리 착시** 갈래는 그대로 유효하다 — 거래당 net R이 거의 안 움직인다는 관찰은
    엔진과 무관한 회계 사실이다).
- 📌 **§0 도구 — 북이 이제 거래별 내역을 낸다**: `--positions book --trades/--equity`
  (+ `--trades-segment`). 표시 열은 `report.trades_to_display_frame` 재사용 + 북 전용 열
  (**칸(종목·TF)·손절가·익절가·리스크금액·net R·재진입·같은 분 익절·탭 시각**). 짝 계약은
  **값으로** 확인해 어긋나면 죽는다. ⚠️ **`--positions` 미지정 + `--trades`는 예전처럼
  per-cell로 접힌다**(두 회계는 다른 경기다 — WAN-213/341). 관측 필드 둘이 새로 생겼다
  (`take_profit_price` · `is_reentry`, 둘 다 순수 관측 · 안 쓰면 비트 재현).
- 📌 **검산 넷**: (a) 팔 A ≡ 인자 없는 채택 북 **0.00e+00** · (a′) **팔 A ≡ WAN-336 `base`
  팔 32값 전부 차이 0**(다른 모듈·다른 날의 독립 기록) · (a″) BTC 4h `full`이 WAN-327 기록과
  일치(거래 52 · +25.80%) · (b) 팔 D의 같은 분 익절이 **467 → 0건**(후보 층도 전 구간 0).
- **측정 전용 · 기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()` 그대로 · 보수
  팔·복리 노브 전부 옵트인) · **「엣지 없음」 불변**(이 축은 *이미 잰 숫자가 얼마나 낙관인가*
  를 묻는다 — 다른 질문) · `pen_5bp`는 **민감도지 실측이 아니다** · 6년 MDD는 폭락 미포함
  **바닥선** · 기본값 전환은 **재-베이스라인 = 사용자 결정** · 실거래 보류 유지.

📌 **보수 가정 위의 타이밍 매칭 널(WAN-350 = 사용자 지시 2026-08-21 「그 엣지없다는 말을
어떻게든 해소해야겠어」 · WAN-346 후속): 체결 낙관을 걷어내도 유의 셀은 77% 남는다
(`oos_warm` 22/46 → 17/46) — 단 「엣지 있음」이 아니라 **per-cell 탐색**이고, 15m OOS
강세는 새 발견이 아니다. 기본값·토대 불변.**
[`docs/decisions/wan350.md`](docs/decisions/wan350.md)(재현: `uv run python -m
backtest.wan350_conservative_null --part null --tf 4h --jobs 3` → `--tf 2h/1h/15m --append` →
`--part verify`, 요약만 `--from-csv`). WAN-346이 **베타**(같은 창 buy&hold 중앙값 −26.6%인데
롱 온리 +854%)와 **복리 착시**(사이징 못 박아도 +854%)를 죽인 뒤 남은 칸은 하나였다 —
「엣지 없음」(WAN-84/88/111/114/124/151/201/248)이 실제로 말한 *"같은 존에 **무작위 시각**으로
들어가도 비슷하다"*를 **오늘 좌표의 보수 가정 위에서** 다시 묻는 것. 팔 A(채택 엔진) 대
팔 D(`pen_5bp` × 같은 분 익절 금지 = WAN-346 팔 D) · 무력화 축 볼린저 · 12종목 × 4TF ×
못 박은 6년 · 따뜻한 연속 OOS(WAN-166) · **핀 없음**.
- 📌 **자를 옛 계열에서 그대로 물려받는다** — 유효 셀 = 거래 ≥20 · 유의 = `p≤0.05` **이면서**
  실제>무작위평균 · 부트스트랩 200회(시드 124). 상수를 새로 쓰지 않고 `wan151`에서 읽으므로
  「판정이 바뀌었다」와 「자를 바꿨다」가 갈린다.
- 📌 **헤드라인**: `oos_warm` 팔 A **22/46** → 팔 D **17/46**(잔존 **77%**) · `full` 39/48 →
  33/48(85%). **WAN-346과 같은 모양**(거래당 실력 0.199R → 0.109R = 55%, 반토막이되 부호는
  남음) — 두 표가 다른 자로 같은 말을 한다. ⚠️ **WAN-88/124의 붕괴와 같은 부류로 인용 금지**
  (그때는 `baseline` 유의 4셀이 `pen_5bp` 한 번에 **전멸**했다).
- 📌 **TF 기울기 — 신호는 15m에 산다**: `oos_warm` 15m **11/12 → 9/12** · 1h 6/12 → 5/12 ·
  2h 4/12 → 2/12 · 4h 1/10 → 1/10(4h는 심볼당 23거래로 두 셀이 표본 게이트에 걸린다). 15m이
  **채택 북 거래의 66%**(WAN-312)라 표의 무게가 그 행에 있다. 🚨 **단 팔 D 15m에서 BTC가
  유의하지 않다**(p=0.145 · 실제 **−1.22%**) — 가장 크고 유동성 높은 종목이 먼저 떨어진다.
- 🚨 **15m OOS 강세는 이 이슈의 발견이 아니다 — 옛 기록에 이미 있었다.** `wan176_null.csv`
  (차가운 절단 · 9종목 · 필터 켜짐)를 같은 자로 세면 **15m OOS가 7/9**(IS 9/9)다. 즉
  CLAUDE.md·WAN-201이 이 계열을 요약해 온 *"유의는 상승장 IS에 몰리고 OOS에서 급감"*은
  **1h(7/9 → 3/9)·4h에서 참이고 15m에서는 약하다**. **이 이슈가 채운 칸은 「15m이 세다」가
  아니라 그 강세가 (a) 오늘 좌표 · (b) 가장 보수적인 체결 가정 · (c) 한 종목에 안 기대고
  남는가이고, 답이 셋 다 예라는 것**이다.
- 📌 **따뜻한 규약에 기대고 있지 않다** — 검산이 낸 차가운 절단 행과 이 격자의 따뜻한 행을
  **같은 9종목 · 같은 TF · 팔 A**로 맞춰 세면 차이가 **1셀**이다(15m 7/9 → 8/9 · 1h 3/9 → 4/9).
  「따뜻한 OOS는 존 재고를 물려받아 유리하다」는 가장 자연스러운 반론이 이 축에서는 안 선다.
  ⚠️ 팔 D의 차가운 판은 **안 쟀다**(비용 두 배).
- 📌 **드물게 편중이 아니다** — `oos_warm` leave-one-out에서 **ETH를 빼도 19/42 · 15/42**이고
  어느 종목을 빼도 유의가 남는다(최악 −SOL 팔 D 14/42). WAN-111 이래 플러스가 거의 매번
  ETH였던 것과 다르다(WAN-142/152/203 계열의 드문 예외). ⚠️ per-cell 널이라 LOO가 **재집계**다
  — 북처럼 지갑을 다시 배치하지 않으므로 「그 종목이 빠지면 자본이 남는다」 효과는 **없다**.
- 📌 **부수 관찰**: 팔 D 15m `oos_warm`에서 **무작위 팔 심볼평균 +0.87%**(12셀 중 7셀 음수)인데
  **실제 +21.44%**(11셀 양수) — 같은 존·같은 거래 수·같은 방향·같은 시각대에서 **타이밍만
  무작위로 바꾸면 수익이 사라진다**. 그 창은 **하락장**이다(buy&hold 12종목 중 8종목 음수 ·
  중앙값 **−25.5%**) — 베타 설명이 이 축에서도 죽는다(WAN-346 §5-5 재확인).
- 📌 **엔진 배선(옵트인 · 끄면 비트 재현)**: `run_random_control_b_segment(no_same_step_tp=,
  eval_from_ms=)`. 🚨 **보수 축은 실제 팔과 무력화 풀 양쪽에 똑같이 건다** — 한쪽만 걸면 p값이
  규칙이 아니라 **가정 차이**를 재므로 `_build_both_pools`가 두 생성을 한 함수에 묶는다.
  체결 렌즈는 새 인자가 없다(`ConfluenceParams`에 실려 있어 풀이 자동으로 물려받는다).
  신설 `run_random_control_b_evals`가 창을 **한 번만 태우고** `full`·`oos_warm`을 함께 낸다
  (15m 한 셀의 후보 생성만 **220초**라 이 공유가 격자를 감당 가능하게 만든다).
- 📌 **검산 — 노브를 끄면 옛 기록이 비트 재현된다**: 팔 A 차가운 1h가 `wan176_null.csv`(필터
  켜짐)·`wan201_matched_null.csv`(필터 꺼짐)와 대조된다. 🚨 검산은 **DOGE·LINK·LTC의 펀딩을
  비운다** — WAN-292 백필(2026-08-12) 이후엔 실데이터가 있어 같은 코드로도 그 CSV가 안 나온다
  (WAN-312가 같은 자리에서 겪었다). ⚠️ **검산 전용**이고 격자는 실데이터로 돈다.
  테스트가 얹는 실데이터 검산 3종: 널의 「실제」 다리 ≡ `harness.run_once` · `no_same_step_tp`를
  켜면 후보 층 카운터가 **실제로 0** · 따뜻한 창이 실제·풀 **양쪽**에서 진부분집합.
- 🚨 **「엣지 찾았다」로 인용 금지 — 이긴 대상이 시장이 아니다.** 무력화 축이 볼린저라 널의
  풀은 **「존 근단가에 무작위 시각으로 들어간 팔」**이고, 이 표의 주장은 거기까지다.
  **per-cell이라 채택 근거가 아니다**(WAN-341 「판단은 북에서만」 — 승격은 **북 널**이고 그게
  다음 단계다) · **유의 ≠ 수익**(WAN-124) · `pen_5bp`는 **민감도지 실측이 아니다**(큐 우선순위는
  틱·호가 WAN-98, Canceled) · **차가운 `oos`는 격자에 없다**(구간마다 재탐지라 후보를 공유할
  수 없어 비용이 두 배 — 차가운 축은 검산이 1h에서 낸다).
- 🚨 **재진입이 이 널에 없다 — 알고 뺐고 그 이유가 검산이다.** 채택 북은 재진입 ON(band,
  WAN-273)인데 매칭 널은 그것을 **구조적으로 표현하지 못한다**(재진입 후보는 부모가 익절해야
  생겨 「같은 개수를 맞춘다」가 정의되지 않는다) · 표본마다 재시뮬이라 비용이 200배 · **검산
  상대가 전부 재진입-off**라 넣으면 「판정이 바뀌었다」와 「축을 하나 더 움직였다」가 안 갈린다.
  재진입이 들어갈 자리는 **북 널**이다.
- 📌 **「선별 대 가격」 갈래를 관문으로 걸지 않았다** — 사용자 결정 2026-08-21(*"싸게 사는 것도
  실력이다"*)이고 그것을 문서에 못 박는 **WAN-352**가 이 리포트를 「이 관문을 안 거는 첫
  대상」으로 명시했다. ⚠️ **쟀는데 문제없다는 뜻이 아니라 안 쟀고 결격으로 세지 않는다는
  뜻**이다(WAN-352는 이 PR 시점 **Todo** · 옛 WAN-131/152 판정은 그때의 기록으로 유효).
- **측정 전용 · 기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()` 그대로 · 보수 팔
  전부 옵트인) · 총수익 %는 복리 착시(WAN-169/213) · 실거래 보류 유지
  (`ALPHABLOCK_LIVE_TRADING=false`).

📌 **「같은 분 왕복」의 방향(WAN-362 = 사용자 실측 2026-08-22 · WAN-336 §6-2 미실행 측정):
같은 분 익절과 같은 분 손절은 같은 원인이 아니다 — 익절 쪽은 「1분봉이 순서를 모른다」는
낙관이 맞고, 손절 쪽은 「존을 깨는 봉에서는 주문이 이미 취소돼 있다」는 취소 규칙이 닫는다.
기본값·토대 불변 · 엔진 코드 무수정.**
[`docs/decisions/wan362.md`](docs/decisions/wan362.md)(재현: `uv run python -m
backtest.wan362_same_minute_roundtrip` · §1은 서버에서 `uv run alphablock same-minute`).
백테는 채택 북 `oos_warm` 6,336건에서 **같은 분 익절 467 : 손절 7**(67:1)인데 페이퍼 장부
32건은 **손절 5 : 익절 2**로 방향이 반대였다. **새 데이터도 격자도 없이** WAN-346 §0 거래별
CSV × 저장 1분봉만으로 쟀다(WAN-328 `path_fill_price` 훅을 켜려면 북을 66분 다시 돌려야
하는데 **필요가 없었다** — 체결가는 CSV에, 봉은 DB에 이미 있다).
- 🚨 **§2-B 검산이 이 표의 자격 증명이다** — 저장 1분봉과 체결가만으로 기하 판정
  (`저가 ≤ 손절가` · `고가 ≥ 익절가` − 손절 우선)이 **467:7을 그대로 재현**한다. 두 가지를
  동시에 뜻한다: 이 표가 엔진과 같은 것을 재고 있고, **엔진은 같은 분 손절을 인색하게 세지
  않는다**(기하가 허락한 7건을 하나도 빠짐없이 냈다).
- 📌 **§3 답 — 최대 MAE가 「정확히 1.000R」인 것은 우연이 아니라 구조다**: (1) 존을 깨는
  상위TF 봉은 `break_time`을 그 봉 **시작 시각**으로 찍고(롱은 `저가 < 존바닥`, **엄격
  부등호**), (2) 서브스텝 루프는 `step.time >= invalidation_time`이면 **체결을 판정하기
  전에** 취소하므로 **그 봉의 어느 1분에서도 체결이 없다**, (3) 남는 유일한 창이 `저가 ==
  존바닥`(무효화 `<` vs 손절 트리거 `<=`)이다. 실측이 그대로 확인한다 — 진입 봉 저가가 존
  바닥 **아래**인 거래 **0건** · **정확히 같은** 거래 **7건**.
- 🚨 **그 취소는 엄밀히 인과적이지 않다** — 봉이 존을 깨는지는 그 봉이 **닫혀야** 아는데
  취소는 그 봉 **시작**부터 걸린다(백테스트가 봉 끝의 사실로 봉 처음의 주문을 걷어낸다).
  같은 분 손절이 7건뿐인 이유가 「1분봉이 순서를 모른다」가 아니라 **여기**다.
- 🔁 ⚠️ **「라이브가 한 봉 늦어서 라이브만 더 한다」는 설명은 WAN-364가 정정했다 — 재인용
  금지**(아래 📌 WAN-364 문단이 정본). **이 이슈의 측정 수치는 전부 유효하고 해석만 바뀐다.**
  롱 지정가는 존 안에 있고 손절선은 존 아랫변이라 **체결이 무효화보다 반드시 먼저**이고,
  무효화 시점엔 취소할 주문이 이미 없다 — **라이브가 실시간으로 취소해도 그 체결은 못
  막는다.** 원인은 라이브 지연이 아니라 **백테스트 쪽 소급 취소**이고, 라이브 지연이 실제로
  만드는 차이는 **무효화 봉이 닫힌 뒤**의 재터치라는 별개의 작은 축이다. 사용자가 본 LTC
  15m 장면(`14:10 진입 → 14:10 손절`)은 라이브가 **정상적으로 한** 매매다.
- 🚨 **「백테가 손실을 숨겼다」로 인용 금지** — 백테는 그 셋업에 **진입하지 않는다**. 숨긴
  손실이 아니라 **없는 거래**다(WAN-336/348/359 익절 축의 *「손실이 아니라 더 오래 보유」*와
  **다른 종류의 차이**). 🔁 그 「없는 거래」의 크기는 **WAN-364가 쟀다**(아래 문단).
- 📌 **§2-A 체결가는 봉의 아래쪽 4분의 1에 놓인다**(위치 중앙 **0.221**) — ⚠️ 이슈가 예상한
  「0에 몰림」은 **절반만 맞다**(≤0.05는 11.9%). 결정적인 것은 **R로 잰 값**이다: 진입 봉이
  손절 쪽으로 간 거리 중앙 **0.118R** vs 익절 쪽 **0.379R**(3.2배 비대칭).
- 📌 **§2-C 비대칭이 그대로 보인다** — 체결가를 봉 안에서 옮기면 같은 분 **익절** 가능
  건수는 0~1,316건으로 크게 움직이는데 **손절** 가능 건수는 **한 건도 안 움직인다**(손절가는
  존 무효화 경계라 체결가에서 파생되지 않는다). ⚠️ **대안 엔진 제안이 아니라 민감도다**
  (롱 지정가를 봉 중간에 「받는」 방법은 없다).
- 🚨 **§2-D 변동성을 통제해도 두 표는 만나지 않는다** — 같은 분 왕복은 진입 봉 범위 상위 두
  십분위에만 있지만(급락 가설의 절반은 옳다) **가장 격렬한 십분위에서도 익절:손절이 163:1**
  이다(익절 51.42% · 손절 0.32%). 「라이브 표본이 급락에 몰렸다」만으로는 손절 5 : 익절 2가
  설명되지 않는다.
- 📌 **§1은 서버 몫이다**(로컬 `paper_trades` **2행** — 러너는 서버에서 돈다, WAN-195/314와
  같은 제약). 신설 `alphablock same-minute`가 날짜(KST)·종목·TF 분해 + Wilson 구간 + 정확
  이항 p + **날짜 하나씩 빼기**를 낸다. 인용한 실측 32건으로는 **두 축 다 갈리되**(빈도
  21.9% vs 7.5% p=0.0083 · 구성 28.6% vs 98.5% p=1.4e-08) **구성이 훨씬 강하다**(필요 표본
  16건 vs **1건**). 🚨 **둘 다 독립 시행 가정 위의 값**이라 7건이 급락 2분에 몰렸다면 빈도
  축은 판정을 잃는다 — **구성 축은 자릿수가 둘 달라 더 오래 버틴다**(0.11% vs 15.6%).
- 📌 **WAN-361에 넘기는 입력**: 「A/B/C 세 갈래가 빠진 손실을 더하지 않는다」는 **성립한다 —
  다만 그 빠진 손실은 세 갈래가 다루는 축에 없다.** A/B/C는 전부 *같은 분 **익절**을 어떻게
  셀까*인데 페이퍼가 본 같은 분 손절은 **애초에 백테스트에 없는 거래**라 익절 계수 규칙을
  어떻게 고쳐도 나타나지 않는다. 어느 쪽을 골라도 여전히 낙관이고 **그 낙관의 크기는 이
  이슈가 재지 않았다**.
- 🚨 **취소 시점을 라이브에 맞추는 것(또는 그 반대)은 재-베이스라인 = 사용자 결정**이고
  **개발자 임의 착수 금지**다(후보 집합 자체가 바뀐다 — WAN-132급 파급).
- **측정 전용 · 엔진 코드 무수정 · 새 관측 필드 없음** · `ConfluenceParams()`·
  `LeverageBookParams()` 불변 · DB 불변(WAN-194) · 손절폭 가드(WAN-76/79)·존폭 필터
  (WAN-159)·배수(WAN-213) 안 건드렸다 · 전부 `baseline`(낙관) 위 값 · **「엣지 없음」
  (WAN-84/88/111/114/124/151/201/248) 불변**(다른 질문) · 실거래 보류 유지
  (`ALPHABLOCK_LIVE_TRADING=false`).

🚨 **소급 취소 룩어헤드(WAN-364 = 사용자 지적 2026-08-23 · WAN-362 §3 잔여): 백테스트는
「이 봉에서 존이 깨졌다」(봉이 끝나야 아는 사실)로 그 봉 처음으로 돌아가 대기 지정가를
취소한다 — 그렇게 지워지는 셋업은 무작위가 아니라 정의상 손절로 끝났을 셋업이다. 취소를
인과로 바꾸면 채택 북 `oos_warm`의 거래당 net R이 **+0.1985R → −0.1798R로 부호가
뒤집힌다**. 측정 전용 · 기본값 불변 · 전환은 재-베이스라인 = 사용자 결정.**
[`docs/decisions/wan364.md`](docs/decisions/wan364.md)(재현: `uv run python -m
backtest.wan364_invalidation_cancel --census-only` → `--arms A --jobs 4` → `--arms B --jobs 4
--append`, 요약만 `--from-csv`).
- 📌 **인과적으로는 발동할 수 없는 규칙이다** — 롱 지정가는 존 안에 있고(밴드가 존보다
  아래면 진입 자체가 기각, WAN-75 규칙 3) 손절선은 존 아랫변이라 순서가 **강제된다**:
  가격 하락 → 지정가 통과(체결) → 계속 하락 → 존 아랫변 돌파(무효화 = 손절). **체결이
  무효화보다 반드시 먼저**라 무효화 시점엔 취소할 주문이 없다 = 이 규칙은 **미래를 알 때만**
  무언가를 지운다. 사용자 지적 그대로다(*"손절을 해야지 뭘 취소한다는 거냐"*).
- 📌 **지우는 층이 둘이다** — (1) **시그널 층**: 무효화 봉에서 난 탭은 `status="cancelled"`라
  후보로 **만들지도 않는다**. (2) **시뮬레이터 층**: 그 전에 걸린 주문도 `step.time >=
  invalidation_time`이면 **체결 판정 전에** 취소된다. 🚨 **한 층만 고치면 결과가 기준선과
  거의 같아 조용히 통과한다** — 노브 하나가 두 층을 함께 바꾼다(옵트인
  `invalidation_cancel="bar_close"` · 안 켜면 비트 재현 · 재진입 경로에도 같이 흐른다).
- 📌 **§2 탐지 층 인구조사(48칸 65초 · 1분봉 안 읽음)**: 무효화 **봉에서** 난 탭이 전체 탭의
  **15.59%**(34,393 / 220,656)이고 **TF를 거의 안 가린다**(15m 15.45% · 1h 15.91% ·
  2h 15.93% · 4h 15.96%). 종목은 13.31%(LINK 15m)~20.17%(XRP 4h).
- 🚨 **§1 판정 — 「부풀렸다」가 아니라 「그게 전부였다」**: 채택 좌표 북(12종목 × 4TF 한 지갑 ·
  재진입 ON · cap_only 5배 · 핀 없음) `oos_warm`에서 거래 **6,336 → 9,514건(+50.2%)** · 승률
  **55.33% → 40.28%(−15.06%p)** · 거래당 net R **+0.1985 → −0.1798** · net R 합 **+1,258R →
  −1,710R** · MDD **22.90% → 100.00%**. 🚨 **WAN-346과 다른 부류다** — 그쪽은 보수 축 둘을
  쌓고도 「크기는 절반, **부호는 남는다**」였는데 여기서는 **부호가 넘어간다**.
- 📌 **되살아난 거래 3,309건(전체의 34.8%)의 정체**: 손절 2,922 · **익절 387(11.7%)** · 승률
  11.70% · 거래당 net R **−0.8958** · 합 **−2,964R**. ⚠️ **「전부 −1R」은 틀렸다** — 그 봉
  안에서 먼저 익절선까지 갔다가 나중에 존을 깨는 경로가 실제로 있다. 다만 팔 A의 +1,258R를
  이 −2,964R가 **통째로 덮는다**.
- 📌 **종목 편중이 아니다** — 종목을 하나씩 빼고 **지갑을 재배치**해도 팔 B는 **13개 지갑
  전부** 거래당 net R이 음수다(−0.1710 ~ −0.1845). ETH·SOL 편중 계열(WAN-111/119/124/151)이
  여기서는 성립하지 않는다.
- 🚨 **「청산 0건」이 여기서 아무것도 보증하지 않는다** — 팔 B는 MDD 100%인데 청산 트리거가
  0이다(사이징이 자본의 %라 연쇄 손실로는 청산 조건이 **구조적으로** 안 걸린다 — WAN-312 §4).
- 📌 **검산 넷**: (a) 팔 A ≡ 인자 없는 채택 북 **0.00e+00**(덤으로 WAN-346 팔 A·WAN-336
  base와 값이 같다 — 다른 모듈의 독립 기록) · (b) 팔 A의 무효화 봉 체결이 전 구간 **0건**
  (정의상) · 팔 B는 후보 층 카운터가 0이면 **실행이 죽는다** · (c) 되살아난 체결이 무효화
  봉 **안**에만 있다(「취소 끔」으로 새지 않았다) · (d) **저장 1분봉 손검증** — BTC 4h
  되살아난 거래에서 체결(16:12)이 존바닥 돌파(18:44)보다 **2시간 32분 먼저**다.
- 🚨 **기본값은 안 바꿨다 — 전환은 재-베이스라인 = 사용자 결정**이고 **개발자 임의 착수
  금지**다. 후보 집합 자체가 바뀌므로 WAN-132(진입가)·WAN-149(존 병합)·WAN-159(존폭 필터)급
  파급이고 **그 위에 쌓인 모든 표가 「그때는 그랬다」로 얼어붙는다**. ❌ **라이브 취소를
  앞당기는 것은 답이 아니다**(순서 논증대로 그래도 체결을 못 막는다 — 라이브의 「한 봉 늦음」은
  **무효화 봉이 닫힌 뒤**의 재터치라는 별개의 작은 축이다).
- ⚠️ **「엣지 없음」(WAN-84/88/111/114/124/151/201/248)이 뒤집힌 게 아니다** — 이 축은 *진입
  규칙이 무작위와 구분되는가*가 아니라 *이미 잰 숫자가 얼마나 낙관인가*를 묻는다. 게다가 이
  결함은 널(대조군) **양쪽에 똑같이 걸리므로** 그 판정을 뒤집을 방향이 아니다. ⚠️ 전부
  `baseline`(낙관) 위 값 · 체결 보수화(`pen_5bp`) 축은 **안 쟀다** · 총수익 %는 복리 착시
  (WAN-169/213) · **판단은 북에서 낸다**(WAN-341) · 손절폭 가드·존폭 필터·배수·재진입·익절
  배수 **안 건드렸다** · 실거래 보류 유지(`ALPHABLOCK_LIVE_TRADING=false`).

📌 **진입 순간 MACD 색(WAN-372 = 사용자 제안 2026-08-25 · 관측 전용): 채택 북 거래의
**95.8%가 「진한 빨강」**이라 사용자가 물은 「진한 빨강일 때만 진입」은 **사실상 아무것도 안
거른다** · 「연한 빨강」(0.5%)만 극단적으로 좋은데 그 지문이 WAN-104의 **반등 체결**과 거의
같다. 기본값·토대 불변 · 채택 권고 없음.**
[`docs/decisions/wan372.md`](docs/decisions/wan372.md)(재현: `uv run python -m
backtest.wan372_macd_color --jobs 4`, 요약만 `--from-csv` · 거래별 색 열은 `uv run python -m
backtest.run --positions book --oos-warm --observe-macd --trades t.csv`). 이 저장소가 MACD 축을
**한 번도 재본 적이 없어** 열어 본 자리다 — 이미 하는 매매에 「그때 색이 무엇이었나」를 붙일 뿐
**매매를 하나도 안 바꾼다**.
- 📌 **분포가 첫 산출물이고 그것이 답의 절반이다** — 롱 지정가는 가격이 **내려와야** 체결되니
  체결 순간이 구조적으로 하락 중이다(진한 빨강 95.8% · 연한 초록 3.7% · 연한 빨강 0.5% ·
  진한 초록 0.04%). **색으로 고를 여지 자체가 거의 없다.**
- 📌 **공식 렌즈 `oos_warm` 거래당 net R**: 연한 빨강 **+0.8627R(48건 · 승률 79.2%)** · 연한
  초록 −0.0310R · 진한 빨강 **−0.1409R(9,055건 · 39.9%)**. 앞구간(`is`)도 연한 빨강이 최선
  (+0.3651R)이라 **뒤집히지 않았다**(완료기준 3 ②). 📌 평균은 소수 대박이 아니라 **승률**이
  만든다(고정 1.5R에서 승률 79.2% → 기대 그로스 +0.98R ≈ 실측 +0.86R + 비용 0.12R).
- 🚨 **그래도 「알파 찾았다」로 인용 금지 — 되돌림 대리변수일 가능성이 크다.** 라이브 `hist`는
  **체결 순간 현재가의 증가함수**라, 롱 체결에서 `hist > hist[1]`(연한 빨강)은 *「존까지
  내려왔는데도 모멘텀이 위로 꺾여 읽힌다」* = 대체로 **살짝 찍고 곧바로 되돌아선 체결**이다.
  그 지문이 **WAN-104 증분 거래와 거의 같다**(승률 76.9% vs **79.2%** · 평균 +0.92R vs
  **+0.8627R** · 희소성도 같은 자릿수). 하필 그 부류가 **큐 우선순위상 실거래에서 가장 안 될
  체결**이다(WAN-96/124).
- 🚨 **「48건은 너무 적지 않나」의 답: 부족한 건 표본 수가 아니다**(사용자 질문 2026-08-27).
  이항검정으로 `oos_warm` **z=+5.56 · p=3.3e−08**(다중비교 ×4 보정해도 1e−7) · net R 95%
  신뢰구간 **[+0.575, +1.150]**이라 **검정력은 충분하다**. 부족한 건 셋이다: (1) **독립성
  미측정**(48건이 시간·종목으로 뭉쳐 있으면 실효 표본이 작다 — 북엔 심볼 열이 없어 이 표로는
  못 본다, WAN-328 선례) · (2) **효과 크기가 구간 간에 안 붙는다**(`is` 59.1% → `oos_warm`
  79.2%, z=+2.58 — **부호는 유지되나 크기는 두 배 흔들린다**, 점추정 인용 금지) · (3)
  **유의성은 「알파냐」를 안 묻는다**(되돌림 대리변수여도 당연히 유의하다). 📌 실용 축에서는
  진짜로 적다 — 6년 12종목 4TF **전부 합쳐 141건 = 연 24건**.
- 🚨 **판정이 사실상 15m 38건에 걸려 있다** — `oos_warm` 연한 빨강 48건 중 38건이 15m이고
  1h·2h는 각 5건으로 **표본 미달**(4h 0건). 20건 문턱을 겨우 넘는 표본이고, 15m은 WAN-124가
  낙관 체결 의존이 가장 깊다고 잰 축이다.
- 📌 **§0 엔진은 옵트인·순수 관측이다** — `strategy/realtime_macd.py`(신설, `RealtimeRsi`·
  `RealtimeBand`와 같은 모양)가 **체결 순간**의 봉내 라이브 히스토그램을 내고
  (`ZoneLimitOutcome.macd_hist`/`macd_hist_prev` → `_Candidate` → `PlacedSetup`), **색 문자열은
  어디에도 안 싣는다**(판정 규칙은 `macd_color` 한 곳). 시점은 **체결 순간**이다(사용자 확인
  2026-08-27) — 탭 봉 종가는 룩어헤드, 직전 확정봉은 가격이 존까지 내려온 구간을 통째로
  버린다(WAN-119). ⚠️ **EMA라 색이 봉 안에서 바뀐다** — 체결 순간의 색으로 확정하고 그 봉이
  어떻게 끝나든 안 바꾼다. **재진입 경로에도 같이 배선했다**(WAN-345 부류 — 회귀 테스트는
  인자를 넘기는 줄이 아니라 **재진입 후보에 색이 실제로 붙는지**로 건다).
- 📌 **검산 셋 전부 0** — (a) 관측 팔 ≡ **인자 없는 채택 북**(수익·MDD·거래 수 `0.0`) =
  「관측 전용」이 라벨이 아니라 숫자다 · (b) 색 귀속 = 전수 · (c) 색 없는 재진입 거래 **0건**.
  미래 절단 불변은 **WAN-377이 만든 자를 그대로** 쓴다(봉 안에서 잘라도 이미 끝난 거래의 색이
  비트 동일 — 룩어헤드가 섞이면 죽는다).
- 📌 **실측 비용**: 48칸 · `--jobs 4` · M1 · **2시간 16분**. ⚠️ WAN-330의 「팔당 66분」과 나란히
  인용 금지 — 그 판은 차가운 절단을 안 돌고 이 실행은 칸마다 `full`·`is`·`oos` 세 창을 돈다.
- ⚠️ **안 잰 것 둘이 곧 판정을 가르는 축이다**: ① 연한 빨강 × 「같은 분 익절」(WAN-336) 교차표
  ② **색 축 체결 보수화**(`pen_5bp`). 직교 축이라 격자가 두 배가 된다. **그 둘 전에는 필터로
  가지 않는다.**
- ❌ **색으로 거르는 팔은 만들지 않았다**(사용자 최종 목표가 필터인 것은 기록돼 있고 이 이슈는
  **순서**를 못 박은 것이다). 🚨 필터로 가면 **북에서 다시 재야 한다** — 색으로 거래를
  걸러내면 비워진 자본·명목 자리를 다른 칸이 써서 이 표의 그 색 행과 **다른 숫자**가 된다
  (WAN-341 · WAN-323). **개발자 임의 착수 금지 · 사용자 결정.**
- **측정 전용 · 기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()` 그대로 · MACD는
  전략 파라미터가 **아니다** · 관측은 전부 옵트인 · 끄면 비트 재현) · **「엣지 없음」(WAN-84/88/
  111/114/124/151/201/248) 불변**(이 표는 *이미 한 거래를 색으로 나눠 본다*이지 *새 신호를
  찾는다*가 아니다) · 전부 `baseline`(낙관) 위 값 · MACD 파라미터 12/26/9는 **스윕 금지**
  (WAN-161) · 실거래 보류 유지(`ALPHABLOCK_LIVE_TRADING=false`).

📌 **존 병합 × 재탭 차단 2×2(WAN-388 = 사용자 설계 2026-08-28): 헤드라인 **+0.0161R**로
판정선을 넘고 메커니즘도 서지만 **값은 거의 전부 「재탭 차단」의 몫이고 병합은 ≈0**이다.
기본값·토대 불변 · 채택 권고 없음(재-베이스라인 = 사용자 결정).**
[`docs/decisions/wan388.md`](docs/decisions/wan388.md)(재현: `uv run python -m
backtest.wan388_merge_retap_census` · `uv run python -m backtest.wan388_merge_x_retap --jobs 4`).
묻는 것은 *「겹치는 오더블록을 뭉치로 보고 그 뭉치에 한 번만 들어가면 거래당 net R이
나아지는가」*다 — 노리는 메커니즘은 WAN-370이 잰 **「넓은 손절 = 싼 거래」**(손절폭 5분위로
비용 R이 0.312 → 0.069로 4.5배 갈린다)이고, 병합은 그 넓은 손절을 **버리지 않고 만든다**
(가드는 좁은 거래를 버리는데 병합은 얇은 것 여럿을 두꺼운 하나로 바꾼다).
- 📌 **§0 — 새 스위치를 만들지 않았다.** 엔진에는 둘 다 이미 있었고(`OrderBlockParams.
  combine_obs` WAN-149 · `ConfluenceParams.retap_mode` WAN-123) **북 후보 생성 경로에만**
  없었다. `wan169.run_cells`/`_Task`에 `combine_obs`(기본 `False` = 채택) ·
  `retap_mode`(기본 `None` = 채택 `"every_tap"`)가 생겼고, 기본값이면
  `OrderBlockParams(combine_obs=False) == OrderBlockParams()`이고 `build_params(retap_mode=
  None) == build_params()`라 **비트 단위로 같다**(테스트가 **값 동등성**으로 고정 — 이름이
  아니라). `PlacedSetup`에 라벨 둘(`tap_index`·`zone_key`)이 붙었다(순수 라벨 · 기본값 =
  후보 기본값 · `is_reentry`·`trigger_time`과 같은 부류) — 없으면 「탭이 N% 준다」와 **「북
  거래가 N% 준다」**를 북 층에서 가를 수 없다.
- 🚨 **`combine_obs`는 탐지 파라미터라 값마다 오더블록을 다시 탐지한다**(WAN-149) — 네 팔이
  payload를 **공유하면 안 된다**(공유하면 라벨만 다른 같은 숫자). 그래서 팔마다 별도
  `run_cells` 실행이고, 🚨 **북은 이어붙일 수 없으므로**(WAN-316) TF가 아니라 **팔로만**
  쪼갠다(`--arms` + `--append`).
- 🚨 **재탭 차단(`"once"`)과 재진입(WAN-273)은 다른 축이다** — 재탭은 *「무효화 전까지 그
  존을 다시 건드릴 때마다 새로 진입」*, 재진입은 *「익절로 나온 뒤 재무장」*이다. `"once"`로도
  재진입은 그대로 돌고, 격자 네 팔 전부 재진입은 채택값(band)으로 켠 채다. 축이 걸리는 자리는
  **어느 시그널 목록을 소비하나**(`signals` vs `retap_signals`)이고 회귀 테스트가 그것으로
  고정한다. ⚠️ **「구성 존별 첫탭 → 클러스터별 첫탭」 팔은 착수 전에 기각됐다** — `tap_index`는
  지금 아무 게이트도 안 타므로(WAN-123) 그 스위치는 **라벨만 바꾸고 진입 집합을 안 바꾼다**.
- 📌 **§1 인구조사(탐지 층 · 48칸 8.3분 · 관문 통과)**: 분리 팔 모든 탭 **220,656**(첫탭
  98,334 · **재탭 122,322 = 55.44%**) → 병합 팔 195,498(**−11.40%**)인데 **첫탭은 −0.03%**이고
  **재탭만 −20.54%**다. **병합이 무는 탭 비율 18.70%** · 폭 배수 중앙 1.62~1.70배 · 클러스터
  구성원 수 중앙값 **1.00**(= 대부분의 클러스터에서 병합은 아무 일도 안 한다). **TF가 이 축을
  거의 안 가른다**(무는 비율 17.4~19.0% · 재탭 몫 54.0~55.6%).
- 🚨 **착수 전 BTC 실측과 한 가지가 크게 다르다** — 재탭 몫이 BTC 3.5년에서 38.5%였는데
  유니버스 6년에서는 **55.4%**다(창·종목이 다르다 — **두 수를 같은 표에 놓지 말 것**). 무는
  비율(20.8% ↔ 18.70%)·폭 배수(1.71배 ↔ 1.62~1.70배)는 확인된다.
- 📌 **관문은 코드가 낸다**(`census.gate_verdict` · `BITE_GATE = 0.05` 상수 + 테스트로 못 박음
  — 결과를 보고 선을 옮기지 못하게) · **탭 가중**이라 얇은 칸이 과대 대표되지 않는다(회귀
  테스트가 그 함정을 동작으로 고정).
- 📌 **착수 전에 못 박은 §2 판정선(코드 상수 + 테스트)**: `병합·첫탭만 − 분리·매탭`의 거래당
  net R이 **+0.005R**(WAN-366/370 노이즈선)을 못 넘으면 **채택 권고 없음**이고, 넘더라도 비용
  분해에서 **「비용 절감 > gross 감소」**가 확인되지 않으면 **메커니즘 미성립**으로 적는다.
  `verdict_for()`가 계산하고 요약이 문장으로 찍는다 — 사람이 표를 보고 정하지 않는다.
- 🚨 **2×2여야 한다** — 대각선 둘만 돌리면 효과가 병합에서 왔는지 재탭 차단에서 왔는지 **못
  가른다**(WAN-131 「선별인가 가격인가」 함정의 이 축 판). 팔이 덜 차면 요약이 그 사실을
  **표에 찍는다**. 검산 넷: (a-1) `split_every` ≡ **인자 없는 채택 북**(파일럿 실측
  **0.00e+00**) · (a-2) `--checksum-book` 독립 생성 · (b) 팔 사이 좌표 불변 · (c) **첫탭만
  팔의 배치 거래에 재탭이 하나도 없다**(라벨이 아니라 **동작**으로 축이 걸렸음을 증명).
- 📌 **§2 격자 판정(주 수치 `oos_warm` · 4팔 × 48칸 · 실측 6시간 38분)**: 거래당 net R이
  `split_every`(채택 북) **−0.1194** → `merge_every` −0.1217 → `split_once` −0.1042 →
  **`merge_once` −0.1033**. **헤드라인 +0.0161R**(`full` +0.0180R)로 못 박은 판정선
  (+0.005R)의 3배이고, 메커니즘도 성립한다(**비용 절감 +0.0131R** vs **gross 변화
  +0.0030R = 증가**).
- 🚨 **그런데 값은 거의 전부 재탭 차단의 몫이다** — 병합 단독이 두 상태 모두 노이즈선 안
  (매탭 **−0.0023** · 첫탭만 **+0.0009**)이고 **상호작용도 ≈0**(+0.0032)이라 두 축은
  **독립**이다. **병합을 빼도 헤드라인이 거의 그대로다**(팔 3 +0.0152 vs 팔 4 +0.0161).
  📌 **둘이 같은 점수에 다른 길로 온다** — 팔 3은 **gross를 올려서**(0.0765 → 0.0894 =
  +17%), 팔 4는 **비용을 줄여서**(0.1960 → 0.1829). 병합이 gross를 깎는 대신 비용을 사는
  교환이 정확히 상쇄된다.
- 🚨 **이슈의 가설과 메커니즘이 다르다** — 이슈는 「병합이 손절폭을 넓혀 **비용**을 줄인다」를
  노렸는데, 재탭 차단이 값을 만든 경로는 **비용이 아니라 gross**(선별)다. 그리고 **병합의 폭
  효과는 예상의 10분의 1**이었다 — §1 폭 배수 중앙값 1.62~1.70배인데 거래 전체 손절폭
  중앙값은 **+9.3%**만 움직인다(**무는 탭이 18.7%뿐**이라 중앙값이 안 밀린다).
- 🚨 **개선분에 채널이 섞여 있고 이 표는 못 가른다** — 재진입 거래가 **1,591 → 4,142(팔 3)·
  3,506(팔 4)** 로 2.2~2.6배가 된다(재탭이 슬롯을 비우자 재진입이 채웠다 — 후보 −57%인데
  거래는 −12.7%만 준다). 「재탭을 뺀 몫」과 「재진입이 늘어난 몫」의 분리는 **후속 이슈**다
  (WAN-323의 「공유 자본이 자리를 비워 준다」 채널과 같은 부류).
- 📌 **leave-one-out 13/13 전부 양수**(+0.0125R ~ +0.0220R) — **ETH 편중 계열(WAN-111/119/
  124/151)이 여기서는 성립하지 않는다.**
- 🚨 **위험의 모양은 안 쟀다 — 이 좌표에서는 원리적으로 못 잰다.** 거래당 기대값이 음수라
  **복리를 꺼도 켜도 지갑 층 지표가 팔을 못 가른다**: 끄면 자본이 0을 뚫어(−1106%) MDD가
  995%·청산 카운터가 매 스텝 발동하고, 켜면 **네 팔 전부 −100%·MDD 100%로 포화**한다
  (검산 (a-1)의 복리 판 실측: `oos_warm` −99.9999% · MDD 99.9999% · 청산 0). 그래서
  `wallet_defined()`가 그 열들을 **비율로 내지 않고 「정의 상실」로 찍는다**(WAN-115 관행 ·
  wan386과 같은 술어). ⚠️ **거래당 net R은 이 함정에 안 걸린다**(분모가 초기 자본 사이징).
- ⚠️ **알고 받은 한계**: 팔 2~4는 **차가운 `is`/`oos`가 없다**(사용자 결정 `--no-cold-segments`,
  시간 절반) — 「앞구간과 독립인가」(WAN-166)는 안 쟀고 판정 줄의 그 행은 `—`다.
  **그리고 여전히 −0.103R이다** — 간극이 13% 줄었을 뿐 **부호는 그대로**(WAN-370: 비용을
  0으로 만들어도 천장이 +0.09R). 🔁 그 「+0.09R」은 **WAN-396이 +0.007R로 정정**했다(허수 진입
  슬리피지 · 필터 끔 좌표는 +0.002R).
- 📌 **부수 산출 — WAN-374(Todo·미착수)의 헤드라인이 함께 나왔다**: 그 이슈의 `D−C`가 이 표의
  `merge_every − split_every`와 **같은 차**이고 답은 **−0.0023R(≈0)** = *「거래는 8.8% 줄지만
  거래당 실력은 안 변한다」*. ⚠️ 그 이슈를 닫거나 축소하는 것은 **PM·사용자 판단**이다.
- 🚨 **WAN-149 §3의 옛 판정을 인용해 결론 내지 말 것** — 그 표는 **옛 엔진**(소급 취소 · 존폭
  필터 켬 · 익절 테이커)이고 `total_return`으로 쟀으며, 스스로 *「두 팔은 아예 다른 거래를 한다
  — 이 표는 못 가른다」*고 적었다. **비용 렌즈로는 아무도 안 봤다.**
- ⚠️ **차트 버그 기록**: 병합 시그널은 합쳐진 존을 싣는데 차트 목록은 원본 단위 아카이브라
  `zone_key`가 안 맞아 근거 박스가 안 보인다(WAN-149가 문서화한 알려진 한계 · **측정에는
  무영향**이지만 병합 팔의 화면은 그 상태다).
- 🚨 **「흑자」로 기대하지 말 것** — WAN-370: 비용을 **0으로 만들어도** 천장이 **+0.09R**이다.
  이 축이 노리는 것은 간극 0.13R의 **일부**이지 부호 전환이 아니다. 🔁 그 「+0.09R」은 **WAN-396이
  +0.007R로 정정**했다(허수 진입 슬리피지 · 필터 끔 좌표는 +0.002R).
- **측정 전용 · 기본값·토대 불변**(`ConfluenceParams()`·`OrderBlockParams()`·
  `LeverageBookParams()` 그대로 · 두 축은 전부 옵트인 · 안 주면 비트 재현) · 판단은 북에서
  (WAN-341) · 핀 없이(WAN-305) · 전부 `baseline`(낙관) 위 값 · 6년 MDD는 폭락 미포함 **바닥선**
  · **「엣지 없음」(WAN-84/88/111/114/124/151/201/248/386) 불변**(이 표는 *같은 셋업을 몇 번에
  나눠 잡나*를 묻는다 — **다른 질문**) · 채택은 **재-베이스라인 = 사용자 결정** · 개발자 임의
  착수 금지 · 실거래 보류 유지(`ALPHABLOCK_LIVE_TRADING=false`).

📌 **재탭 차단의 귀속(WAN-389 = WAN-388이 스스로 남긴 후속): 값은 **재탭을 뺀 몫**이고
재진입은 그 몫을 **부풀린 게 아니라 깎는다** — 이슈 가설과 **부호가 반대다**. 재진입을 끄면
재탭 차단 효과가 **+0.0152R → +0.0440R로 커진다(2.9배)**. 기본값 불변 · 채택 권고 없음.**
[`docs/decisions/wan389.md`](docs/decisions/wan389.md)(재현: `uv run python -m
backtest.wan389_retap_attribution --jobs 4`, 요약만 `--from-csv`). WAN-388이 「이 격자는 못
가른다 · 후속 이슈 감이다」로 남긴 자리다 — 그 표에서 재탭 차단 팔은 사실 **두 가지를 동시에**
했다(재탭 거래를 뺐고 **재진입 거래를 2.6배로 늘렸다**). 격자는 재탭 × 재진입 **2×2**(존은 넷
다 채택값 분리 — 병합 축은 WAN-388이 ≈0으로 닫았다).
- 📌 **판정 줄** `oos_warm`: 재탭 차단(재진입 켬) **+0.0152R** · (재진입 끔) **+0.0440R** ·
  차 = 「재진입이 채운 몫」 **−0.0288R**. `full`도 같은 방향(+0.0172 · +0.0423 · −0.0251).
  두 효과 다 노이즈선(±0.005R)의 **5.8~8.8배**라 선에 걸려 있지 않다.
- 🚨 **채우는 것 자체는 사실이다**(재진입 거래 1,591 → 4,142 = 2.60배) — 그런데 **그렇게
  채워 넣은 거래가 빼낸 재탭 거래보다 나빠서** 잠재 이득의 **65%가 관측값에 도달하기 전에
  사라진다**. 재진입은 이 축의 **교란이 아니라 세금**이었다.
- 📌 **슬롯 경합은 양방향이다**(부수 관측) — 재진입을 끄면 이번엔 **재탭이** 빈자리를
  가져간다(재탭 거래 4,824 → 4,912). 같은 슬롯을 두고 두 후보 경로가 경쟁한다는 것이 한 표
  안에서 두 번 보인다. **북에서만 생기는 채널**이고 per-cell로는 원리적으로 안 보인다
  (WAN-323 반익절 래더와 같은 부류 · WAN-341).
- 📌 **메커니즘은 비용이 아니라 gross다** — `split_once_no_reentry`는 비용이 거의 안 변하는데
  (0.1960 → 0.1900) **gross가 +0.0765 → +0.1242로 62% 오른다**(승률 40.08% → 42.05%).
  WAN-388이 건 관문(「비용 절감 > gross 감소」)과 **방향이 다르다**.
- 📌 **다음 행동은 둘 다 살아 있다**(이슈는 배타로 놓았다): **재탭 선별이 진짜**이고 재진입을
  걷어내면 **더 크다**(`retap_mode` 재-베이스라인을 물을 근거가 WAN-388보다 강해졌다) ·
  **슬롯 배분도 진짜**인데 「효과를 만든 채널」이 아니라 **「효과를 깎는 채널」**이다(재진입이
  슬롯을 가져가는 우선순위는 아무도 고른 적 없는 기본값이고 이 표가 거기 **−0.0288R의
  가격표**를 처음 붙였다).
- 🚨 **채택 권고 없음** — 거래가 **−36%**(14,825 → 9,452)라 「덜 매매해서 좋아 보이는 것」과의
  구분이 이 표만으로 안 끝나고(WAN-378), 재탭 차단을 다 인정해도 `oos_warm`은 **−0.0658R로
  여전히 깊은 마이너스**다(WAN-370: 비용 0이어도 천장 +0.09R — 🔁 그 「+0.09R」은 **WAN-396이
  +0.007R로 정정**했다(허수 진입 슬리피지 · 필터 끔 좌표는 +0.002R).). ❌ **재진입을 끄자는 제안이
  아니다** — `reentry=True`는 WAN-273 **사용자 결정**이고 재진입 끈 팔은 **귀속용 반사실**이다.
- 📌 **컴퓨트 사실(다음 이슈가 인용할 값)**: `reentry`는 후보 생성이 아니라 **배치** 축이라
  (`_segment_cells(include_reentry=)` · base 후보 불변) 재탭 모드마다 payload를 **한 번만**
  만들고 배치를 두 번 한다 — **팔 넷이 후보 생성 둘**이다. 실측 **2.80시간**(48칸 · `--jobs 4`
  · M1 · 차가운 구간 생략)이고 **후보 생성 103.6분 + 64.3분에 배치는 두 팔 합쳐 6초**다.
  ⚠️ 그 비대칭이 위험이기도 하다 — 같은 payload로 두 팔을 만드니 **「재진입 끔」이 이름만
  그럴** 여지가 크고, 그래서 검산이 재진입 거래 **0건**을 동작으로 요구한다.
- 📌 **검산 넷 통과**: (a) `split_every` ≡ `wan388_merge_x_retap_grid.csv`의 같은 팔(정수 열은
  **0.00e+00** · 부동소수 열만 8.50e-17 = CSV 텍스트 왕복 잡음) = **다른 모듈·다른 실행이 같은
  숫자** · (b) 재진입 끈 팔 재진입 거래 전 구간 0 · (c) 첫탭만 팔 재탭 거래 전 구간 0 ·
  (d) 네 팔 칸 수·심볼 수 불변.
- ⚠️ **종목 leave-one-out은 안 돌렸다**(개발자 판단 · wan389.md §3-3에 이유와 실측 비용 기록).
  착수 시 비싸다고 짐작해 뺐는데 배치가 사실상 공짜임이 실행 중 드러났다(4팔 합쳐 ~3분이었을
  것) — **짐작이 틀렸고 표준 관문 하나가 빠졌다**. 되돌리려면 후보 생성 ~2.8시간을 다시 한다
  (payload 미저장). 필요하면 `--loo`로 낸다.
- ⚠️ **매칭 널을 안 거쳤다** · 전부 `baseline`(낙관) 위 값 · **위험의 모양은 이 좌표에서 못
  잰다**(거래당 기대값이 음수라 MDD·청산이 「정의 상실」 — 요약이 `—`로 찍는다, WAN-388 §2) ·
  **측정 전용 · 기본값·토대 불변**(`ConfluenceParams()`·`OrderBlockParams()`·
  `LeverageBookParams()` 그대로) · 판단은 북에서(WAN-341) · 핀 없이(WAN-305) ·
  **「엣지 없음」(WAN-84/88/111/114/124/151/201/248/386) 불변**(*같은 셋업을 몇 번에 나눠 잡나*
  를 묻는다 — **다른 질문**) · 채택은 **재-베이스라인 = 사용자 결정** · 개발자 임의 착수 금지 ·
  실거래 보류 유지(`ALPHABLOCK_LIVE_TRADING=false`).

📌 **재탭 × 재진입 × 익절 배수 세 축(WAN-394 §1 = 사용자 지적 2026-08-31): 순진하게 더하면
+0.047R로 양수인데 **셋을 함께 흔들면 +0.0039R ± 0.0079로 0과 구분되지 않는다** — 약속된
이득의 **92%가 사라진다**. 순진한 덧셈이 틀린 **세 번째** 사례. 기본값·토대 불변 · 채택
권고 없음.** [`docs/decisions/wan394.md`](docs/decisions/wan394.md)(재현: `uv run python -m
backtest.wan394_retap_reentry_tp --jobs 4 --no-cold-segments --loo`, 요약만 `--from-csv`).
두 표가 각각 다른 축을 고정해 **세 축이 만나는 칸이 격자에 없었다**(WAN-389는 배수 1.5R
고정 · WAN-381은 재탭·재진입 켬 고정). 12종목 × 4TF 한 지갑 · 못 박은 6년 · 가드 0.30% 고정 ·
**핀 없음**.
- 📌 **판정 줄(`oos_warm`)**: 채택 −0.1194R · ＋두 축 끔 +0.0536 · ＋배수 0.6R +0.1130 →
  **순진한 덧셈 +0.0471R** vs **실측 +0.0039R** = **상호작용 −0.0433R**. `full`도 같은 방향
  (+0.0478 vs +0.0162 = −0.0315R). **축들이 서로를 잡아먹는다.**
- 🚨 **「양수가 나왔다」로 인용 금지 — 이유가 넷이고 하나만으로도 충분하다**: (1) **부호가
  안 정해졌다**(±0.0079의 2σ 안 — 요약 코드가 `sign_is_decided`로 「0을 넘었다」를 안 찍는다,
  WAN-381 −0.0023 ± 0.0057과 같은 자리) · (2) 🚨 **종목 하나에 걸려 있다**(`−ETH`로 지갑을
  재배치하면 **−0.0064R로 부호가 넘어간다** — WAN-111 계열) · (3) 🚨 **계좌는 죽어 있다**
  (그 점 MDD **99.45%** · 청산 **12건**. ⚠️ **16점 중 지갑 층이 정의되는 것은 그 한 점뿐**
  이고 나머지 15점은 자본이 0을 뚫어 「정의 상실」 · 청산 **2,575~13,723건** — WAN-381의
  **「거래당 0 ≠ 계좌 본전」**이 그대로 실현됐다) · (4) 거래를 **32% 덜 한다**(14,825 →
  10,114, WAN-378).
- 📌 **배수를 당기면 「같은 분 익절」에 더 기댄다**(거래 수 몫 1.5R **7%** → 1.0R 13% →
  0.8R 18% → 0.6R **26%**) — 하필 그 실측 양수가 0.6R 점에서 나왔다. WAN-336/348이 잰
  낙관(틱이 지지하는 것은 그중 30%)의 몫이 그만큼 크다는 뜻이다.
- 📌 **검산 여섯 전부 통과** — (a) 1.5R 네 칸 ≡ `wan389_retap_attribution_grid.csv` 80행
  **1.78e-15** · (b) 배수 4점 ≡ `wan381_exit_scales_grid.csv` 가드 0.30% 48행 **8.88e-16**
  (둘 다 CSV 텍스트 왕복 끝자리 = 같은 계산) · (c) 재진입 끈 점의 재진입 거래 0건 ·
  (d) `once` 점의 재탭 거래 0건 · (e) 배수 넷이 같은 진입 집합 · (f) `is_reentry` 필터 ≡
  엔진 `include_reentry=False` — (c)~(f) 전부 **0.00e+00**. 📌 **(a)·(b)가 자격 증명이다**
  — 다른 모듈이 다른 날 각자 후보를 만들어 같은 숫자를 냈다(세 축을 한 격자에 넣어도 기존
  행이 안 움직인다).
- 📌 **새 배선은 하나다** — WAN-386 팔 후보는 base와 재진입을 **이미 합친** 목록이라
  `scoped()`가 `is_reentry`인 후보를 **빼서** 재진입 끈 팔을 만든다. 그것이 엔진의
  `include_reentry=False`와 같은 집합이라는 것은 주장이 아니라 **검산 (f)**다.
- 🚨 **gross 자가 둘이라 이름으로 가른다**(WAN-393 §2 함정) — `gross_r`(슬리피지 **전**,
  WAN-370/388/389)과 `mean_gross_r_after_slippage`(슬리피지 **후**, WAN-381/386)를 **둘 다
  싣고** 검산도 자를 따라 갈린다((a)는 전자 · (b)는 후자). **두 열을 나란히 빼지 말 것.**
- ⚠️ **알고 받은 한계**: **차가운 `is`/`oos`가 없다**(`--no-cold-segments` · WAN-389와 같은
  선택) — 「앞구간이 고른 것이 뒷구간에서 뒤집히는가」(WAN-161)를 **이 표는 답하지 않고**
  요약이 그 사실을 스스로 찍는다 · **재무장 일정이 배수마다 고정**(WAN-387 — 낮은 배수 행이
  그 위의 값) · leave-one-out은 **판정 줄의 네 점**만 돈다(`--loo-all`이 16점 전부).
- 📌 **실측 비용**: **3.79시간**(48칸 × 재탭 2모드 · `--jobs 4` · M1 — `every_tap` 후보 생성
  144.8분 + `once` 77.6분 · **배치·LOO는 합쳐 5.1분 = 청구서의 2%**). ⚠️ 다른 모듈의 셀
  비용과 섞지 말 것(WAN-316이 못 박은 사고 부류).
- ⚠️ **측정 전용 · 기본값·토대 불변**(`ConfluenceParams()`·`OrderBlockParams()`·
  `LeverageBookParams()` 그대로 · 축은 전부 옵트인 · 끄면 비트 재현) · 판단은 북에서
  (WAN-341) · 핀 없이(WAN-305) · 전부 `baseline`(낙관) 렌즈 위 값이고 체결 보수화
  (`pen_5bp`)는 범위 밖 · 🚨 **「흑자」로 기대하지 말 것**(WAN-370: 비용을 0으로 만들어도
  천장이 **+0.09R** — 🔁 그 「+0.09R」은 **WAN-396이 +0.007R로 정정**했다(허수 진입 슬리피지 ·
  필터 끔 좌표는 +0.002R).) · **「엣지 없음」(WAN-84/88/111/114/124/151/201/248/386)
  불변**(이 표는 *같은 셋업을 몇 번에 나눠 잡고 어디서 챙기나*를 묻는다 — **다른 질문**) · ❌ **재진입을
  끄자는 제안이 아니고**(WAN-273 사용자 결정 · 끈 팔은 반사실) ❌ 익절 배수 기본값(1.5R)
  전환도 WAN-81/90 소관 · 채택은 **재-베이스라인 = 사용자 결정** · 개발자 임의 착수 금지 ·
  실거래 보류 유지(`ALPHABLOCK_LIVE_TRADING=false`).

📌 **후보 payload 디스크 캐시(WAN-394 §0): 격자 비용의 대부분이 「같은 후보를 다시 만드는
것」이었다 — 이제 미스 칸만 계산한다.** [`docs/ops/wan394-payload-cache.md`](docs/ops/wan394-payload-cache.md).
`run_cells(payload_cache=PayloadCache())`를 **명시**해야 돈다(안 주면 비트 재현). 실측
BTC 4h 한 칸 **3.9분 → 1.9초**, 캐시 유무 산출이 **39열 × 32행 0.00e+00**.
- 🚨 **키를 손으로 나열하지 않는다** — 키가 `wan169_leverage_book._Task` **그 자체**라
  (`dataclasses.fields`를 훑는다) *「payload를 바꾸는 것은 전부 키에 있다」*가 구조적으로
  참이고 **새 축이 자동으로 들어온다**. 뒤집으면 「가드·재진입 **배치**·복리를 바꿔도
  히트한다」가 규칙이 아니라 **타입의 성질**이다(셋 다 배치 인자라 `_Task`에 아예 없다).
- 🚨 **소스 지문 두 겹** — `trade_store.engine_source_revision()`(WAN-253) +
  `payload_cache.RUNNER_SOURCE_FILES`(엔진 목록에 **없으면서** 후보를 만드는
  `wan169_leverage_book.py` 배선 · `wan228_reentry_census.py` 재무장 파생). 후자가 빠지면
  「재진입 파생을 고쳤는데 캐시가 히트하는」 구멍이 남는다 — WAN-364(소급 취소)가 6년치
  표를 얼린 그 부류를 캐시가 **자동화**하는 자리다. **엔진·러너를 한 글자만 고쳐도 전
  칸 미스**이고 그게 맞는 동작이다(실제로 이 이슈 개발 중 두 번 갈렸다).
- 📌 **익절 배수만 예외** — 키에서 빼고 **부분집합 매칭**(저장은 합집합)이다. 근거는
  `derive_arm_candidates`의 배수-독립성이고 테스트가 `「1.5R만 요청」 ≡ 「넷 중 1.5R」`을
  동작으로 건다. 🚨 하나라도 모자라면 **미스**다(부분만 채우면 격자에 구멍이 뚫린 채
  「히트」로 보고된다 — WAN-335 부류).
- 📌 **부수 발견: 기준 팔이 안 읽는 관측을 켜고 있었다** — `derive_arm_candidates`는
  `ARM_BASE`에서 `cand.confirmation`을 **아예 안 읽는데**(트리거 판독은 `taker` 팔 전용)
  `run_cell`의 조건이 `bool(confirmation_arms)`라 기준 팔만 요청해도 관측이 켜졌다. 그
  관측(WAN-383 §0)은 체결 셋업마다 「탭 → 존 무효화」를 한 번 더 훑는다. 조건을
  `any(arm != ARM_BASE …)`로 좁혔다 — **수치 불변**(격자 CSV 39열 × 32행 `0.00e+00`) ·
  실측 절감 BTC 4h **4.4분 → 3.9분(약 11%)**. ⚠️ 15m에서 얼마나 커지는지는 **안 쟀다**.
  배수 축만 쓰는 옛 격자(**WAN-381** 4시간 55분)도 같은 비용을 물고 있었다.
- 📌 **적재는 칸이 나오는 대로** 한다 — 다 모아서 저장하면 47칸 중 46칸을 돌고 죽었을 때
  **아무것도 안 남는다**(48칸 격자를 실제로 돌리다 발견). ⚠️ 단 `executor.map`이 **제출
  순서**로 결과를 내므로 「완료 순서대로」가 아니라 **「제출 순서대로 나오는 대로」**다 —
  앞 칸이 느리면 뒤 칸이 그만큼 기다린다(`as_completed` 전환은 별건).
- ⚠️ **자동 삭제 없음**(WAN-194/297) — `--prune-stale`은 기본이 **세기**이고 삭제는
  `--apply` · 기준이 하나도 없으면 **거부**한다. **DB에 안 넣는다**(`backtest/cache/payloads/`,
  gitignore). `payload_cache.py`는 `ENGINE_SOURCE_FILES`에 **넣지 않았다** — 넣으면 캐시
  코드를 손볼 때마다 야간 타임라인 캐시(WAN-297/325)까지 무효화된다(직렬화 형식은
  `CACHE_SCHEMA_VERSION`이 담당 · `timeline_cache`와 같은 규약).
- 📌 **WAN-390(LOO)의 「3분짜리 답에 2.8시간의 입장료」가 이걸로 풀린다** — WAN-394 §1 실측
  에서 **배치·LOO가 청구서의 2%**(5.1분 / 3.79시간)였다.

📌 **존폭 필터 매칭 대조군(WAN-142 = WAN-133 §후속 1·2): MDD 개선은 「덜 베팅해서」가 아니다 —
단 「선별이라서」도 아직 아니다. 기본값 유지 · 채택 권고 없음.**
[`docs/decisions/wan142.md`](docs/decisions/wan142.md)(재현: `uv run python -m
backtest.wan142_zone_width_filter_verdict`, 요약만 재생성은 `--from-csv`). WAN-133이 (a) 선별
축 후보를 냈지만 손익 표가 **거래 수 오염 게이트**(필터가 거래를 −56~69% 줄인다)에 걸려
인용 불가였다. 이 표가 그 분모를 만든다 — **같은 구간의 기본 후보에서 필터와 같은 개수를
무작위 추출한 매칭 대조군**(시드 20개, 단측 순위 p).
- 📌 **공식 렌즈 OOS 네 셀 전부 p=0.048(하한) — 필터가 20개 시드를 전부 이긴다.** 15m OOS
  필터 MDD **4.94% vs 매칭 10.44%**(수익 +16.16% vs +4.22%) · 1h OOS **4.48% vs 8.03%**
  (+12.42% vs +2.35%). 표본 축소가 설명하는 MDD 개선은 **27~45%**이고 나머지는 환원되지 않는다.
- 📌 **이 저장소에서 드물게 ETH 편중이 아니다** — OOS 필터 leave-one-out이 **6/6 플러스**이고
  **ETH를 빼도 15m +17.55% · 1h +5.96%**다(매칭 팔은 같은 자리에서 −2.04%). 「플러스는 전부
  ETH가 만든다」(WAN-84/88/111/114/124 계열)가 여기서는 성립하지 않는다.
- 📌 **`pen_5bp`가 판정을 안 바꾼다** — 네 셀 전부 p=0.048 유지(두 팔이 같이 깎인다).
  WAN-88의 `baseline` 유의 4셀이 `pen_5bp` 한 번에 전멸한 것과 **성질이 다르다**.
- 🚨 **그런데 「선별 알파를 찾았다」로 인용 금지 — 이 표가 배제한 대안은 「표본 축소」 하나뿐이고
  「기하」는 열려 있다.** 필터 팔은 손절을 존 무효화 경계에 **그대로** 두므로 좁은 존을 고르면
  1R이 작아져 고정 1.5R 익절이 가까워진다 — **승률이 그 서명이다**(OOS `default` 48.42% ·
  `matched` 47.08% vs **`filter` 57.97%**, 1h는 49.19%·46.86% vs **58.76%**). 매칭 팔은 기본과
  승률이 같은데 필터만 +10%p 뛴다. WAN-133이 k·ATR 장벽으로 이 관문을 통과시켰지만 그건
  **옛 엔진**(`tap` 밴드 + 병합 존) 위의 판정이라 **오늘의 엔진에서 재확인된 적이 없다** —
  **그 재검이 채택 논의의 선행 조건**이다(후속 이슈).
- 🚨 **15m은 거래 잔차 +24%가 필터에 유리한 방향으로 남는다** — 후보 수는 정확히 맞췄지만
  동시 1포지션 시퀀서가 두 팔을 다르게 깎아 **매칭이 24% 더 거래한다**(노출↑ = MDD↑).
  **1h는 잔차 2%라 깨끗하니 판정의 무게는 1h에 더 싣는다.**
- 📌 **§0: 병합 폐지(WAN-149)가 존폭 분포를 실제로 내렸다** — 존 **개수**는 0.1~0.4%만 다른데
  심볼평균 p33이 15m 0.96→**0.89** · 1h 0.99→**0.93**이다(병합이 만들던 넓은 박스가 사라진다).
  그래서 문턱은 **매 셀 IS에서 다시 잡는다**. ⚠️ **WAN-133 표와 셀을 직접 비교하지 말 것** —
  이 모듈은 **핀을 하나도 쓰지 않는다**(그쪽은 `pin_band_bar`·`LEGACY_OB_PARAMS`로 고정).
- ⚠️ **사이징 바닥(WAN-79)이 필터 표본을 먼저 깎는다** — TRX 15m OOS 필터가 **17거래**로
  무너져 판정에서 빠졌다(15m OOS가 5심볼인 이유). WAN-133 Part C 충돌이 새 엔진에서도 그대로다.
- ⚠️ **「엣지 없음」(WAN-84/88/111/114/124)이 뒤집힌 게 아니다 — 다른 질문이다.** 저 판정들은
  **진입 규칙이 무작위와 구분되는가**를 물었고 이 표는 **이미 진입한 셋업 중 무엇을 버릴
  것인가**를 물었다. 낙관 렌즈(`baseline`) 위의 값인 것도 그대로다.
- **기본값·토대 불변**(측정 전용, 실거래 보류 유지). 채택은 **사용자 결정**이고 그전에 기하
  관문 재검이 선행이다 — **개발자 임의 착수 금지**. 🔁 **그 재검은 WAN-152가 냈다 — 판정
  (a) 선별이고, 다만 승률 상승의 과반은 실제로 기하였다**(아래 📌 문단).

📌 **존폭 필터 「선별 대 기하」 분리(WAN-152 = WAN-142 §다음 1): 장벽을 존폭에서 떼어내도
필터 우위가 남는다 — (a) 선별, 두 작업 TF 모두. 채택 권고는 없음(기본값 불변).**
[`docs/decisions/wan152.md`](docs/decisions/wan152.md)(재현: `uv run python -m
backtest.wan152_selection_vs_geometry --timeframes 1h` → `--timeframes 15m --append`, 요약만
재생성은 `--from-csv`). WAN-142가 「표본 축소」를 배제하고 남긴 마지막 대안을 잰다 — 손절을
**진입가 ∓ 1.5·ATR**로 고정하면(WAN-133 k·ATR 선례 · WAN-143이 새 밴드에 배선한
`stop_loss_override`) 1R이 존폭과 무관해져 **좁은 존이 익절선을 당기지 못한다**. 창·심볼·자·
시드는 WAN-142 그대로이고 움직인 것은 장벽뿐이다. **핀을 하나도 쓰지 않는다**(회귀 테스트가
후보 집합으로 고정).
- 📌 **네 OOS 셀 전부 p=0.048(하한)** — `atr` 장벽에서 **매칭 팔은 마이너스인데 필터만
  플러스**다(15m +5.34% vs −5.10% · 1h +2.72% vs −3.27%). IS도 같은 부호다. **존폭은 이
  저장소에서 처음으로 두 관문(표본 축소·기하)을 오늘의 엔진 위에서 통과한 선별 축 후보다.**
- 🚨 **단 「기하가 아니다」가 아니라 「기하만은 아니다」다** — (필터 − 매칭) **승률 격차의
  과반이 장벽 고정으로 사라진다**(1h +12.59%p → +5.68%p = 55% 소멸 · 15m +8.95%p → +3.35%p =
  63%). WAN-142가 본 「승률 +10%p」의 과반은 **익절선이 가까워진 산수**였고, 판정은 그 나머지와
  **수익 우위**가 무작위를 이기느냐로 냈다.
- 🚨 **「필터를 켜면 돈을 더 번다」로 인용 금지** — ATR 장벽 자체가 엔진을 나쁘게 만든다(기본
  팔 OOS 1h +8.57%→**−2.75%** · 15m +10.63%→**−7.82%**). 그 팔의 필터 우위에는 「덜 나쁜 쪽을
  골랐다」가 섞여 있다. 이 표가 재는 것은 수익 수준이 아니라 **존폭이 무작위보다 나은 정보를
  갖는가** 하나다.
- 📌 **ETH 편중이 아니다** — 필터 팔 OOS leave-one-out이 네 셀 전부 플러스이고, `atr` 장벽에서
  ETH를 빼도 1h +0.91% · 15m +5.67%다(같은 자리 매칭 팔은 **어느 심볼을 빼도 마이너스**).
- 🚨 **판정의 무게는 15m 쪽이다 — WAN-142와 반대다.** 판정 셀(`atr` OOS)의 거래 잔차가 15m
  +3.2%로 깨끗한데 **1h는 +11.8%로 5% 초과**다(매칭이 더 많이 거래 = 필터에 유리한 잔차).
  WAN-142는 잔차 때문에 1h에 무게를 실었으니 두 표를 겹쳐 읽을 때 주의할 것.
- 📌 **ATR 장벽이 사이징 바닥 충돌을 없앤다** — `zone` 15m OOS는 TRX 필터가 16거래로 무너져
  판정에서 빠지는데(WAN-142의 5심볼 셀) `atr`에서는 82거래로 **6심볼 전부 유효**하다.
  ⚠️ **가드 변경 근거로 쓰지 말 것**(WAN-76/79 소관, 변경은 재-베이스라인).
- ⚠️ **「엣지 없음」(WAN-84/88/111/114/124/145)이 뒤집힌 게 아니다 — 다른 질문이다**(WAN-142와
  같은 구분). 낙관 렌즈(`baseline`) 위의 값이고 **존폭 축 체결 보수화는 이 표에서 안 쟀다**
  (WAN-142의 `pen_5bp` 불변 확인은 `zone` 장벽에서다).
- ⚠️ **WAN-142와 셀을 직접 비교하지 말 것** — 이 표는 두 장벽의 **공통 풀**(ATR 워밍업 셋업
  제외, 2~4% 축소) 위다. **기본값·토대 불변**(측정 전용, 실거래 보류 유지). 채택은 **사용자
  결정**이고 **개발자 임의 착수 금지** — 선행은 wan152.md §5(존폭 축 체결 보수화 · 문턱 민감도 ·
  자매 이슈 WAN-131). 🔁 **그 선행 중 체결 보수화·문턱 민감도는 WAN-154가 냈다**(아래 📌 문단
  — `zone` 축은 두 TF 관통 생존 · 문턱 3종 전부 유의).

📌 **손절폭 축 통합 측정(WAN-154 = WAN-153 흡수): 존폭 필터 우위는 세 자(장벽) 전부에서,
문턱 3종 전부에서, 판돈을 덜 걸고도 선다 — 기본값 불변 · 채택 권고 없음.**
[`docs/decisions/wan154.md`](docs/decisions/wan154.md)(재현: `uv run python -m
backtest.wan154_stop_width_audit --timeframes 1h` → `--timeframes 15m --append`, 요약만
재생성은 `--from-csv`). WAN-152 모듈을 **확장**해(새 파이프라인 없음) 셋째 장벽
`zone_height`(1R = 존 높이·진입가 무관)와 거래당 실현 손익비 열을 넣고, 렌즈 2 × 가드 5 ×
문턱 3 격자를 한 실행으로 냈다. 재산출된 `wan152_barrier_pnl.csv`의 **기존 1,104행은 비트
단위 재현**(열만 확장).
- 📌 **§0 세 자 강건성: OOS 6셀(3장벽 × 2TF) 전부 p=0.048(하한)** — 필터 우위가 자에 의존하지
  않는다. leave-one-out도 사실상 전부 플러스(**ETH 편중 아님**, WAN-142/152 계열).
  ⚠️ `zone_height` 장벽은 **매매 규칙이 아니라 통제용 인공 자**다(손절이 무효화 경계 아래) —
  「존높이 손절 성적」으로 인용 금지. 실전 규칙(무효화 손절 + 존높이 익절)은 **WAN-155** 소관.
- 📌 **§1′ 필터는 손익비가 아니라 승률로 번다** — 좁은 존이라 비용의 R 비중이 오히려 크고
  (`cost_r` 중앙값 OOS 15m 0.259 vs 0.193) 손익분기 승률도 높게 요구되는데, 승률(57.4% vs
  47.2%)이 그걸 덮어 거래당 순 R이 압도한다(`mean_net_r` OOS: 15m 필터 **+0.184R** vs 기본
  **+0.000R** vs 매칭 −0.020R · 1h +0.293/+0.096/+0.022). ⚠️ `harness.mean_r`은 **승률의
  대수적 재탕이라 쓰지 않았다**(PM 정정) — 실현 손익 ÷ 그 거래의 리스크 금액이다. 📌 기본
  팔 15m의 **0.000R**은 「엣지 없음」 계열과 정합적. `total_return`과 `mean_net_r` 우위가
  갈리는 셀은 **없다**.
- 📌 **§2 판돈 우려는 반대로 판명 — (a) 필터가 덜 걸고도 이겼다(과소평가)** — 좁은 존은
  레버리지 1배 상한에 더 걸려(발동률 15m 98.3% vs 82.5%) 실효 리스크가 **더 작은데도**
  (0.484% vs 0.623%) 이겼다. `atr` 축은 판돈이 같아 이 교란 자체가 없다. 📌 15m은 기본
  팔도 발동률 83.8% — **15m 실효 리스크는 이름(1%)과 달리 0.6%대**다(필터 채택 논의 시
  알고 결정할 것).
- 📌 **§3 가드(0.3%)의 존재 이유가 손익으로 처음 확인됐다** — 버킷 표(가드 끄고 측정):
  좁은 손절 구간은 **생존율이 최고인데 돈은 잃는다**(<0.2%: 생존 65.8% · `cost_r` 0.814 ·
  손익분기 승률 72.6% · `mean_net_r` **−0.414R**). WAN-133의 「가장 잘 살아남는 구간을
  가드가 자른다」는 생존율로는 참, 손익으로는 **가드가 옳았다**(0.3% ≈ 여유 음수→양수 경계).
  §3-B 판정: **세 장벽 전부 (a) 이득** — 15m +3.3~5.9%p·수익/MDD 개선, **1h는 무영향**
  (걸리는 거래가 거의 없다). 🚨 **TRX 15m 가설 확인** — `zone` 장벽 필터 후보의 **92.6%를
  가드가 잘라 16거래로 붕괴**(`atr` 장벽에선 52.8% → 82거래 유효). ⚠️ 가드 변경 제안이
  아니다(WAN-76/79 소관 · 재-베이스라인 = 사용자 결정).
- 🚨 **§4 체결 보수화는 갈린다 — `zone`은 버티고 통제 장벽의 15m은 유의성을 잃는다**:
  `pen_5bp`에서 `zone` 15m·1h와 `atr`·`zone_height` 1h는 p=0.048 유지(두 팔이 같이 깎임 —
  WAN-88식 전멸과 다름), **`atr`·`zone_height` 15m은 p=0.095로 소멸**(격차 부호는 남음).
  즉 「세 자 전부」는 **렌즈 조건부**다 — 관통을 요구하면 15m에서 `zone` 한 자로 줄어든다
  (15m 체결 민감성 WAN-96/124의 존폭 축 재현).
- 📌 **§5 문턱 1/3은 자유 파라미터가 아니다** — 하위 1/4·1/3·1/2 × 두 TF 전부 p=0.048,
  IS 문턱값의 OOS 분위도 안정(15m 0.250/0.327/0.490).
- **기본값·토대 불변**(측정 전용, 실거래 보류 유지). 존폭 필터 채택은 **사용자 결정**이고
  **개발자 임의 착수 금지** — 남은 선행: 낙관 렌즈 자체(틱·호가 WAN-98 Canceled) · TRX류
  가드 충돌 방침 · §2의 실효 리스크 사실 인지. 자매 **WAN-155**(존높이 익절)가 이 배선을
  물려받는다. 🔁 **채택 결정은 WAN-158이 물었고 사용자는 「옵트인만」을 골랐다**(아래 📌).

📌 **존폭 필터 옵트인 배선(WAN-158 = 사용자 결정 B): 파라미터·CLI 축은 생겼고 기본값은
그대로 꺼짐이다 — 재-베이스라인이 아니다.** [`docs/decisions/wan158.md`](docs/decisions/wan158.md).
사용자 결정("B로 가자"). WAN-142/152/154가 관문 셋(표본 축소·기하·체결 보수화+문턱
민감도)을 통과시켰지만 그 결정을 물을 이슈가 없어 멈춰 있던 자리를 닫았다.
- **새 파라미터** `ConfluenceParams.max_zone_width_atr`(기본 `None` = 꺼짐 = 전부 매매) ·
  `zone_width_atr_length`(기본 14). 규칙은 **`존폭 ÷ ATR ≤ 문턱`이면 진입**이고 ATR은 **탭 봉
  직전 확정봉**에서 읽는다(측정 모듈 `wan117`의 `zone_width_atr`과 같은 정의 — 탭 봉 ATR은
  룩어헤드다). 거르는 지점은 `build_zone_limit_candidates`의 지정가 확정 **이후**다.
- ⚠️ **단위는 ATR 배수지 퍼센트가 아니다** — 권고 문턱은 **15m 1.24 · 1h 1.32**(출처:
  `wan154_threshold_stability.csv`의 하위 1/3 분위가 종목·TF를 안 가리고 1.16~1.39에 몰린다).
  `0.0124` 같은 분수를 넣으면 거의 전부가 걸러진다(WAN-112 단위 함정의 반대 방향).
  **분위가 아니라 절대값으로 노출한 것이 이 이슈의 핵심 설계 결정**이다 — 실시간에는 IS 공통
  풀이 없어 분위를 그대로 옮기면 실거래 배선에서 다시 막힌다(WAN-132가 `tap`에서 겪은 부류).
- **기본 실행은 예전과 비트 단위로 같다**(`None`이면 ATR을 계산조차 하지 않는다). 회귀
  테스트가 **라벨이 아니라 동작**으로 고정한다 — 켜면 같은 셋업이 실제로 사라지는지, 엔진이
  직전 확정봉을 읽는지(두 봉 ATR 사이 문턱), 워밍업 NaN이 **통과가 아니라 기각**인지.
- ⚠️ **종가 진입(A안)과 함께 쓰면 거부한다**(`evaluate`·`build_params`·`Grid` 각각 ValueError) —
  A안은 이 필드를 읽지 않아 조용히 무시하면 라벨만 붙는다(WAN-95의 교훈).
- 🚨 **「켜면 돈을 더 번다」로 인용 금지** — 새 수치를 내지 않았다(격자 미실행). 선행 표의
  경고가 그대로다: 전부 `baseline`(낙관) 위의 값 · 승률 상승의 **과반은 기하**(WAN-152:
  55~63%) · **TRX는 `zone` 장벽 15m에서 후보의 92.6%를 손절폭 가드가 잘라 16거래로 무너진다**
  (가드 변경은 WAN-76/79 소관 · 재-베이스라인이라 **손대지 않았다**) · 거래가 3분의 1로 준다.
  **「엣지 없음」(WAN-84/88/111/114/124/151) 불변**(그쪽은 *진입 규칙이 무작위와 구분되는가*를
  물었고 존폭 축은 *이미 진입한 셋업 중 무엇을 버릴 것인가*를 묻는다 — 다른 질문).
- **범위 밖(명시)**: 기본값 전환·옛 리포트 재산출·`LEGACY_*` 고정(= A안) · 손절폭 가드 ·
  실시간 러너 배선(WAN-45) · 대시보드 폼(WAN-102, 이번엔 **CLI까지**) · 새 격자.
  **토대 불변 · 실거래 보류 유지**(`ALPHABLOCK_LIVE_TRADING=false`).

📌 **존폭 필터 폐지(WAN-384 = 사용자 결정 2026-08-27 「끄자」 · WAN-159 번복):
`max_zone_width_atr` 기본값이 `1.28`(좁은 존만) → `None`(**꺼짐 = 존 두께로는 안 거른다**)이다.**
[`docs/decisions/wan384.md`](docs/decisions/wan384.md). **측정이 아니라 전환이다** — 근거는
WAN-378 §1b 격자가 이미 냈고(오늘 인과 엔진 · 채택 북 · 핀 없음 · 문턱 9점 × 가드 4점 ×
진입가 3팔) **새 격자는 안 돌렸다**.
- 📌 **판정은 「눈금을 옮겨라」가 아니라 「끄는 쪽이 낫다」** — `oos_warm` · 가드 0.30% ·
  재진입 ON · **거래당 net R**(WAN-341: 판단은 북에서): `끔` **−0.1207** > 2.60 −0.1239 >
  1.80 −0.1292 > 1.55 −0.1297 > **1.28(옛 채택) −0.1309** > 1.15 −0.1374 > 1.00 −0.1480 >
  0.90 −0.1511 > 0.80 −0.1651. **조일수록 나빠진다.** 세 겹으로 지지된다: **진입가 3팔**
  (`bollinger`·`proximal`·`mid`)이 독립적으로 같은 방향 · **네 구간**(`full`·`is`·`oos_warm`·
  `oos`) 전부 같은 방향 · **IS→OOS 뒤집힘이 12칸 중 3칸**뿐이고 그 셋은 전부 가드 0.40% 열이다
  (WAN-161이 익절 배수 축에서 8칸 중 7칸 뒤집힌 것과 **정반대**).
- 📌 **고른 이유는 성적이 최선이라서가 아니다** — ⚠️ `끔`과 `2.60`의 차이는 **+0.0032R로
  규약 폭(±0.005R) 안**이라 *「끔이 2.60보다 낫다」*는 주장할 수 없다(이 표가 말하는 전부는
  *「느슨한 끝이 조인 끝보다 낫다」*까지다). 근거는 **자유 파라미터를 하나 없애는 것**이고,
  1.28은 애초에 **데이터가 고른 값이 아니었다**(WAN-159 결정문이 그렇게 적었다).
- 🚨 **「끄면 이긴다」로 인용 금지** — `끔`도 **−0.1207R로 여전히 마이너스**이고(WAN-378의
  **108팔 전부 음수**, 최선 −0.0986R) **거래가 56% 늘어난다**(9,515 → **14,843**)라 총 손실·
  노출은 커진다. 고른 것은 **「덜 잃는 쪽」**이다. 전부 `baseline` 위 값이고 존폭 축 **체결
  보수화(`pen_5bp`)는 안 쟀다**. **「엣지 없음」(WAN-84/88/111/114/124/151/201/248) 불변.**
- 📌 **왜 지금까지 켜져 있었나(기록)** — (1) 채택 근거(WAN-142/152/154의 관문 셋)가 **소급
  취소 버그가 있던 per-cell 엔진**의 판이고, WAN-365가 그걸 고친 뒤 문턱 축을 제대로 훑은 것은
  **WAN-378이 처음**이다. (2) 🚨 **판정 자를 바꾸면 부호가 뒤집히는 자리다** — 옛 방식(per-cell
  총수익 %)으로는 *「조일수록 손실이 준다」*로 보이는데(15m −76.8% → −22.6%) 그건 **거래를 74%
  줄여 복리 손실 총액이 준 것**이지 남은 거래의 질이 좋아진 게 아니다(WAN-341이 손익으로
  재확인된 자리).
- 📌 **손절폭 가드(0.3%)와 혼동하지 말 것 — 그쪽은 필터를 꺼도 여전히 크다.** 같은 격자의
  「문턱 끔 × 가드」 칸: 가드 끔 **−0.2821** → 0.25% −0.1330 → **0.30% −0.1207** → 0.40%
  **−0.1032**. 🚨 **가드(끔→0.30% = +0.1614R)가 존폭(끔→1.28 = −0.0101R)보다 16배 큰 일을
  한다.** 곡선이 **0.40%에서 아직 오르는 중**이라 「폭을 정해야 한다」는 사용자 지적은 유효하고
  그건 **WAN-381** 소관이다 — ⚠️ 가드 효과가 필터를 끄면 조금 작아지므로(+0.2140R → +0.1788R
  = 두 규칙이 일부 **같은 거래**를 자르고 있었다) **WAN-381은 필터 끈 좌표에서** 돌려야 한다.
  **이 결정은 가드를 안 건드렸다**(`min_stop_distance_fraction=0.003` 유지 · WAN-76/79 소관).
- 📌 **파급 처리 — 핀의 방향이 WAN-132/149/159와 반대다.** `LEGACY_MAX_ZONE_WIDTH_ATR`가
  **이미 `None`**이라(WAN-159가 그렇게 만들었다) 이번에 보존할 것은 **필터를 켠 채** 낸
  리포트다. 그래서 **둘째 세대 상수 `harness.LEGACY_ZONE_WIDTH_FILTER_ON`(= `1.28`)**을 새로
  두고 **50개 모듈을 명시 고정**했다(목록은 wan384.md §4-1). 🚨 **파일 스캔 가드만으로는
  부족하다** — 「그 이름을 썼다」와 「값에 못 박았다」를 안 가르면 통과(passthrough) 한 줄에
  속는다(PM 검토가 `wan169` 누락을 그 구멍에서 잡았다, wan384.md §4-1-2). **호출부를
  가로채는 스파이 테스트를 함께** 둘 것. 🚨 **`ADOPTED_CELL_KWARGS`에는
  넣지 않았다** — 그 dict의 계약이 *「`book_cli.run_book`이 쓰는 것과 같다」*라 옛 핀을 넣으면
  그 계약이 거짓이 된다(WAN-305 「기본값 방향」 사고의 재현). 각 호출부에 **명시**로 넣었다.
  ⚠️ **옛 상수는 지우지 않았다**(지금은 기본값과 같아 무동작이지만, 그 핀을 단 모듈은 「이 표는
  필터를 안 켠 판」을 값으로 선언하고 있다). **채택 성과(wan95)만 재산출** — ⚠️ 옛 판과 셀 비교
  금지(매매하는 셋업 집합이 통째로 다르다).
- 📌 **트립와이어 둘이 설계대로 울렸다** — wan366 `_assert_adopted_base()`·wan376
  `assert_adopted_base()`가 「채택 문턱이 움직였으면 시끄럽게 죽어라」였고 실제로 죽었다. 둘 다
  라벨을 함께 고쳤다(wan366 사다리 꼭대기 `L4`는 이제 「**WAN-383까지의** 채택 기본값(존폭
  1.28)」이고, 문턱을 센티넬로 물려받던 것도 **명시**로 바꿨다 — 안 그러면 `L2`~`L4`가 「필터
  1.28」 라벨을 단 채 필터 없이 돈다).
- 🚨 **순서 — 이 결정이 다른 이슈를 막는다.** 필터를 끄면 매매하는 셋업 집합이 통째로 바뀌므로
  (거래 +56%) **필터 켠 채로 잰 새 표는 전부 무효**다: **WAN-383**(확인 진입 `P*`)은 이 결정
  **뒤에** 돌리고, **WAN-381**(가드 곡선)·**WAN-374**(존 병합)·**WAN-375**(조건부 손익비)도
  착수 전에 이 결정을 볼 것.
- **다른 토대 불변**(오프셋 2bp · `intrabar_live` · `unconditional` · 고정 1.5R · 롱 온리 ·
  `combine_obs=False` · `invalidation_cancel="bar_close"` · 가드 0.3% · 레버리지 북 cap_only
  5배 · 재진입 band) · **실거래 보류 유지**(`ALPHABLOCK_LIVE_TRADING=false`).

🔁 **아래 WAN-159 문단은 WAN-384가 번복했다 — 지금 기본값은 다시 `None`(꺼짐)이다**
(바로 위 📌 WAN-384 문단이 정본). 파급 처리·「1.28은 데이터가 아니라 사용자 판단」이라는
기록은 그대로 유효하고, **기본값 서술만 낡았다.**

📌 **존폭 필터 채택(WAN-159 = 사용자 결정, WAN-158 결정 B→A 승격): `max_zone_width_atr`
기본값이 `None`(꺼짐) → `1.28`(존폭 ÷ ATR ≤ 1.28인 **좁은 존만** 매매)다.**
[`docs/decisions/wan159.md`](docs/decisions/wan159.md). 사용자 결정("우리는 좁은 존을 기본으로
하자" → "좋아"). **측정이 아니라 전환이다** — 관문 셋은 WAN-142/152/154가 이미 통과시켰고
(위 문단), 이 이슈는 그 위의 기본값 이동 + 파급 처리다. 새 격자는 안 돌렸다.
- 📌 **문턱 1.28은 데이터가 아니라 사용자 판단이다** — 측정 권고 15m **1.24** · 1h **1.32**의
  중간이다. TF별 값을 두면 앞구간에 맞춰 고르는 자유 파라미터가 하나 늘어(WAN-108) 하나로
  통일했다. ⚠️ **「데이터가 1.28을 골랐다」·「켜면 돈을 더 번다」로 인용 금지.**
- 📌 **끄기(`None`)와 미지정(채택 기본값)이 갈라진다** — 옛 규약에서 둘은 같은 `None`이었다.
  `build_params`는 센티넬 `UNSET`으로 미지정을 표현한다(미지정 = 1.28 물려받음, 명시적 `None`
  = **끈다**). CLI `--max-zone-width-atr none` = 끄기, 인자 미지정 = 1.28. `UNSET`은 `enum.Enum`
  이라 `--jobs` 병렬 피클을 넘어도 싱글턴이다. 안 가르면 「필터 끔」 라벨을 단 채 1.28로 도는
  **이중 필터**가 된다(WAN-91/95/112/123 부류).
- 📌 **파급 처리(WAN-132/149와 같은 부류이되 더 넓다)**: 옛 수치를 결론에 박아 둔 B안 리포트를
  **`max_zone_width_atr=None`(= `harness.LEGACY_MAX_ZONE_WIDTH_ATR`)으로 명시 고정**했다 — 특히
  **wan133/142/152/154의 이중 필터**(후보를 자기가 걸러 좁은 존 팔을 만드는데 엔진이 먼저 1.28로
  거르면 대조군·매칭이 오염된다). ⚠️ **밴드를 tap에 못 박은 동결 스냅샷만 고정한다** — 밴드를
  고정하지 않고 `== ConfluenceParams()`를 주장하는 **추적 모듈**(wan143·wan145·wan149·wan151·
  wan89)은 새 기본값 1.28을 **따라가게 둔다**(끄면 그 설계 계약이 깨진다 · 옛 CSV는 스냅샷이고
  재측정은 별도 이슈 — WAN-132→WAN-145 패턴). **wan155·wan161은 문턱을 자기 입력으로 명시**하므로
  손대지 않았다(센티넬 덕분에 `filter_on=False`의 `None`이 이제 끄기로 올바로 해석돼 비트 재현).
  A안 리포트(wan81/87/91)는 필터를 안 읽고 close 팔을 명시적으로 끈다. **채택 성과(wan95)만
  재산출**했다. 목록은 wan159.md §4.
- 🚨 **알고 받는 손해**(사용자가 알고 선택): 전부 `baseline`(낙관) 위의 값 · 승률 상승의 **과반은
  기하**(WAN-152: 55~63%, 1R이 좁아져 고정 1.5R 익절이 가까워짐) · **TRX 15m은 손절폭 가드에
  후보의 92.6%가 잘려 16거래로 붕괴**(가드는 WAN-76/79 소관 · 손대지 않았다) · 거래가 3분의 1로
  준다. **「엣지 없음」(WAN-84/88/111/114/124/151)은 그대로**(다른 질문 — *이미 진입한 셋업 중
  무엇을 버릴까*). 단 **이 축은 드물게 ETH 편중이 아니다**(종목 하나씩 빼도 필터 팔 OOS 네 셀
  전부 플러스, WAN-142/152/154).
- **범위 밖**: 손절폭 가드 · 새 격자 · 익절(WAN-155/161) · 러너(WAN-45) · 다른 토대 불변
  (오프셋 2bp · `intrabar_live` · `unconditional` · 고정 1.5R · 롱 온리 · `combine_obs=False`),
  **실거래 보류 유지**(`ALPHABLOCK_LIVE_TRADING=false`). ⚠️ 엣지 판정 계열(WAN-84/88/111/114/
  124/151)이 「필터 꺼진 시절」 기록으로 얼어붙는다 — 새 기본값 위의 재검은 별도 이슈다.

📌 **문턱 × 배수 민감도(WAN-161 = 사용자 질문 「1.28이 바뀌면 새로 돌려야돼?」): 판정 (a)
「문턱에 종속」 — 답은 **「예, 다시 돌려야 한다」**이되 **무효화의 실질 크기는 작다**. 기본값
불변.** [`docs/decisions/wan161.md`](docs/decisions/wan161.md)(재현: `uv run python -m
backtest.wan161_threshold_x_tp_multiple --tf 1h` → `--tf 15m --append`, 요약만 재생성은
`--from-csv`). WAN-155가 배수 스윕을 **문턱 1.28 한 점에서만** 돌려 비어 있던 자리다 —
문턱 4개(`필터 끔`·1.15·1.28·1.55) × 배수 4개 = **16조합** × 15m·1h × IS/OOS × 6심볼,
못 박은 창 · `baseline` 단독 · 오늘의 채택 기본값 · 가드 0.003 고정 · 자는 현행 `entry_r`.
**핀을 하나도 쓰지 않는다.**
- 📌 **IS 최적 배수가 문턱마다 갈린다** — 1h: 필터 끔 **1.0R** · 1.15 **2.0R** · 1.28
  **1.0R** · 1.55 **1.0R** / 15m: 필터 끔·1.15 **2.0R** · 1.28·1.55 **3.0R**. 🚨 **TF마다
  방향이 정반대라**(1h는 조이면 낮은 배수 · 15m은 높은 배수) **「문턱을 조였으니 배수를
  이렇게」라는 조정 규칙이 나오지 않는다**(WAN-108/143이 경계한 자유 파라미터 자리).
- 🚨 **「1.5R은 틀린 값」·「문턱마다 다른 배수를 채택하라」로 인용 금지** — 판정 (a)는
  **절차적 사실**(문턱을 만지면 표를 다시 돌려라)이지 채택 권고가 아니다. **크기를 같이
  읽을 것**: IS 최적이 현행 1.5R을 이기는 폭은 **1h 1.28에서 1.20%p뿐**(최대 5.46%p),
  15m 최대 13.35%p인데 **곡선이 고원**이다(리포트가 「IS 최적 − 1.5R」·「곡선 폭」을
  **열로** 낸다 — argmax만 보고 결론 내지 말라고 코드가 찍는다).
- 📌 **더 큰 발견 — 배수 선택은 「구간」도 안 넘어간다: IS→OOS 뒤집힘 8셀 중 7**(1h 4/4 ·
  15m 3/4). 즉 (a)는 **배수 argmax 자체가 불안정하다**는 더 넓은 사실의 한 단면이고,
  **WAN-90의 IS→OOS 뒤집힘(과최적화 서명)을 존폭 축에서 재현**한 것이다. ⚠️ **「뒷구간이
  X를 골랐다」는 채택 근거가 아니다**(WAN-155 §3 관행) — OOS 최적은 **뒤집힘을 세는 데만** 쓴다.
- 📌 **그래서 현행 1.5R 유지 비용이 거의 없다** — OOS 최적 대비 **1h는 네 문턱 중 셋에서
  0**(필터 끔만 −2.65%p), 15m은 −0.78~−2.76%p. ⚠️ **「뒷구간이 1.5R을 골랐다」로 읽지 말 것**
  — 1.5R은 이 격자와 무관하게 이미 채택된 값이고(WAN-81/90) 이 줄은 **유지 비용**이다.
- 📌 **사슬은 실측으로 작동한다**(문턱 → 1R → 목표 절대 거리): 손절폭 중앙값이 필터 끔 →
  1.15에서 **1h 1.017%→0.593% · 15m 0.465%→0.317%**. 🚨 **그 대가로 손절폭 가드(0.3%)가
  15m에서 후보의 44~46%를 자른다**(1h 11.2%→20.1%) — **가드 변경 제안이 아니라**(WAN-76/79
  소관) 문턱 결정이 **가드와 물려 있다는 사실**의 기록이다.
- 🚨 **TRX 15m이 예고대로 무너진다** — 문턱 1.15에서 **12거래** · 1.28에서 **18거래**로
  20건 게이트에 걸려 그 셀들이 5심볼이 됐다(WAN-154/155의 TRX 충돌 재현). 게이트는 주의문이
  아니라 **코드**이고, 곡선 위의 얇은 점도 빠진다(얇은 셀의 극단값이 「최적 배수」로 올라오면
  민감도 판정이 통째로 흔들린다).
- ⚠️ **편중은 그대로** — 1h 필터 끔 OOS +8.74%는 **ETH를 빼면 +1.11%**, 15m 1.28 OOS
  +16.96%는 **SOL을 빼면 +9.58%**. 단 **필터 켠 1h 팔은 6/6 플러스**라 존폭 축이 ETH 편중이
  아니라는 WAN-142/152 계열과 정합적이다.
- **검산**: 문턱 1.28 × 1.5R ≡ `wan155_tp_ruler_vs_multiple.csv`의 `filter_on` 팔 · 필터 끔
  × 1.5R ≡ 그 기준점 팔 — **각 24행 × 5지표 차이 0**(메모리 원값 대 CSV는 9.7e-17 =
  부동소수 끝자리. 코드가 **일치 · 잡음 · 불일치를 다르게 찍는다** — WAN-151 패턴).
  실행 중 불변 둘도 동작으로 검산한다: 같은 문턱의 4배수는 진입 집합이 비트 일치 · 조인
  문턱의 셋업은 느슨한 문턱의 **부분집합**(개수가 아니라 **집합**으로 — 개수만 보면 같은
  개수의 다른 셋업이 통과한다).
- 📌 **이 모듈은 WAN-159 전후 어느 쪽에서 돌려도 같은 행을 낸다** — 문턱이 **자기 입력**이고
  「필터 끔」 팔에 `None`을 **명시적으로 덮어쓴다**. `harness.build_params`의 `None`은 규약상
  「손대지 않는다」라 그 덮어쓰기가 없으면 기본값 전환 후 **「필터 끔」이 라벨만 남고 조용히
  1.28로 돈다**(WAN-91/95/112/123 부류). 회귀 테스트가 **넓은 존이 실제로 매매되는지**로 고정.
- ⚠️ **안 잰 것**: 존폭 축 **체결 보수화**(`baseline` 단독) · 4h·1d(표본) · 자 축(WAN-155가
  (b)로 닫음) · 가드 축(고정) · 더 촘촘한 문턱(4점뿐). **「엣지 없음」(WAN-84/88/111/114/
  124/151) 불변** — 익절 배수는 알파가 아니라 **위험의 모양**만 바꾼다(WAN-90).
- **기본값·토대 불변**(측정 전용, 실거래 보류 유지). 📌 **WAN-159에 주는 입력**: 문턱을 고르는
  결정이 손익비 표를 무효화하되 **실질 크기는 작고**, 문턱 선택은 **가드와 물려 있다** — 알고
  고르는 것과 모르고 고르는 것은 다르다.

📌 **필터 켠 엔진의 가드 재측정(WAN-197 = 사용자 질문 「필터 키고 새로 재야 할 것 같은데?」):
판정 (c) TF에 갈린다 — 15m (a)이득 · 1h (c)무영향 · 4h (b)손해(표본 게이트). 기본값 불변.**
[`docs/decisions/wan197.md`](docs/decisions/wan197.md)(재현: `uv run python -m
backtest.wan197_guard_with_filter --timeframes 1h` → `--timeframes 15m --append` →
`--timeframes 4h --append`, 요약만 `--from-csv`). WAN-154 §3-B가 손절폭 가드(0.3%)를 **존폭
필터 끈 채**(엔진 필터 off + 필터를 후보로 직접 구성) 쟀는데, WAN-159가 필터 1.28을 채택
기본값으로 올린 뒤라 「필터 켠 오늘 엔진」의 가드 효과가 없었다. 이 모듈은 wan152/154 골격을
재사용하되 **엔진 필터 1.28을 켠 채택 엔진의 `default` 팔**(= 인자 없는 `backtest.run`) 위에서
가드만 스윕한다(매칭 대조군·세 장벽 없음 · 축은 가드 하나). 9종목 · 못 박은 6년 창 ·
`baseline` 단독 · 신규 3종목 펀딩 대리(BTC 도너).
- 📌 **공식 렌즈 OOS 심볼평균(수익 끔 → 0.3%)**: **15m +36.13% → +37.00%(Δ+0.87%p · 수익/MDD
  4.55 → 6.16 · MDD 7.93% → 6.00%)** · **1h +18.41% → +18.36%(Δ−0.05%p · 무영향)** · **4h
  +8.52% → +8.35%(Δ−0.17%p · 표본 게이트 대조군)**. 하나의 가드로 세 TF를 동시에 좋게 못 해
  기본값 유지(TF마다 다른 가드 = IS 자유 파라미터, WAN-108/143).
- 📌 **필터를 켜도 가드는 15m에서 제 일을 한다(위험조정)** — `total_return` 이득은 WAN-154(필터
  끈 판, 15m +3.3~+5.9%p)보다 **작아졌지만** 수익/MDD가 뚜렷이 오른다(가드가 필터가 남긴 좁은
  존 중 **가장 좁은 손절**을 더 쳐낸다, 후보의 38.5%). 가드는 수익 장치가 아니라 신뢰성
  장치라는 WAN-76 취지가 필터 켠 엔진에서도 성립한다. 1h는 넓은 손절이라 가드가 10.7%만
  건드려 무영향(WAN-154와 같다).
- 🚨 **이슈의 붕괴 우려는 뒤집혔다 — 가드는 좁은 존 종목을 죽이지 않고 오히려 구한다.**
  이슈는 WAN-154 `zone` 장벽 TRX 15m 붕괴(16거래)를 근거로 좁은 존 종목 표본이 무너질까
  걱정했으나, 필터 1.28(절대 문턱)은 WAN-154의 분위 1/3 필터보다 후보를 덜 줄여 **TRX 15m이
  372 → 79거래로 20건 게이트를 통과**하고 **수익이 −15.52% → −0.28%(+15.24%p) 회복**한다
  (가드가 자른 것이 큰 손실의 원천). BTC 15m도 +0.37% → +5.19%. 반대로 LINK 15m은 좋은
  좁은-손절도 함께 잘려 −12.45%p다(종목에 갈리되 심볼평균은 이득 · **ETH 편중 아님**,
  leave-one-out). **실제 20건 붕괴는 4h OOS BTC(15)·LINK(19)에서만** — 좁은 존이 아니라 긴
  TF의 표본 희소성이다.
- 🚨 **검산(실데이터 비트 일치)**: 후보 전체를 가드 0.3%로 시퀀싱한 `total_return`·거래 수·
  MDD·승률 ≡ `harness.run_once` 프로덕션(`test_full_list_guard_default_reproduces_production`)
  = 이 표의 `default` 팔이 곧 「인자 없는 backtest.run」이라는 직접 증거. 필터가 실제로 후보를
  걷어내는 것도 고정(켠 것 < 끈 것).
- ⚠️ **측정 전용 · 기본값 변경 아님** — 가드 기본값(0.3%) 전환은 WAN-76/79 소관 · 재-베이스라인
  = 사용자 결정 · 개발자 임의 착수 금지. 전부 `baseline`(낙관) 렌즈 위의 값(`pen_5bp` 미측정) ·
  「엣지 없음」(WAN-84/88/111/114/124/151) 불변 · **토대·기본값 불변**(`min_stop_distance_fraction
  =0.003` 유지, 실거래 보류 유지). ⚠️ OOS 규약은 전체창 후보를 시각으로 가른 **따뜻한 판**
  (WAN-155/161 계열)이라 차가운 절단(WAN-89/145/151/164)과 OOS 절대 수준 병기 금지.

📌 **숏 부검(WAN-89): 숏이 진 건 숏이라서가 아니라 「모든 탭에 `RSI ≥ 70`」이었기 때문이다 —
WAN-87 비활성화의 근거가 소멸했다(기본값은 그대로 `False`).**
[`docs/decisions/wan89.md`](docs/decisions/wan89.md)(재현: `uv run python -m
backtest.wan89_short_autopsy --tf 1h --jobs 6` → `--tf 15m --jobs 4 --append`, 요약만 재생성은
`--from-csv`). 창은 WAN-111/114/117/134/137/138과 같은 못 박은 창, 6심볼 · 15m·1h ·
`baseline` 단독.
- 🔁 **WAN-84의 「OOS 숏 −3.82%」를 재인용 금지** — 그 표는 WAN-100 교정 **하루 전**에 돌아
  B안이 `tap_index`를 안 읽었고(첫 탭에도 재탭용 극단 게이트), 그래서 **모든 숏 진입이
  `RSI ≥ 70`을 요구**했다. 하락장에 RSI 70은 드물다 → 그 수치는 "하락장에서 숏을 쳤을 때"가
  아니라 **"하락장에서 강한 반등이 났을 때만 골라 숏을 쳤을 때"** 의 성적이다.
- 📌 **하락장(OOS)에서 숏은 지지 않는다** — 공식 렌즈 OOS 심볼평균 `short_only` **15m
  +2.56%(3/6) · 1h +4.14%(4/6)**, 롱과 함께 돌릴 때 기여 `both`−`long_only` **+5.89%p(15m) ·
  +6.77%p(1h)**(롱 온리 대비 2~3배). ⚠️ **단 전 구간에서는 15m이 정반대다** — 상승장 IS에서
  15m 숏은 **−12.70%**(1h는 +17.90%). "숏은 언제나 유리"가 아니라 **하락장 한정**이고
  15m은 장세에 갈린다.
- 📌 **원인은 RSI 게이트다(가설 D·E 지지)** — 게이트만 되돌린 사다리에서 **`extreme`(=
  WAN-84가 실제로 돌린 엔진)이 6개 (TF×구간) 셀 전부 꼴찌**이고 OOS는 두 TF 다 마이너스
  (15m −5.19% · 1h −3.23% — WAN-84의 −3.82%와 부호·자릿수가 맞는다). 체결률을 72~74% →
  25~32%로 3분의 1 토막 내면서 **남긴 표본의 승률이 떨어진다**(1h OOS 47.17%→40.94%) =
  쳐낸 쪽이 아니라 남긴 쪽이 나빴다(WAN-114 롱 축 판정의 숏 재현, 크기는 훨씬 크다).
  **첫 탭 면제 누락 한 가지**(`extreme`→`first_tap_free`)만도 **6/6 셀 플러스**(+3.02~
  +17.16%p). ⚠️ **「게이트 제거는 언제나 이득」으로 확대 금지** — `first_tap_free`→
  `unconditional` 한 단은 **15m 전 구간·IS에서 오히려 손해**다.
- **가설 A·B는 기각** — 볼린저 기각률(롱 3.5% vs 숏 3.6~3.9%)·규칙2 비중·1R 축소·수량 증폭이
  **롱/숏 대칭**이라 숏 손실을 가르지 못한다. 📌 봉내 라이브 밴드(WAN-132)에서 규칙 3은
  셋업을 없애기보다 **주문이 걸려 있는 시간을 줄인다**(체결률 100%→73~78%). **가설 C는 (c)
  TF에 갈린다**(1h 반박·15m 부합) — 추세 필터는 IS에서 고르는 자유 파라미터라 근거가 약하다.
- **펀딩은 설명력이 없다** — 숏 `funding_pct`는 부호 정상(수취)이되 명목가의 **0.01% 미만**
  이다. WAN-91이 남긴 "숏 과소평가" 우려는 이 규모다(부호 버그 가드는 통과).
- **1h 숏은 손절 위치에 털린다** — 손절 후 20봉 안에 진입가 아래로 되돌아온 비율이 숏
  **65.95%** vs 롱 52.41%. ⚠️ **15m에는 그 비대칭이 없다**(55.19% vs 56.01%).
- ⚠️ **「엣지 찾았다」로 인용 금지** — OOS `short_only`는 **ETH를 빼면 두 TF 다 마이너스**
  (15m −1.84% · 1h −0.52%)이고, 낙관 렌즈 위의 값이며, **숏 축 매칭 널은 아직 안 냈다**.
  뒤집힌 것은 「숏은 하락장에서도 손실이다」 하나뿐이고 「엣지 없음」(WAN-84/88/111/114/124)은
  그대로다.
- **기본값·토대 불변**(측정 전용, `short_enabled=False` 유지, 실거래 보류 유지). 숏 재활성화는
  **사용자 결정**이다 — 권고는 wan89.md §6(재-베이스라인 결정 이슈 · 숏 축 매칭 널 ·
  손절 위치 · WAN-139와 장세 라벨 공유). 🔁 **그 「숏 축 매칭 널」은 WAN-145가 냈다 — 판정
  (c)라 재활성화 권고는 없다**(아래 📌 문단).

📌 **새 밴드 엣지 재검(WAN-145 = WAN-132 §6 · 구 WAN-148 흡수): 롱 축 판정은 밴드를 바꿔도
안 움직이고, 처음 잰 숏 축은 (c)라 채택 근거로 약하다 — 기본값 불변.**
[`docs/decisions/wan145.md`](docs/decisions/wan145.md)(재현: `uv run python -m
backtest.wan145_new_band_null --tf 1h --jobs 6` → `--tf 15m --jobs 4 --append` · `uv run python
-m backtest.wan145_entry_ablation` 동일 패턴, 요약만 재생성은 `--from-csv`). WAN-132가 파급을
**재산출이 아니라 `tap` 고정**으로 처리하면서 **엣지 판정 계열(WAN-124 널 · WAN-114 분해)이
옛 밴드의 기록으로 얼어붙어** 있었다 — 하필 두 판정의 주인공이 볼린저인데 그 밴드를 바꿔
놓고 옛 밴드에서만 재고 있던 셈이다. **옛 표는 손대지 않고 새 모듈 둘**로 냈다. 창·심볼·TF·
자·부트스트랩 시드는 **전부 그대로**이고 움직인 것은 밴드(+렌즈 `baseline` 단독)뿐이다.
- 📌 **§1 롱 축: 판정 유지** — 공식 렌즈 유효 셀 24개 중 유의 **11개로 WAN-124(`tap`)와 같은
  개수**이고 TF 분포도 같은 모양이다(**새 밴드 15m 8/12 · 1h 3/12** vs `tap` 15m 9/12 ·
  1h 2/12). 수익 수준은 조금 내려간다(15m OOS +8.63% → **+5.64%** · 1h OOS +5.28% →
  **+2.98%**). ⚠️ 이 델타를 **「전환 비용」으로 단독 인용 금지** — WAN-132가 낸 전환 비용은
  게이트가 켜진 단(`L2`) 기준이고 이 표는 게이트 없는 오늘의 기본값이다.
- 📌 **§2 부품 분해: 볼린저가 유일한 플러스 부품이고 15m에서만이라는 WAN-114 결론도 유지** —
  공식 렌즈 OOS 볼린저 증분 **15m +16.70%p(6/6심볼) · 1h −2.80%p(2/6)**(`tap` 판은 +16.20%p ·
  −1.18%p). RSI 게이트(`L0r→L1`)는 **네 셀 중 셋이 음수**고 게이트 제거(`L2→L2u`)는 **네 셀 중
  셋이 양수**라 **WAN-123 결정이 새 밴드에서도 지지된다**. 1h는 여전히 규칙 층 전체가 값을 못
  더한다(IS에서 존-단독이 이긴다). ⚠️ 이 사다리에서 **채택 기본값은 `L2`가 아니라 `L2u`**다
  (WAN-114 라벨은 WAN-123 이후 거짓 — 표에 찍히는 문장만 교정하고 설정은 물려받았다).
- 📌 **§3 숏 축(처음 재는 축): 판정 (c) — TF·구간에 갈린다.** `short_only` 유의 8/24(15m 3/12 ·
  1h 5/12) · `both` 13/24(15m 6/12 · 1h 7/12). 숏은 롱의 「15m ≫ 1h」 패턴을 **따르지 않고**
  유의성이 **1h IS(상승장)** 에 몰린다. 🚨 **OOS 플러스는 네 셀 전부 ETH 하나가 만든다**
  (`short_only` 15m +2.56%→**−1.84%** · 1h +4.14%→**−0.52%**, `both` 15m +11.53%→**−2.04%** ·
  1h +9.74%→**−1.92%**) — ⚠️ **롱도 같다**(15m +5.64%→−1.88% · 1h +2.98%→−3.80%). 숏의 결함이
  아니라 엔진 전체의 성질이다.
- **결정: 숏 재활성화 재-베이스라인은 제안하지 않는다**(판정이 (a)가 아니다). `short_enabled`
  기본값 **`False` 유지** · 토대 불변 · 실거래 보류 유지. 사용자가 경고를 알고도 `both` 전환을
  원하면 **별도 결정 이슈**다(개발자 임의 착수 금지, 선행은 숏 축 체결 보수화·손절 위치).
- 📌 **검산 3개가 전부 차이 0** — (1) §1·§3 실제 팔 **72행 ≡ `wan89_short_autopsy.csv`**
  (수익·거래 수·`buy_hold`), (2) §2 `L1`·`L2` **48행 ≡ `wan119_intrabar_live_band.csv`의
  `L1`·`L2i`**, (3) §2 `L2u` **24행 ≡ §1 `long_only` 실제**(두 새 모듈이 서로를 검산).
- ⚠️ **「엣지 찾았다」로 인용 금지** — 유의 셀은 `baseline`(낙관) 위의 값이고, 이 널은
  **「선별」과 「가격」을 가르지 못한다**(풀은 존 근단가·실제는 밴드가 — **WAN-131** 소관이고
  이 표가 그 이슈의 새 밴드 기준선이다). 숏 축 판정은 롱 축 「엣지 없음」의 반박이 아니다.
  🔁 **단 이 표는 병합 존 위의 기록으로 얼어붙었다 — 분리 존 판은 WAN-151이 냈다**(아래 📌).

📌 **분리 존 엣지 재검(WAN-151 = WAN-149 §7): 판정도 부품 순위도 존 정의에 기대고 있지
않았다 — 기본값 불변.** [`docs/decisions/wan151.md`](docs/decisions/wan151.md)(재현: `uv run
python -m backtest.wan151_split_zone_null --tf 1h --jobs 6` → `--tf 15m --jobs 6 --append` ·
`uv run python -m backtest.wan151_entry_ablation` 동일 패턴, 요약만 재생성은 `--from-csv`).
WAN-149가 존 병합을 폐지하면서 파급을 **재산출이 아니라 `LEGACY_COMBINE_OBS` 고정**으로
처리해 **엣지 판정 계열 6개(WAN-84/88/111/114/124/145)가 전부 병합 시절 기록으로 얼어붙어**
있었다 — 즉 **지금 매매하는 엔진의 엣지 판정이 하나도 없었다**. WAN-132 → WAN-145와 **같은
패턴**으로 옛 표는 손대지 않고 **새 모듈 둘**로 냈다. 움직인 축은 **존 하나**이고 밴드·게이트·
오프셋·렌즈·자·시드·창·심볼은 전부 그대로다(사다리·팔 정의는 **import로** 물려받는다).
- 📌 **§1 대조군 실험(매칭 널): 판정 (c) 유지** — 공식 렌즈 유효 24셀 중 유의 **9개**
  (**15m 6/12 · 1h 3/12**)로 병합 판(11개 · 15m 8/12 · 1h 3/12)과 **같은 모양**이다. 움직인
  것은 15m IS 두 셀뿐이고 **OOS 네 셀은 전부 같다**. 무력화 축은 볼린저(WAN-124 계승).
- 📌 **§2 부품 분해: 볼린저가 유일한 플러스 부품이고 15m에서만이라는 결론도 셋째로 재확인** —
  공식 렌즈 OOS 볼린저 증분 **15m +17.65%p(6/6심볼) · 1h −3.92%p**(병합 판 +16.70%p ·
  −2.80%p). RSI 게이트(`L0r→L1`)는 네 셀 중 셋 음수, **게이트 제거(`L2→L2u`)는 네 셀 중 셋
  양수라 WAN-123 결정도 분리 존에서 지지된다**.
- ⚠️ **1h는 규칙 층 전체가 값을 못 더한다** — OOS 존-단독 `L0` +3.03% > 채택 기본값 `L2u`
  +2.25%(−0.77%p). **크기가 0 언저리라 「뒤집혔다」로 읽지 말 것**(병합 판은 +0.49%p였다) —
  읽을 수 있는 건 "1h 규칙 층 기여는 0과 구분되지 않는다"까지다(15m은 +12.36%p).
- 📌 **수익 델타가 WAN-149 §3 격자와 같은 방향·자릿수다**(15m IS −0.35%p / OOS +2.20%p ·
  1h IS +5.82%p / OOS −0.73%p) — **다른 모듈·다른 파이프라인의 독립 검산**이다. ⚠️ 단
  **「분리가 15m OOS 수익을 올렸다」로 단독 인용 금지**(WAN-149와 같은 이유 — 플러스 심볼은
  오히려 3/6 → 2/6).
- 📌 **검산: 사다리의 채택 기본값 단(`L2u`) 24행 ≡ §1 널의 실제 팔 24행**(공개 CSV 대 CSV로
  차이 0 · 메모리 원값 최대 9.02e-17 = 부동소수 끝자리). 코드가 **일치 · 잡음 · 불일치를 다르게
  찍고** 회귀 테스트가 세 갈래를 동작으로 고정한다(조용한 통과 금지).
- 🚨 **「엣지 찾았다」로 인용 금지 · OOS 플러스는 여전히 ETH가 만든다** — 15m OOS 심볼평균
  +7.84% → **ETH 제외 +0.01%**, 1h OOS +2.25% → **−4.93%**. `baseline`(낙관) 위의 값이고
  **「선별」과 「가격」을 가르지 못한다**(WAN-131 소관 — 이 표가 그 이슈의 **분리 존 기준선**).
- ⚠️ **안 잰 것**: 체결 보수화 축(`baseline` 단독이라 `pen_5bp` 미측정) · **숏 축**(이슈가
  범위에서 뺐다 — WAN-145 §3이 병합 존에서 (c)로 냈다). 둘 다 필요해지면 별도 이슈.
- **기본값·토대 불변**(측정 전용, 실거래 보류 유지). 옛 병합 고정 md/csv는 **손대지 않았다**.

📌 **오늘 엔진 롱+숏 재검(WAN-164 = 사용자 질문 「존필터 걸린 상태 롱숏 같이는 아직 안
돌려봤잖아」): 숏 축 판정 (c) — 오늘 엔진(분리 존 + 필터 1.28)에서도 숏은 값을 안 더한다.**
[`docs/decisions/wan164.md`](docs/decisions/wan164.md)(재현: `uv run python -m
backtest.wan164_short_today_engine --tf 1h --jobs 6` → `--tf 15m --jobs 6 --append`, 요약만은
`--from-csv`). 숏을 실제로 켠 측정은 **딱 둘**(WAN-89·WAN-145 §3)뿐이었고 둘 다 **병합 존 ·
필터 없음**이라 오늘 엔진 위에 숏 표가 없었다 — 팔은 WAN-89, 널 골격은 WAN-145를 그대로
재사용하고 **움직인 축은 존 정의(병합→분리)** 하나다(필터 1.28·봉내 라이브는 `ConfluenceParams()`
에서 자동으로 따라온다).
- 📌 **숏 축 종합 (c) — 유의는 거의 IS다.** 공식 렌즈 매칭 널에서 `short_only` 유효 23셀 중
  유의 **6개**(15m 3/11 · 1h 3/12)인데 **그중 5개가 IS**이고 **OOS 유의는 15m 1/5 · 1h 0/6**
  로 사실상 없다. `both`도 유효 24 중 유의 11(15m 6/12 · 1h 5/12)이 IS에 몰린다. 숏은
  상승장에서 국소적으로 무작위를 이기지만 **하락장(OOS)에서는 수익이 나도 무작위와 구분되지
  않는다**(WAN-145 병합 존 (c)가 분리 존에서 유지).
- 📌 **숏 단독 OOS는 플러스지만 작다** — 심볼평균 15m +1.85% · 1h +3.80%, `both`−`long_only`
  기여 OOS 15m **+2.00%p** · 1h **+4.25%p**. WAN-89가 무너뜨린 "숏은 하락장에서도 손실"은
  오늘 엔진에서도 유지되지만, 「엣지」는 별개 질문이고 답은 **아니오**에 가깝다.
- 📌 **드물게 ETH 편중이 아니다** — `short_only` OOS leave-one-out이 **어느 심볼을 빼도
  플러스**(15m 최악 ex-ETH +0.13% · 1h 최악 ex-XRP +3.01%). WAN-89/145에서 숏 OOS 플러스가
  전부 ETH였던 것과 대조적(존폭 필터가 좁은 존만 남겨 편중이 풀림). ⚠️ **널을 못 이기므로
  편중이 아닌 것이 채택 근거는 아니다.** `both` 1h는 여전히 ETH가 절반을 만든다(ex-ETH +3.42%).
- 📌 **검산 통과** — `long_only` 24셀에서 널 실제 다리(`run_random_control_b_segment`) ≡
  성과 표(`harness.run_once`) `total_return` **최대 절대차 0.00e+00**(비트 일치). 필터를 끄면
  표준 CLI가 WAN-151을 재현하고, 켜면 이 모듈과 일치한다 — 차이는 **필터**이지 배선 오류가 아니다.
- 🚨 **OOS 수치를 WAN-155/161과 나란히 인용하지 말 것 — 구간 컨벤션이 다르다.** 이 계열
  (WAN-89/145/151/164)은 **IS/OOS를 먼저 자르고 각 조각에서 따로 탐지·체결**한다
  (`harness.slice_market`, OOS 차갑게 시작 · 누수 없음 · **표준 CLI `backtest.run --oos`와 같음**).
  WAN-155/161은 **전체 창 후보를 시각으로 분리**해 OOS가 IS 시절 존을 물려받는다(거래 수 더 많고
  더 낙관적). **IS는 두 컨벤션이 비트로 같고 OOS만 갈린다**(롱 1h OOS 1.5R: 이 계열 심볼평균
  +5.31% vs WAN-161 +10.23%). 어느 쪽도 버그가 아니라 다른 OOS 질문이다.
  🔁 **WAN-166이 그 후자를 「따뜻한 연속 OOS」로 정식화해 정본 리포트 기준으로 세웠다**
  (`--oos-warm`, 아래 📌 문단 — 따뜻 = 주 수치 · 차가움 = 스트레스 병기).
- ⚠️ **「엣지 없음」(WAN-84/88/111/114/124/151)은 그대로** — 숏 축 판정은 롱 축 「엣지 없음」의
  반박이 아니다(다른 질문). 이 널은 「선별」과 「가격」을 못 가른다(WAN-131 소관). 전부
  `baseline`(낙관) 렌즈 위의 값이고 숏 축 체결 보수화(`pen_5bp`)는 안 쟀다.
- **기본값·토대 불변**(측정 전용 · `short_enabled=False` 유지 · 실거래 보류 유지). **숏
  재활성화 재-베이스라인은 제안하지 않는다**(판정이 (a)가 아니다) — 사용자가 경고를 알고도
  원하면 별도 결정 이슈(개발자 임의 착수 금지).

📌 **채택 좌표(필터 1.28 × 9종목 × 6년) 매칭 널(WAN-201 = 사용자 「그걸 무작위랑 돌려봐야지」):
판정 (c) 유지 · WAN-164의 「필터가 15m 롱 유의 강화」는 9종목에서 평평해진다 · 기본값 불변.**
[`docs/decisions/wan201.md`](docs/decisions/wan201.md)(재현: `uv run python -m
backtest.wan201_matched_null_filter_nine`, 요약만 `--part summary`). WAN-151/164/176 매칭 널
기계(볼린저 무력화 축)를 그대로 재사용하고 움직인 축은 **필터·유니버스·창**뿐이다.
- 🚨 **이슈 전제가 뒤집혀 있었다 — `wan176_null.csv`(§2)가 이미 「필터 켜짐 × 9종목 × 6년」
  이다.** WAN-176 §2 널은 `arm.params()`를 핀 없이 써 채택 기본값 `max_zone_width_atr=1.28`을
  물려받는다(WAN-159 커밋이 WAN-176의 조상). 검산이 **필터 켜짐 9종목이 `wan176_null.csv`를
  비트 재현**(7.11e-15)함을 못 박아 증명한다. 그래서 이슈 헤드라인은 이미 있었고, 이 이슈는
  비어 있던 모서리 **필터 꺼짐 × 9종목 × 6년**을 채워 2×2(종목수 × 필터)를 완성한다. ⚠️ 이슈의
  검산 요구(「필터 끄면 wan176 §2 재현」)는 전제가 뒤집혀 성립하지 않는다.
- 📌 **필터 꺼짐 × 9종목 판정 (c)** — 유효 54셀 중 유의 34(15m 16/18 · 1h 11/18 · 4h 7/18),
  유의는 상승장 IS에 몰리고 OOS에서 급감. **4h 표본 게이트가 6년 창에서 해소**(OOS 심볼당
  45.4거래 · 옛 3년 「판정 불가」 풀림, WAN-176 §1과 정합).
- 📌 **2×2 필터 축(공통 TF 15m·1h)**: 6종목3년 꺼짐 9/24 → 켜짐 13/24(강화, WAN-164)인데
  **9종목6년 꺼짐 27/36 → 켜짐 26/36으로 평평** — WAN-164의 「필터가 유의를 강화」는 9종목에서
  살아남지 않는다. 유니버스·창 축(9→27, 13→26)은 **창 혼입**이 있어 순수 종목 효과가 아니다.
- ⚠️ **「엣지 찾았다」로 인용 금지** — (a) 필터 꺼짐×9의 ETH leave-one-out은 여섯 셀 전부 부호
  유지(드물게 편중 아님, WAN-142/152 계열)지만 널을 못 이기는 셀이 있어 채택 근거 아님 ·
  (b) 「선별」과 「가격」을 못 가른다(WAN-131 소관). **「엣지 없음」(WAN-84/88/111/114/124/151)은
  같은 질문의 새 좌표**이고 판정은 (c) 그대로다.
- **측정 전용 · 기본값·토대 불변**(펀딩 대리 미적용 = 널 계열 관행 · 실거래 보류 유지).

📌 **좁은 존 선별 심화 — 문턱 스윕 × 매칭 널(WAN-203 = 트랙 1, 사용자 「15m이 메인」):
메인 TF(15m)는 4관문을 전 문턱에서 통과하지만 궤적은 (평평/역전)이라 「좁을수록 정보 많음」은
성립하지 않는다 — 1.28이 마진 정점 근처다. 기본값 불변.**
[`docs/decisions/wan203.md`](docs/decisions/wan203.md)(재현: `uv run python -m
backtest.wan203_narrow_zone_selection --timeframes 1h,4h --jobs 4` → `--timeframes 15m --jobs 4
--append`, 검산 `--checksum`, 요약만 `--from-csv`). WAN-152 기계 재사용 + 절대 존폭÷ATR 문턱
축(`pnl_rows_for_cell(abs_threshold=)`, `None`이면 분위 경로 비트 재현). 필터 = `zone_width_atr
≤ 문턱` × 기하 통제 `atr` 장벽 × 매칭 널 20시드. 9종목 · 못 박은 6년 · 15m·1h·4h · `baseline`
(+ `pen_5bp` 관문 3). 엔진 필터 1.28은 끔(이중 필터 방지). 판정 자 `zone` vs `atr`(컴퓨트
예산으로 `zone_height` 생략).
- 📌 **판정 — 조일수록 마진이 커지는가(`atr` OOS)**: **15m (평평/역전)** — 1.6 +28.18%p ·
  **1.28 +32.02%p(정점)** · 1.0 +23.92 · 0.8 +18.68 · 0.6 +10.51, **전부 p=0.048·n=9**(표본
  붕괴 없이 진짜 감소). **1h (평평/역전)**(1.0 정점) · **4h (강화, 얇음)**(1.6→1.28 두 점만
  유효, 나머지 붕괴). → **메인 15m의 답은 「아니오」** — 1.28이 정점 근처이고 더 조이면 감소한다.
  ⚠️ **「데이터가 1.28을 최적으로 골랐다」로 인용 금지**(정점이 "근처"일 뿐 · WAN-159 1.28은
  사용자 판단).
- 📌 **15m은 4관문 전 문턱 통과** — 매칭 널(p=0.048) · OOS · `pen_5bp`(p=0.048) · ETH LOO
  (어느 종목 빼도 +, SOL·DOGE 포함). **이 저장소 존폭 선별 축의 가장 강한 증거**다. 1h는 1.28만
  ETH LOO에서 −SOL −0.23%(SOL 의존), 4h는 1.28·1.0만 유의.
- 📌 **가드 × 좁은 문턱 충돌** — `zone` 장벽 15m은 0.8에서 11%(TRX)·0.6에서 44%(BNB·BTC·TRX·
  XRP)가 20건 미만 붕괴하는데 **`atr` 장벽은 전 문턱 0% 붕괴**(손절이 존폭과 무관) — 그래서 15m
  궤적이 깨끗한 n=9로 측정된다. 가드 불변(WAN-76/79 소관).
- 📌 **Phase 1b(연속 사이징) 착수 안 함** — 이슈가 「1a 결과에 따라」로 뒀고, 1a가 그 전제
  (좁을수록 정보 많음)를 기각했다. 좁을수록 마진이 정점 후 감소하므로 「좁은 데 더 베팅」은 알파
  없이 노출만 키운다(+ 레버리지 상한·가드 충돌). 「전제 미성립」으로 닫는다.
- 🚨 **「엣지 찾았다」로 인용 금지** — 전부 `baseline`(낙관) 렌즈 위(관문 3은 `pen_5bp`까지지만
  틱·호가 WAN-98 별개) · 「선별」과 「가격」을 못 가른다(WAN-131 소관). **「엣지 없음」(WAN-84/88/
  111/114/124/151/201)은 다른 질문**(진입 규칙이 무작위와 구분되는가)이라 뒤집는 게 아니다 —
  존폭 축은 *이미 진입한 셋업 중 무엇을 버릴까*를 묻는다.
- 📌 **성능(별도 트랙)** — 15m·6년이 초선형으로 느려 토대 성능 버그 둘을 찾아 **비트 동일**로
  고쳤다(BTC 1h 920행 최대차 0.0 실측): 밴드 시딩 O(N×M)→O(N×W)(`realtime_band.seed_from_closed`
  꼬리만 커밋) · 서브스텝 슬라이스 O(N×M)→O(N)(`substeps[start:]` 복사 대신 `start` 오프셋+`islice`).
  **비트 동일이라 측정 결과와 무관 → 성능 수정은 별도 최적화 PR로 분리**(측정 PR 제외). 최적화
  후에도 15m은 셀당 ~37분(봉내 밴드 `value()` 고유 비용).
- **기본값·토대 불변**(측정 전용, `ConfluenceParams()` 불변, 실거래 보류 `ALPHABLOCK_LIVE_TRADING
  =false` 유지). 채택은 사용자 결정 · 개발자 임의 착수 금지.

📌 **따뜻한 연속 OOS = 정본 리포트 기준(WAN-166 = 사용자 결정 2026-07-22): `--oos-warm`이
`oos_warm`(주 수치)과 `oos`(과최적화 스트레스)를 병기한다.**
[`docs/decisions/wan166.md`](docs/decisions/wan166.md). 사용자 지적("차트는 계속 움직이고
오더블록도 계속 생기는데, 자른 곳을 시작점이라 두는 게 애초에 말이 안 되지 않아?") 수락.
차가운 절단(`--oos`)은 존 재고 0 + 지표 재워밍업으로 시작하는, 실전에 없는 상황이다 —
따뜻한 판은 전 구간을 연속으로 태워 존·지표를 데운 뒤 **탭이 OOS 경계 이후인 셋업만 신선한
초기자본으로** 평가한다(`Segment.eval_start_fraction` · `run_zone_limit_backtest_verbose
(eval_from_ms=)`). 결정은 엄격히 인과적 — **미래 절단 실험이 동작으로 고정한다**(미래 봉을
잘라도 그 전에 청산까지 끝난 거래가 비트 동일, `test_warm_oos_has_zero_future_leakage`).
- 📌 **두 방식은 다른 질문에 답한다 — 그래서 병기다**: 따뜻 = "실전 손익이 얼마인가"(현실
  추정) · 차가움 = "안 본 데이터에도 통하나"(보수적 하한 · 튜닝 기간과의 독립). **두 수치의
  차이 자체가 강건성 신호**다(크면 그 성과가 물려받은 존 재고에 기댄다는 뜻).
- 📌 **워밍업 = 앞구간 전체이고 파라미터가 아니다** — 길이를 노브로 열면 IS에서 고르는
  자유 파라미터가 하나 는다(WAN-108이 경계한 자리). 평가 경계는 차가운 OOS와 **같은 지점**
  이라 두 행의 `start_time`·`num_bars`가 비트 일치한다(`eval_boundary_ms`가 `slice_market`
  하한과 같은 식). WAN-155/161의 전체창 라벨링과 실질이 같되 **하네스 세그먼트의 1급
  속성**으로 정식화된 것이 차이다(리포트마다 다시 짜지 않는다).
- 📌 **실측(못 박은 창 2023-07-14~2026-07-15 · 6심볼)이 두 옛 계열을 교차 검산한다** —
  따뜻 OOS 심볼평균 1h **+10.23%**(= WAN-161과 일치) · 15m **+14.03%**(ex-TRX +16.96% =
  WAN-161과 일치 — 그쪽 TRX 셀은 18거래 게이트 탈락) vs 차가움 1h **+5.31%**(= WAN-164
  계열과 일치) · 15m +10.11%. 강건성 신호(따뜻−차가움)는 1h +4.92%p · 15m +3.92%p —
  1h 우위의 절반 가까이가 물려받은 재고의 몫이다. 📌 **1h는 차가움 4/6 → 따뜻함 6/6
  플러스**(BTC −2.39%→+3.47% · SOL −0.69%→+5.41%) — 차가운 1h OOS 마이너스의 일부는
  전략이 아니라 **절단 인공물**이었다. ⚠️ 「따뜻한 판이 더 좋다」로 인용 금지 — 성과를
  올리는 장치가 아니라 실전 질문에 맞는 자다. 원본:
  `backtest/reports/wan166_warm_vs_cold_{1h,15m}.csv`.
- ⚠️ **제한(조용히 무시하지 않고 거부)**: B안(지정가) **단일 포지션 전용** — A안은 봉 단위
  자본이 워밍업 손익에 오염되고, 다중 포지션·`--walkforward`는 배선하지 않았다(별도 이슈).
  경계에 걸친 워밍업 포지션이 평가 초입 슬롯을 잠그는 효과는 모델링하지 않는다(WAN-155/161
  규약과 같음 — 워밍업 셋업은 배치조차 안 한다).
- **기존 표 불변** — 차가운 판정(WAN-89/145/151/164)·전체창 판정(WAN-155/161)의 옛 표는
  손대지 않았고 `--oos`도 그대로다(IS·full·oos 행은 `--oos-warm`에서도 비트 동일 — 회귀
  테스트 고정). **옛 판정의 따뜻한 판 재산출은 별도 이슈다.** 기본값·토대 불변(측정/도구
  전용, 실거래 보류 유지).

📌 **레버리지 북(WAN-169 = 사용자 정의 2026-07-22 · 구 WAN-170 흡수): 칸=(종목,TF)마다
1포지션 · 여러 칸 동시 · 한 지갑 공유 — 옵트인 엔진 `backtest/leverage_book.py` + 측정
판정 (a), 기본값 불변.** [`docs/decisions/wan169.md`](docs/decisions/wan169.md)(재현:
`uv run python -m backtest.wan169_leverage_book --jobs 6`, 요약만은 `--from-csv`). 사용자
정의: *"타임프레임이 다른 곳에서 다중 진입이 허용된다고 한 거야. 같은 프레임에서는 익절하기
전엔 한 번만 들어가야지."* 현행 다중 포지션(WAN-103, 같은 TF 안 존 겹쳐 잡기)과 **반대
방향**이다 — 칸 안 스택 금지, 칸 사이를 공통 시간축·공유 자본으로 묶는다.
- 📌 **레버리지 = 매 거래 사이징 N배**(사용자 확정 — cap-only 아님): 리스크 1%→N% ·
  거래당 명목 천장 N× · 북 상한 자본×N을 `scale_sizing_params`가 **한 곳에서 함께** 키운다.
  ⚠️ 따라오는 성질: 거래 크기와 상한이 같은 배율이라 **상대 여유가 배수 불변** — "배수를
  올리면 겹칠 자리가 는다"가 아니라 모든 것이 N배로 커진다(테스트가 동작으로 고정).
- 📌 **straddle 회계 = (b) 배치 안 함**(사용자 결정): 워밍업에 탭이 나 평가 경계를 넘어
  사는 포지션은 평가 초입의 칸·자본·레버리지 자리를 점유하지 않는다(WAN-166/155/161 규약
  일치). 경계 판정은 진입 시각이 아니라 **탭 시각**이고, 미래 절단·straddle 비점유·칸
  하나짜리 북 ≡ 채택 시퀀서 비트 일치를 `tests/test_leverage_book.py`가 동작으로 고정한다.
- 📌 **판정 (a)** — 모든 스코프(15m·1h·both) × 구간(oos_warm 주·oos 스트레스)에서 배수
  N>1이 1배의 수익/MDD를 청산 트리거 0으로 이겼다(both·oos_warm 5배: +5688%/MDD 40.65%/
  최대 동시 리스크 9.82%). 🚨 **단 그 자(총수익/MDD)의 N-단조 상승은 상당 부분 복리
  산수다** — 양(+) 기대값을 수백 번 복리로 돌리면 총수익은 N에 지수적, MDD는 100% 유계.
  「5배가 안전하다」로 읽지 말 것 — **결정의 실제 내용은 낙폭**(5배 MDD 따뜻 40.65% ·
  차가움 48.93%)이다. 북 1배가 격리 평균(+12%)을 크게 이기는 것도 배수가 아니라 **거래
  빈도**(한 지갑이 12칸 셋업을 순차로 다 받음)이고, full 구간 조 단위 %는 달성 가능 성과가
  아니다(거래당 작은 낙관이 횟수만큼 지수 증폭).
- 📌 **북의 실측 동시 열림 최대 2~3칸** — census(WAN-167)의 7칸은 자본 제약 없는 격리
  겹침이고, `risk_pct` 북은 명목 상한이 겹침을 잘라낸다(`fixed_notional` 시드 분할은 5칸).
  leave-one-out은 어느 종목을 빼도 전 배수 플러스(복리 희석 — 「엣지 찾았다」 인용 금지).
- ⚠️ **「엣지 없음」(WAN-84/88/111/114/124/151) 불변** — 레버리지는 위험의 모양만 바꾼다
  (WAN-90 계열). 옛 WAN-108의 「2안이 진다·높은 배수는 위험조정으로 진다」는 **옛 엔진**
  값이라 재인용 금지(이 표가 오늘 엔진 첫 측정 — `fixed_notional` 칸 분할 정의도 달라
  직접 비교 금지). **기본값·토대·사이징 기본값 불변**(옵트인 · 배수/북/사이징을 기본값으로
  올리는 것은 재-베이스라인 = 사용자 결정 · 실거래 보류 `ALPHABLOCK_LIVE_TRADING=false` 유지).

📌 **레버리지 북 9종목 재측정(WAN-180 = WAN-169 후속): 결합 vs cap-only 두 팔 + 밀림
회계·기회비용 — MDD 30% 예산(차가움) 승자는 결합 both 3배, 기본값 불변.**
[`docs/decisions/wan180.md`](docs/decisions/wan180.md)(재현: `uv run python -m
backtest.wan180_leverage_book_nine --jobs 6`, 요약만은 `--from-csv`). WAN-169 기계를 9종목 ·
WAN-175/176 못 박은 6년 창(2020-09-15~2026-07-22)으로 옮기고, **cap-only 레버리지**
(`LeverageBookParams.leverage_mode="cap_only"`, 옵트인 — 북 명목 상한만 N배 · 거래당
리스크·천장은 1배 그대로)를 엔진에 추가했다(기본 `"combined"` = WAN-169 그대로 · 안 켜면
비트 재현, 회귀 테스트가 동작으로 고정). 같은 창 6종목 북(`universe=six`)을 병기해 6→9
증가분을 창 이동과 분리했다.
- 📌 **신규 3종목(DOGE·LINK·LTC) 펀딩은 BTC 시계열로 대리한다(사용자 지시 2026-07-23)** —
  이 종목들은 `funding_rate` 테이블에 **0행**이라 그대로 두면 펀딩비 0으로 성과가
  부풀려진다. 대리 = 기존 6종목 중 확정 펀딩 평균 최고(= 롱에게 가장 비싼, 보수적) 종목
  (`apply_funding_proxy`, `--no-funding-proxy`로 끔). 자기 데이터가 있으면 덮지 않고, 대체
  칸은 `engine_*` 배선 검산에서 뺀다(검산은 기존 12칸 담당 — 비트 일치 확인).
- 📌 **팔 A(결합)는 밀림이 배수 불변**(명목상한 스킵 ~1,620건 평평 — 상대 여유 불변 성질의
  9종목 재현)이고 **MDD가 배수를 따라간다**(both 차가움 10.6% → 5배 44.1%). 📌 **팔 B
  (cap-only)는 밀림을 정말로 산다** — 명목상한 스킵 1,640 → **7건**(5배), 거래 +45%, 동시
  최대 6칸. 🚨 **이슈가 경고한 「동시 노출 증가」는 실측에서 뒤집혔다** — cap-only의 최대
  동시 리스크(5.65%)·MDD(14~16% 평평)가 **결합보다 낮다**(거래당 1% 유지 × 칸 수 유한).
  그 대가는 절대 수익 성장 둔화(복리 항이 N%가 아니라 1%)다.
- 📌 **MDD 30% 예산(차가움)**: 결합 **both 3배**(+45,389% / MDD 29.01%)가 예산 안 수익
  최대. WAN-169 6종목·3년에서 「따뜻만 통과」였던 both 3배가 9종목·6년에선 **차가움도
  통과**(32.2%→29.0%)했고, 대신 **15m 3배가 탈락**(27.3%→**32.7%**) — "심볼이 늘면 통과
  배수가 낮아질 수 있다"는 경고가 15m 스코프에서 실현됐다. 팔 B 최상은 both 5배(+4,105% /
  MDD **14.25%**) — 낙폭 절반, 수익 성장 포기의 교환. **채택은 사용자 결정.**
- 📌 **밀림 기회비용(팔 A) 부호 판정 = 놓친 수익(밀림 = 손해)** — 명목상한 스킵의 가상
  net R 합 +210~+222R(승률 55% ≈ 실현 거래 54%). 밀림은 나쁜 셋업을 거르는 안전장치가
  아니라 **용량 부족으로 흘린 같은 질의 셋업**이고, 이것이 cap-only의 존재 이유를 실측으로
  뒷받침한다. ⚠️ **격리 상한(상호 밀어냄 무시 · `baseline` 위) — 부호만 읽을 것.**
- ⚠️ **수익률 절대값 인용 금지**(수백~수천 거래의 복리 값 · WAN-169 경고 그대로) ·
  leave-one-out은 어느 종목을 빼도 전 배수 플러스(복리 희석) · **「엣지 없음」(WAN-84/88/
  111/114/124/151/176) 불변**. **기본값·토대 불변**(cap-only는 옵트인 · 기본값 승격은
  재-베이스라인 = 사용자 결정 · 실거래 보류 유지).

📌 **레버리지 북(cap_only 5배)이 채택 기본 회계다(WAN-213 = 사용자 결정 2026-07-30 「전부
다」 · 프로젝트 최대 규모 재-베이스라인): 포지션 회계가 「동시 1포지션」에서 **북**으로 바뀌었다.**
[`docs/decisions/wan213.md`](docs/decisions/wan213.md). 파라미터 하나가 아니라 **회계 모델
자체**가 「칸=(종목,TF)마다 1포지션 · 여러 칸 동시 · 한 지갑 공유 · cap_only 5배」로 바뀐다.
- 📌 **`LeverageBookParams()` 기본값이 채택 북이다**(cap_only · 배수 5) — `ConfluenceParams()`가
  채택 전략을 내는 것과 대칭. WAN-169 중립 기준점(배수 1 · combined)은 `LEGACY_BOOK_PARAMS`로
  뺐고, 옛 결합 리포트(wan169)는 `leverage_mode="combined"` **명시 핀**으로 고정했다.
- 📌 **인자 없는 `backtest.run`이 북을 돈다** — `--positions`가 분기: 미지정/`book` = 채택 북,
  `single` = per-cell 동시 1포지션(옛 리포트 대조), 숫자 = per-cell 다중 포지션(WAN-103).
  북은 칸을 가로지르는 공유 자본이라 per-cell 행이 아니라 **구간당 집계 행**을 낸다(새 모듈
  `backtest/book_cli.py`가 실행 · wan169/wan180 함수를 그대로 재사용해 wan180 셀과 **구성상
  비트 일치**). 정본 OOS = `backtest.run --oos-warm`(북 경로에 warm/cold 배선됨, WAN-166 규약).
- 🚨 **레버리지는 알파가 아니라 위험의 모양만 바꾼다**(WAN-90) — 총수익 %(both full +12.77억%대)는
  수천 거래 복리 착시라 **실현 수익 아님**. 판단 근거는 **MDD(both 5배 6년 19.55% · oos_warm
  14.25%)·최대 동시 리스크(6.93%)·청산(0건)**. 6년 MDD ~20%는 천장이 아니라 **바닥선**(창이
  2020-09 시작이라 2018·2020-03 폭락 미포함). **「엣지 없음」(WAN-84/88/111/114/124/151/201)
  불변**(다른 질문) · 전부 `baseline` 위 값 · 실거래 보류 유지(`ALPHABLOCK_LIVE_TRADING=false`).
- 📌 **페이퍼 러너 배선은 별도 이슈(WAN-171)** — 북 모드는 전략·비용 격자·거래별 출력·
  `--walkforward`·미끄러지는 창을 아직 **거부**한다(채택 기본값만 · 필요해지면 별도 이슈).

📌 **채택 북이 「익절 후 존 내 재진입」을 band 규칙으로 켠다(WAN-273 = 사용자 결정 2026-08-09):
인자 없는 `backtest.run`(채택 북)이 재진입을 돈다.** [`docs/decisions/wan273.md`](docs/decisions/wan273.md).
파라미터가 아니라 **채택 북의 후보 집합**이 바뀐다 — 익절 후 같은 존에 다시 지정가를 걸되,
재무장 지정가를 **봉내 라이브 밴드로 재산정(`band`)**한다(엔진 진입가 산정과 같은 사슬,
WAN-132). 근거는 WAN-272 CSV(band가 `pen_5bp`에서 가장 튼튼: 수익/MDD band 119.69 > freeze
99.16 > off 83.88).
- 📌 **`book_cli.run_book` 기본값이 채택 값이다**(`reentry=True` · `reentry_entry_rule="band"`,
  `ADOPTED_REENTRY_ENTRY_RULE`) — 인자 없는 `run_book()`이 곧 채택 북. warm/cold OOS도 재진입
  포함(`--oos-warm`). 재진입은 `LeverageBookParams` 필드가 아니라 **후보 생성 축**이라 별도로
  표현된다.
- 📌 **`--reentry` 미지정 = 채택(band 켬) · `off` = WAN-273 이전 재진입-off 북(옛 CSV 비트
  재현)** — 둘은 다르다(WAN-159 `none` vs 미지정과 같은 규약). `on` = band 별칭,
  `freeze`/`zone`/`band` = 규칙 명시. 안 가르면 「off」 라벨을 달고 조용히 band로 도는 이중
  배선(WAN-91/95/112/123/159 부류) — 회귀 테스트가 **동작**으로 고정(미지정 ≡ band 켬,
  off ≡ 재진입 없는 북 비트 재현). per-cell(`--positions single`/숫자)과 명시 재진입은 함께
  못 쓴다(exit 2).
- 🚨 **알고 받는 손해**(사용자가 경고 알고 선택): 재진입 켜면 MDD 12.17%→15.59% · 최대 동시
  리스크 9.80%→11.36%(청산 0건). 전부 `baseline`(닿으면 체결) 낙관 위 값 · band는 밴드가
  체결이라 큐 우선순위상 가장 안 될 체결에 기댄다 · 6년 MDD는 폭락 미포함 **바닥선**.
  **「엣지 없음」(WAN-84/88/111/114/124/151/201/248) 불변**(재진입은 알파가 아니라 위험의 모양,
  WAN-90) · 총수익%는 복리 착시(WAN-213).
- 📌 **파급이 예외적으로 작다** — `LeverageBookParams`·`ConfluenceParams` 기본값 불변(재진입은
  어디에도 안 실림). `build_book_rows` 기본값 `include_reentry=False` **그대로**(측정 모듈
  wan169/180/261/264/269/271이 명시 입력이라 무영향). **wan95는 재산출·핀 대상이 아니다** —
  per-cell 리포트라 재진입을 아예 안 돌아 비트 재현. 채택 북 새 정본 수치는 WAN-272 CSV(band).
- **라이브/페이퍼 러너 재진입 배선은 별도**(WAN-45/171 · 실거래는 WAN-241/235 넷팅 선행) ·
  다른 토대 불변 · 실거래 보류 유지(`ALPHABLOCK_LIVE_TRADING=false`).

📌 **손절 갭-체결 민감도(WAN-276 = 사용자 논의 2026-08-09): 채택 북(5배)의 「청산 0건·MDD
~20%」는 불가능한 체결 가정 위 값이라 두 축으로 보수화해 스트레스했다 — 판정: 1분봉 경계
안에서 청산은 여전히 0건, MDD는 α에 단조로 깊어지되 폭이 작다. 기본값 불변.**
[`docs/decisions/wan276.md`](docs/decisions/wan276.md)(재현: `uv run python -m
backtest.wan276_stop_gap_fill --tf 1h --jobs 6`, 요약만 `--from-csv`). 현행 엔진은 손절 발동
봉에서 `exit_price = active_stop`(손절가 그 값)으로 체결한다 — "지정가처럼 정확 + 시장가처럼
무조건 체결"을 공짜로 가정. 두 옵트인 팔로 보수화(끄면 비트 재현): **팔 1** 시장가 슬리피지
`exit = active_stop − α·(active_stop − 봉저가)`(롱, α∈{0,.25,.5,1} · 1=봉 저가=1분 최악) ·
**팔 2** 지정가 미체결(갭 관통 봉 미체결 → 손절가 복귀 봉 체결 · 안 돌아오면 데이터종료 홀드).
- 📌 **엔진 배선은 전부 옵트인**: `simulate_zone_limit_trade(stop_slippage_alpha=0.0,
  limit_stop_nonfill=False)` + `ZoneLimitOutcome.exit_extreme`(손절 봉 불리 극값, 순수 관측) →
  `build_zone_limit_candidates`·`run_cells` 통과. α는 후보의 청산가만 바꾸므로 `exit_extreme`
  로 **사후 변환**(`apply_stop_slippage`)해 재시뮬 없이 얹는다(15m·6년 α마다 재생성 회피).
  회귀 테스트가 α=0 비트 동일·팔 2 갭 미체결·두 인자 동시 거부를 동작으로 고정.
- 📌 **판정(요약 = `wan276_stop_gap_summary.md`)**: 청산 트리거 전 시나리오 **0건**(1h·4h·
  both=1h+4h · α=0~1 · 팔 2 · full·oos_warm·oos) — 「5배 안전」이 아니라 갭 슬리피지가 이
  사이즈(거래당 1% 리스크 · cap_only 명목 상한 · 최대 동시 리스크 5~8%)를 청산 사거리로 못
  민다는 뜻. MDD는 α 단조 심화하되 20%대 안(full 1h 11.23→16.25% · both 10.35→18.55% ·
  oos_warm 1h 7.90→16.25%로 약 2배지만 청산 없음) — cap_only가 거래당 리스크를 1배로 묶어
  손절 손실이 레버리지로 증폭 안 됨(WAN-180). 급락일 20건 손절 평균손실 −4.64→−5.31%(α=1).
- 📌 **팔 2(지정가 미체결)는 전 9개 (스코프×구간) 셀에서 α=0과 비트 동일** — 1분봉 해상도에서
  손절 봉이 손절가 **전체를 갭 관통**하는 일이 사실상 없다(가격이 어느 1분봉 안에서 손절가를
  스치며 정상 체결). ⚠️ 「지정가 손절이 항상 체결」이 아니라 **1분봉 해상도 한계**다 — 진짜
  갭(1분 미만·거래정지)은 틱·호가(WAN-98) 소관이고, **15m으로 내려도 체결은 같은 1분봉이라 이
  축은 안 갈린다**(15m `--append`는 팔 1 α·채택 both=15m+1h 좌표용이지 팔 2 때문이 아니다).
- ⚠️ **1분봉 경계이지 진값이 아니다** — α=1도 1분 해상도 상한 · 호가 깊이는 미측정(WAN-98
  틱·호가, Canceled 소관). **진입 쪽 낙관(WAN-96)은 별개 축**(이 표는 청산만) · 전부
  `baseline` 위 값 · **「엣지 없음」(WAN-84/88/111/114/124/151/201/248) 불변**(위험의 모양,
  WAN-90). **측정 전용 · 기본값·토대·손절폭 가드·`LeverageBookParams()` 불변**(실거래 보류
  유지). 실거래 손절 주문 종류(시장가/지정가)는 집행 결정(WAN-241) · 채택은 사용자 결정.

🚨 **위 WAN-365 시점 배너가 이 문단에도 적용된다** — 아래 수치는 「소급 취소」
엔진의 값이다([`docs/decisions/wan365.md`](docs/decisions/wan365.md)).

📌 **손실을 R 배수로 못 박는 스트레스(WAN-312 = 사용자 요청 2026-08-16 「손절 밀리는 걸
1.5R까지 본다면」): 청산은 k=2까지도 0건인데 그 열로 안전을 읽으면 안 된다 — 「청산 0건」은
「살아남는다」가 아니라 「한 방에 죽지 않는다」다. 기본값 불변.**
[`docs/decisions/wan312.md`](docs/decisions/wan312.md) · 🔁 **채택 좌표 완성은
[`wan316.md`](docs/decisions/wan316.md)**(재현: `uv run python -m
backtest.wan312_stop_r_multiple --tf 15m,1h,2h,4h --jobs 4 --skip-engine-check`, 요약만
`--from-csv`). WAN-276/277의 α 축은 **1.5R을 표현할 수 없다**(α=1이 그 1분봉이 실제로 간
저가라 봉이 1.2R까지만 갔으면 그게 천장) — 그래서 자를 **R 배수 k ∈ {1.0, 1.25, 1.5, 2.0}**
로 바꾸고 두 변형을 함께 냈다: **`무조건`**(`exit = 진입가 − k·R`, 1분봉이 **못 보는 갭까지**
덮는 **가정**) · **`봉극값`**(`max(진입가 − k·R, 봉저가)`, 1분봉이 **실제로 본 데까지만** —
k가 크면 α=1로 수렴하는 α 축의 다리). **두 팔의 차이가 곧 「1분봉이 못 보는 몫」**이다.
좌표는 오늘 채택 그대로(12종목 × 4h·2h·1h + `both` · 재진입 ON(band) · cap_only 5배 ·
유동성 한도 채택값 · **핀 하나도 없음**, WAN-305).
- 📌 **§2 「실효 동시 리스크」 열 신설이 주 산출물**(`BookStats.max_effective_concurrent_risk
  _ratio` · `run_leverage_book(stress_risk_multiple=k)`, 둘 다 옵트인 · k=1.0이면 비트 재현).
  기존 `max_concurrent_risk`는 **주문 낼 때 계획한** 합이라 손절 체결 보수화에 **설계상
  불변**인데(α가 이 열을 못 움직인 이유) **채택 북이 5배인 근거가 바로 그 열**이었다.
  실측: `both`·oos_warm **계획 11.76% → k=1.5 실효 17.66%**(이슈가 예측한 「계획 11%는 실제
  17%」 그대로). 📌 **계획값 자체가 이미 5%가 아니다** — 실측 최대 동시 **14칸**이라 「거래당
  1% × 5칸 = 5%」라는 읽기가 성립하지 않는다.
- 🚨 **청산 0건인데 자본은 −98%가 된다** — `both`·oos_warm MDD가 k=1 19.58% → k=1.5 41.45%
  → **k=2 98.21%**(총수익 −98.06%)인데 **청산 트리거는 전 셀 0건**이다. 청산 검사는 **한
  시점에 열린 포지션이 전부 동시에** 손절날 때만 걸리는데 사이징이 **현재 자본의 %** 라 손실이
  쌓이면 포지션도 함께 작아진다 — **연쇄 손실로는 그 조건이 구조적으로 안 걸린다**.
  WAN-169/213/276/277이 「청산 0건」을 판정 열로 써 온 자리의 **한계가 여기서 처음 명시**된다
  (요약이 `RUIN_MDD=50%` 선을 함께 찍는다).
- 📌 **그 파괴는 `무조건` 팔에서만 일어난다** — `봉극값` 팔은 k=2에서도 `both` oos_warm MDD
  **22.80%**(k=1 대비 +3.2%p)다. **1분봉이 실제로 본 범위 안에서는 이 사이즈가 버티고,
  무너지는 것은 그 너머를 가정했을 때다**(두 팔 MDD 차 k=2에서 평균 60.3%p). ⚠️ **`무조건`
  팔은 측정이 아니라 가정**이고 진값은 틱·호가(WAN-98, Canceled) 소관이다.
- 📌 **검산 둘**: (a) k=1.00이 **인자 없는 채택 북**(`book_cli.build_book_rows` = `backtest.run
  --oos-warm`)과 **최대 절대차 0.00e+00** · (b) `--legacy-wan277`로 wan277 `alpha_0.00`을
  **6.94e-17(비트 일치)** 재현. 🚨 **단 되돌려야 할 축이 넷이었다** — 좌표 셋(9종목·펀딩
  대리·유동성 한도 끔)만으로는 3.74e-02가 남았고(거래 수·승률·동시 리스크는 비트 일치인데
  수익률만 어긋남 = 비용 축), 원인은 **데이터가 그 사이 바뀐 것**이었다: WAN-277 실행 시점엔
  DOGE·LINK·LTC 펀딩이 0행이라 BTC 대리가 걸렸는데 **WAN-292 백필(2026-08-12) 뒤엔 실데이터가
  있어 대리가 무동작**이다. 📌 **함의 — WAN-277 계열 수치는 이제 코드만으로 재현되지 않는다**
  (입력 데이터가 그 형태로 존재하지 않는다). **핀은 파라미터를 고정하지 데이터 상태를 고정하지
  못한다** · 대리의 「데이터가 생기면 저절로 꺼진다」는 안전 설계가 **동시에 옛 표의 재현성을
  끊는다**. 재현 전용 복원기가 `restore_pre_backfill_funding`(검산 외 사용 금지)이다.
  leave-one-out 416행 전부 청산 0건이고
  k=2 MDD가 94.12%(`-new3`)~98.38%(`-ETH`)라 **종목·유니버스 확장이 만든 결과가 아니다**.
- ⚠️ **`both`에서는 거래 「수」가 k에 미세하게 움직인다**(3TF 판 6,134 → 6,087 · 4TF 판은
  19,018 → 18,656으로 **−1.90%**, 최대 동시 칸도 17 → 13) — 후보는 그대로지만 손실이 커지면
  공유 자본이 줄어 몇몇 셋업이 명목 상한·사이징에서 밀린다(단일 TF는 거래 수·승률이 k에
  완전 불변).
- 🔁 **위 `both` 수치는 전부 「15m 없는 3TF 지갑」이다 — 채택 좌표(4TF)는 WAN-316이 냈다**
  (아래 📌 문단이 정본). 새 격자에서 그 3TF 지갑은 **`both_no15m` 라벨로 비트 단위 보존**된다.
- ⚠️ **손절선을 1.5R로 넓히는 안이 아니다**(후보 집합·진입가·손절가 불변, 진 거래의 손실
  크기만) · 총수익%는 복리 착시(WAN-169/213) · 6년 MDD는 폭락 미포함 **바닥선** · 진입 쪽
  낙관(WAN-96)은 별개 축 · **「엣지 없음」(WAN-84/88/111/114/124/151/201/248) 불변**(위험의
  모양, WAN-90). **측정 전용 · 기본값·토대·손절폭 가드·`LeverageBookParams()` 불변** ·
  배수·손절선 변경은 **재-베이스라인 = 사용자 결정**(실거래 보류 유지).

🚨 **위 WAN-365 시점 배너가 이 문단에도 적용된다** — 아래 수치는 「소급 취소」
엔진의 값이다([`docs/decisions/wan365.md`](docs/decisions/wan365.md)).

📌 **채택 좌표(4TF 한 지갑) 완성(WAN-316 = WAN-312 스핀아웃): 15m을 붙여도 판정의 방향은
불변이고 크기만 깊어진다 — 「1분봉이 본 범위 안에서는 버틴다」가 15m에서도 선다. 기본값 불변.**
[`docs/decisions/wan316.md`](docs/decisions/wan316.md). 🚨 **북은 이어붙일 수 없다** — 칸을
**한 프로세스의 공통 시간축·한 지갑**으로 묶으므로 15m `--append`는 단일 TF 행만 늘린다.
그래서 네 TF를 한 실행으로 돌렸고, WAN-312가 낸 3TF 지갑은 **덮지 않고 `both_no15m` 라벨로
보존**했다(같은 실행·같은 payload라 한 표에서 비교된다).
- 📌 **검산 (a)가 이 이슈에서 처음 뜻을 갖는다** — 4TF `both`의 k=1.00이 **인자 없는 채택 북**
  (`backtest.run --oos-warm`)과 **0.00e+00**. 3TF 판에서는 `both`가 채택 좌표가 아니라 이
  등식이 성립할 수 없었다. 📌 **검산 (c) 신설**(`--check-legacy-grid`): 단일 TF(1h·2h·4h) +
  새 `both_no15m`이 옛 격자와 **격자 96행 · LOO 416행 최대차 0.00e+00** — 「보존」이 라벨이
  아니라 **같은 숫자**이고, 15m 칸이 다른 스코프로 새지 않았다는 증거이기도 하다.
- 📌 **15m이 가장 민감하다(이슈 가설 확인)** — `봉극값` k=2 oos_warm MDD가 15m **31.58%** 로
  1h(17.27%)의 1.8배 · 4h(11.22%)의 2.8배이고, `무조건` 팔은 **15m만 k=1.5에 이미 66.97%** 로
  50% 선을 넘는다(1h 39.90% · 4h 17.10%). 손절폭이 좁아 봉-내 갭의 R 비중이 크다는 예상 그대로다.
  🚨 **그래도 결론은 안 뒤집힌다** — 15m `봉극값` 최악이 32.47%(oos)로 파괴선 아래다.
- 📌 **4TF 지갑은 3TF보다 뚜렷이 깊다**(oos_warm): 거래 2,132 → **6,336** · MDD k=1 19.58% →
  **22.90%** · **k=1.5 `무조건` 41.45% → 84.43%(+42.98%p)** · k=2 `봉극값` 22.80% → **37.00%**.
  WAN-312의 「크기는 15m이 붙으면 달라진다」가 맞았다.
- 🚨 **계획 대 실효 동시 리스크는 이슈 예상이 절반만 맞았다 — 구간에 갈린다**: `full`은 예상대로
  오르는데(칸 14→17 · 계획 11.66%→**12.82%**) **`oos_warm`은 오히려 내려간다**(11.77%→
  **11.50%** · 실효 17.66%→**17.25%**). 계획 열은 칸 수의 합이 아니라 **특정 시점의 동시 합**
  이고, 15m 칸의 자기 지갑 계획값이 다른 TF보다 **낮다**(oos_warm 15m 4.40% vs 1h~4h 약 6.0%
  — 좁은 손절 → 수량↑ → 명목 상한에 더 자주 걸림, WAN-154 §2의 북 축 재현). ⚠️ **「그래서 4TF가
  더 안전하다」로 읽지 말 것** — 같은 구간에서 낙폭은 더 깊다(= 낙폭을 만드는 것은 한 시점의
  동시 노출이 아니라 **연쇄 손실**이다).
- 📌 **청산은 격자 144행 · leave-one-out 624행 전부 0건**인데 4TF `무조건` k=2는 MDD **100.00%**
  다 — WAN-312 §4-(2)의 한계가 칸을 12개 더 붙여도 그대로다(대체 지표는 WAN-317). LOO는 어느
  종목을 빼도 k=1.5 MDD 70.50%(`-new3`)~88.19%(`-ETH`)라 **유니버스 확장이 만든 결과가 아니다**.
- 📌 **부수 기록 — 15m 셀 비용은 ~37분이 아니라 ~16분이다**(48칸 · `--jobs 4` · 67분 실측).
  ~37분은 **WAN-203의 값**(문턱 스윕 × 매칭 널 20시드)이라 이 모듈에 옮기면 2배 과대 추정이
  된다. **다른 모듈의 셀 비용을 그대로 인용하지 말 것.**
- **측정 전용 · 기본값·토대·손절폭 가드·`LeverageBookParams()` 불변** · 핀 없음(WAN-305) ·
  `무조건` 팔은 가정(진값은 틱·호가 WAN-98, Canceled) · 총수익%는 복리 착시 · 6년 MDD는
  **바닥선** · **「엣지 없음」 불변** · 실거래 보류 유지(`ALPHABLOCK_LIVE_TRADING=false`).

- **진입 방식** = **존 근단 지정가 + 오프셋 2bp**(`entry_mode="zone_limit"`,
  `zone_limit_ref="proximal"`, WAN-95 / `zone_limit_offset_bps=2.0`, **WAN-112** — 존 근단보다
  2bp 체결 쉬운 쪽(롱=위)에 건다. 위 📌 오프셋 문단 참고: 데이터가 아니라 사용자 판단이다).
  존에 지정가를 걸어두고 가격이 닿는 순간 체결한다 — 사용자의 실매매 그대로다.
  체결 판정은 1분봉 서브스텝(`backtest/substep.py`)으로 시뮬레이션하므로 **지정가
  백테스트는 1분봉이 필요하다**. RSI도 체결 순간 봉내 값으로 판정한다
  (`rsi_mode="realtime"`) — 지정가는 봉 중간에 체결되므로 확정봉 RSI를 쓰면 체결
  시점과 판정 시점이 어긋난다. 두 필드는 **한 세트**다.
- **진입가** = 활성(병합) 오더블록 탭 시 **볼린저밴드(SMA 20 ± 2σ)** 로 재산정
  (`deviation_filter` 기본 켜짐). 밴드가 존 위/겹침/아래에 있으면 각각 존 경계 진입/
  밴드가 진입/진입 없음(롱·숏 대칭, `deviation_entry_price` 참고).
  **밴드의 20번째 표본은 「체결 순간의 현재가」다**(`band_bar="intrabar_live"`, **WAN-132**
  — 트레이딩뷰가 형성 중인 봉에 띠를 그리는 방식이자 사용자가 보고 주문을 거는 값). 밴드가
  봉 안에서 계속 움직이므로 **지정가도 매 서브스텝 재산정**된다. ⚠️ 옛 기본값 `"tap"`
  (탭 봉 최종 종가)은 룩어헤드였다 — 아래 📌 WAN-132 문단.
- **진입 조건** = **RSI 게이트 없음 — 첫 탭·재탭 모두 무조건 진입**(워밍업 NaN이어도)
  (`rsi_gate_mode="unconditional"`, `retap_mode="every_tap"`, **WAN-123**). 재탭은 계속
  평가하되 그 탭에 RSI 조건이 걸리지 않는다. 병합 존(`combine_obs=True`)은 구성 존이
  개별적으로 자신의 첫 탭을 셀 수 있다(WAN-81 §5, 구 WAN-82 버그 흡수 — 이미 진입한
  클러스터에 새로 편입된 존도 자기 몫의 첫 탭에서 진입한다).
  ⚠️ **WAN-81의 「첫 탭 면제 + 재탭 RSI 게이트」는 WAN-123이 뺐다**(아래 📌 WAN-123 문단이
  정본) — `rsi_gate_mode="first_tap_free"` 옵트인으로 되돌릴 수 있다. 그 옛 규칙은
  **WAN-100 전까지 채택 경로(B안 지정가)에서 실제로 돌지도 않았다**(아래 「첫 탭 면제
  누락」). 첫 탭 면제는 `tap_index`를 아는 **호출부의 책임**이고(`rsi_gate_passes`는 모드만
  보고 첫 탭인지 알 수 없다), B안이 그 배선을 빠뜨려 첫 탭에도 재탭 게이트가 걸렸다.
  ⚠️ **새 진입 경로를 붙이면 두 배선을 함께 한다**: `tap_index` 전달 + `unconditional`
  면제. 후자를 빠뜨리면 게이트를 뺀 기본값에서도 조용히 RSI가 걸린다.
- **존폭 필터 = 꺼짐**(`max_zone_width_atr=None`, **WAN-384** — 사용자 결정 2026-08-27
  「끄자」, WAN-159의 `1.28` 폐지). **존의 두께로는 거르지 않는다.** 필터 자체는 옵트인으로
  존치한다(`--max-zone-width-atr 1.28`, ATR은 탭 봉 **직전 확정봉** · 거르는 지점은 지정가
  확정 이후 · 단위는 **ATR 배수**지 퍼센트가 아니다 · 지정가(B안) 전용이라 종가 진입(A안)과
  양수 문턱을 같이 주면 거부). 🚨 **옛 리포트 핀의 방향이 두 세대다** — WAN-158 이전 판은
  `harness.LEGACY_MAX_ZONE_WIDTH_ATR`(= `None`, 지금은 무동작)이고 **WAN-159~383에 필터를 켠
  채 결론을 낸 판은 `harness.LEGACY_ZONE_WIDTH_FILTER_ON`(= `1.28`)**이다. 근거·경고는
  아래 📌 WAN-384 문단.
- **숏 비활성화**(`short_enabled=False`, WAN-87 — [WAN-86 결정 1](docs/decisions/wan86.md)에
  따라 WAN-81의 숏 활성화를 사용자가 데이터에 근거해 다시 번복). WAN-84(PR #64) OOS
  롱/숏 분해에서 롱 +3.07%(9셀) vs 숏 −3.82%(7셀)로 숏이 롱 수익을 상쇄함을 확인했다.
  숏 경로 코드는 삭제되지 않았으므로 `short_enabled=True`로 재검증 여지를 남겨둔다.
  ⚠️ **단, 숏 제거가 "엣지"를 만들지는 않았다(WAN-88)** — 아래 「엣지 판정」 참고.
  🔁 **그리고 그 −3.82%는 재인용 금지 — WAN-89가 현행 엔진에서 부호를 뒤집었다**(아래 📌
  WAN-89 문단이 정본). **기본값은 그대로 `False`이고**(전환은 사용자 결정), 다만 **비활성화의
  근거가 소멸했다**: 그 표의 숏은 **모든 탭에 `RSI ≥ 70`을 요구**했다(WAN-100 이전 B안 버그).
- **익절** = **고정 1:1.5R**(`take_profit_mode="fixed_r"`, `take_profit_r=1.5`, **WAN-90
  검증·유지** — 위 📌 문단). 1R = 진입가 → 진입 근거 오더블록 무효화 경계까지 거리. 볼린저로
  진입가가 유리해지면(롱=더 낮게) 1R이 줄어 익절 목표도 그만큼 가까워진다. **EMA·VWMA는
  익절 판정에서 완전히 빠졌다**(`use_line_take_profit=False`) — 옛 선 도달 익절은
  `take_profit_mode="line"`으로 켤 수 있지만 기본이 아니다.
- **익절 비용 = 메이커 2bp · 슬리피지 0**(`BacktestConfig.take_profit_liquidity`, **WAN-370**
  — 아래 📌 문단). 부분 익절(래더)도 같다. 손절·만료는 테이커 4bp＋5bp **불변**. 🚨 **체결
  판정은 안 바뀐다** — 「닿으면 체결」 그대로라 **수수료는 지정가인데 체결은 낙관**이다.
  실측(`oos_warm` 채택 북): 거래당 net R **−0.1798 → −0.1309**(+0.0489R) · **부호 불변**.
- **손절** = 진입 근거 오더블록의 무효화(breaker). 이 규칙은 변경 없음.
- **무효화 취소는 봉 마감에 발효한다 — 소급 취소하지 않는다**(`invalidation_cancel="bar_close"`,
  **WAN-365** — 아래 📌 문단). 존이 깨지는 봉의 대기 지정가를 봉 **시작**으로 되돌려 취소하지
  않고, 그 봉 안의 체결은 살려 **손절 규칙**이 결과를 내게 한다. `--invalidation-cancel bar_open`
  = WAN-365 이전 동작(옛 CSV 비트 재현, **미지정과 다르다**) · 옛 리포트는
  `harness.LEGACY_INVALIDATION_CANCEL`로 명시 고정돼 있다. ⚠️ **봉이 닫힌 뒤에는 두 값 모두
  취소한다** — 「취소를 끄는」 규칙이 아니다.
- **포지션 회계** = **레버리지 북(cap_only 5배)**(`LeverageBookParams()` 기본값, **WAN-213**
  — 위 📌 문단). 칸=(종목,TF)마다 1포지션 · 여러 칸 동시 · 한 지갑 공유. 인자 없는
  `backtest.run`이 이 북을 돈다(`--positions single`으로 옛 동시 1포지션 per-cell 경로).
  ⚠️ 옛 「동시 1포지션」은 `--positions single`로 존치 — 옛 리포트 대조용.
- **재진입 = 켜짐, band 규칙**(채택 북이 「익절 후 존 내 재진입」을 봉내 라이브 밴드로 재무장,
  **WAN-273** — 위 📌 문단). 인자 없는 `backtest.run`(채택 북)이 재진입을 돈다. `--reentry off`
  = WAN-273 이전 재진입-off 북(옛 CSV 비트 재현, 미지정과 다르다) · `freeze`/`zone` 옵트인
  존치. per-cell(`--positions single`/숫자)과는 함께 못 쓴다. 📌 **페이퍼 러너도 같은 규칙으로
  재진입한다(WAN-274)** — `live.zone_limit_runner`가 익절 청산 후 같은 존에 band 지정가를 다시
  걸고(엔진 `on_position_exit`, 무기한 대기), 백테스트 채택 북과 같은 규칙을 물려받는다(러너가
  자기 버전을 만들지 않음). 설정 `live_reentry_entry_rule` 기본 `"band"` · `"off"`로 옵트아웃 ·
  `"freeze"`/`"zone"`은 라이브 미배선(거부). ⚠️ **실거래는 여전히 별도 게이트**(경로 자체 없음 —
  WAN-241/235 넷팅 선행) · `ALPHABLOCK_LIVE_TRADING=false` 유지.
- **존 병합 없음 — 원본 존 단위로 탐지·진입·손절한다**(`combine_obs=False`, **WAN-149**
  사용자 결정 2026-07-21). 겹치는 오더블록을 하나로 접지 않는다. ⚠️ 옛 기본값 `True`는
  옵트인으로 **존치**한다 — 아래 📌 WAN-149 문단.
- **재탭 경로도 병합 존 경계를 쓴다**(WAN-81 갭B) — `retap_mode="every_tap"`을 켜도
  `combine_obs`의 병합 존 기준이 유지된다(과거엔 재탭 경로가 병합을 무시하고 원본
  존 단위로 되돌아가는 버그가 있었다). ⚠️ **WAN-149 이후 이 문장은 옵트인 경로
  (`combine_obs=True`)에만 해당한다** — 채택 기본값에는 병합 존이 없다.

⚠️ **볼린저 진입가 × 지정가의 채택 규칙(WAN-95)**: 둘 다 켠 조합에서 **볼린저가
이긴다** — 지정가를 존 근단이 아니라 **볼린저가 재산정한 가격에 건다**. 코드 근거:
`backtest/zone_limit_backtest.py`의 `build_zone_limit_candidates`가
`limit_price = params.zone_limit_price(ob)`로 존 근단을 잡은 **직후**
`deviation_filter`가 켜져 있으면 `limit_price = deviation_entry_price(...)`로
덮어쓴다(밴드가 존보다 불리하면 그 셋업은 진입 자체를 건너뛴다 — WAN-75 규칙 3).
즉 "사전 지정가 vs 사후 재산정"의 모순은 없다: 밴드 값은 **탭 봉 시점에 확정**되고
그 가격으로 주문이 걸린 것으로 취급한다. 이 조합은 WAN-95 이전에도 기본값은 아니었지만
**미검증 경로는 아니다** — WAN-70/73/76/84 검정이 모두 `zone_limit + realtime` +
기본 `deviation_filter`로 돌았다.
그 대가로 남는 알려진 한계: 밴드는 탭 봉의 SMA20(= 그 봉 종가 포함)으로 계산되는데
체결은 그 봉 **내부**에서 일어날 수 있다(같은 봉의 종가를 알아야 계산되는 가격에
그 봉 도중 체결). 엄밀히 룩어헤드다 — WAN-95는 이 성질을 **바꾸지 않고 기록만 했다**
(바꾸면 WAN-70/84 검정의 엔진 정의가 흔들리므로 별도 이슈에서 판단한다).
🔁 **WAN-95의 「실무 영향은 SMA20 한 틱 차이 수준이라 작다」는 측정으로 반박됐다 —
재인용 금지**(WAN-115/119, 위 📌 두 문단이 정본). 전제는 먼저 WAN-114가 흔들었고
(볼린저가 여러 부품 중 하나가 아니라 **유일한 플러스 부품**이었다), WAN-115가 밴드 봉 기준을
바꾸자 **15m OOS 증분이 −4.88%p 움직이며 채택 기본값의 부호까지 뒤집혔다**(+3.91% → −0.97%).
각주 크기가 아니다.
⚠️ **단 그 −4.88%p를 「룩어헤드의 몫」이라 부르지 말 것 — WAN-119가 개정했다.** 미래 정보만
빼고 현재 봉은 쓰는 `intrabar_live`는 오히려 **+0.50%p 번다**(15m OOS +4.41%). 즉 룩어헤드를
빼는 것 자체는 **비용이 0에 가깝고**, `prev_closed`가 잃은 건 **현재 봉의 실재하는 정보**였다.
🔁 **그리고 WAN-132가 그 「별도 이슈에서 판단한다」를 닫았다 — 기본값이 `intrabar_live`다**
(아래 📌 문단이 정본). 즉 **위 문단이 기록한 룩어헤드는 채택 경로에서 사라졌다**.
**밴드 정의는 네 가지가 존재한다**(`DeviationFilterParams.band_bar`: `"intrabar_live"`
**기본값** = WAN-119/132(미래 없음 · 현재 봉 씀 = 트레이딩뷰/실매매) · `"tap"` = WAN-131까지의
기본값(룩어헤드) · `"prev_closed"` = WAN-115(미래 없음 + 현재 봉도 버림) ·
`"intrabar_causal"` = WAN-120(직전 1분봉 종가 = 잔여 1분마저 0)). 옛 수치를 결론에 박아 둔
리포트는 `harness.LEGACY_BAND_BAR`(= `"tap"`)로 **명시 고정**돼 있다.
⚠️ **`intrabar_causal`는 정본 후보가 아니라 감사 도구다** — B안(지정가) 전용이고 **A안은
ValueError로 거부한다**(봉 단위에 직전 1분봉 종가가 없다). 정본으로 올리면 대시보드·
`live.runner`·스윕이 깨진다(WAN-120 §4).

**지정가 확정 순서(WAN-99)**: `zone_limit_price(ob)`(존 근단) → `deviation_entry_price`
(볼린저가 이김, 기각 시 진입 없음) → `apply_zone_limit_offset`(체결 오프셋). 오프셋은
**맨 마지막**에 얹히므로 볼린저 재산정 가격 위에 적용되고, 볼린저가 기각한 셋업을
되살리지 않는다. 그래서 `apply_zone_limit_offset`은 `zone_limit_price`와 **일부러
분리돼 있다** — 한 메서드로 합치면 존 근단 계산 시점(볼린저 이전)에 오프셋이 묶여
순서가 강제되지 않는다. 여기서 확정된 가격이 1R(진입가→무효화 경계)과 고정 R 익절
목표의 기준이다.

⚠️ **차트 표시선 ≠ 익절선**: 대시보드는 EMA 20/60/120/240/365를 그리지만
(`ConfluenceParams.display_ema_lengths`) `take_profit_mode="line"`으로 켤 때만 쓰는
익절 판정선은 EMA 60 + VWMA 100 뿐이다(`tp_ema_lengths=(60,)`, `tp_vwma_length=100`,
WAN-66). WAN-23 명세가 두 선 집합을 한 배열로 뒤섞어 적어 EMA 20에서 조기 익절하던
버그(WAN-66)를 낳았으니, 표시선과 익절선을 절대 다시 한 필드로 합치지 말 것.

⚠️ **기존 성과 수치 무효**: WAN-19/22/46/50/58/68/70/71/73/74/75/76 등의 백테스트
결과는 모두 WAN-81 이전 구 엔진 기준이다. WAN-81/WAN-84 리포트(숏 활성화 전제)도
WAN-87 이후에는 **채택 기본값과 다른 설정**이므로 현재 기본값 성과로는 무효다 —
숏 활성화 자체의 검증 기록으로만 유효하다.

**WAN-95가 여기에 한 겹을 더 얹는다**: `wan87_long_only_summary.md`와 WAN-91 펀딩
리포트도 **더 이상 채택 기본값 성과가 아니다** — 둘 다 `entry_mode="close"`(탭 봉 종가
시장가)로 산출됐는데, 사용자의 실매매이자 새 기본값은 **존 지정가**다. 즉 그 표들은
"사용자가 하지 않는 매매"의 손익이다. 두 리포트 모듈은 재현을 위해 당시 엔진을
명시적으로 고정해 뒀다(`SHORT_DISABLED_PARAMS`/`ENGINE_PRESETS`에
`entry_mode="close"` 고정). 채택 기본값(지정가 + 메이커 + 롱 온리 + 펀딩) 재산출과
종가 대비 대조표·체결률은 `backtest/reports/wan95_zone_limit_summary.md`(재현:
`python -m backtest.wan95_zone_limit_report`) 참고.

⚠️ **펀딩비 배선(WAN-91)**: `default_backtest_config`(`backtest/sweep.py`)가
`settings.backtest_funding_enabled`(기본 `True`)을 `BacktestConfig.funding_enabled`에
싣는다(`risk_sizing`과 동일 패턴). 이 플래그만으로는 손익이 바뀌지 않는다 — 호출부가
`data.FundingRateStore.get_rates(symbol, ...)`로 조회한 `funding_rates`를
`evaluate()`/`run_backtest()`에 **별도로** 넘겨야 실제 펀딩비가 반영된다. 위
WAN-19~87 리포트들은 전부 이 배선 이전(또는 배선 이후에도 `funding_rates`를 넘기지
않은 채로) 산출됐으므로 펀딩비 미반영 상태다(**WAN-88이 매칭 널 경로에 이 배선을 뚫었다** —
`run_random_control_b_segment(funding_rates=...)`가 실제·널 양쪽에 동일하게 펀딩을 붙인다.
안 넘기면 기본값이 그대로라 WAN-70/84 CSV는 재현된다) — 크립토 무기한선물은 보통 롱이 펀딩을
지불하므로 롱 온리 채택 기본값(WAN-87) 수익률은 실제보다 소폭 부풀려져 있을 수
있다. `backtest/reports/wan91_funding_cost_summary.md`(재현: `python -m
backtest.wan91_funding_cost_report`)의 펀딩 on/off 델타 재산출로는 그 영향이
대체로 total_return 기준 ±0.1~2%p 수준으로 작아, 위 문단의 무효 판정 자체를
뒤집지는 않는다 — 단, 15m은 이 리포트의 그로스/넷 분해로 "비용(수수료·슬리피지)이
신호를 잠식"함이 원칙적으로 확인돼 채택 대상에서 제외를 권고한다(과최적화가
아니라 비용 근거). 1h는 비용 가정에 민감하니 모니터링 대상으로 남긴다.
**단, 그 15m 제외 권고의 근거였던 비용은 종가 진입(테이커) 기준이라 WAN-95가
뒤집었다** — 지정가 채택으로 왕복 비용이 0.18%→0.11%로 줄자, 15m은 3심볼 전부
마이너스(평균 −19.3%, MDD 38.9%)에서 **3심볼 전부 플러스**(평균 **+82.3%**, MDD 15.2%)로
반전됐다(`wan95_zone_limit_summary.md`, **WAN-100 교정 엔진 기준 재산출값**).
"15m의 문제는 신호가 아니라 비용"이라는
WAN-91의 진단은 옳았고, 비용을 실매매(메이커)에 맞추자 결론이 반대가 된 것이다.
그렇다고 **15m 채택이 확정된 것은 아니다** — 서브스텝 시뮬레이터는 "지정가에 닿으면
체결"로 보므로(큐 우선순위 미모델링) 체결률(15m 약 50%)이 낙관적이고, 승률은 오히려
55.2%→51.7%로 떨어졌다. 15m 판단은 체결 가정을 보수화한 재검정 후에 내린다.
**→ WAN-96이 그 재검정을 수행해 「15m 제외」로 결론냈고**(아래), **WAN-99가 지정가
오프셋(체결을 사는 반대편 수단)으로도 그 결론이 뒤집히지 않음을 확인했다**(아래).
⚠️ 단 WAN-96/99의 그 수치들은 **WAN-100 교정 전** 엔진 산출이다 — 아래 「첫 탭 면제
누락」 참고. WAN-97이 교정 엔진으로 재판정해 **제외 결론은 유지하되 근거를 통째로 갈았고**
(WAN-96의 −5.8%·0/3은 소멸, 관통 민감도·OOS 심볼 편중이 새 근거),
🔁 **WAN-107이 공식 렌즈(`baseline`)에서 그 새 근거마저 기각해 「15m 제외」를 번복했다**
([`docs/decisions/wan107.md`](docs/decisions/wan107.md)). **지금 정본은 15m·1h 공동 작업 TF**
(위 📌 TF 문단) — 이 문단의 "15m 채택이 확정된 것은 아니다"는 **당시 서술**이다.

⚠️ **채택 경로에서 「첫 탭 면제」가 통째로 빠져 있었다 — 교정(WAN-100)**:
`backtest/reports/wan100_first_tap_free_summary.md`(재현: `python -m
backtest.wan95_zone_limit_report`). WAN-81이 정한 「첫 탭은 RSI 무관 무조건 진입」은
**A안(`ConfluenceStrategy._evaluate_entry`)에만 구현돼 있었고, 채택 기본 진입 경로인
B안(지정가)은 `tap_index`를 아예 읽지 않았다.** `rsi_gate_passes`는 첫 탭 면제를 스스로
하지 않는다(모드만 보고는 첫 탭인지 알 수 없다 — 면제는 호출부 책임이라는 계약인데
B안이 그 계약을 안 지켰다). 그 결과 B안은 **첫 탭에도 재탭용 극단 게이트**(롱
`RSI<=30`)를 걸었고, `live_rsi is not None` 조건 때문에 **워밍업 구간 첫 탭은 아예
막혔다** — 명세와 정반대다. 즉 **WAN-95/96/99의 모든 지정가 수치는 문서에 적힌 규칙이
아니라 그보다 빡빡한 규칙으로 산출됐다.**

교정 후 채택 기본값의 이동 폭이 크다(`wan95_zone_limit_summary.md` 재산출):
**체결률 약 29% → 약 50%**, 15m 평균 수익률 +15.8% → **+82.3%**, 1h +9.1% → **+35.8%**.
거래 수는 15m 기준 428→658(BTC)처럼 1.5배 안팎으로 는다. 규칙(정본)에 코드를 맞춘
것이고 그 반대가 아니다 — 사용자 확정 사항이다.

⚠️ **따라서 WAN-96/99 표는 교정 전 엔진 기준이고, 그 결론은 흔들린다.** 두 요약 md
헤더에 경고를 명시했다. WAN-96의 「15m 제외」는 **최악 가정(관통 5bp + 50% 탈락)에서
15m이 −5.8%(0/3)로 무너진다**는 근거였는데, 교정 엔진에서 그 축만 다시 돌리면
**15m +3.57%(3/3), 1h +8.34%(3/3)로 부호가 뒤집힌다** — 즉 제외·보류의 근거가 성립하지
않는다(`wan100_first_tap_free_summary.md` §4). **그렇다고 15m 채택은 아니다**: 15m은
시드 분산이 커(BTC −7.7%~+17.4%) 평균 +3.57%에 개별 마이너스 시드가 섞여 있고,
"수익이 스치듯 닿은 체결에 몰린다"는 WAN-96의 **진단 자체는 교정 후에도 살아 있다**
(`pen_5bp`만 걸어도 15m 수익의 63%가 사라진다: +82.3%→+30.6%, 거래는 6%만 감소).
WAN-97이 IS/OOS 축까지 포함해 재판정했고([`docs/decisions/wan97.md`](docs/decisions/wan97.md),
원본 `wan97_tf_fill_grid.csv`·`wan97_tf_fill_oos.csv`), 결론은 **당시 공식 기준선
`pen_5bp` · 15m/1d 제외 · 1h 작업 TF · 4h 보류**였다. ⚠️ **그 두 축은 이후 모두 번복·개정됐다**
— 렌즈는 WAN-104가 `baseline`으로 번복했고, **「15m 제외」는 WAN-107이 번복해 15m을 1h와
함께 작업 TF로 되돌렸다**(위 📌 TF 문단이 정본). 즉 **이 문단의 「15m 채택은 아니다」는
더 이상 현재 규칙이 아니다** — 당시 서술로만 읽을 것. WAN-99 CSV는 회귀 테스트가 고정한
1셀만 교정 엔진으로 갱신돼 **엔진이 섞여 있으니 셀 간 비교 금지**.

⚠️ **체결 가정 보수화 결과 — 15m 제외 권고(WAN-96, 🔁 폐기됨)**: `backtest/reports/
wan96_fill_conservatism_summary.md`(재현: `python -m backtest.archive.wan96_fill_conservatism_report`).
**아래 수치는 전부 WAN-100 교정 전 엔진 기준이고, 그 권고(15m 제외)는 이제 결론까지
번복됐다** — 근거는 WAN-100 교정에서 소멸했고(아래), 결론은 WAN-107이 뒤집었다(위 📌 TF
문단이 정본). **이 문단은 당시 기록이며 인용 대상이 아니다.**
WAN-95의 15m 반전은 **"닿으면 체결"이라는 낙관 가정에 의존**한다. 체결 규칙을 보수화하면
15m 평균 수익률이 +15.8%(3/3 플러스) → **−5.8%(0/3 플러스)**(관통 5bp + 50% 탈락)로
무너진다. 1h도 같은 최악 가정에서 +9.1% → −1.7%(0/3)다.

핵심 근거는 거래 수와 수익의 비대칭이다: **`pen_5bp`는 거래를 4.7%만 줄이는데
(461→439) 수익은 15.8%→1.3%로 사라진다**. 즉 수익이 **지정가를 스치듯 닿고 되돌아간
체결**(관통 없는 체결)에 몰려 있는데, 그 체결이야말로 실거래에서 큐 우선순위 때문에
**가장 안 될 가능성이 높은** 체결이다. 반면 체결 편향 진단은 체결군이 미체결군보다
**나쁘다**고 나온다(15m −0.18R) — 모순이 아니다. 그 진단은 공통 가상 진입가(탭 봉
종가)로 셋업의 방향성만 재므로 지정가의 가격 이점을 뺀 값이다. 둘을 합치면: 지정가
진입의 수익은 **셋업이 좋아서가 아니라 더 좋은 가격에 들어가서** 나오고, 그 가격은
체결 가정을 조금만 현실화하면 사라진다.

보수화 옵션은 **파라미터로만 존재하고 기본값은 바꾸지 않았다**
(`fill_penetration_bps=0.0`, `fill_dropout_rate=0.0`, `fill_dropout_seed=0`) — WAN-95
결과가 그대로 재현돼야 비교가 성립하기 때문이다. 탈락률 0이면 난수를 뽑지 않으므로
기본 실행은 WAN-95와 비트 단위로 동일하다. 이슈의 (b) 큐 모델(거래량 대비 체결 확률·
부분 체결)은 1분봉 거래량으로 큐 위치를 복원할 수 없어 **구현하지 않았다** — 틱·호가
데이터가 필요하다.

📌 **오프셋 2bp 채택(`zone_limit_offset_bps=2.0`) — WAN-112가 WAN-104 결정 2를 번복**:
[`docs/decisions/wan112.md`](docs/decisions/wan112.md). 사용자 결정("baseline에 offset 2bp").
공식 렌즈는 `baseline` 그대로 유지하고, **체결 신뢰성 보험으로 2bp를 고정**한다.

⚠️ **「데이터가 2bp를 골랐다」로 인용 금지 — 정반대다.** 순수 수익 최대화로는 **0bp가 이기고**
(IS 심볼평균 +16.22% vs +15.79%, **−0.43%p**), 2bp를 원한 심볼은 **SOL 하나뿐**이다. 2bp는 그
0.43%p를 **체결 신뢰성과 맞바꾸는 사용자 판단**이다 — 실거래에선 큐 우선순위 때문에 "스치듯
닿은 체결"이 가장 안 될 가능성이 높은데 하필 수익이 거기 몰려 있고(WAN-96 비대칭), 2bp가 그
체결을 관통 체결로 바꾼다. 실제로 체결률이 오르지만 **폭은 작다**(IS 49.16%→49.78%, OOS
47.53%→48.48%). ⚠️ **OOS에서 2bp가 이기는 것(+8.53% vs +8.18%)을 근거로 쓰지 말 것** —
노이즈와 구분되지 않고, **OOS는 선택 축이 아니다**. ⚠️ 이 대조는 **1h 전용**이다(WAN-104 격자가
1h만 돌았다) — 15m 축의 교환비는 아직 안 쟀다.

⚠️ **단위 함정: 2bp는 `2.0`이지 `0.0002`가 아니다**(필드가 bp 단위, `/10_000`으로 나눈다).
WAN-112 이슈 본문의 "0.0 → 0.0002"는 분수 표기이고, 그대로 넣으면 **사실상 무효과**라 결정을
반영했다고 믿으면서 0bp를 돌게 된다(WAN-91/95와 같은 부류의 조용한 실패). 회귀 테스트가 막는다.

⚠️ **`build_params`/CLI 격자의 `offset_bps=None`은 "채택 기본값에 맡긴다", `0.0`은 "0bp를
달라"다** — 가르지 않으면 CLI 기본 실행만 혼자 옛 엔진(0bp)을 돈다. 단 **종가 진입이 섞인
격자는 기본이 0bp**다(A안은 오프셋을 안 읽으므로 두 팔을 맞춰야 진입 방식이 격리된다).

📎 **아래는 WAN-104의 원 분석 — 「데이터의 답」으로는 전부 유효하고 「현재 기본값」으로만
무효다**(원본: `wan104_offset_oos_grid.csv`, `wan104_offset_incremental_summary.md`).
WAN-97 §3-3이 "증거 부족"으로 넘긴 판정을 IS/OOS + 증분 분해로 확정했던 내용이다.
**근거는 WAN-97·WAN-99와 모두 다르니 옛 근거를 재인용하지 말 것**:

- **공식 렌즈(`baseline`)에서 IS가 아예 0bp를 고른다**(IS: 0bp +16.22% > 2bp +15.79% >
  5bp +11.90% > 10bp +7.47%). OOS 검증까지 갈 것도 없이 **선택 단계에서 탈락**한다.
- **오프셋을 원하는 심볼은 SOL 하나뿐**이다(IS 2bp 선호). BTC·ETH는 0bp를 선호하고,
  SOL이 절대 수익률이 커서 평균을 끌어당기는데도 **평균은 여전히 0bp를 고른다**.
- ⚠️ **이 판정은 렌즈에 의존한다** — `pen_5bp`(옛 공식)에서는 **IS가 2bp를 고르고 그것이
  OOS에서 0bp를 이긴다**(+6.70% vs +5.71%). **렌즈를 안 바꿨다면 2bp를 채택했을 것이다.**
  오프셋과 `pen_5bp`는 **대체재**이기 때문이다: `pen_5bp`가 매기는 관통 벌점을 오프셋이
  주문 가격으로 되사는 구조라, 벌점이 없으면 되살 것이 없다. `pen_5bp`에서 2bp가 벌던
  +0.99%p는 새 알파가 아니라 **벌점 환급**이었다. "WAN-104가 오프셋을 기각했다"고 반쪽만
  인용하지 말 것 — 정확히는 **"공식 렌즈가 `baseline`이라는 전제 아래" 기각**이다.
- **"존을 안 찍고 반등한 진입"은 실제로 더 좋은 진입이다**(증분 승률 76.9% vs base 48.4%,
  평균 +0.92R vs +0.21R — 9/9 셀에서 방향 일치, OOS도 83.3% vs 46.3%). **그런데 너무
  드물다**: 2bp가 사는 셋업은 **13건**인데 세금은 **917건 전부**에 매겨진다(1.4%).
  base 부류는 오프셋이 바뀌어도 같은 917셋업이라 세금을 직접 잴 수 있다 — 2bp→10bp에서
  증분은 +302를 더 버는데 base는 **−3,186**을 잃는다(교환비 약 10배 불리).
  ⚠️ 증분 13건은 WAN-84 유효 기준(20건) 미달이라 **크기는 못 믿고 방향만** 본다.
- **반등 갭은 1~3bp에 몰려 있지 않다**: 최빈 밴드가 **`(5, 8]`** 이고 3bp 이내는 45개 중
  16개(36%)뿐이다. 볼륨이 있는 바깥 밴드에서 품질이 무너진다(`(8, 10]` 승률 **40%** <
  base 48.4%). 0bp가 놓친 934셋업 중 **889개(95%)** 는 10bp로도 체결되지 않는다 —
  오프셋이 건드릴 수 있는 대상이 아니다.
- 큰 오프셋이 무너진다는 **구조적 세금 메커니즘은 여전히 확인된다**(오프셋↑ = 1R 확대 =
  고정 R 익절 원거리화 → 승률·수익 단조 감소). 채택한 2bp도 이 세금을 낸다 — MDD가
  9.45%→10.15%로 오히려 조금 나빠지는 게 보험료의 일부다.

⚠️ **WAN-112 파급 — 어느 리포트가 움직였고 어느 리포트가 고정됐나**: 채택 기본값 성과
(`wan95_zone_limit_summary.md`)는 **2bp로 재산출**했다(진입가 → 1R → 고정 R 익절 목표가 전부
2bp만큼 이동). 반면 **WAN-88/96/103/110 리포트 모듈은 오프셋을 0bp로 명시 고정**해 당시 엔진의
기록으로 보존했다(WAN-95가 `SHORT_DISABLED_PARAMS`에 `entry_mode`를 못 박은 것과 같은 패턴) —
그 리포트들의 결론 문장이 0bp 수치에 박혀 있어 기본값을 따라 움직이면 본문과 어긋난다.
WAN-99/104는 오프셋을 **축으로 명시**해 돌리므로 무관하다. 🔁 **WAN-107(채택 TF)·WAN-108/110
(다중 포지션) 판정은 WAN-130이 채택 기본값에서 확인했다 — 둘 다 유지다**(아래 📌 WAN-130
문단). 0이면 입력 가격을 그대로 반환하므로 그 고정 리포트들은 비트 단위로 재현된다.

📌 **채택 기본값에서의 재확인(WAN-130): TF 순위도 다중 대 단일 부호도 유지다 — 재산출이
아니라 확인이다.** [`docs/decisions/wan130.md`](docs/decisions/wan130.md)(재현: `uv run python
-m backtest.run --symbol BTCUSDT,ETHUSDT,SOLUSDT --tf 15m,1h,4h --positions single,3 --oos
--start 2023-07-15 --end 2026-07-15 --jobs auto`). WAN-107/108/110 판정이 **오프셋 0bp ·
밴드 `tap` · RSI 게이트 on** 위에 서 있던 것을, 오늘의 채택 기본값(2bp × `intrabar_live` ×
`unconditional` × `baseline` 단독)에서 창을 못 박고(2023-07-15~2026-07-15, 3심볼) 다시 냈다.
- **TF 순위 유지** — 단일 포지션 OOS 심볼평균 **15m +21.10% > 1h +12.40% > 4h +0.58%**
  (IS도 같은 순위). 4h의 「보류(표본 부족)」도 **OOS 심볼당 18.3거래**로 유효 기준 20건
  미달이라 그대로다. 「15m·1h 공동 작업 TF」는 채택 기본값에서도 성립한다.
- **다중 대 단일 부호 유지** — OOS에서 **15m은 다중 승**(+21.10% → +35.31%, 수익/MDD
  1.64→1.93), **1h·4h는 단일 승**(1h +12.40% → +9.40%, 수익/MDD 1.54→0.68). 두 작업 TF가
  여전히 정반대이므로 **기본값(동시 1포지션) 유지** 근거가 성립한다. MDD는 세 TF 전부
  다중이 더 크다.
- 🚨 **옛 표와 이 표의 차이를 어느 한 축의 몫으로 읽지 말 것** — 네 축(오프셋·밴드·게이트·
  렌즈)이 **동시에** 움직였고 창도 다르다. 확인된 것은 **순위·부호**이지 크기가 아니다.
- ⚠️ **WAN-110(체결 보수화)은 확인 불가** — WAN-128에 따라 `baseline` 단독이라 `pen_5bp`
  축을 안 돌았다. 15m 다중 우위는 **낙관 렌즈 위의 값**이고 WAN-110 경고는 반증되지 않았다.
- ⚠️ **「엣지 없음」(WAN-84/88/111/114/124) 그대로** — 15m OOS +21.10%는 ETH를 빼면
  +10.02%, **1h +12.40%는 ETH를 빼면 +0.17%**(사실상 0). 3심볼이라 WAN-111의 6심볼 희석도
  반영돼 있지 않다.
- **기본값·토대 불변**(확인 전용, 실거래 보류 유지). 옛 md/csv는 **손대지 않았다**.
- 📌 **도구: 범용 CLI에 포지션 정책 축이 생겼다** — `--positions single,3`(단일 = 채택
  기본값 경로 그대로, 숫자 = 다중 포지션 명목 상한 배수). 인자를 안 주면 예전과 비트 단위로
  같은 행이 나오고, 다중 팔이 **실제로 포트폴리오 엔진을 타는지**를 테스트가 동작으로
  고정한다(라벨만 붙는 WAN-95/112/123 부류 방지). 종가 진입(A안)과는 함께 못 쓴다.

⚠️ **`entry_mode`는 라벨이 아니라 경로 스위치다(WAN-95)**: `close`는 A안
(`backtest.sweep.evaluate` → `BacktestEngine`), `zone_limit`은 B안
(`backtest.zone_limit_backtest.run_zone_limit_backtest`)에서만 유효하고, 두 진입점이
불일치를 `ValueError`로 거부한다. 예전엔 이 필드가 리포트에 찍히는 **라벨일 뿐**이라
`zone_limit`으로 두고 A안 엔진을 돌려도 아무 소리 없이 "종가 진입 결과에 zone_limit
라벨"이 붙었다. A안 전용 도구(스윕·워크포워드·대시보드·CLI 리포트)는
`backtest.sweep.CLOSE_ENTRY_DEFAULTS`로 **자기가 A안임을 선언**한다. 같은 이유로
리포트 헬퍼(`backtest/report.py`)는 파라미터를 못 받으면 진입 방식을 기본값으로
지어내지 않고 `"unknown"`으로 적는다.

⚠️ **WAN-103/108의 「동시 다중 포지션」과 채택 북(WAN-213)은 다른 것이다** — WAN-103 다중은
**한 (종목,TF) 안에서** 존을 겹쳐 잡는 것(per-cell)이고, WAN-213 북은 **칸을 가로질러** 한
지갑을 공유하는 것이다. 아래 「기본값은 여전히 동시 1포지션」은 **WAN-213 전까지의 서술**이다 —
지금 채택 회계는 레버리지 북이고(위 📌 WAN-213 문단), per-cell 단일/다중은 `--positions
single`/숫자로 존치한다.

⚠️ **동시 다중 포지션은 옵션이고 기본값은 여전히 「동시 1포지션」이다(WAN-103 · 재판정
WAN-108)**: [`docs/decisions/wan103.md`](docs/decisions/wan103.md)(재현: `python -m
backtest.archive.wan103_portfolio_leverage_report` + `python -m
backtest.archive.wan108_multi_position_reappraisal`). 여러 존에 동시 진입하는 포트폴리오 회계
(`backtest/portfolio.py`)와 포트폴리오 레버리지(= **열린 명목 합** ≤ 자본 × leverage)를
만들었지만, **`ConfluenceParams()`도 `risk_sizing`(사이징 모드·레버리지)도 바꾸지 않았다** —
`PortfolioParams`를 명시적으로 넘기는 새 진입점(`run_zone_limit_portfolio_backtest`)에서만
돈다. 기존 경로는 비트 단위로 그대로다.

📌 **「다중 포지션은 나쁘다」는 일반 결론으로 인용 금지 — WAN-108이 「1h에서 나쁘다」로
좁혔다**(wan103.md 「WAN-108 재판정」 절). **작업 TF 둘이 정반대를 가리킨다**:
- **15m OOS는 다중이 이긴다** — series 심볼평균 **+23.26% → +38.81%(3배)**, 3/3 심볼 플러스,
  수익/MDD도 **2.34 → 2.59**로 개선(ETH 제외해도 +9.16% → +13.03%). pooled 15m은
  +65.93% → +139.82%, 통합 책(15m/1h/4h/1d)은 **+36.09% → +196.79%**.
  ⚠️ **이 수치는 전부 `baseline`(낙관) 전용이다 — 렌즈 표기 없이 인용하지 말 것**(WAN-110):
  `pen_5bp`에서 우위가 +15.54%p → **+2.28%p**로 줄고 「3/3 심볼 플러스」도 **2/3**이 된다.
- **1h OOS는 단일이 이긴다** — **+8.18% → +5.66%(3배)**, MDD **5.96% → 8.94%**, 수익/MDD
  **1.37 → 0.63**. (WAN-103 수치가 그대로 재현됐다 — 갈린 원인은 엔진이 아니라 **TF 축**이다.)
- **기본값 유지 근거**: 하나의 기본값으로 둘 다 좋게 할 수 없고, "TF마다 다른 포지션 정책"은
  **IS에서 고르는 새 자유 파라미터**다. 또 15m의 승리는 `baseline`(낙관) 위에 서 있는데
  다중 포지션은 **체결에 더 의존**하므로(OOS 거래 772→838건) 체결 보수화 축의 재검이
  선행돼야 한다 — 그게 15m 축 후속 이슈의 질문이었다.
  🔁 **WAN-110이 그 재검을 끝냈다: 통과하지 못했다**(wan103.md §F, 재현: `python -m
  backtest.archive.wan110_multi_position_fill_conservatism`). 같은 셋업 풀에 렌즈만 조이면 **15m
  다중 우위가 +15.54%p → +2.28%p(`pen_5bp`)로 마진의 85%가 사라진다**(거래는 5%만 감소).
  WAN-96 비대칭도 **다중에서 더 심하다**(`pen_5bp` 잔존 수익 단일 48.9% vs 3배 35.2%) —
  즉 다중이 더 버는 몫은 유독 "스치듯 닿은 체결"에 몰려 있다. **기본값 유지 결론은
  그대로이고 근거가 하나 늘었다.** ⚠️ 스트레스 렌즈의 부호 역전(−1.62%p)을 "단일이
  이긴다"로 인용하지 말 것 — 시드 5개 중 3개는 오히려 다중이 앞선다(평균을 2개가 끌어내린다).
  읽을 수 있는 건 "다중 우위가 사라진다"까지다.

📌 **사이징 2안(사용자의 「1/N 시드 × N배」)은 기각됐다 — 업사이드가 없다(WAN-108)**:
`execution.sizing`에 `sizing_mode`가 생겼지만 **기본값은 `"risk_pct"`(현행)** 이다.
`"fixed_notional"`(명목 = 자본 × `notional_fraction`, 손절 거리 무관)은 **수익이 더 낮고
MDD가 더 크다**(15m OOS: 1안 3배 +38.81%/MDD 14.98% vs 2안 5배 **+18.89%/MDD 19.61%**;
1h OOS 2안 5배는 **−7.93%/MDD 22.93%**, pooled 1h는 **−25.00%/MDD 48.23%**).
- ⚠️ **2안은 동시 1포지션에서도 진다**(15m OOS single: +23.26% vs **+19.15%**) — 즉 문제는
  겹침·레버리지가 아니라 **사이징 그 자체**이고, 이 기각은 다중 포지션 논쟁과 **독립**이다.
- ⚠️ **「청산 0건」을 2안의 안전 근거로 쓰지 말 것**(wan103.md §5의 경고가 그대로 유효).
  청산은 1안·2안 모두 0이지만 **최대 동시 리스크는 1안 12.0% vs 2안 55.7%(4.6배)** 다.
  1안에서 레버리지가 명목 천장일 뿐이었던 건 **리스크 사이징 덕**이고, **2안에서 레버리지는
  실제 위험 배수**다. 청산이 안 난 건 실측 손절 거리(명목 대비 ~4~8%)가 자본을 다 먹을 만큼
  멀지 않아서다.
- ⚠️ **「역대 최대 겹침 N배」는 최적이 아니다**(15m 포함해 유지): IS에서 잰 N을 OOS에
  적용하면 더 낮은 레버리지에 진다(15m: N=8 +128.87% < 3배 +139.82%; 1h: N=5가 **꼴찌**
  +15.13% < 1배 +20.48%). **OOS 자연 겹침이 IS의 N을 넘긴다**(1h 5→6, multi_tf 11→12) —
  "N=역대 최대"는 미래의 최대가 아니다. N은 시간의 **0.13%**(1h)·**0.004%**(multi_tf)에만
  성립하는 상태다.

⚠️ **비용 가정(WAN-95 기준 · 청산 쪽은 WAN-370이 개정)**: 테이커 4bp(`BacktestConfig.fee_rate`)
/ **메이커 2bp**(`maker_fee_rate`, WAN-95에서 `None`→`0.0002`로 배선 — 예전엔 지정가 진입에도
테이커가 붙었다) / 슬리피지 5bp(테이커 체결에만). **요율 값 자체는 그대로다.** 붙는 자리는:
**진입(지정가)=메이커 2bp·슬리피지 0** · **익절(부분 익절 포함)=메이커 2bp·슬리피지 0**
(`take_profit_liquidity`, **WAN-370** — 실거래 집행이 지정가 reduce-only이므로) · **손절·만료·
데이터 종료=테이커 4bp＋슬리피지 5bp**(가격이 반대로 가서 시장가로 터는 청산).
  📌 **페이퍼 장부도 2026-09-01부터 같은 값을 문다**(WAN-371 — 그전에는 진입·익절을 전부
  테이커로 계산했다). 📌 **이 절은 「엔진이 실제로 하는 것」이고 원래 맞았다** —
어긋나 있던 것은 **비용 분해**였다(WAN-396: 진입 슬리피지 5bp를 허수로 계상 · `net R`은 무영향 ·
위 🚨 WAN-396 문단). 🔁 **옛 문장
「청산=항상 테이커」는 거짓이다 — 재인용 금지**(옛 회계는 `harness.LEGACY_TAKE_PROFIT_LIQUIDITY`
로 명시 고정한다). 테이커 4bp→5bp 등 요율 현실화는 여전히 WAN-92 범위.

⚠️ **라이브/페이퍼는 아직 지정가를 집행하지 못한다(WAN-45)**: `ConfluenceStrategy`는
`entry_mode`를 읽지 않는 **A안 시그널 생성기**라, 실시간 러너(`live.runner`)는 채택
기본값이 지정가로 바뀐 뒤에도 여전히 종가 기준 시그널·알림을 낸다. 대시보드도 상위TF만
로드하므로 종가 진입(A안)으로 명시 고정돼 있다(화면 배지가 "A안(봉 마감 종가)"로 표시).
즉 **백테스트(채택 규칙) ↔ 라이브(집행 가능 규칙)가 갈라져 있다** — WAN-45(러너 ↔
LimitOrderBook 배선)가 이 간극을 닫는다.

📌 **라이브 페이퍼 러너는 이제 기본으로 웹소켓 틱으로 돈다(WAN-256 = 사용자 재-베이스라인
2026-08-05, WAN-246 배선의 후속): `Settings.live_tick_feed_enabled` 기본값이 `False` → `True`다.**
[`docs/decisions/wan256.md`](docs/decisions/wan256.md). WAN-246(PR #208)이 틱 주문 처리를 옵트인
(기본 꺼짐)으로 배선했고, 사용자 결정("그게 이 시스템이 돌아야 할 방식이면 기본값이어야 한다")으로
그 스위치를 **기본 켜짐**으로 올렸다. 러너가 ccxt.pro `watch_trades`로 심볼별 실시간 체결가를 받아
대기 지정가 체결을 더 빨리 **감지**한다(1분봉 확정 + 폴링 간격만큼의 지연을 줄인다).
- ⚠️ **그 대가로 아무 설정 없이도 라이브(페이퍼)가 백테스트(1분봉)보다 미세해진다** — WAN-246
  §2/§3 판정 (b)의 "알고 받는 차이"가 이제 **기본 상태**다. **백테스트는 1분봉을 유지한다**(백테스트
  틱 승격 = 별도 재-베이스라인 + WAN-98 선행, 이 이슈 범위 아님). 갈림은 경계 근처 저관통 체결에서
  `intrabar_live` 밴드 재산정이 만드는 미세한 지정가 차이에 국한된다(`live/tick_parity.py`가 잰다).
- ⚠️ **틱이라고 "닿으면 체결" 낙관(WAN-96)이 사라지지 않는다** — 큐 우선순위·부분 체결은 호가·체결
  데이터(WAN-98, Canceled) 소관이라 틱 가격만으로는 여전히 상한이다. 틱은 체결을 더 빨리 **감지**할
  뿐 더 정확히 **판정**하지 않는다.
- **옵트아웃 보존** — `ALPHABLOCK_LIVE_TICK_FEED_ENABLED=false`면 옛 1분봉 폴링(비트 동일)으로
  되돌아간다. 테스트·CI는 러너 진입점을 실행하지 않고 `price_feed`를 직접 주입하므로 기본값이
  `True`여도 **웹소켓을 실제로 열지 않는다**(회귀 테스트가 동작으로 고정).
- **페이퍼 한정 · 실주문 없음** — 공개 체결 스트림만 구독한다. 전략·토대(진입가 · 존폭 필터 1.28 ·
  고정 1.5R · 레버리지 북) 불변, `ALPHABLOCK_LIVE_TRADING=false` 유지.

📌 **러너 재시작 정책 = 「복원 + 따라잡기」(WAN-306 = 사용자 결정 2026-08-14, 옛 「버리고
새로 걸기」 개정): 페이퍼는 재시작 여부와 무관하게 끊긴 적 없는 것처럼 동작한다.**
[`docs/decisions/wan306.md`](docs/decisions/wan306.md). 러너가 매 서브스텝 엔진 상태
(전이 맥락 · 탭 시도 표식 · 형성 중 봉 누적 · 대기 주문 생애 스칼라 — 재진입 재무장의
무기한 포함 — · `occupied_zone` · 1분봉 커서)를 `live_engine_state` 테이블에 영속화하고
(`live/engine_state.py` — 재파생 가능한 존 대장·시딩·ATR은 담지 않고 복원 시 확정봉 창
재공급으로 재구성), 기동 시 복원한 뒤 죽어 있던 구간의 확정 1분봉을 순서대로 재생해 그
사이의 체결·청산·만료·재무장을 페이퍼로 집행한다(뒷북이 아니라 따라잡기 — 복원된 주문이
실제로 걸려 있었다). 08-13 실사례(재시작 직후 즉시 체결 = 탭 세션 로컬 리셋, WAN-305
픽스처)의 재발을 재시작 불변 테스트(`tests/test_runner_restart_state.py`)가 동작으로
막는다: 연속 실행 ≡ 재시작+따라잡기(장부·거래·엔진 스냅샷 3중 지문, 합성 + 실데이터).
- ⚠️ **`discarded_restart`는 이제 예외 경로다** — 공백이 따라잡기 한도
  (`live_catchup_max_hours`, 기본 48h) 초과(명시 초기화 + 로그 · 조용한 재구성 금지) ·
  스냅샷 없음 · 복원 불가에만 남는다. 24봉 만료 시계·재진입 재무장은 재시작을 넘어
  유지되고, 복원된 주문은 WAN-298 벽시계 재평가에서 제외된다(따라잡기가 봉 계수로 정확한
  봉에서 만료를 낸다).
- 📌 **창 공급 순서가 일반화됐다** — `poll_series`가 소비할 1분봉마다 「그 봉이 닫힌
  시점까지 확정된 상위TF 창」을 먹인다(정상 운영은 예전과 같고, 여러 상위TF 봉을 한 번에
  소비하는 재생에서만 창이 경계마다 전진해 연속 가동과 같은 호출 순서를 재현한다).
- **수집기까지 함께 죽어 1분봉 자체가 없으면 따라잡을 수 없다** — 상위TF 봉을 통째로
  건너뛰는 구멍은 기존대로 주문을 측정 무효로 폐기한다(지어내지 않는다). 전략 기본값·토대
  불변 · 실거래 보류 유지.

📌 **수집기 봉 결측 사고 해부 + 재발 방지(WAN-314 = WAN-313 스핀아웃): 「기동 백필 ~
스트림 접속」 창에 확정된 봉은 아무도 다시 요청하지 않아 영구 구멍이 됐다 — 이제 접속
직후 꼬리 따라잡기가 매 (재)접속마다 그 창을 메운다.**
[`docs/decisions/wan314.md`](docs/decisions/wan314.md). 2026-08-16 사고(15m 23:00 KST 봉이
기존 9종목 전부 결측 · 러너 15m 평가 30분+ 정지)의 원인 판정 (a): 신규 심볼(ADA·DOT·BCH)
초기 백필이 **직렬 기동 구조**(백필 → 갭 복구 → 펀딩 → 접속)에서 스트림 접속을 수십 분
늦췄고, 스트림은 접속 이후 확정봉만 주므로 그 사이 확정된 봉이 빠졌다 — 유니버스 확장
때마다 재발하는 구조였다.
- 📌 **갭 복구는 결함이 아니었다(§2)** — 23:25의 「갭 없음」은 정확한 보고다. 그때 23:00
  봉은 내부 구멍이 아니라 **아직 안 받은 꼬리**였다(`find_gaps`는 설계상 꼬리를 갭으로
  안 센다). 꼬리가 지나간 뒤에는 정확히 그 봉을 잡아 메운다 — 사고 타임스탬프 재현
  테스트가 동작으로 고정(`test_wan314_*`). 갭 복구 코드는 무수정.
- 📌 **재발 방지 2겹**: ① `stream_klines(on_connect=)` + `TailCatchup` — 접속 성립 **직후**
  꼬리 백필을 백그라운드로 돌려 「접속 이후 확정 = 스트림」 + 「호출 이전 확정 = 따라잡기」
  합집합이 전 구간을 덮는다(재접속 공백도 같은 경로로 치유). ② **REST 백필은 형성 중 봉을
  저장하지 않는다**(`open_time + tf <= now`만) — 옛 코드는 형성 중 봉을 `closed=True`로
  저장해, 확정 순간 스트림이 죽어 있으면 **부분 봉이 확정 라벨을 달고 영구히 남았다**
  (사고의 22:45 봉 — 구멍보다 나쁘다: 어떤 점검에도 안 보인다).
- 📌 **건너뛴 평가가 이제 보인다(§3)** — 러너가 결측으로 건너뛰면
  `live_runtime_state.data_gap_skips`(시리즈·구간·횟수·해소 시각)에 남고 Health 탭
  「데이터 결측으로 건너뛴 평가」 카드가 그린다. 기록은 재시작을 넘어 보존되고, 구멍이
  메워지면 지워지지 않고 「해소」로 남는다 — 성적표의 빈칸이 「기회 없음」인지 「데이터
  결측」인지 구분된다. **건너뛰기 동작 자체는 불변**(없는 봉으로 계산 금지, WAN-156 §3).
- 서버 대조(부분 봉 검증 · 1m 같은 창 결측 · 로그 타임라인)는
  `scripts/wan314-server-forensics.sh`(읽기 전용, WAN-195 패턴 — 세션에 SSH 없음). 잔여
  구멍은 `uv run alphablock backfill --repair`(사용자 수행). **전략·기본값·토대 불변** ·
  실거래 보류 유지.

📌 **운영 위생 묶음(WAN-318 = WAN-315 실행 중 발견): doctor 주기가 실행 시간보다 짧아
DB 풀스캔이 상시 걸려 있었다 · 정상 정지가 `failed`로 보였다 · 잘린 백업이 정상 백업과 같은
이름으로 남았다.** 운영/도구 전용 — **전략·엔진·토대 불변**(`ConfluenceParams()`·
`LeverageBookParams()` 그대로 · `ALPHABLOCK_LIVE_TRADING=false` 유지).
- 📌 **doctor 타이머가 둘로 나뉘고 주기가 내려갔다** — `alphablock-doctor.timer`(전수 =
  `PRAGMA quick_check` 포함) **기본 15min → 1d** · 신설 `alphablock-doctor-light.timer`
  (`--skip-quick-check`) **기본 1h**. 근거: 서버 실측(2026-08-17) 전수 1회가 **18분+**
  (4.0GB · CPU 37초 · 나머지는 디스크 I/O 대기)인데 주기가 15분이라 **끝나기 전에 다음
  차례가 왔다**. 두 유닛 모두 `Nice=19` · `IOSchedulingClass=idle`로 수집기·러너에 디스크를
  양보한다. ⚠️ **점검 항목을 줄인 게 아니다**(무엇을 보는가는 WAN-194 소관) — 같은 doctor를
  두 주기로 나눠 돌린다. ⚠️ **싼 판도 공짜가 아니다**(로컬 7.3GB 실측: 전수 174초 · 싼 판
  **90초** — 인구조사가 6,500만 행을 센다). 그래서 1h이지 옛 15min이 아니고, **주기를 줄일
  때는 서버 실측을 먼저 볼 것**(회귀 테스트가 기본값을 분 단위로 잠근다).
- 📌 **collector·live·dashboard에 `SuccessExitStatus=143`** — `systemctl stop`의 SIGTERM
  종료(128+15)가 `failed`로 남아 **정상 정지와 크래시가 화면에서 구분되지 않았다**(실제
  오진). ⚠️ **doctor 유닛은 일부러 예외다** — 이상 시 종료 코드 1 = `systemctl --failed`
  감시가 설계의 전부라(WAN-185) 넣으면 감시가 조용히 죽는다.
- 📌 **백업은 `scripts/db-backup.sh`로만 한다**(신설) — 임시 이름으로 받아 **검증**(헤더
  page_count × page_size = 실제 크기 · 스키마 읽힘 · 저널 없음)에 성공해야 최종 이름을 붙이고,
  실패하면 `.FAILED`로 격리 + 종료 코드 1. 사고(2026-08-17): 경합 중 끊긴 `.backup`이
  **잘린 1.5GB 파일을 4.0GB 백업과 똑같은 이름으로** 남겼다(WAN-194 「실패가 성공과 같은
  모양」 계열). 기존 백업 점검은 `--verify-only`. `paper-reset.sh`도 `cp` 대신 이걸 쓴다.
- 📌 **배포하면 과거 타임라인 캐시가 전부 미스된다 — 되채우는 절차가 런북에 있다**
  (`docs/ops/wan239-nightly-timeline-cache.md` 「배포 뒤」). 캐시 키의 엔진 소스 지문이
  바뀌기 때문이고 **그 무효화는 설계대로다**(WAN-106/253 — 지문 정의를 바꿔 미스를 없애는
  것은 「엔진 고쳤는데 옛 결과를 꺼내 주는」 사고의 복귀다). 크론은 어제치만 돌므로 배포
  뒤에는 사람이 날짜를 다시 돌린다. 🔁 **되채우기는 WAN-297이 `--days N` 한 줄로 만들었다**
  (런북의 `for` 루프를 대신한다). ⚠️ **대시보드 「채택 좌표 전부」 모드가 매번 버튼을
  요구하던 것은 이 문제가 아니었다**(그 모드가 디스크 캐시를 안 읽었다) — 🔁 **WAN-297이
  고쳤다**(아래 📌 문단이 정본).
- 📌 **대시보드 라벨이 좌표를 따라간다** — 「9종목」 하드코딩이 `full_universe_shape()`
  (`len(DEFAULT_SYMBOLS)`·`len(DEFAULT_TIMEFRAMES)`)에서 파생된다. 12종목이 된 뒤
  (WAN-307) 화면에 **「9종목 × 4TF = 48셀」**이라는 자기모순이 떠 있었다 — 이 저장소가 가장
  경계하는 「라벨과 동작이 어긋남」(WAN-91/95/112/123/159)이 사용자 화면에 노출된 것이다.
  회귀 테스트가 **좌표를 바꾸면 라벨도 바뀌는지**로 잠근다.
- 📌 **템플릿 변경은 `deploy.sh`로 반영되지 않는다** — 유닛 파일은 `install-systemd.sh`를
  다시 돌려야 갱신된다(절차: `docs/ops/server-migration.md` §4b, 백업은 §4c).

📌 **러너 정지 감시를 등록했다(WAN-344 §4 — §1~§3은 「크래시 아님」으로 닫힘): 러너가 죽은 걸
아는 유일한 장치(`alphablock watch`)가 서버 어디에도 안 돌고 있었다.**
[`docs/decisions/wan344.md`](docs/decisions/wan344.md) · 런북
[`docs/ops/wan344-runner-watch.md`](docs/ops/wan344-runner-watch.md). 운영/도구 전용 —
**전략·엔진·기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()` 그대로) · DB 불변
(WAN-194) · 실거래 보류 유지(`ALPHABLOCK_LIVE_TRADING=false`).
- 🚨 **왜 조용한 위험인가** — 수집기와 러너는 **별개 프로세스**라 러너만 죽으면 봉은 계속
  신선하고(→ `stale_series` 백업 경보도 안 울림) 대시보드도 정상으로 보인다. **doctor(1d·1h)는
  DB 무결성·인구조사를 보지 러너 생존을 안 본다.** 서버 실측(2026-08-20): `crontab`·
  `systemctl list-timers`·`scripts/systemd/` **세 곳 전부에 watch 경로 없음** — 그 기간 정지
  공백 11분·34분·41분 동안 아무 경보도 안 갔다.
- 📌 **§1~§3은 조사로 닫혔다 — 재시작 10회는 크래시가 아니다**(크래시 증거가 앱 로그·journald·
  커널 로그 **세 축 전부 0** · 정지 로그는 `Deactivated successfully` = 정상 SIGTERM · 그 기간
  배포 다수). 34·41분 공백은 **(b) 사람이 정지 후 늦게 시작**이다(`Restart=always`는 **정상
  정지에는 재시작하지 않는다**). 🚨 **그래서 유닛의 `Restart`·`StartLimit*`를 손대지 않았다** —
  원인이 크래시가 아닌데 재시작 정책을 늘리면 증상만 가린다.
- 📌 **상주 service 가 아니라 timer + `--once`**(`alphablock-watch.timer`, 기본 10min =
  `health_watch_interval_seconds` 600초와 같은 값 · `ALPHABLOCK_WATCH_INTERVAL`): (1) 쿨다운·복구
  상태가 `health_watch_state_path` JSON 으로 **이미 영속화**돼 매번 새 프로세스로 돌아도 중복
  경고가 없고(= 이슈 §4-3의 답), (2) **상주 프로세스는 죽으면 그 사실을 알 장치가 또 필요하다**
  (감시의 감시) — 타이머는 다음 주기에 그냥 다시 뜬다(doctor 패턴). `install-systemd.sh watch` ·
  `all` 포함이고 회귀 테스트가 **`all` 분기의 실제 호출**로 잠근다(이 이슈의 본문이 정확히
  「목록에 없어서 안 돌았다」이므로 라벨이 아니라 동작으로).
- 🚨 **`--require-delivery`(신설 옵트인)가 유닛의 핵심** — 종료 코드 **0** 정상 · **1** 전송
  실패 · **2** 텔레그램 미설정. 이게 없으면 미설정 시 워치가 **조용히 드라이런으로 접고 0을
  낸다** = 「감시는 도는데 아무 데도 안 가는」 상태가 systemd 에서 성공으로 보인다(WAN-321이
  고친 실패 부류와 같은 자리). ⚠️ 그래서 이 유닛에도 **`SuccessExitStatus`를 넣지 않는다**
  (doctor와 같은 이유 — 넣으면 `systemctl --failed` 감시가 조용히 죽는다).
- ⚠️ **기본 동작은 안 바뀐다** — 플래그 없이는 예전처럼 미설정 시 드라이런 + 0. 딱 하나 바뀐
  것은 **`--test-message`가 실패 시 1/2를 낸다**는 것이다(**확인이 실패했는데 0을 내면 그건
  확인이 아니다** — 이슈 §4-4).
- 📌 **부수 수리: 전송 실패가 쿨다운을 걸고 있었다**(`apply_delivery_failures`, 순수 함수).
  옛 동작은 `reconcile` 결과를 그대로 저장해 **실패해도 「방금 보냄」으로 기록** → 그 뒤 1시간
  동안 **재시도조차 안 했다**. 이제 실패한 것만 되돌린다(첫 경고는 지우고 · 리마인더는 옛
  타임스탬프 유지 · 복구 알림은 기록을 되살림). WAN-321의 평문 폴백은 **서식** 실패를 구제할
  뿐 네트워크 실패는 남는다.
- ⚠️ **남은 것은 서버 1회 실증**(세션에 SSH 없음, WAN-195/314와 같은 제약):
  `./scripts/install-systemd.sh watch` → `alphablock watch --test-message`로 **폰에 실제로
  도착하는지**. **등록만 하고 도착을 안 보면 같은 자리다.** ⚠️ 유닛 템플릿은 `deploy.sh`로
  반영되지 않는다(`install-systemd.sh` 재실행 — WAN-318 §7).
- ⚠️ **WAN-343의 원인이 아니다**(거기서 재시작 가설은 실측으로 기각 — 클러스터 3개 중 1개만
  겹침). 두 이슈의 결론을 섞지 말 것. **부수 발견: journald 보존이 3시간**이라 과거 재시작
  조사가 막혔다(파일 로그 덕분에만 과거를 봤다) — `journald.conf` 조정은 **별도 조각**이고
  런북에 확인 명령만 적었다.

📌 **doctor 경보 두 겹 수리(WAN-321 = WAN-320 서버 확인 중 발견): 「정상이 실패로 보임」 +
「경보가 아예 안 감」.** [`docs/decisions/wan321.md`](docs/decisions/wan321.md). 운영/알림
전용 — **전략·엔진·기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()` 그대로 ·
`ALPHABLOCK_LIVE_TRADING=false` 유지) · **doctor 점검 항목도 불변**(무엇을 보는가는 WAN-194
소관 · 이 이슈는 **그 결과를 종료 코드·알림으로 옮기는 법**만 고친다).
- 📌 **장부의 성격이 갈렸다 — 누적 vs 현재 상태.** `CUMULATIVE_LEDGER_TABLES`
  (`live_limit_orders`·`live_runner_sessions`·`paper_trades`)는 0행이면 이상이라 **종료
  코드에 반영**하고, `STATE_LEDGER_TABLES`(`open_positions`)는 **0행이 정상**이라(포지션은
  닫힌다) 리포트에 정보로만 찍는다. `LEDGER_TABLES`는 두 집합에서 **파생**되므로 새 장부를
  넣으려면 성격을 골라야 한다(회귀 테스트가 분류의 전체성을 고정).
- 🚨 **WAN-194의 신호는 지워지지 않았다 — 두 겹으로 그대로 잡힌다**: (1) `paper_trades` 0행은
  누적이라 걸리고, (2) 「`filled` + 처분 NULL」은 `orphan_fills`가 **더 정확히** 잡는다(그
  열을 넣은 것이 WAN-194다). 정밀한 자와 옛 어림자가 함께 걸려 있었고 **어림자만 울고
  있었다** — 싼 판 1시간 주기라 하루 24번(WAN-318이 계기). `systemctl --failed`가 상시
  빨가면 진짜 이상과 구분되지 않는다(WAN-194 「실패가 성공과 같은 모양」·WAN-318 §3 「정상
  정지가 failed」의 **거울상**).
- 📌 **`empty_ledgers` 속성은 일부러 없앴다** — 이름이 옛 뜻(「비면 경고」)으로 읽혀 호출부가
  조용히 이어받으면 거짓 경보가 돌아온다. `empty_cumulative_ledgers`(종료 코드) ·
  `empty_state_ledgers`(정보)로 갈라 **성격을 고르도록 강제**했다(「라벨과 동작이 어긋남」
  WAN-91/95/112/123/159 예방).
- 🚨 **텔레그램 경보가 죽어 있었다 — WAN-185 감시의 두 다리 중 하나.** 경고문에 실리는 것이
  하필 테이블·열 이름이라 `open_positions`의 **밑줄**을 레거시 Markdown이 「닫히지 않은
  이탤릭」으로 읽고 **400으로 거부**했다 = 진짜 손상이 나도 폰에는 아무것도 안 갔다. 게다가
  그 실패가 `WARNING` 한 줄이라 **「경보를 못 보냈다」가 경보되지 않았다.**
- 📌 **고친 자리는 클라이언트다 — 호출부를 안 고쳐도 전 경로가 낫는다.** `send_message`가
  **서식 파싱 실패(400)만은 평문으로 딱 한 번** 다시 보낸다(평문에는 파싱이 없어 같은 이유로
  재실패할 수 없다 = 루프 아님 · 서식 무관 400과 네트워크 소진은 재전송 안 함). doctor
  경고문 자체도 **평문**으로 바뀌었다(`parse_mode=None`). 최종 실패 로그는 `ERROR`로 올랐고
  doctor는 **경고 본문을 함께** 남긴다. ⚠️ **종료 코드는 안 건드렸다**(무결성 판정 전용).
- 📌 **doctor 하나만의 함정이 아니었다**(완료기준 5 조사) — `data/repair.py`(예외 메시지) ·
  `data/collector.py`(복구 사유) · `live/zone_limit_notifier.py`(거부 사유 — `REJECT_CODE_*`
  는 `min_stop_distance`처럼 **밑줄 포함**) · `live/health_watch.py`가 전부 자유 텍스트를
  레거시 Markdown 안에 끼워 넣는다. 표는 wan321.md §2. ⚠️ **폴백은 그물이지 면허가 아니다**
  — 걸리면 서식 없이 도착하고 `WARNING`이 남는다.
- ⚠️ **서버 실증은 남아 있다**(세션에 SSH 없음) — 배포 후 doctor 이상 시 텔레그램이 실제로
  도착하는지 1회 확인. 파싱이 안 깨지는 것 자체는 테스트가 고정한다.

📌 **저장 4h·1d 봉의 부분 봉 — 범위·크기·재발 방지(WAN-327 = `alphablock verify` 서버 실측
2026-08-18): 손상은 07-12~07-24 사고이되 **전 이력에는 옛 무리가 더 있었고**, 4h 백테 영향은
**측정됐다**. 기본값·토대 불변 · **DB에 아무것도 쓰지 않았다.**
[`docs/decisions/wan327.md`](docs/decisions/wan327.md)(재현: `uv run alphablock partial-bars
--timeframes 4h 1d` · `uv run python -m backtest.wan327_partial_bar_impact --part scan|cell|book`,
요약만 `--from-csv`).
- 📌 **판정자가 가격이 아니라 거래량이다** — `저장 거래량 < 리샘플 × 0.99`가 부분 봉의 판정
  (`data.partial_bars.PARTIAL_VOLUME_RATIO`). 「가격이 틀렸는가」로 가르면 **놓친다**: 그 버킷의
  고가·저가가 잘리기 전에 이미 찍혀 있으면 부분 봉이어도 가격이 맞는다. 실측에서 **2023-08-16 ·
  2024-10-28 1d 무리는 가격 오차 0.0bp인데 거래량이 94~96%** 라, 가격 자로 재면 통째로 안 보인다.
- 🚨 **`verify`가 보던 창(최근 500버킷) 밖에 옛 무리가 있었다** — 4h **2023-11-10 · 2024-10-29**
  (각 6종목 6버킷 · 거래량 76~82%)와 1d **2023-08-16 · 2024-10-28**. 7월 무리보다 **훨씬 얕지만**
  (7월은 거래량 0.4~2%까지 떨어진다) 「끝난 한 번의 사고」가 아니라 **드물게 반복되는 부류**다.
  전 이력 스캔이 아니면 안 보인다 — 그래서 `alphablock partial-bars`(신설, 읽기 전용 · 손상 있으면
  종료 코드 1)가 있다.
- 🚨 **신규 6종목이 「깨끗한」 게 아니라 검사가 성립하지 않는다** — DOGE·LINK·LTC·ADA·DOT·BCH의
  4h·1d는 **1분봉에서 집계돼 들어온 것**(`data.aggregate`, WAN-175/307)이라 **정의상** 1분봉 합과
  같다. 중립 창 실측 지문: 저장 값이 리샘플과 **비트 단위로 100% 일치**(BTC·ETH는 67~68%). 즉 이
  스캔도 `verify`의 정합성 검사도 그 종목들에는 **아무것도 물어보지 못한다**(자기 자신과의 비교).
  ⚠️ 「신규 종목은 안 깨진다」로 인용 금지.
- 📌 **§3 감시의 자를 갈랐다 — 「손상」과 「거래량 노이즈」를 가른다.** `_REL_TOL=1e-6` 한 자가
  「봉이 반토막 났다」와 「거래량 끝자리가 다르다」를 똑같이 「불일치」로 찍어 **상시 빨간불**이
  됐고, 그 안에서 진짜 부분 봉이 묻혔다(PM이 이 이슈에서 두 번 오독 · 한 번은 우선순위까지
  올렸다가 되돌렸다 — WAN-318 §3 · WAN-321과 같은 부류). 이제 `ParityReport.ok`가 **손상만**
  보고 노이즈는 `noise`로 따로 보고한다(지우는 게 아니라 **가르는** 것 · 필드 단위 상세는 그대로).
  ⚠️ **점검 항목을 줄인 게 아니다** — 무엇을 보는가는 불변이고 **결과를 종료 코드로 옮기는 법**만
  바뀌었다. 분류는 `classify_bucket` **한 곳**에서만 내려 전 이력 스캔과 `verify`의 자가 갈라지지
  않는다.
- 📌 **§3 재발 방지는 코드로 논증했다(데이터로는 못 한다)** — WAN-314는 **08-17 머지**인데 손상은
  **3주 앞선 07-24에 멈췄다**. OHLCV를 쓰는 프로덕션 경로 셋이 전부 막혀 있다: REST 백필은
  `open_time + tf <= now`만 저장 · 스트림은 `only_closed=True`(확정 이벤트만) · 집계는 구성 하위봉이
  빠짐없을 때만 만들고 `DO NOTHING`이라 덮지도 않는다. ⚠️ **못 막는 자리 하나 기록**: 가드가 전부
  **쓰는 쪽**에 있고 `OhlcvStore._load_native`는 `closed`를 **필터하지 않는다** — 오늘은 `closed=0`을
  남기는 경로가 없어 잠재적이지만, `only_closed=False` 호출부가 하나 생기면 되살아난다.
- 📌 **§2 4h 백테 영향은 「거의 없다」 — 24셀(12종목 × full·oos_warm) 중 2셀만 움직이고 둘 다
  BTC다**(full +25.80%→**+27.58%**(+1.77%p) · oos_warm +3.02%→**+4.47%**(+1.45%p) · 거래 52→53 ·
  **MDD는 어느 셀도 불변**). 🚨 **「손상 봉이 있다」와 「백테가 달라진다」는 다른 말이다** —
  ETH·SOL은 BTC와 **같은 37봉**이 손상됐는데 거래도 손익도 **하나도 안 바뀐다**(손상 봉이 탐지·
  탭·진입가 어디에도 안 걸렸다 · 4h 6년에서 손상 비중 0.29%). BTC에서 달라진 것은 **없던 셋업
  하나가 생긴 것**이라, 부분 봉은 엔진을 틀리게 만들기보다 **기회를 지우는** 쪽으로 작용했다
  (표본 1건 · 방향 일반화 금지). ⚠️ **「그러니 고칠 필요 없다」가 아니다** — 이 표는 채택 창 안
  4h 손익 하나를 잰다(창 밖 07-23·24 · 러너가 읽는 최신 구간 · 화면은 별개). **급하지 않다**까지다.
- 📌 **채택 북도 같은 답을 낸다** — 늘어난 거래가 **정확히 1건**(883→884 full · 334→335
  oos_warm)이고 **MDD는 두 구간 다 비트 단위로 불변**(15.87% · 8.28%) · 청산 0. per-cell과
  북이 서로를 검산한다. ⚠️ 수익 델타(+38%p · +3.5%p)를 크기로 읽지 말 것 — 수백 거래 복리
  위의 값이라 거래 1건이 그렇게 보인다(WAN-169/213). ⚠️ 이 북 표는 **차가운 절단을 안 냈다**
  (델타 측정이라 `cold_segments=False` — 성과 **수준**을 인용할 표가 아니다).
- 📌 **§2 반사실은 비파괴다** — 「고친 판」은 **손상 봉만** 1분봉 합으로 갈아끼운 **메모리 사본**
  (`repair_frame` · `harness.load_market_data(repair_htf_from_1m=)` · `wan169.run_cells(
  repair_partial_bars=)` — 전부 옵트인, 끄면 비트 단위로 같다). ⚠️ **「교정 팔이 정답」이 아니다**:
  손상 구간 **밖**에서는 저장 4h가 1분봉 합보다 0.0~0.3% 크다(우리 1분봉 쪽이 모자라다) — 그래서
  전부 덮지 않는다. 같은 이유로 **수정 방법은 (b) 거래소 재수집**이고 (a) 1분봉 재생성은 기각이다
  (집계 경로가 `DO NOTHING`이라 애초에 안 덮기도 한다).
- ⚠️ **로컬 DB는 상위TF가 채택 창 끝(2026-07-22)에서 멈춘다** — 07-23·24의 손상은 이 표에 **없다**
  (채택 창 밖이라 백테 영향에는 빠져도 되지만 「총 몇 개」는 창 안 기준). 서버 총계와 **같은 수로
  인용 금지**.
- **측정·도구 전용 · 엔진·기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()`) ·
  **DB 불변**(자동 수정·자동 VACUUM 없음, WAN-194) · 실거래 보류 유지.

📌 **타임라인 캐시가 「채택 좌표 전부」까지 먹인다(WAN-297): 화면이 세션 상태가 아니라
디스크를 읽고, 담기는 것이 청산 거래에서 **셋업 전부**로 넓어졌다.** 도구/UI 전용 —
**엔진·기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()` 그대로 · 실거래 보류
유지). 런북: [`docs/ops/wan239-nightly-timeline-cache.md`](docs/ops/wan239-nightly-timeline-cache.md).
- 🚨 **사용자가 겪은 화면은 캐시 미스가 아니었다**(2026-08-15) — 야간 크론이 48셀을 **적재
  성공**으로 찍은 날짜인데도 「실행 버튼을 누르세요」가 떴다. 두 모드가 **다른 계층**을 읽고
  있었기 때문이다: 「라이브 칸만」은 `load_cached_day`(디스크), 「채택 좌표 전부」는
  `st.session_state`(그 세션뿐). 즉 크론이 디스크를 아무리 채워도 그 모드는 **쳐다보지도
  않았다**. 이제 세 겹으로 읽는다 — 세션 → **디스크 캐시** → 미스면 버튼 안내(자동 재계산은
  여전히 없다, WAN-239 §3).
- 📌 **캐시 버전 `wan305.1` → `wan297.1`: 담기는 행이 「청산 거래만」에서 「셋업 전부」로**
  (미진입·미체결·건너뜀 포함, WAN-295 3열 대조가 읽는 행). 한 캐시가 두 모드를 다 먹이려면
  **넓은 쪽**을 담아야 한다 — 좁은 판을 담고 넓게 읽으면 「계산은 됐는데 미체결 행이 없는」
  조용한 실패가 된다(WAN-91/95/112/123/159 부류). `persist_day`가 `backtest_timeline_by_cell`
  → **`backtest_setup_by_cell`**을 탄다. ⚠️ **옛 적재분은 버전이 갈라져 자동 미스**이므로
  배포 뒤 되채워야 한다(아래 `--days`). `alphablock trades` 표는 그중 **청산 행만** 싣는다
  (둘이 비트 동일함은 실데이터 회귀 테스트가 이미 고정).
- 📌 **화면 버튼과 야간 크론이 같은 함수를 탄다(완료 기준 4)** — 버튼은
  `compute_and_persist_day`(= `persist_day` + 되읽기)라 **계산한 그 행이 곧 디스크에 담기고
  화면은 담긴 행을 그린다**. 「화면에는 떴는데 캐시에는 없다」가 구조적으로 불가능하다
  (두 경로가 각자 계산하면 산출물이 갈라진다 — WAN-146의 교훈).
- 📌 **§0 실측(2026-08-17 · 로컬 M1 · 직렬 `jobs=1` · WAN-313 벡터화 후)**: 12종목 × 4TF =
  **48셀 cold ~71초**(2026-07-20). ⚠️ **런북의 옛 「27칸 ~15초」와 나란히 읽지 말 것**(자가
  다르다 — 그쪽은 9종목 3TF · `--jobs 8`). ⚠️ **데이터가 있는 날로 재야 한다** — 로컬 DB가
  12종목 중 3종목만 덮는 날짜로 재면 53~56초가 나오는데 **빠른 게 아니라 덜 계산한 것**이다.
  본문의 「~35초」는 9종목·벡터화 이전 값으로 **당시 기록으로 유효**하다.
- 📌 **`--days N`(되채우기)·`--prune-cache`(정리)가 생겼다** — 배포마다 엔진 소스 지문이 바뀌어
  과거 캐시가 통째로 미스되는데(설계대로 · WAN-106/253), 크론은 어제치만 돈다.
  `alphablock trades --day <오늘> --persist-cache --days 7`이 손으로 돌리던 `for` 루프를
  대신한다(오래된 날부터 · 중간에 멈춰도 앞쪽이 이어진다). 정리는 **세기가 기본, 삭제는
  `--prune-apply`** 이고 **기준(옛 리비전 · `--prune-before`)이 하나도 없으면 종료 코드 2로
  거부**한다 — 자동 삭제·자동 VACUUM은 없다(WAN-194 원칙: 무엇을 지웠는지 모르는 DB를
  저장소가 스스로 만들지 않는다).
- ⚠️ **백테 수익률은 기대수익이 아니다** — 전부 `baseline`(닿으면 체결) 낙관 렌즈 위 값이고
  **「엣지 없음」(WAN-84/88/111/114/124/151/201) 불변** · 페이퍼↔백테 차이는 설계상 알려진
  것(틱 vs 1분봉 WAN-256 · 큐 우선순위 미모델 WAN-98). **크론 등록은 코드가 하지 않는다**
  (런북에 사람이 1회 — WAN-195 선례).

📌 **하루치 타임라인 적재가 같은 1분봉을 종목당 4번 읽고 있었다 — 48회 → 12회(WAN-324 =
WAN-297 후속): §0 프로파일 먼저, §1은 비트 동일.** 성능/도구 전용 —
[`docs/decisions/wan324.md`](docs/decisions/wan324.md)(재현: `uv run alphablock
timeline-profile --day <날짜> --shape per-cell|shared`, 신설 `live/timeline_profile.py`).
**엔진·전략·기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()` 그대로 · DB에
아무것도 쓰지 않는다 · 실거래 보류 유지).
- 📌 **이 경로의 단계 비중은 여기 표가 정본이다** — ⚠️ **「1분봉 로딩이 1조합 실행의 ~36%」를
  인용하지 말 것**(그건 **3년 격자** 값이고 이 경로는 **120일 창**이다 — 다른 모듈 값 인용
  사고). 로컬 M1 48셀 직렬 실측: **서브스텝 평가 72.2%** · 1분봉 SQL 25.0%(48회 41.9s) ·
  탐지 1.5% · 상위TF SQL 1.3% · **펀딩 0%**(이 경로는 `funding=False`라 조회조차 안 한다).
- 🚨 **「1분봉이 지배적」은 로컬에서 성립하지 않는데도 §1을 착수했다 — 근거는 서버의
  `user`/`real` 분해다.** 서버 6분 23초 중 `user`가 2분 13초(**133s**)인데 이건 로컬 CPU
  단계(탐지+평가 ≈ **123s**)와 거의 같다 = **서버가 느린 이유는 CPU가 아니다**. 남는 약
  250초가 I/O이고 이 경로에서 I/O를 내는 단계는 SQL 읽기 하나다(로컬은 7.8GB DB의 페이지
  캐시가 뜨거워 그 비용이 안 보인다). ⚠️ 그래서 **로컬 −19%는 서버 기대치의 하한으로,
  「I/O 250초가 전부 1분봉」은 상한으로** 읽는다 — 판정은 서버 프로파일이 낸다.
- 📌 **바뀐 것은 로드뿐이다** — 신설 `harness.iter_market_data_by_timeframe`이 **가장 넓은
  창을 한 번 읽어 TF별로 자른다**(1분봉 하한이 상위TF 첫 봉 `open_time`이라 TF마다 다르다).
  병렬 단위도 셀 → **심볼**로 올라가 같은 종목의 4TF가 한 번 읽은 것을 나눠 쓴다.
- 🚨 **로더가 지연 산출인 것은 장식이 아니다** — 120일치 1분봉은 TF마다 사본이라 네 TF를
  한꺼번에 들면 워커가 다섯 벌을 동시에 이고, 그건 이 최적화가 기대한 방향
  (`BrokenProcessPool` 완화)과 **정반대**다. 적재 경로는 TF를 하나씩 소비·폐기해 살아 있는
  사본이 «공유 원본 + 지금 TF» 둘뿐이다. 넷을 나란히 봐야 하는 곳(프로파일러·테스트)만
  `load_market_data_by_timeframe`(= `dict(iter_…)`)을 쓴다. ⚠️ **화면 버튼은 직렬 유지**.
- 🚨 **비트 동일 게이트**: 하루치 캐시를 변경 전후 코드로 각각 적재해 **48셀 · 100행 ·
  17개 필드 전부 차이 0**(별도 스크래치 캐시 DB — 공용 DB 불변). 회귀 테스트는 **읽은
  횟수**로 잰다(이름만 `shared`이고 여전히 TF마다 읽으면 걸린다 — WAN-91/95/112/123/159
  부류 방지). **빨라졌는데 숫자가 달라지면 그건 버그다**(WAN-203 패턴).
- ⚠️ **서버 재측정(6분 23초 대비)은 남아 있다**(세션에 SSH 없음, WAN-195/314와 같은 제약).
  로컬 하루치 **CPU 시간은 141.9s → 114.6s(−19%)** — ⚠️ 벽시계는 같은 머신의 다른 측정과
  경합해 출렁이니(load 7.3~9.0) **`user`로 읽을 것**. `--jobs` 기본값 재검토(§2)는 서버 실측 뒤 별건.
- ⚠️ **캐시 미스 자체는 줄이지 않는다**(배포마다 엔진 지문이 바뀌어 무효화되는 것은 설계대로 —
  WAN-106/253) · **`--warmup-days`를 줄여 빠르게 하는 것은 답이 아니다**(워밍업이 캐시 지문에
  들어가 화면이 그 캐시를 안 읽는다).

📌 **엔진이 바뀌어 캐시가 미스면 빈 화면 대신 「옛 엔진 결과」를 라벨 달아 보여준다
(WAN-325 = 사용자 요청 2026-08-18 「그냥 두면 안돼? 안없어진채로?」).** 도구/UI(표시 계층)
전용 — [`docs/decisions/wan325.md`](docs/decisions/wan325.md). 배포로 엔진 소스가 바뀌면
과거 날짜가 통째로 미스가 되는데(설계대로 · WAN-106/253/318) 하루치 재계산이 **서버 6분
23초**(WAN-322 실측)라, 옛 날짜를 훑어보는 것만으로 그 비용을 치러야 했다. 📌 **행은 이미 안
지워진다** — 삭제 경로는 `--prune-cache --prune-apply` 하나뿐이라(WAN-194/297) 옛 엔진 셀은
DB에 그대로 있었고 **화면이 오늘 지문으로만 찾아서 안 보였을 뿐**이다. 그래서 이 이슈는
「지우지 말자」가 아니라 **「보관 중인 걸 보여주자」**다.
- ⚠️ **리비전 키를 느슨하게 한 게 아니다** — 금지된 것은 옛 결과를 **조용히** 새 결과인 척
  내주는 것이지 **라벨을 달아** 내주는 것이 아니다. 세 겹으로 경계를 지킨다: (1) **지금 엔진
  셀이 있으면 언제나 그쪽이 이긴다**(옛 판이 더 최근에 적재됐어도 — 테스트가 그 상황을 만든다),
  (2) **배지가 실제로 읽은 판을 가리킨다**(`DayCacheResult.label`이 옛 판의 이름·지문으로
  바뀐다 — 배지는 오늘인데 행은 옛 엔진인 상태가 바로 금지된 실패다), (3) 폴백은 **옵트인**
  (`load_cached_day(allow_stale=True)`)이고 기본값·자동 재계산 금지(WAN-239 §3)는 불변이다.
- 📌 **고르는 단위가 셀이 아니라 「엔진 판」이다**(`CachedEngine`·`day_engines`) — 미스인 칸마다
  가까운 셀을 주워 오면 한 표에 여러 리비전이 섞인다(금지). 엔진 정체는 **(엔진 소스 지문,
  엔진 버전)** 쌍이고, 순위는 **커버리지 → 적재 시각**이다(옛 리비전이 여럿이어도 표에 오르는
  것은 하나 · 고른 판에 없는 칸은 **다른 판에서 메우지 않고** 그냥 미스).
- 🚨 **캐시 버전이 다른 셀은 후보에서 뺀다 — 여기가 조용한 실패의 자리다.** 캐시 버전은 「행의
  의미」다(`wan305.1` = **청산 거래만** · 지금 `wan297.1` = **셋업 전부**). 옛 버전 셀을 「옛
  엔진 결과」라며 3열 대조에 내주면 **미체결·건너뜀 행이 통째로 빠진 표가 「계산됨」으로** 떠서
  WAN-297이 이름 붙인 「계산은 됐는데 미체결 행이 없는」 실패가 재현된다. 옛 행은 **지우지 않고
  그냥 안 쓴다**.
- 🚨 **3열 대조(페이퍼 | 차이 | 백테)는 옛 판일 때 통째로 끈다** — 그 카드가 재는 것은 **집행
  차이**인데 옛 엔진 백테에서 오늘 페이퍼를 빼면 **엔진이 바뀐 몫**이 되고, 그대로 두면 🔴
  판정 갈림이 무더기로 뜬다(WAN-295가 재려던 것과 다른 것). 「Δ 막대만 비우기」를 안 고른 이유는
  **좌·우 열이 나란히 서 있으면 눈이 알아서 빼기 때문**이다. 같은 이유로 「백테스트만 진입한 줄
  N건」 경고도 끄고 시각순 표 캡션이 바뀐다(표 자체는 라벨과 함께 그대로 뜬다 — 볼 것은 다 본다).
- 📌 **적재 시각이 이제 실제로 남는다** — `persist_day`가 `created_at`을 호출부에 맡겨 두고
  아무도 안 넘겨 **전부 0**이었다(크론도 화면 버튼도). 지금은 적재 시각을 찍고 배너가 KST로
  보여 준다(WAN-172 — 저장은 UTC epoch). ⚠️ 옛 적재분은 0이라 **「적재 시각 미상」**으로
  밝힌다(지어내지 않는다).
- **터미널도 같다** — `alphablock trades`의 배지가 옛 판의 지문을 달고 상태 줄이 그 사실을
  밝힌다. **엄격 조회는 `--no-stale`**(스크립트가 「오늘 엔진으로 적재됐나」를 판정할 때).
- **되채우기는 여전히 필요하되 급하지 않다** — 런북(`docs/ops/wan239-nightly-timeline-cache.md`
  「배포 뒤」)이 그 변화를 적었다. **적재 성능은 WAN-324 소관**이고 이 이슈가 그쪽의 급함을 줄인다.
- **엔진·전략·기본값·토대 불변** · 삭제·자동 정리 없음(WAN-194/297 원칙) · 백테 수익률은
  `baseline`(낙관) 위 값 · **「엣지 없음」(WAN-84/88/111/114/124/151/201) 불변** · 실거래 보류
  유지(`ALPHABLOCK_LIVE_TRADING=false`).

📌 **타임라인 탭 첫 화면 = 「채택 좌표 전부」 × 어제(KST)(WAN-326 = 사용자 결정 2026-08-18·
20): 「아직 계산 안 됨」이 첫인상이던 진짜 원인은 라디오가 아니라 **날짜**였다.** 도구/UI의
**기본 선택**만 바꾼다 — [`docs/decisions/wan326.md`](docs/decisions/wan326.md).
**엔진·전략·기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()`) · DB에 아무것도
쓰지 않는다(WAN-194) · 새 격자 없음 · 실거래 보류 유지.
- 📌 **날짜 기본값이 「오늘」이라 정의상 항상 캐시 미스였다** — 야간 크론은 **전일(KST)** 을
  KST 00:30에 적재하는데 화면은 오늘을 열었다. 이제 `default_timeline_day()` = **어제(KST)**
  다(크론이 채워 둔 날짜라 버튼 없이 즉시 뜨고, 오늘은 **진행 중이라 불완전** — WAN-334에서
  당일이 27셋업으로 전날 60건보다 적게 잡힌 것이 데이터가 아니라 **시각** 탓이었다).
  🚨 **「캐시에 있는 가장 최근 날짜를 찾아 연다」로 똑똑하게 만들지 말 것**(첫 렌더에 DB
  조회가 붙고 「왜 그저께가 떴지」를 설명할 수 없다) — **어제 고정**이고 자정~00:30의 30분은
  WAN-325 폴백·계산 버튼이 덮는다. 날짜 위젯은 불변(오늘·과거 아무 날이나 계속 고른다).
- 📌 **대조 대상 기본이 「채택 좌표 전부」다 — 근거는 「전제가 깨졌다」이다.** 좁은 쪽이
  기본이던 이유는 **무거워서**였는데(48셀 온디맨드 · 세션 끊기면 날아감) WAN-297이 두 모드를
  같은 디스크 캐시에 물려 **조회 비용이 사실상 같아졌다**. **`index=`를 명시**한다 — 옛
  기본값은 「리스트 0번」이라는 사실만으로 정해져 있어 **선택지 재배열만으로 조용히 뒤집힐**
  수 있었다. 「라이브 칸만」은 **존치**(사용자 결정 — 지우지 않는다).
- 🚨 **「라이브 칸만」은 「표시 필터」다 — 「재구성」이 아니다**(WAN-340 대비). 지금은 대조
  백테가 per-cell이라 두 읽는 법이 **같은 값**을 내지만, 북은 칸이 **한 지갑을 공유**하므로
  (WAN-213/316) 그 칸들만으로 회계를 **새로 구성**하면 **자본 경합이 달라져 다른 지갑**이
  된다 = 라디오를 옮길 때마다 같은 날의 같은 셋업이 다른 숫자로 보인다(「라벨과 동작이
  어긋남」 WAN-91/95/112/123/159 부류). **좁히기 인자가 계산 범위로 흘러가면 안 된다.**
  📌 오히려 이 결정의 근거가 세진다 — WAN-340 이후 화면이 그리는 지갑은 **언제나 48칸
  전체**라, 기본이 넓은 쪽인 것이 **화면과 회계가 일치하는 유일한 선택**이 된다.
- 📌 **미스 안내에 나갈 길을 셋 준다**(실행 버튼 · 날짜를 어제나 그 이전으로 · 「라이브
  칸만」으로 좁히기) — 기본이 넓어져 이 안내가 **첫인상**이 될 수 있다. **자동 재계산 금지
  (WAN-239 §3)는 불변**이다.
- ⚠️ **배포 순서 권고(코드 의존 없음)**: **WAN-338**(캐시 버전 `wan297.1`→`wan335.1` 배포 후
  전량 되채우기) **뒤에** 배포할 것. 되채우기 전에는 옛 적재분이 전부 미스이고 WAN-325 폴백은
  **캐시 버전이 다른 셀을 후보에서 뺀다** — 그 사이에는 첫 화면이 48칸 전부 「아직 계산 안 됨」이다.

📌 **`stop-width --with-backtest`가 야간 캐시를 읽는다 + 좁히기가 양쪽에 걸린다(WAN-335 =
2026-08-19 실사용 중단): 그리고 그 과정에서 **캐시가 조인 키를 버리고 있었다**는 결함이
드러났다.** [`docs/decisions/wan335.md`](docs/decisions/wan335.md). 도구/성능 전용 —
**엔진·전략·기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()`) · DB에 아무것도
쓰지 않는다(WAN-194) · 실거래 보류 유지.
- 📌 **§1 느림의 원인은 알고리즘이 아니라 「이미 한 계산을 다시 하기」였다** — 야간 크론이
  담는 것(`persist_day` → `backtest_setup_by_cell`)이 **정확히** 이 도구가 쓰는 셋업 행인데
  (WAN-297이 담기는 행을 「셋업 전부」로 넓힌 이유가 그것이다) 디스크를 무시하고 매번 구웠다.
  `alphablock trades`는 즉시 뜨는데 `stop-width`는 **4칸으로 좁혀도 안 끝났다**. 이제 캐시를
  먼저 읽고 **미스인 칸만** 계산한다(칸 낱개 경로 `backtest_setup_by_cells` 신설 —
  `backtest_setup_by_cell`이 그 위의 래퍼라 **다른 계산이 아니다**). ⚠️ **WAN-324와 다른
  문제다**(그쪽은 「한 번의 계산을 싸게」 · 프로파일 선행. 둘 다 하면 곱해서 빨라진다).
- 🚨 **캐시가 셋업 행의 다섯 칸을 버리고 있었다 — 이 이슈의 진짜 발견.** WAN-297이 payload를
  넓히면서 `timeline_cache_rows` **열은 거래 행 시절 그대로**라 왕복에서 `reserve_ms`·
  `limit_price`·`stop_price`·`take_profit_price`·`tap_index`가 통째로 사라졌다. 하필
  **`tap_index`는 조인 키의 일부**(`live.setup_compare.setup_key`)이고 **`stop_price`는 손절폭
  그 자체**라, 캐시에서 읽은 행으로는 파리티 비교가 **성립 자체를 안 했다**(화면엔 「짝 0건」만
  — WAN-333이 고친 것과 **글자 그대로 같은 증상**, 원인만 다르다). 열을 넓히고 열 순서를
  `_ROW_COLUMNS` **한 곳**에서만 읽게 했다(INSERT·SELECT 두 벌은 열을 늘릴 때 한쪽만 고쳐
  값이 옆 칸에 들어간다). 왕복 무손실은 **행 전체 동등성**으로 고정한다.
- 📌 **캐시 버전 `wan297.1` → `wan335.1`** — 행의 의미가 바뀌었으므로 규약대로 올린다. 옛
  적재분은 **지문이 갈라져 자동 미스**이고 **지워지지 않는다**. ⚠️ **배포 뒤 한 번 전부
  되채워야 한다**(`alphablock trades --persist-cache --days N` — 런북
  [`docs/ops/wan239-nightly-timeline-cache.md`](docs/ops/wan239-nightly-timeline-cache.md)).
  옛 버전 셀은 **옛 엔진 판 폴백 후보에서도 빠진다**(WAN-325 §캐시 버전).
- 🚨 **옛 엔진 판(stale)은 기본 거부다 — `trades`의 `--no-stale`과 반대로 엄격이 기본이다.**
  이건 **파리티 측정**이라 엔진이 다른 두 판을 빼면 「집행 차이」가 아니라 **「엔진이 바뀐
  몫」**이 섞인다(WAN-325가 3열 대조를 끄는 것과 같은 이유). 완화는 `--allow-stale`이고 그때는
  **미스를 계산으로 메우지 않는다**(한 표에 두 엔진을 섞지 않는다). 캐시 무시 재계산은
  `--recompute` — 옛 동작이 그대로 도달 가능해야 두 경로 결과가 같은지 실제로 대조할 수 있다
  (회귀 테스트가 **렌더 본문 동등성**으로 고정 — 빨라지는 것이 숫자를 바꾸면 버그, WAN-203 선례).
- 📌 **미스일 때 몇 칸을 계산하는지 먼저 찍는다** · `--warmup-days`가 기본값과 다르면 지문이
  갈라져 **전 칸 미스**임을 화면이 밝힌다(조용히 느려지면 「왜 안 끝나지」가 반복된다).
  📌 **캐시와 도구의 워밍업 기본값은 실제로 같다**(둘 다 `None` → `DEFAULT_WARMUP_DAYS`) —
  이슈가 「확인 필요」로 남긴 자리의 답이다.
- 🚨 **§2 좁히기가 백테 쪽에만 걸려 정상 좁히기가 고장처럼 읽혔다** — 「라이브 셋업 60건 ·
  백테 3건 → 짝 없음 라이브 57」은 **48칸과 1칸을 비교한 값**인데, 하필 그 줄이 WAN-333이
  「배선 오류를 지목하려고」 넣은 것이라 PM이 **「백테가 20배 적게 본다」는 판정을 WAN-334에
  기록했다가 다음 실행에서 스스로 정정**했다. 이제 좁히기가 **양쪽에** 걸리고 인구조사가
  **분모도 밝힌다**(`라이브 셋업 1건(창 전체 2건 중 이 좌표)`).
- 📌 **§3(라이브 체결 분포)은 일부러 창 전체를 본다 — 의도된 비대칭이다.** 좁힌 TF를 주고도
  그날 체결이 어느 TF에 있었는지가 보이는 성질이 실사용에서 유용했다(`--tf 15m`을 줬는데 §3이
  1h까지 보여줘 체결이 두 TF에만 있음을 알았다). 좁히기는 **§1(조인)에만** 걸고, 좁혀 돌리면
  §3 머리에 그 사실을 찍는다(안 밝히면 다음 사람이 §3을 좁힌 표로 읽는다).
- ⚠️ 전부 `baseline`(낙관) 위 값 · 대조 백테는 per-cell 단일 포지션이라 북의 용량 제약이 없다
  (WAN-213/234 규약) · **「엣지 없음」(WAN-84/88/111/114/124/151/201) 불변** — 이 이슈는 도구가
  답을 낼 수 있게만 했다. 존폭 필터(1.28)·손절폭 가드(0.3%)는 **손대지 않았다**(WAN-159 ·
  WAN-76/79 소관 · 재-베이스라인 = 사용자 결정).

📌 **손절폭 가드 탈락률은 「라이브 대 백테」가 아니라 레짐·종목·존 두께가 정한다(WAN-328 =
`fills --day 2026-08-17` 실측 92%의 해부): §2 가설 (b) 기각 · 15m 탈락의 71.8%는 진입가를 어디에
잡아도 걸리는 얇은 존이다. 기본값·토대 불변.**
[`docs/decisions/wan328.md`](docs/decisions/wan328.md)(재현: `uv run python -m
backtest.wan328_stop_width_parity --timeframes 4h 2h 1h --jobs 4` → `--timeframes 15m --append`,
요약만 `--from-csv`). 서버 실측이 「체결 13건 중 12건(92%)이 손절폭 가드(0.3%)에 걸렸다」를 냈고
백테 값은 6년 평균 15m 38.5%(WAN-197)·44~46%(WAN-161)라 두 배가 벌어졌다 — 이슈는 그 격차를
라이브의 **틱 재산정이 진입가를 존 무효화 경계에 붙이는 것**으로 가설했다.
- 📌 **§2 판정 (b) 기각 — 틱 재산정은 진입가를 경계에 붙이지 않는다.** 엔진은 밴드 표본으로
  1분봉 **종가**를 쓰면서 터치는 **저가**로 판정하는데(롱), 라이브는 틱마다 둘을 **같은 가격**
  으로 굴린다. 같은 체결 봉을 틱 추종(`p ≤ 지정가(p)`의 고정점)으로 다시 풀면 **체결가 차이
  중앙값 0.00bp** · 가드 탈락률 15m **24.7% → 25.1%(+0.4%p)** 다. 54%p 격차를 설명할 크기가
  아니다. 📌 **오히려 반대 방향의 발견** — 「틱으론 그 봉에서 미체결」이 15m 체결의 **11.4%**
  라, 이 비대칭은 라이브 손절폭을 좁히는 게 아니라 **백테 체결 수를 늘린다**.
- 📌 **§1 갈리는 지점 = 존 자체** — 진입가는 존 안으로 클램프되고 손절 참조가는 존 무효화
  경계라 **손절폭 ≤ 존 높이 + 오프셋**이 항상 성립한다. 그 천장이 가드보다 낮으면 진입가를
  **어디에 잡아도** 걸린다: 가드 탈락 중 그 부류가 **15m 71.8% · 1h 29.2% · 2h 6.8% · 4h 0%**.
- 🚨 **애초에 비교가 성립하지 않았다** — 38.5%는 **6년 평균**이고 관측은 **저변동성 하루
  13건**이다. 같은 엔진 백테의 월별 탈락률이 **1h 2021-02 0.0% ↔ 15m 2023-09 67.0%**로 흔들리고
  2026년 15m만 봐도 **19.8~52.3%**, 2026년 **TRX 15m은 94.5% · BTC 65.5% · BNB 56.3%** 다.
  그날 거부 12건은 **백테가 가장 많이 거부하는 종목들(TRX·BTC·LTC·BCH)에 몰려 있었다.**
  ⚠️ **「라이브에 문제가 없다」는 뜻은 아니다** — 그날 구성을 백테 비율로 가중한 기대치는
  13건 중 약 6건(≈48%)이라 92%는 여전히 높다. 다만 12건 중 6건이 **35분 안**에 서로 다른
  종목에서 나 서로 독립이 아니고(암호화폐는 같이 조용해진다) **유효 표본이 13보다 작다**.
- 📌 **존폭 필터(1.28)와 손절폭 가드(0.3%)는 같은 양을 두 자로 잰다** — 필터는 ATR 상대,
  가드는 가격 상대로 **존의 두께**를 재므로 **ATR/가격 비가 낮은 저변동성 구간에서 겹친다**
  (탈락률과 존 천장 중앙값이 월별로 정확히 반대로 움직인다). 새 발견이 아니라 WAN-159 「알고
  받는 손해」·WAN-161 §사슬·WAN-154 §3(TRX 92.6%)·WAN-197이 적어 둔 자리의 실측 근거다.
- 📌 **잔차 판정은 서버에서만 난다 — 도구를 실었다**(`alphablock stop-width`,
  `live.stop_width_parity`). 로컬은 `live_limit_orders` **0행**이고 상위TF도 채택 창 끝
  (2026-07-22)에서 멈춰 사고 당일이 아예 없다(WAN-195/314와 같은 패턴). `--days N` = §3 최근
  N일 손절폭·거부 사유 분포 · `--with-backtest` = §1 같은 셋업 1:1 조인(귀속 `진입가`/`존/
  경계`/`동일`, 가드 판정이 갈린 줄은 🔴). 조인은 **존 정체성**으로만 하고
  `live.setup_compare`의 조인을 **재사용**한다(재진입 구제 조인 WAN-305 포함) — 짝이 안 지어진
  셋업은 존이 다르다는 뜻이라 손절폭 비교가 성립하지 않아 표에서 빠진다. ⚠️ `--days N`과
  `--with-backtest`는 함께 못 쓴다(백테 대조가 하루 단위 워밍업 규약에 묶여 있다).
- 📌 **엔진 추가는 옵트인 · 관측 전용** — `simulate_zone_limit_trade(observe_path_fill=)` +
  `ZoneLimitOutcome.path_fill_price`(`exit_extreme` WAN-276 · `mfe_r` WAN-90과 같은 부류).
  안 켜면 비트 재현이고, **켜도 체결가·상태·손익이 동일**함을 회귀 테스트가 고정한다(관측이
  대상을 바꾸면 그 순간 이 측정은 무효다). `probe_limit`은 `LiveLimitProvider` 필수 멤버가
  **아니라** 별도 `PathProbeProvider`다(필수로 넣으면 기존 라이브·테스트 공급자 전부가 측정
  전용 메서드 하나로 계약 위반이 된다) — 대신 켜 놓고 공급자가 안 내면 **거부**한다.
  `SetupDiagnostic`에 `zone_height`·`path_fill_price`가 붙었고 둘 다 진단 전용이다.
- ⚠️ **「가드를 낮추면 거래가 는다」로 인용 금지 — 손익을 재지 않았다.** WAN-154 §3이 손익으로
  본 것은 반대다(가장 좁은 손절 구간은 생존율 최고인데 `mean_net_r` −0.414R). 가드는 수익
  장치가 아니라 신뢰성 장치다(WAN-76/197). **가드(0.3%)도 존폭 필터(1.28)도 안 건드렸다** —
  전자는 WAN-76/79, 후자는 WAN-159 소관이고 **재-베이스라인 = 사용자 결정**이다.
- **측정·도구 전용 · 엔진 동작·기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()`)
  · DB 불변(WAN-194) · 전부 `baseline` 위 값 · **「엣지 없음」(WAN-84/88/111/114/124/151/201)
  불변** · 실거래 보류 유지(`ALPHABLOCK_LIVE_TRADING=false`).

📌 **백테 미체결 사유를 라이브와 같은 낱말로(WAN-339 = 사용자 관찰 2026-08-19 · §1만 · §2는
WAN-340으로 이관): 판정은 맞았고 어휘가 틀렸다 — 백테는 사유를 이미 들고 있는데(`diag.status`)
`filled` 참/거짓만 읽어 넷을 한 낱말로 뭉갰다. 기본값·토대 불변.**
[`docs/decisions/wan339.md`](docs/decisions/wan339.md). 화면 가운데 열은 **정확히** 「둘 다
미진입 · 매칭」을 찍고 있었다(SOL·15m·롱 04:53) — 문제는 좌우 낱말이 달라 보여(`무효화` ↔
`미체결`) **「매칭 30」이라는 집계를 못 믿게 되는 것**이다. WAN-333이 「짝을 못 지었는데 매칭이라
보고하던」 고장을 고쳤다면 이건 그 **잔여**(짝은 맞게 지었는데 **읽으면 틀려 보이는** 버전).
- 📌 **매핑 — 머리 낱말은 언제나 라이브와 같고 백테가 더 아는 것만 괄호로 덧붙인다**(사용자
  결정 1 · `건너뜀(존폭)`·`거부(사유)`와 같은 관행): `no_touch` → **`미체결`** ·
  `cancelled_expired` → **`미체결(만료)`** · `cancelled_invalidated` → **`무효화`** ·
  `cancelled_condition_failed` → **`조건취소`**. 세 `cancelled_*`는 `ZoneLimitStatus`와
  `LimitOrderStatus`에 **문자열까지 같이** 있어 두 시스템이 같은 사건을 같은 낱말로 낸다.
  📌 **`no_touch`와 `cancelled_expired`를 가르는 것이 요점**이다 — 라이브는 둘 다 `미체결`로
  뭉개므로(`_live_status`) 머리 낱말이 대칭을 지키고 `(만료)`가 24봉 유효기간이라는 **백테만의
  정보**를 살린다.
- 🚨 **미매핑은 조용히 흡수하지 않고 `?코드`로 찍는다** — 새 상태가 조용히 「미체결」로 빨려
  들어가면 이 이슈가 그대로 재발한다(라이브 폴백과 같은 규약). ⚠️ `FILLED_*`는 일부러 표에 안
  담았다(이 표는 `diag.filled`가 거짓인 셋업 전용이고 화면은 `BASELINE_FILL` 탈락률 0 고정이라
  올 수 없다 — 오면 드러나야 한다). 회귀 테스트는 **라벨 문자열이 아니라 동작으로** 건다:
  머리 낱말을 `_live_status`가 **실제로 내는 값**과 대조하므로 어느 한쪽만 어휘를 바꾸면 깨지고,
  열거형에 상태를 더해도 깨진다(돌연변이 확인 — 옛 동작으로 되돌리면 네 테스트가 깨진다).
- 📌 **판정 로직은 안 건드렸다 — 바뀐 것은 사람이 읽는 낱말뿐이다.** 매칭/갈림은
  `STATUS_BACKTEST_CLOSED`(진입했는가) 하나만 보므로(`setup_compare._bt_entered`) 집계·조인·
  손익이 **하나도 안 움직인다**. 실측(15m · 4일 × 2종목): 전부 `미체결`이던 3건이 `미체결` 2 ·
  `미체결(만료)` 1 · `무효화` 1로 갈린다(나머지 행은 불변).
- 🚨 **캐시 버전 `wan335.1` → `wan339.1`** — 라벨 문자열이 `timeline_cache_rows.status`에 그대로
  저장되므로 안 올리면 **옛 날짜는 「미체결」, 새 날짜는 「무효화」**로 한 화면에 두 어휘가
  공존한다. 📌 **배포 순서(사용자 결정 2): 이 PR 배포 → 그 다음 WAN-338 전량 되채우기** — 그러면
  두 번의 버전 상승을 **되채우기 한 번**으로 해소한다(뒤집으면 두 번 돈다). 옛 적재분은 자동
  미스이고 **지워지지 않는다**(WAN-194/297).
- **범위 밖**: **§2(사이징 0의 바닥 사유)는 WAN-340으로 이관**(per-cell 경로에 배선하면 북으로
  갈아탈 때 통째로 버려진다 — 북에는 `SkippedSetup`이 이미 있다) · 회계 모델(per-cell vs 북)도
  WAN-340 · 손절폭 가드(WAN-76/79) · 존폭 필터(WAN-159) · 조인 자체(WAN-333이 고쳤다).
- **표시층 전용 · 엔진·기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()`) ·
  DB 불변(WAN-194) · 백테 수익률은 `baseline`(낙관) 위 값 · **「엣지 없음」(WAN-84/88/111/114/
  124/151/201) 불변** · 실거래 보류 유지(`ALPHABLOCK_LIVE_TRADING=false`).

📌 **파리티 조인이 깨졌는데 측정기가 「일치합니다」로 보고했다(WAN-333 = 사용자 화면 2026-08-18):
조인 키는 멀쩡했고 고장은 둘 — ① `stop-width --with-backtest`가 조인에 **거래 행**을 먹여 백테
쪽 키가 통째로 `None`이었고, ② 화면·요약이 **짝 없는 줄을 「일치」로 셌다**. 기본값·토대 불변.**
[`docs/decisions/wan333.md`](docs/decisions/wan333.md). 표시 계층 + 진단 + 배선 수리다.
- 🚨 **①이 「짝 0건」의 원인이고 워밍업·좌표와 무관하다** — `cell_timeline_trades`(→
  `backtest_timeline_rows`)가 만드는 행에는 `zone_start_time`·`zone_confirmed_time`·`tap_index`가
  **인자로 넘어가지 않아**(전부 기본값 `None`) `setup_key`가 정의상 `None`이고, 모든 백테 행이
  orphan으로 떨어진다. 존 정체성을 싣는 것은 **셋업 행**(`cell_setup_timeline` →
  `backtest_setup_rows`) 하나뿐이다. 사용자가 `--warmup-days`를 늘렸다 줄였다 한 것은 이 고장을
  움직일 수 없었다. **`cmd_stop_width`가 셋업 행을 쓰도록 고쳤고**, 회귀 테스트가 **어느 함수가
  불리는지**로 고정한다(라벨이 아니라 동작).
- 🚨 **②가 이 이슈를 긴급으로 만든 것이다 — 고장이 「정상」으로 보고됐다.** 짝을 못 찾은 줄은
  한쪽이 비어 **양쪽 다 「미진입」**이 되고 옛 셈법 `matched = total − diverged`가 그것을
  **「일치」로 집계**했다(🔴 갈림도 안 뜬다). 요약을 셋으로 갈랐다: `matched`는 **짝지어졌고**
  판정이 같은 줄만 · `diverged`는 뜻 불변 · `paired`/`unpaired_live_only`/`unpaired_backtest_only`
  신설. ❌ **「둘 다 미진입 = 매칭」 규칙 자체는 안 건드렸다**(정상 동작 · 테스트가 고정) — 달라진
  것은 그 규칙이 **짝지어진 줄에만** 적용된다는 것뿐이다. 화면에 「짝 없음(대조 불가)」 카드·칩·
  주황 테두리가 서고, CLI는 **조인 인구조사**(양쪽 셋업 수 · 키 있는 수 · 짝 수)를 항상 찍으며
  한쪽 키가 통째로 없으면 **「배선 오류」로 지목**한다.
- 📌 **화면(3열 대조)의 조인은 실제로 동작한다 — 실데이터로 확인했다.** 로컬은
  `live_limit_orders` 0행이라(러너는 서버에서 돈다) 이 경로가 한 번도 안 돌았는데, **재시작 불변
  테스트의 러너 리그**(WAN-306)로 못 박은 창의 실제 1분봉을 재생해 페이퍼 장부를 만들고 같은
  하루를 `cell_setup_timeline`으로 돌려 키를 대조했다(2026-07-18·20 × BTC·ETH × 15m·1h): 키
  교집합 2/4/6/2로 **짝이 지어지고**, 짝지어진 줄의 손절폭 귀속은 **`동일`**이었다(0.409% =
  0.409%). ⚠️ **(b)「라이브와 백테가 다른 존을 본다」는 확인되지 않았다** — 서버 장부로 확정하는
  것이 §4다.
- 📌 **사용자가 본 빈칸은 「진짜 한쪽만 있는 줄」 + 「라벨이 가린 것」이었다**: 무효화 봉 안의
  탭(백테는 `status="cancelled"`로 떨어뜨리고 라이브는 탐지가 한 봉 늦어 그 안에서 체결한다 —
  `limit_engine` 독스트링이 이미 「알려진 근사」로 적어 둔 자리) · 슬롯 점유(라이브만
  `건너뜀(슬롯참)` 행을 남긴다) · **라벨**(`combine_obs=False`(WAN-149)에서 같은 시각에 겹친 두
  존을 탭하면 서로 다른 셋업 2건인데 라벨이 심볼·TF·방향·시각만 찍어 **복사된 줄처럼** 보였다).
- 📌 **라벨이 「어느 존의 몇 번째 탭」인지 밝힌다** — `BTC·15m·롱·09:00 · 존 07-19 15:00→07-20
  08:00 · 탭 1`. 존 정체성은 `(존 시작, 존 확정)`이라(`limit_engine.ZoneId`와 같은 정의) 두
  시각을 함께 찍으면 서로 다른 존은 반드시 다른 라벨을 얻는다. 회귀 테스트는 문자열이 아니라
  **「갈라진다」는 성질**로 고정한다. ❌ **행을 합치거나 지우지 않았다**(겹친 두 존은 실제로 다른
  셋업 — WAN-149 채택값).
- 📌 **`stop-width`에 `--symbol`·`--tf`가 생겼다(탐색용 옵트인)** — 48셀을 무조건 다 도는 것 말고
  수단이 없어 사용자가 실제로 막혔던 자리다. **기본값은 채택 좌표 전부 유지**(WAN-305 원칙)이고
  좁힌 표는 헤더가 **좌표를 밝힌다**. 일관성 점검으로 **`compare`에도 같이 넣었다**(`trades`는
  이미 있었다). ⚠️ WAN-324의 나머지(1분봉 로더 공유 · 48회→12회)는 **안 가져왔다**(별개 작업).
  ⚠️ **손절폭 잔차 판정은 좁히지 말 것** — 표본이 필요하고 좁힌 표는 그 좌표에서만의 결론이다.
- 📌 **「대조 백테는 per-cell 단일 포지션이라 북의 용량 제약이 없다」를 화면·CLI에 적었다** —
  페이퍼는 레버리지 북(공유 자본, WAN-213)이라 **「자리가 없어서」 못 들어간 셋업을 백테는 그냥
  들어간다**. 지금까지 그 사실은 엔진 판 이름 토큰 `단일포지션`에만 있고 **함의는 어디에도 없었다**
  (「라벨은 맞는데 함의가 안 보이는」 자리). ⚠️ 회계 모델을 바꾸자는 제안이 **아니다**(WAN-213/234
  규약 · 재-베이스라인 = 사용자 결정) — 근사의 **존재를 보이게** 하는 것까지다.
- ⚠️ **§4(서버 실행)는 남았다** — `uv run alphablock stop-width --day 2026-08-17 --with-backtest
  --jobs 4`(워밍업은 **기본값 120일**로). 그날 거부 건의 백테 손절폭 · 잔차 귀속 · 「사이징 수량
  0」 56건의 바닥 사유가 남은 완료 기준이다. 🚨 그 바닥 사유가 북 용량 계열
  (`notional_exhausted`·`capacity_cap`)이면 **WAN-328의 「레버리지 북의 용량 제약이 아니라 손절폭
  단독」 판정이 흔들린다**.
- **표시·진단·배선 전용 · 엔진 동작·기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()`)
  · 존폭 필터(1.28)·손절폭 가드(0.3%) **안 건드렸다**(WAN-159 · WAN-76/79 소관) · 전부 페이퍼 ·
  실거래 보류 유지(`ALPHABLOCK_LIVE_TRADING=false`).

📌 **짝 없는 셋업을 귀속시키는 도구 + 가격 축 「손상 vs 노이즈」(WAN-337 = WAN-334 잔여 ·
§2는 사용자 결정 2026-08-20 「337 하면서 같이보자」): 조인이 빼던 셋업을 처음으로 재고, 호가
한 칸짜리 가격차가 🚨로 찍히던 것을 갈랐다.**
[`docs/decisions/wan337.md`](docs/decisions/wan337.md). 도구·분류기 전용 — **엔진·전략·
기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()`) · DB 불변(WAN-194) · 새 격자
없음 · 실거래 보류 유지.
- 🚨 **§1의 판정 표는 아직 없다 — 서버 몫이다**(로컬 `live_limit_orders` **0행**). 이 PR은
  **답을 낼 수 있는 도구**를 실었고 표는 `alphablock stop-width --day … --with-backtest
  --unpaired`가 낸다(wan337.md §3). 실측 배경: 08-17 라이브 60 · 백테 49 · 짝지어짐 46 ·
  **짝 없음 라이브 14**(08-18도 14). 📌 **부호가 단서다 — 라이브가 항상 더 많이 본다**
  (= 라이브가 백테는 모르는 존을 알고 있다).
- 🚨 **유력 가설 둘이 착수 전에 이미 기각됐다 — 다시 시간 쓰지 말 것**: (1) **워밍업 존
  재고**(백테는 120일만 데우는데 러너는 존 기억을 유지, WAN-306) — 그날 60건 중 워밍업 창
  **밖이 0건**이고 최고령 존도 17일 전이다. 즉 CLAUDE.md가 적어 둔 「알려진·의도된 비대칭」이
  **아니라 설명되지 않은 차이**다. (2) **저장 봉 손상** — 판정 사흘의 15m 손상이 0/0/1봉이고
  그 1봉도 0.1bp다(`partial-bars` 실측).
- 📌 **부류는 셋이 아니라 넷이다 — (0)을 안 빼면 재진입이 결함으로 둔갑한다.**
  `(0) 키 없음` · `(a) 존 없음`(탐지 **입력**) · `(b) 확정 시각`(탐지 **로직**·봉 경계) ·
  `(c) 탭 순번`(틱 대 1분봉 = 알려진 비대칭 WAN-256). `setup_key()`는 **재진입 행에 일부러
  `None`을 내므로**(WAN-305) 그 부류를 안 빼면 채택 규칙대로 한 재진입 매매가 「탐지 로직
  결함 (b)」로 오분류된다 — 회귀 테스트가 동작으로 막는다.
- 🚨 **반대편 풀은 「짝 없는 것」이 아니라 「전부」다** — 라이브 탭 1이 짝을 못 지은 이유가
  「백테는 탭 0만 안다」인데 그 탭 0은 **이미 라이브 탭 0과 짝지어져 소비**돼 있다. 짝 없는
  것끼리만 대조하면 그 셋업이 (c)가 아니라 (a)로 떨어져 **후속이 통째로 뒤집힌다.**
- 📌 **기본 표는 안 바뀐다** — `--unpaired`는 **덧붙이는 옵트인 블록**이고 조인도 다시 하지
  않는다(`build_setup_comparisons`의 그 결과를 받는다). 짝 없는 셋업을 손절폭 표에 넣으면
  다른 존의 손절폭을 빼는 무의미한 Δ가 나온다(WAN-333이 고친 부류) — **제외는 옳은 설계**이고
  이 이슈는 「왜 짝이 없는지」만 따로 낸다. `--with-backtest` 없이 `--unpaired`만 주면 거부한다.
- 🚨 **§2 가격 축의 자는 절대 bp가 아니라 「틱 배수」다 — 두 축은 같은 순서로 정렬되지도
  않는다.** 로컬 15m 전 이력(6종목 · `price_only` 21건) 실측: **1틱짜리가 bp로는 0.0145 ~
  1.3286bp로 91배 흩어지고**(BTC 2024-10-28 ↔ SOL 2026-07-18 — 둘 다 정확히 1틱), 거꾸로
  **bp가 작은 쪽이 틱으로는 큰 경우**가 있다(BTC 2026-07-16은 0.2337bp인데 **15틱**). 즉
  **어떤 bp 문턱을 고르든 둘 중 하나는 반드시 반대로 찍는다.**
- 📌 **문턱 = 10틱**(`PRICE_NOISE_TICKS`) — 같은 21건이 `1(9건)·2(3건)·3·5`(호가 잔돈)와
  `12·15·36·41`·`289·314·475`(손상)로 나뉘고 **5와 12 사이가 유일한 빈 구간**이다. 중간 무리
  (12~41틱)가 손상으로 남는 것이 맞다 — 전부 WAN-327의 **2026-07-12~24 손상 무리 안**이고 전부
  `close` 필드다. ✅ **2024-03-27은 손상으로 남고**(475·289·314틱) 2023-09-12·2024-10-28·
  2025-08-29는 노이즈가 된다. ⚠️ **자를 느슨하게 해 경보를 줄이려는 값이 아니다** — 회귀
  테스트가 2024-03-27의 **실제 값**으로 「이 문턱이 그 손상을 지우지 않음」을 고정한다.
  **점검 항목은 불변**이고 **결과를 종료 코드·보고로 옮기는 법**만 바뀐다(WAN-327 §3 ·
  WAN-318 §3 · WAN-321과 같은 문장).
- 🚨 **호가 눈금은 「그 두 봉의 값」에서 읽는다(`infer_price_tick`) — 시리즈 단위 추정은
  틀린다.** 틱 크기가 시간에 따라 바뀌므로 6년 이력에서 뽑으면 SOL이 `0.0001`로 잡히는데
  2023-09의 SOL 값은 `17.979`(= `0.001` 눈금)라 같은 **2틱이 20틱으로** 부풀려진다. 📌 틀리는
  방향은 안전하다(추정이 실제보다 **고운** 쪽 → 손상으로 찍힌다).
- ⚠️ **2026-08-03·2026-08-19는 로컬에서 확인 불가**(로컬 15m이 2026-07-22에서 멈춘다).
  08-19 ETH(0.1bp)는 약 4.5틱이라 노이즈로 빠질 것이고, **08-03(0.9bp · 거래량 99.6%)은
  예측하지 않는다** — 🚨 손상으로 남아도 그게 맞을 수 있다(거래량도 함께 어긋난 봉이다).
  **문턱을 완료기준의 예상에 맞추지 않았다 — 자는 분포가 정한다.**
- ⚠️ **범위 밖**: 손절폭 가드(0.3%)·존폭 필터(1.28) **안 건드렸다**(WAN-76/79 · WAN-159 소관 ·
  재-베이스라인 = 사용자 결정) · 봉 수정·재수집(WAN-327 판정대로 거래소 재수집 · 사용자 수행) ·
  2024-03-27의 15m 백테 영향 측정 · 1h·2h 스캔. **「엣지 없음」(WAN-84/88/111/114/124/151/201)
  불변** · 전부 `baseline`(낙관) 위 값 · 실거래 보류 유지(`ALPHABLOCK_LIVE_TRADING=false`).

📌 **「존 없음」의 갈래를 가르는 자 + 가설 둘 기각(WAN-343 = WAN-342 판정 후속): 짝 없는
셋업의 76.5%인 `(a) 존 없음`은 **관찰이지 원인이 아니었다** — 후보 넷 중 「라이브 운영
이벤트」와 「탐지 컨텍스트 창」이 실측으로 기각됐고, 남은 갈래는 새 도구가 서버에서 가른다.
기본값·토대 불변.** [`docs/decisions/wan343.md`](docs/decisions/wan343.md)(도구:
`alphablock stop-width --day … --with-backtest --zone-audit`).
- 📌 **`(a) 존 없음`이 재는 것은 「같은 칸의 백테 **셋업 행**에 그 존 시작이 없다」뿐이다** —
  셋업 행이 없는 이유는 넷이고 **후속이 서로 완전히 다르다**: `창 밖`(대조 도구의 워밍업
  근사) · `존 미탐지`(**엔진 파리티 결함**) · `무효화 선행`(**문서화된 근사**) · `탭 기록
  없음`(탭 판정 축). **가르지 않으면 알려진 근사를 결함으로, 결함을 근사로 읽는다.**
- 🚨 **§2-3 「탐지 컨텍스트 창」 기각 — 창은 크게 다른데 탐지는 같다.** 라이브 러너는 확정
  상위TF 봉 **1500개**(`live_signal_lookback_bars`)를, 대조 백테는 **120일**치
  (`DEFAULT_WARMUP_DAYS`)를 탐지기에 먹여 창 길이가 TF마다 다르고 **방향까지 반대**다
  (15m은 백테가 7.7배 길고 **4h는 라이브가 3.5배 길다**). 그런데 로컬 실측(6종목 × 15m·1h·4h ·
  공통구간 존 **666개**)에서 불일치가 **6개(0.90%)** 이고 **전부 창 시작 +5~25봉**이다(워밍업
  경계 효과). 📌 **정체성만 같은 게 아니라 생애까지 같다** — 함께 잡힌 존의
  `break_time`·`swept_time`·`tapped_times` **불일치 0건**. 창 길이는 그 구간의 존을 만들지도
  지우지도 **더 일찍/늦게 죽이지도 않는다**. `tests/test_zone_detection_window.py`가 동작으로
  고정한다(이 검사가 죽으면 「라이브와 백테가 같은 엔진」이라는 파리티 전제가 흔들린다).
  ⚠️ **「창 차이가 아무 데도 안 난다」는 뜻은 아니다** — 창 **밖** 존은 당연히 한쪽에만 있고
  (4h는 라이브가 130일 더 본다) 그건 창 길이의 정의다. 도구가 `창 밖` 사유로 따로 센다.
  ⚠️ **러너 창·백테 워밍업 값을 바꾸자는 게 아니다** — 오히려 **바꿀 근거가 없다**(바꾸면 옛
  대조 표가 통째로 무효가 된다).
- 📌 **§2-1(무효화 시점)은 코드가 이미 자백해 둔 자리다** — `limit_engine.on_htf_bars`가
  *"백테스트는 `break_time`(무효화 봉 시작)에 취소하지만 라이브는 그 봉이 닫혀 탐지가 확인된
  지금이다 — **한 봉 늦는 알려진 근사**"* 라고 적어 뒀다. 그 사이에 라이브가 탭·체결하면
  백테에 없는 셋업이 생겨 **정확히 `(a)`로 보인다**. 직접 서명은 **「무효화 봉 = 라이브 탭 봉」**
  이고 도구가 그 건수를 따로 센다. ⚠️ 가설이지 판정이 아니다 — 서버 표가 정한다.
- 📌 **§1(라이브 운영 이벤트)도 기각** — 시각 클러스터 3개 중 러너 재시작과 겹치는 것은
  **1개뿐**이고 앞의 두 클러스터 15건은 세션 한가운데다(서버 실측 2026-08-20). ⚠️
  `data_gap_skips`는 대조하지 않았다(현재 상태 스냅샷) · 08-18 `10:04` **1건**은 재시작 몫일 수
  있다. 부수로 나온 「3.5일에 재시작 10회」는 **WAN-344**로 빠졌다 — **결론을 섞지 말 것**.
- 🚨 **크기(짝 없는 셋업 / 라이브 셋업) = 08-17 23.3% · 08-18 37.8%를 「파리티 대조가 그만큼
  틀렸다」로 읽지 말 것** — 짝 없는 셋업은 손절폭 표에서 **빠지고**(WAN-333이 그렇게 고쳤다)
  편향 점검 Δ가 0.000%p·−0.014%p로 작다. **WAN-334의 손절폭 판정은 안 뒤집힌다.** 이 값이 재는
  것은 「대조가 **못 보는** 셋업의 비중」이다.
- 📌 **도구 설계에서 지킨 것**: 다시 분류하지 않는다(`attribute_unpaired`가 낸 그 행을 감사 —
  두 벌로 갈라지면 같은 셋업이 두 블록에서 다른 부류를 얻는다, WAN-333/335) · **짝 없는 셋업이
  있는 칸만 굽고 몇 칸인지 먼저 찍는다**(48칸은 하루치 6분+, WAN-322/335) · **야간 캐시가 이
  계층을 대신하지 못한다**(캐시에 담기는 것은 셋업 **행**이지 존 대장이 아니다) · 대장을 못 받은
  칸은 `대상 아님`이다(빈 대장을 「미탐지」로 세면 **없는 파리티 결함**을 만든다, WAN-95 부류) ·
  **백테만 있는 행도 `대상 아님`**(러너는 존 대장을 영속화하지 않는다 — 재파생 가능한 값이라
  스냅샷에서 뺐다, WAN-306. 지어내지 않는다, WAN-194) · **부류 (0)은 감사하지 않는다**(재진입은
  설계상 무키 — 안 빼면 채택 규칙대로 한 재진입 매매가 「탐지 결함」으로 둔갑한다, WAN-305) ·
  **동률·과반 미달이면 한 사유로 닫지 않는다**(WAN-161 규약).
- 🔁 **그 서버 실행은 WAN-353이 냈고 판정은 `무효화 선행` 과반이다 — 아래 📌 문단이 정본**
  (읽는 법이 미리 못 박아 둔 대로 CLAUDE.md 문단으로 닫혔다 · 수정 이슈 없음). ⚠️ 실행 예시의
  `--jobs 4`는 **그대로 쓰지 말 것** — 서버는 2코어이고(WAN-354) `.env`의
  `ALPHABLOCK_BACKTEST_JOBS`가 이기게 두는 것이 지금 규약이다(WAN-356).
- **측정·도구 전용 · 엔진·전략·기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()` ·
  존폭 필터 1.28은 WAN-159 · 손절폭 가드 0.3%는 WAN-76/79 소관) · **DB 불변**(WAN-194) · 봉
  재수집(WAN-327/329 계열)은 **범위 밖이고 이미 배제됐다**(판정 사흘 15m 손상 0봉) · 전부
  `baseline`(낙관) 위 값 · **「엣지 없음」(WAN-84/88/111/114/124/151/201) 불변** · 실거래 보류
  유지(`ALPHABLOCK_LIVE_TRADING=false`).

📌 **판정: 라이브는 무효화를 「한 봉 늦게」 안다 — 짝 없는 라이브 셋업의 82.1%가 그
**알려진 근사**다(WAN-353 = WAN-343 §5 서버 몫 · 서버 실행 2026-08-22). 엔진 파리티 결함이
아니고 수정 이슈도 없다. 기본값·토대 불변.** 도구·읽는 법은 WAN-343(위 📌 문단), 실행 순서는
[`docs/ops/wan353-zone-audit.md`](docs/ops/wan353-zone-audit.md) +
`scripts/wan353-server-zone-audit.sh`. 좌표: 08-17·08-18 두 날 · 엔진 `eng:3fe56dd1c3a6` ·
두 날 모두 **캐시 적중 48/48칸**.
- 📌 **메커니즘 — `live.limit_engine.on_htf_bars`가 소스에 이미 자백해 둔 그것이다**:
  *"백테스트는 `break_time`(무효화 봉 시작)에 취소하지만 라이브는 그 봉이 닫혀 탐지가 확인된
  지금이다 — **한 봉 늦는 알려진 근사**"*. 그래서 **백테가 이미 죽인 존에 라이브가 주문을
  걸고**, 그 셋업이 대조에서 정확히 `(a) 존 없음`으로 보인다.
- 🚨 **결정적 증거는 과반이 아니라 이것이다 — 무효화 선행 23건이 「무효화 봉 = 라이브 탭 봉」
  0봉 차이 23/23 = 100%.** 과반(82.1%)은 표본에 따라 흔들릴 수 있어도 이 100%는 **메커니즘을
  직접 지목한다**. 실측 크기(사유별, 부록-2):

| 사유 | 08-17 | 08-18 | 합계 |
| -- | --: | --: | --: |
| **무효화 선행** | 12 | 11 | **23 (82.1%)** |
| 설명 안 됨 | 2 | 3 | 5 (17.9%) |
| **판정 대상** | 14 | 14 | **28** |
| 대상 아님(백테 쪽 행) | 3 | 3 | 6 |

- 📌 **`(b) 확정 시각 = 0건`(두 날 34건 전부)이 판정을 한 번 더 굳힌다 — 존 **탐지 로직**은
  갈리지 않는다.** 갈리는 것은 **무효화를 아는 시점** 하나다. WAN-343 §2(공통구간 존 666개 중
  불일치 6개 · 생애 필드 불일치 0건)와 같은 방향이다. 조인 키 조각별(부록-1): `(0) 키 없음`
  3 · `(a) 존 없음` **26** · `(b) 확정 시각` **0** · `(c) 탭 순번` 5 = 34.
- 🚨 **크기를 읽을 때 — 28건은 독립 28건이 아니다. 「하루 12건씩 샌다」로 인용 금지.** 두 날
  모두 **소수의 클러스터**가 건수를 만든다: **08-17은 12건 중 9건이 06:38~06:40 세 분 안**에
  몰려 있고(종목은 BTC·SOL·BCH·DOT로 분산 — 시장이 한 번 훅 움직여 여러 존이 **동시에**
  무효화된 순간) **08-18은 11건 중 7건이 TRX 한 종목**(00:12~01:00, **종목 편중**)이다. 사건
  수로 세면 훨씬 적고 **유효 표본은 이틀 · 수 클러스터**다. ⚠️ 이 판정 자체가 **이틀치**다.
- ⚠️ **5건(17.9%)은 이 근사로 설명되지 않는다 — 지우면 다음 사람이 100%로 읽는다.** 존도
  살아 있고 그 봉 탭 기록도 있는데 백테가 셋업을 안 냈다: 08-17 ETH 1h 05:52 · 08-17 XRP 15m
  16:45 · **08-18 BTC 15m 10:04**(= WAN-343 §1이 「재시작과 겹치는 유일한 클러스터」로 남긴 그
  1건 — **재시작으로도 무효화 선행으로도 설명되지 않는다**) · 08-18 ETH 15m 10:42 · 08-18 BTC
  15m 22:00. 📌 **되살릴 조건**: 날을 더 봤을 때 이 부류가 **비율로 뚜렷이 커지거나**(17.9%
  이상) **한 종목·한 시각에 몰리면** 별도 이슈로 뺀다. 지금은 5건 · 종목 셋(BTC 2 · ETH 2 ·
  XRP 1)이라 패턴이라 부를 근거가 없다.
- 🚨 **읽는 순서 — 도구가 판정을 **두 번** 찍는데 **부록-2가 정본**이다.** 부록-1의 얕은
  판정문은 `(a) 존 없음`이 과반인 것을 보고 *"데이터 파리티 결함이라 별도 이슈로 뺍니다"* 라고
  찍는데, **`--zone-audit`을 켜면 그 문장이 무효가 된다**(부록-2가 바로 그 `(a)`를 쪼개려고
  만든 자이고, 쪼개 보니 대부분이 알려진 근사였다). 이 저장소가 반복해 경계하는 **「라벨과
  동작이 어긋남」**(WAN-91/95/112/123/159) 자리다 — 지금은 사람이 읽고 거른다.
- 📌 **`대상 아님` 6건은 불일치가 아니라 설계상 측정 불가다** — 전부 백테 쪽 재진입 행이고,
  러너가 존 대장을 영속화하지 않아(WAN-306 — 재파생 가능해서 스냅샷에서 뺐다) 같은 자로 잴 수
  없다. 지어내지 않는다(WAN-194).
- 📌 **WAN-334의 손절폭 판정은 안 뒤집힌다** — 편향 점검(짝 있는/없는 라이브 셋업의 손절폭
  중앙값) **08-17 Δ 0.000%p**(6건 vs 7건) · **08-18 Δ −0.014%p**(2건 vs 4건). 「짝 없는 셋업이
  유독 얇은 존만 모여 있었다」는 우려는 지지되지 않는다. 짝지어진 셋업의 손절폭 잔차도 그대로
  없다(동일 6 · 진입가 1 · **가드 판정 갈림 0/7**). ⚠️ **08-18은 표본이 2건·4건이라 사실상
  아무 말도 못 한다** — 판정의 무게는 08-17에 싣는다.
- ⚠️ **`data_gap_skips`(WAN-314 §3) 대조는 아직 안 했다** — 현재 상태 스냅샷이라 판정 두 날
  당시 기록이 덮였을 수 있다. 스크립트 §5가 그 절을 내고, **없으면 「확인 불가」로 적는다**
  (지어내지 않는다). ⚠️ WAN-343 §1의 러너 재시작 기각은 이 대조 **없이** 난 판정이고, 그
  기각은 시각 클러스터 대조로 이미 섰다 — **두 개를 섞지 말 것**.
- 📌 **스크립트는 `--jobs`를 기본으로 넘기지 않는다** — 서버 `.env`의
  `ALPHABLOCK_BACKTEST_JOBS`(WAN-356)가 이겨야 하는데 `config/settings.py` 규약상 **명시적
  `--jobs N`이 환경변수를 이기기** 때문이다. 러너와 코어를 다투는 게 걱정될 때만 `-j 1`처럼
  명시한다. 📌 `--jobs`는 결과를 안 바꾸는 순수 성능 노브라(WAN-121) **판정은 어느 쪽이든 하나도
  안 움직인다**.
- **측정·도구 전용 · 엔진·전략·기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()` ·
  존폭 필터 1.28은 WAN-159 · 손절폭 가드 0.3%는 WAN-76/79 소관 · 러너 창·백테 워밍업 값도
  **바꿀 근거가 없다**, WAN-343 §2) · **DB 불변**(WAN-194 · 스크립트가 쓰는 단계는 야간 크론과
  같은 되채우기 하나뿐) · 봉 재수집(WAN-327/329 계열)은 이미 배제됐다(판정 사흘 15m 손상 0봉) ·
  전부 `baseline`(낙관) 위 값 · **「엣지 없음」(WAN-84/88/111/114/124/151/201) 불변** · 실거래
  보류 유지(`ALPHABLOCK_LIVE_TRADING=false`).

📌 **1분보다 잘게 보는 데이터 — 하루치 실측 도구(WAN-347 §0): 「1초봉 vs 체결내역」은 고를 수
있는 두 값이 아니었다 · 선물엔 1초봉이 없고 체결내역 REST는 최근 2일뿐이다. 판정은 서버 표
대기 · 기본값·토대 불변.** [`docs/decisions/wan347.md`](docs/decisions/wan347.md) · 런북
[`docs/ops/wan347-tick-probe.md`](docs/ops/wan347-tick-probe.md)(도구: `alphablock tick-probe`,
읽기 전용 · `data/tick_probe.py`). WAN-336이 잰 「채택 북 `oos_warm` 순손익의 약 48%가 진입한
그 1분 안 익절에 실려 있다」의 진값을 좁히려면 1분보다 잘게 보는 데이터가 필요한데, **모으기
전에 크기부터 재는 것**이 §0이다. ⚠️ **이 PR은 자(measuring stick)까지다** — 상시 수집기(§1)도
의심 구간 백필(§2)도 만들지 않았고 **프로덕션 DB에 새 테이블도 안 만들었다**(감당 여부가 결정
전이라 candidate 스키마는 스크래치에만).
- 🚨 **전제가 먼저 뒤집혔다 — USDⓈ-M 선물에는 1초봉이 없다**(REST `-1120 Invalid interval` ·
  아카이브 404). **1초봉은 현물 전용 상품**이라 그걸 고르면 *다른 시장의 가격*으로 선물의 봉내
  순서를 추정하게 된다 = 자료가 아니라 **대리변수**다. 표는 그 칸을 지우지 않고 **「없음」으로**
  남긴다(회귀 테스트가 동작으로 고정 — 조용히 빼면 다음 사람이 다시 조사한다).
- 🚨 **§2는 REST로 불가능하다** — 선물 체결내역 REST는 **최근 2일**만 준다(`-4166 Search window
  is restricted to recent 2 days only`). 일자별 아카이브(`data.binance.vision`)가 유일한 길이고,
  **채택 창 시작(2020-09-15)의 BTC·SOL 아카이브가 실재한다**(HTTP 200 실측). ⚠️ 두 사실은 주장이
  아니라 **거래소 응답 그대로**다(도구가 칸마다 한 번씩 찔러 표에 적는다 — 재현 가능).
- 📌 **PM 추정 「1초봉이 두 자릿수 작다」는 종목에 갈리고 한산한 쪽은 역전된다** — 체결내역 ÷
  1초봉(SQLite): **BTC 26.5배 · SOL 4.1배 · TRX 0.78배**. 구조적 이유: **1초봉은 거래가 없어도
  초마다 한 행이라 바닥이 고정**(종목·일 정확히 86,400행). **추정으로 골랐으면 틀렸다.**
- 📌 **담는 그릇이 자료 종류만큼 결정을 가른다** — BTC 하루 체결내역: zip 23.9MB → 최소열 gzip
  **13.9MB** → **SQLite 196.0MB**(행당 ~100B, 인덱스 포함). 12종목 환산 하루 ≈ SQLite **927MB**
  (1년 330GB) vs 최소열 gzip **70MB**(1년 25GB) — DB 4GB · doctor 전수 18분+ 박스에서 SQLite
  전량은 감당 밖이다(WAN-318 §1 · 저장으로 끝나지 않고 doctor·백업·`VACUUM`이 같이 는다).
- 📌 **그런데 우리가 물은 질문은 전량을 요구하지 않는다** — WAN-336의 **467건**에 필요한 것은 그
  **1분들**뿐이다. 분당 행 수(BTC 1,431 · SOL 232 · TRX 46) × 행당 ~100B면 **전부 BTC급이어도
  ≈67MB**(앞뒤 1분 여유 3배로 잡아 ≈200MB). **비싼 것은 「전부 계속」이지 「문제의 그 분들」이
  아니다.** ⚠️ 이 문단은 실측이 아니라 **실측에서 환산한 값**이다(분당 행 수는 하루 평균).
- 📌 **권고 = 순서를 뒤집는다: §2 먼저 · §1은 좁게(또는 나중에)** — §2는 자매 이슈 **WAN-348**
  (「같은 분 익절이 진짜인지 틱으로 직접 잰다 · 표본 100건」)의 데이터 공급이 된다. ⚠️ 사용자
  결정(「둘 다」)을 바꾸자는 게 아니라 **착수 순서**다. §1을 하면 최소 열 + 파일 보관이지 SQLite
  전량이 아니다. **채택은 사용자 결정**이고 **개발자 임의 착수 금지**.
- ⚠️ **표의 시간 두 열만 머신 값이다** — 행 수·바이트는 거래소가 준 파일과 결정적 산출이라
  머신 무관이라 **크기 판정은 이미 선다**. 서버 실행이 더하는 것은 **시간**과 **그 박스의 실제
  여유**다(로컬 값을 서버로 옮기는 것이 이 저장소가 두 번 틀린 실수 — WAN-203 · WAN-194).
- 📌 **부수 수리(한 글자)** — `alphablock --help`가 **통째로 죽어 있었다**(argparse가 help에 `%`
  서식을 적용하는데 WAN-328의 `가드 0.3%`가 `ValueError`를 냈다 · `origin/main` 재현). 회귀
  테스트가 **모든 하위 명령의 help를 실제로 렌더**한다(파서를 만드는 것만으로는 안 잡힌다).
- **측정·도구 전용 · 엔진·전략·기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()`) ·
  **DB 불변**(WAN-194) · 호가창(depth)·큐 우선순위는 범위 밖(**WAN-98은 Canceled 유지**) ·
  백테를 틱/1초봉으로 올리는 것은 **거대한 재-베이스라인**이고 별건 · ⚠️ **모은다고 백테 수치가
  바뀌지 않는다**(이 데이터는 얼마나 낙관인지 재는 **자**이지 엔진 입력이 아니다) ·
  **「엣지 없음」(WAN-84/88/111/114/124/151/201/248) 불변** · 실거래 보류 유지
  (`ALPHABLOCK_LIVE_TRADING=false`).

📌 **봉 안 절단 불변 테스트(WAN-377 = 사용자 요구 2026-08-25 「이전처럼 손절안하고 취소시키고
이런일은 절대없길 바란다」): 「그 시점에 알 수 있던 것만」으로 다시 돌려도 이미 끝난 셋업이 비트
동일한가 — 오늘 저장소는 초록불이고, 옛 엔진에서는 실제로 문다.**
[`docs/decisions/wan377.md`](docs/decisions/wan377.md)(재현: `uv run pytest
tests/test_wan377_intrabar_cut_invariance.py -q`). **테스트 전용 · 엔진 동작·기본값·토대 불변 ·
새 수치 없음**(`ConfluenceParams()`·`LeverageBookParams()` 그대로 · 실거래 보류 유지).
- 🚨 **기존 미래 누수 테스트가 WAN-364를 못 잡은 이유가 이 이슈의 출발점이다** — 자는 하나뿐이었고
  (`tests/test_harness.py::test_warm_oos_has_zero_future_leakage`, WAN-166) 그 절단 지점이 **상위TF
  봉 경계**라 봉이 원자적으로 들어가거나 빠진다. WAN-364는 **봉 *안*의** 룩어헤드였다(09:03 체결을
  09:11 무효화로 09:00까지 소급 취소) — 봉 끝에서 자르면 봉이 통째로 들어가 차이가 없고, 봉
  시작에서 자르면 비교 대상이 없다. **거래당 실력이 부호까지 뒤집힌 결함이었는데도 안 걸렸다.**
- 📌 **설계 (가)(사용자 결정): 상위TF 봉 N을 빼지 않고 T까지의 1분봉으로 재구성해 넣는다.** 봉을
  빼는 (나)는 성립하지 않는다 — 엔진이 「상위TF 봉 → 그 안의 1분봉 서브스텝」으로 걸어가므로 봉
  N이 없으면 그 안의 체결이 **평가조차 되지 않아** 불일치가 룩어헤드가 아니라 「봉을 뺐기 때문」이
  된다. ⚠️ 반쪽 봉은 `closed=True`로 넣는다(엔진이 확정봉만 걸어가므로 미확정으로 두면 조용히
  (나)가 된다) · `data.resample.resample_ohlcv`는 못 쓴다(부분 버킷을 설계상 거부한다).
- 📌 **픽스처는 1분봉에서 상위TF를 접어서 만든다** — 기존 합성 픽스처는 두 시리즈를 **다른 시드로
  따로** 만들어 상위TF 봉이 자기 1분봉의 집계가 아니다(그러면 「반쪽 봉」이 애초에 정의되지 않는다).
- 🚨 **돌연변이 확인이 이 이슈의 핵심 완료기준이다** — `invalidation_cancel="bar_open"`(옛 동작,
  옵트인 존치)에서 **실제로 실패한다**. 손으로 만든 무효화 봉 픽스처(상위TF 골격은
  `tests/test_order_blocks.py::_BULL_BARS`와 같은 시나리오)가 「+3분 체결 → +5분 익절 → **T=+8분
  절단** → +9분 존 하단 이탈」을 만든다: 전체 데이터에서는 소급 취소가 그 거래를 지우고 절단판에는
  살아 있다. 📌 **지워지는 것이 하필 익절 거래다** — 소급 취소는 「지는 거래만」 지우는 규칙이
  아니라 *「그 봉이 어떻게 끝나는지 알아야 내릴 수 있는 결정」*이라 방향과 무관하게 룩어헤드다.
- 🚨 **이 자가 지키는 범위(인용 시 함께 읽을 것)**: ✅ **셋업 층**(진입시각·진입가·청산시각·
  청산가) · ✅ **존 대장**(반쪽 봉이 유령 존을 안 만드는지, 절단 지점마다 `cut ⊆ full`) ·
  ⚠️ **시퀀서는 범위 밖**(정본 시퀀서가 같은 시각 경합을 **청산 시각 = 미래 정보**로 가른다 —
  WAN-181이 「어떤 온라인 집행도 재현 못 하는 바닥 잡음, 6년 창 경합 19회·+0.92%p」로 이미 기록한
  **별개 부류**). 그래서 거래 층은 **「차이가 나도 셋업 층까지 내려가지 않는다」**로만 걸고, 제외한
  부류는 조용한 구멍으로 두지 않고 그 기전까지 테스트가 못 박는다 · ❌ 데이터 준비 단계 누수나
  「뒷구간 보고 파라미터 고르기」는 다른 부류다 · ❌ 합성 픽스처 전용(절단마다 재실행이라 실데이터
  6년 격자는 안 돈다 — 실데이터 축은 WAN-364 §6이 갖고 있다).
- 📌 **앞으로 지켜 주는 자리** — WAN-372(MACD를 봉 안에서 계산)·WAN-376(`mid` 진입가·후처리
  필터)이 둘 다 「봉 안에서 값을 정하는」 코드를 더한다. **기존 `test_warm_oos_has_zero_future_leakage`
  는 고치지도 지우지도 않았다**(따뜻한 OOS 경계를 지키는 다른 자 — 나란히 둔다).

📌 **확인 진입 Phase 1 관문(WAN-383 §0·§1 = 사용자 설계 2026-08-27): 세 팔 다 살아남았다 —
§1이 답한 것은 「확인 규칙이 버는 돈의 대부분을 버리는가」 하나이고 답은 **아니다**(15~34%).
손익(§3)은 안 쟀다 · 기본값·토대 불변.**
🔁 **그 §3은 WAN-386이 냈고 판정은 (b)다 — 세 팔 다 기준 팔보다 나쁘고 `2 − C`가 음수다**
(아래 📌 WAN-386 문단이 정본). ⚠️ **§1의 「세 팔 다 생존」을 「확인 진입이 낫다」로 인용
금지** — 그 관문은 *「버는 돈의 대부분을 버리는가」* 하나만 물었고, 손익은 반대로 나왔다.
[`docs/decisions/wan383.md`](docs/decisions/wan383.md)(재현: `uv run python -m
backtest.wan383_confirmation_entry --jobs 4`, 파일럿 `--pilot`, 요약만 `--from-csv`). 묻는 것은
*「존에 닿자마자 말고 「떨어지다 멈췄다」가 확인된 뒤에 들어가면 나은가」*다(거래의 95.8%가
진한 빨강에서 진입하고 그 뭉치가 −0.14R로 진다 — WAN-372).
- 📌 **§0 엔진 — `P*`(진한 빨강을 벗어나는 최소 현재가)는 순수 함수다**
  (`strategy.realtime_macd.strong_red_exit_price`). 봉내 라이브 히스토그램이 현재가의 **순증가
  아핀 함수**라 해가 유일하고, **직전 확정봉 상태만** 쓰므로 룩어헤드가 없다. 🚨 **임계값이
  `hist[1]`의 부호에 따라 갈린다** — `hist[1] < 0`이면 `h*=hist[1]`(연한 빨강으로), `≥ 0`이면
  `h*=0`(초록으로). **이슈 본문의 닫힌 식은 앞 갈래만 적어 두었고**, 뒤 갈래를 빠뜨리면 그
  자리에서 트리거가 영원히 안 온다. 회귀 테스트가 두 갈래를 `P*` 바로 아래/위의 **색**으로 건다.
- 📌 **관측은 시뮬레이터 밖에서 한다** — `simulate_zone_limit_trade`가 **청산에서 멈추는데**
  §1이 묻는 것이 「트리거가 왔을 때 기준 팔은 이미 나갔는가」라 청산 **이후**까지 봐야 한다.
  `zone_limit_backtest.scan_confirmation`이 탭부터 **존 무효화까지** 한 번 더 훑는다(대기 길이는
  파라미터가 아니라 「존 무효화까지」 하나 — 사용자 확인). 팔 `C`의 오프셋은 팔 `2`의 평균
  거리라 미리 못 박으므로 **신고점 사다리**(1bp 해상도)를 실어 격자를 두 번 안 돈다.
  **순수 관측이라 켜도 후보·체결·손익·북 배치가 비트 동일**하고(실데이터 고정) base·재진입
  **양쪽**에 걸린다(WAN-345 부류 — 테스트가 재진입 후보에 **실제로** 붙었는지로 건다).
- 📌 **§1 판정(`oos_warm` · 14,843거래 · 기준 팔 −0.1207R)**: 「이미 익절한 뒤 트리거」의
  **이익 비중**이 팔 `1` 33.6% · 팔 `2` **15.3%** · 팔 `C` 27.7%로 전부 선(50%) 아래 → **세 팔
  다 생존**. 도달률 51.1/66.4/52.2% · 손절폭 배수 1.55/**1.39**/**2.12**배 · 창 닫힘 99.9%.
- 🚨 **가장 큰 발견 — 손실이 「트리거 안 옴」에 몰려 있다**: 그 뭉치가 거래당 **−0.84~−0.95R** ·
  승률 8.9~12.8%이고 기준 팔 **손실의 50.9~74.0%**를 차지한다. WAN-114/123의 RSI 게이트 실패
  모드와 **정반대**다. ⚠️ **단 「확인이 좋은 거래를 고른다」로 읽지 말 것 — 상당 부분이 정의상
  그렇다**(가격이 안 오르면 ① 트리거가 안 오고 ② 그 거래는 손절로 끝난다 = **한 사건의 두
  얼굴**). 그 상관을 가르는 유일한 방법이 **실제로 늦게 진입해 손익을 재는 것**(§3)이다.
- ⚠️ **이슈가 걱정한 채널은 실측으로 확인됐다** — 확인을 기다리면 손절폭이 **1.39~2.12배**가
  된다. 15m 손절폭 중앙값 0.458%(WAN-328)인데 팔 `C` 트리거는 진입가 **+1.026%** 위라 고정
  1.5R 목표(위 0.687%)를 **이미 지나친 자리에서 산다**. 팔 `2`는 +0.307%로 PM 어림(+0.37%)과 맞다.
- 🚨 **팔 `2`의 25.3%는 반등 확인이 아니다** — `P*`가 이미 현재가 아래라 가격이 하나도 안
  올랐는데 걸린다(시그널선 따라잡기 = **EMA 산수**, 네 TF에서 23.5~26.3% 고름). 그래서 그
  부류의 진입가를 `P*`가 아니라 **`max(P*, 현재가)`** 로 잡는다(없는 가격 이점 금지, WAN-370).
- ⚠️ **팔 `C`의 「같은 1분」이 크다**(트리거의 27.2% = 2,105건) — 이 구현은 그것을 **확인 팔에
  유리한 쪽**(「아직 들고 있음」)으로 넣었으므로 반대 극단에서는 팔 `C`의 이익 비중이 27.7%보다
  **크게 오른다**. 진값은 두 극단 사이다(WAN-336). 팔 `1`·`2`는 59·565건이라 거의 안 흔들린다.
- 📌 **§3(Phase 2 손익 격자)은 이 PR에 없다 — 규모 때문이고, 안 한 것을 밝힌다.** ① 이 PR이
  만든 것은 **관측**이지 **팔**이 아니다(진입 시점·진입가·주문 종류가 다른 새 진입 경로 배선이
  필요) · ② **실측 비용**: 이 실행(채택 북 한 팔 + 관측)이 **18,609초 = 5시간 10분**(48칸 ·
  `--jobs 4` · M1)이라 팔마다 후보를 다시 만드는 3팔이면 **≈15시간** · ③ **북은 이어붙일 수
  없어**(WAN-316) 팔 단위로만 쪼갠다. 🚨 **이 5시간 10분이 이 모듈의 정본 비용**이고 다른
  모듈 값을 옮기지 말 것(이 이슈에서도 WAN-316의 「15m 칸 ~16분」을 옮겼다가 **실측 76분/칸**
  으로 교정했다 — WAN-203 → WAN-312와 같은 실수). ⚠️ 파일럿(37분 미완)은 **테스트 스위트와
  CPU를 나눠 쓴 값**이라 인용 금지(WAN-330 규약).
- 📌 **§3 우선순위(§1이 넘기는 관측)**: 팔 `2`가 세 축(도달률 최고 · 이익 손실 최소 · 손절폭
  증가 최소)에서 같은 방향이라 첫 후보 · **`2 − C`가 진짜 답**(MACD가 실제로 더한 값,
  WAN-131 통제) · 팔 `2`의 「상승 없이 발동」 25.3%를 갈라 보는 것이 핵심.
- ⚠️ **딱 하나 똑같이 못 하는 것 — 진입 수수료**: `기준`은 메이커 2bp(지정가), `1`·`2`·`C`는
  **테이커 4bp + 슬리피지 5bp**(올라갈 때 발동하는 트리거라 주문 종류가 물리적으로 다르다).
  §3에서 정직하게 물린다 — 이 이슈가 지는 가장 흔한 방식이 「비용을 싸게 잡는 것」이다(WAN-370:
  손절폭 0.3~0.4% 구간은 이미 거래당 **0.46R**을 비용으로 낸다).
- **측정 전용 · 기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()` 그대로 · 관측은
  전부 옵트인 · 끄면 비트 재현 · 핀 없음 WAN-305) · 전부 `baseline` 위 값 · 판단은 북에서
  (WAN-341) · **「엣지 없음」(WAN-84/88/111/114/124/151/201/248) 불변**(이 축은 *어느 시점에
  들어가나*를 묻지 *진입 규칙이 무작위와 구분되는가*를 묻지 않는다 — **다른 질문**) ·
  실거래 보류 유지(`ALPHABLOCK_LIVE_TRADING=false`).

📌 **확인 진입 손익 격자(WAN-386 = WAN-383 §3 · 가드 축은 사용자 결정 2026-08-28): 판정 (b) —
확인을 기다리면 나빠지고, **MACD는 「그냥 정해진 만큼 오르면 산다」를 못 이긴다**(`2 − C`가
음수). 채택 권고 없음 · 기본값 불변.**
[`docs/decisions/wan386.md`](docs/decisions/wan386.md)(재현: `uv run python -m
backtest.wan386_confirmation_pnl --jobs 4`, 파일럿 `--pilot`, 요약만 `--from-csv`, 독립 경로
검산 `--checksum-book`). 좌표는 채택 그대로(12종목 × 4TF 한 지갑 · 못 박은 6년 · 존폭 필터 끔
WAN-384 · cap_only 5배 · 재진입 band · 익절 메이커 2bp · 인과 취소 · **핀 없음**).
- 📌 **§0 엔진 — 관측(WAN-383)에서 「실제로 늦게 진입하는 팔」로**: `substep.
  simulate_fixed_entry_exits`(확정 진입을 **여러 익절 목표로 한 순회에** 청산 판정) ·
  `_Candidate.entry_liquidity`(기본 메이커 = 비트 동일) · `backtest/confirmation_arm.py`(트리거
  판독 + 후보 변환) · `run_cells(confirmation_arms=, confirmation_multiples=)` →
  `CellPayload.arm_candidates`. **안 켜면 채택 북이 비트 재현된다.**
- 📌 **후보 생성이 「팔마다 한 번」이 아니라 통째로 한 번이다** — 팔 변환은 후보를 **새로
  만들지 않고**(같은 탭·같은 존·같은 손절 참조가) 진입 시각·진입가·주문 종류·청산만 갈아끼우고,
  배수는 청산만 바꾸며(WAN-137/143 훅) 가드는 배치 축이다(WAN-197). 실측 **17,386초 = 4시간
  50분**(48칸 · `--jobs 4` · M1)이고 그중 후보 생성이 16,833초 · **배치 60조합 + LOO 52판 +
  검산이 553초**다. ⚠️ 이슈 견적은 ≈15시간이었다. **다른 모듈에 이 값을 옮기지 말 것**
  (WAN-203 → WAN-312 · WAN-383이 세 번 데인 자리) · **파일럿 값(BTC 4h 단칸 273초) 인용 금지**.
- 📌 **주 수치(`oos_warm` · 가드 0.30% · 채택 배수 1.5R · 거래당 net R)**: `기준` **−0.1194**
  (14,825거래) · `1_봉마감` −0.1374(6,701) · `C_고정오프셋` −0.2106(6,826) · `2_교차`
  −0.2352(9,233). **판정 줄**: `1 − 기준` −0.0180 · `2 − 기준` −0.1158 · 🚨 **`2 − C`
  −0.0246**.
- 🚨 **`2 − C`가 이 이슈의 진짜 답이다** — 팔 `C`는 팔 `2`의 평균 거리를 그대로 고정 오프셋으로
  쓴 통제군인데(오프셋 **1.026%** = WAN-383 §1 실측을 **입력으로 못 박음**) 채택 배수에서 팔
  `2`가 그 통제군보다 **나쁘다**. WAN-131의 *「볼린저 기여의 84%가 선별이 아니라 그냥 가격」*과
  **같은 방향**이고, WAN-114/123의 「모멘텀 확인이 쳐낸 쪽이 오히려 좋았다」에 사례가 하나 더
  붙는다. ⚠️ 높은 배수(2.5·3.0R)에서 부호가 바뀌지만 **잡음선(±0.005R) 언저리**라 읽을 것이 없다.
- 🚨 **비용만의 이야기가 아니다 — gross에서도 진다**(수수료·펀딩 전 `기준` **−0.0421** <
  `1` −0.0729 < `2` −0.1580 < `C` −0.1736). 수수료를 0으로 만들어도 **순위가 그대로다** — 확인
  진입이 진 것은 테이커 비용이 아니라 **진입가가 나빠졌기 때문**이다(WAN-370의 「비용 0에서도
  천장 ＋0.09R」과 같은 자리 — 🔁 그 「+0.09R」은 **WAN-396이 +0.007R로 정정**했다(허수 진입
  슬리피지 · 필터 끔 좌표는 +0.002R).). ⚠️ 슬리피지는 체결가에 녹아 있어 이 열에서
  안 빠진다.
- 📌 **§1의 「손실 회피」는 손익으로 갚아지지 않았다** — WAN-383 §1이 *「확인이 건너뛰는 거래가
  정확히 지는 거래다」*(안 온 뭉치 −0.84~−0.95R · 기준 팔 손실의 51~74%)라 했지만, 늦게 사면
  손절폭이 1.39~2.12배가 되고 거래가 절반 이하로 줄어 슬롯·자본이 재배치되며 테이커를 문다.
  §1이 스스로 경고한 *「한 사건의 두 얼굴」*(가격이 안 오르면 트리거도 안 오고 그 거래도 손절)이
  **값으로 바뀌지 않았다**는 것이 §3의 답이다.
- 📌 **드물게 강건하다** — 네 구간(`full`·`is`·`oos_warm`·`oos`) 전부 `기준` > `1` > (`2`·`C`) ·
  가드 3점에서 **1등이 안 흔들린다**(뒤집히는 건 꼴찌 둘의 자리) · **leave-one-out 4팔 전부
  부호 유지**(ETH·SOL 편중 계열 WAN-111/119/124/151이 여기서는 없다). IS→OOS 배수 뒤집힘은
  2/4팔이고 **argmax는 채택 근거가 아니다**(WAN-161).
- 🚨 **가드 교란은 실측으로 거대한데 판정을 못 뒤집었다** — 채택 배수에서 가드가 자르는 셋업이
  `기준` **17,803~37,148건**인데 `C_고정오프셋`은 **0건**이다(확인 팔은 손절폭이 넓어 가드가 할
  일이 없다 — 이슈가 착수 전에 지목한 불공정 그대로). **그 교란이 확인 팔에 유리한 방향인데도
  기준 팔이 이긴다.** ⚠️ **「그러니 가드를 올려라」로 인용 금지**(WAN-76/79 소관 · 재-베이스라인).
- 🚨 **지갑 층 열은 이 좌표에서 뜻을 잃었다** — 복리를 꺼도(사이징 초기 자본 고정) **잔고가 0을
  뚫는다**(`oos_warm` 기준 팔 −1,106% · 최악 −1,554%). 자본이 음수를 지나면 「자본 대비 비율」
  (MDD·수익/MDD·동시 리스크·실효 리스크·청산)은 분모가 부호를 바꿔 무의미하므로 요약이 **비율을
  내지 않고 「정의 상실」로 찍는다**(WAN-115 부호 함정 관행의 이 축 판 · 회귀 테스트가 「오해를
  부르는 비율이 본문에 안 나온다」로 고정). **읽을 수 있는 것은 거래당 net R · 승률 · 거래 수 ·
  최대 동시 칸뿐**이다. ⚠️ **팔의 성질이 아니라 좌표의 성질**이다(WAN-378 「108팔 전부 음수」).
- 📌 **검산 전부 비트 일치(`0.00e+00`)** — (a-1) 기준 팔 후보 ≡ 엔진 base+재진입(48칸 × 4구간 ·
  청산까지 · 불일치 칸 0) · (a-2) 기준 팔 지갑 ≡ 채택 북 지갑(복리 켬 · 4구간 × 6지표) ·
  (b) 같은 팔의 다섯 배수는 진입 집합 비트 일치 · (c) 확인 팔 전부 테이커/기준 팔 전부 메이커를
  **라벨이 아니라 후보의 값으로**. ⚠️ **독립 경로 검산(후보 재생성)은 옵트인이고 안 켰다** —
  요약이 그 사실을 밝힌다.
- ⚠️ **알려진 한계 — 재무장 일정(재진입)은 기준 팔의 것을 쓴다.** 재진입 후보는 base 후보의
  per-cell 시퀀싱에서 나오므로(WAN-261) 팔마다 다시 파생하면 재무장 **시점**까지 팔에 따라
  움직여 이 표가 재는 축이 둘이 된다(이슈 설계 「셋업·나머지 전부 동일」). **잔여는 WAN-387**.
- 🚨 **「그럼 기준 팔이 좋다」로도 읽지 말 것** — 기준 팔도 **−0.1194R**이다. 이 표가 고른 것은
  **가장 덜 나쁜 팔**이지 흑자가 아니다.
- **측정 전용 · 기본값·토대 불변**(`ConfluenceParams()`·`LeverageBookParams()` ·
  `min_stop_distance_fraction=0.003` 그대로 · 확인 팔은 전부 옵트인 · 끄면 비트 재현) ·
  전부 `baseline` 위 값 · 체결 보수화(`pen_5bp`)는 범위 밖 · 판단은 북에서(WAN-341) ·
  **「엣지 없음」(WAN-84/88/111/114/124/151/201/248) 불변**(*어느 시점에 들어가나*를 묻지
  *진입 규칙이 무작위와 구분되는가*를 묻지 않는다 — **다른 질문**) · 실거래 보류 유지.

📌 **체결 ≠ 진입 — 「체결됐는데 포지션이 없다」는 손상이 아니라 정상 동작일 수 있다
(WAN-194)**: [`docs/decisions/wan194.md`](docs/decisions/wan194.md). 지정가 체결
(`live_limit_orders`)과 페이퍼 진입(`open_positions`) 사이에는 집행 계층의 거부 관문이
있다 — **손절 거리 < 0.3%**(`min_stop_distance_fraction`, WAN-79) · 명목 상한 · 이미
오픈 포지션 · 리스크 한도. **백테스트도 같은 자로 거부하므로**(`_to_trade`의 `qty <= 0`
→ 후보 폐기) 거부 자체는 버그도 파리티 위반도 아니다.
- 🚨 **버그였던 것은 그 거부가 아무 곳에도 안 남은 것이다** — DB에는 `filled`만 남고
  텔레그램은 조용히 반환, 로그는 INFO 한 줄이었다. 그래서 정상 거부가 **DB 손상과 글자
  그대로 같은 모양**이었고(WAN-194가 손상 의심으로 열린 이유) 사용자 질문에 장부가 답을
  갖고 있지 않았다. 이제 `entry_status`(`entered`/`rejected`) + `entry_reject_reason`이
  남는다.
- 📌 **`filled` + `entry_status IS NULL` = 「진짜 유실」의 고유 서명이다** — 러너가
  체결 기록과 포지션 쓰기 **사이에서** 죽으면 그 모양만 남는다(두 쓰기는 원자적이지
  않고, 이 이슈는 유실을 없앤 게 아니라 **보이게** 했다). ⚠️ WAN-194 이전 체결은 열이
  없어 전부 NULL이라 **판별 불가**다 — 창을 자를 것(`--orphans-since`).
- 📌 **거래가 되는 비율 = 체결률 × 진입%**(`fill_report`가 두 열을 병기). "체결률 81%"
  (WAN-123/124)를 거래 성립률로 오독하지 말 것. ⚠️ **옛 체결률 수치가 무효화된 건
  아니다**(백테스트도 같은 가드로 후보를 버린다) — 새로 보이게 된 것은 그 손실의 크기다.
- 📌 **`alphablock doctor`(신설)** — DB 무결성·위생 점검 한 줄(`PRAGMA quick_check` ·
  테이블 인구조사 · `.recover` 산출물 `lost_and_found` · 처분 미기록 체결 · 공간·WAL·
  디스크). 이상이면 **종료 코드 1**이라 cron·감시에 물릴 수 있다. **자동 VACUUM·자동
  복구는 의도적으로 없다**(전자는 DB 독점 락 · 후자는 "누가 복구했는지 모르는 DB"를
  저장소가 스스로 만들게 된다). 3.5GB DB 실측 12초.
- **기본값·토대 불변** — 손절폭 가드 0.3%는 **손대지 않았다**(WAN-76/79 소관 · 변경은
  재-베이스라인 = 사용자 결정). 실거래 보류 유지.

📌 **손상 타임라인은 07-22였고 「283만 행 쓰레기」의 절반은 유일본 캔들이었다(WAN-195 =
WAN-194 잔여 §1·§4)**: [`docs/decisions/wan195.md`](docs/decisions/wan195.md). 서버 실행
이슈로 스핀아웃됐지만 **로컬 증거만으로 §1·§4가 닫혔다**. 두 전제가 뒤집혔다.
- 📌 **복구 시각 = 2026-07-22 22:38:48 KST**(로컬 `ohlcv.db`의 birth). 근거는 mtime이 아니라
  **SQLite change counter**다 — 복구본은 **101**(새로 만들어진 파일 = `.recover` 산출물,
  `lost_and_found`를 갖고 태어났다)이고 손상 직전 DB는 **16,519**(오래 산 DB)다. 앞뒤가 맞는다:
  손상 직전 DB의 마지막 봉 22:33 · 마지막 세션 22:26 → 복구본 생성 22:38 → 다음 세션 22:47.
- 🚨 **로컬 `.corrupt.bak`(07-26 01:09)을 「사고 시각의 단서」로 인용 금지** — 둘(`.bak`·`.old`)은
  **byte 동일**이고 4초 간격 연속 복사다. **파일 시각과 내용 시각이 3.5일 다르다**(내용은
  07-22 22:33에서 멈춤). WAN-194 본문의 그 추정은 이것으로 교정된다.
- 📌 **§5 최유력 벡터가 바뀌었다 — FUSE 마운트다.** `data/`의 `.fuse_hidden*` 23개는 SQLite
  **`-shm`(WAL-index)** 파일이고(매직 `18e2 2d00`) birth가 전부 01:09:01이다 = 그 시점
  `data/` 위에 **FUSE 파일시스템이 마운트돼 있었고 SQLite 연결 23개가 열려 있었다**.
  네트워크·FUSE fs 위의 SQLite는 advisory locking이 깨져 **손상의 교과서적 벡터**다.
  ⚠️ WAN-194가 꼽은 「07-24 쓰기 도중 강제종료」는 **복구 시각보다 뒤라 원인일 수 없다**.
  **「로컬 `data/`가 서버 마운트인가」의 답: 지금은 아니다(로컬 디스크), 그때는 마운트였다.**
- 🚨 **`lost_and_found` 283만 행의 절반은 쓰레기가 아니다** — 9필드 145만 행이 **`ohlcv` 5m
  캔들**(6심볼 · 2023-07-11~2026-07-18)인데 **살아 있는 `ohlcv`의 5m은 0행**이다. 즉 "손상으로
  사라졌다"던 5m 데이터셋은 사라진 게 아니라 **갇혀 있었다**. 나머지 138만 행은 그 PK 인덱스
  조각이다. 복원 실측 1,445,500행 · 품질 검사 전부 통과(OHLC 위반 0 · 정렬 위반 0).
- 📌 **매매 장부는 여기서 복원할 수 없다 — 「없다」가 아니라 「담길 수 없다」.** `.recover`는
  가장 넓은 고아 행에 맞춰 `c0..cN`을 만드는데 이 산출물은 **9열**이다. `paper_trades`(17열)·
  `open_positions`(10열)는 **구조적으로** 못 들어간다. 복원 소스는 **로컬 스냅샷의
  `paper_trades` 2행**(BTC 1h, 07-13·07-14)이 유일하고, 그마저 07-14까지다.
- 🚨 **WAN-194의 `--drop-recovery-artifacts`에는 실제 위험이 있었다** — 행 수만 보여 주는
  리포트 + 무조건 드롭이라 **유일본 5m 145만 행을 말없이 지웠을 것이다**. 그래서
  `census_recovery_artifacts()`(안을 TF별로 분해 · 유일본/중복 구분) · `could_contain()`(열 수
  논증) · **`--salvage-ohlcv`**(복원, 기존 행은 절대 안 덮어씀) · **드롭 가드**(유일본이 남으면
  거부 + 종료 코드 1, `--force`로 무시)를 넣었다. 안 켜면 기존 동작은 **비트 재현**.
- **서버 몫은 스크립트로 넘겼다**(이 세션도 SSH 없음): `./scripts/wan195-server-forensics.sh`
  (읽기 전용 수집) + wan195.md §5 런북(**드롭 전에 `--salvage-ohlcv` 먼저** · VACUUM은 러너
  정지 후 사람이 · **cron 등록은 임의로 하지 않았다** — 이슈가 금지).
- **기본값·토대 불변** · 손절폭 가드 **손대지 않았다** · 실거래 보류 유지. 5m 복원은 백테스트
  좌표를 바꾸지 않는다(채택 TF는 15m·1h·4h이고 5m을 읽는 리포트가 없다).

🚨 **페이퍼 지갑이 수수료·슬리피지를 안 물고 있었다 — 고쳤다(WAN-392, 2026-08-31): 이제
비용을 계산하는 곳이 장부 하나다.** [`docs/decisions/wan392.md`](docs/decisions/wan392.md).
`build_execution_engine`이 `PaperBroker()`를 **비용 모델 없이** 만들어 `fill.fee == 0`이었고,
`realized = gross − fill.fee`가 그대로 **그로스**라 지갑에 그로스가 쌓였다. 그런데 같은
청산의 장부 행(`paper.store.build_record`)은 공용 `CostModel`로 `fee_pct`·`slippage_pct`를
정확히 계산하고 있었다 — **장부는 아는데 지갑이 몰랐다.**
- 🚨 **크기**: 서버 장부 실측(2026-07-31~08-31 · 약 100거래) 그로스 **+845.86** vs 진짜 net
  **−936.89**(차 **1,782.75** = 수수료 792.33 + 슬리피지 등 ≈990) — **성적이 +8.5% →
  −9.4%로 뒤집힌다.** 거래별로도 손절이 **정확히 −1.000R** · 익절이 **정확히 +1.500R**이라
  비용이 하나도 안 들어간 숫자였다.
- 📌 **고친 방향은 「장부가 권위」다**(이슈 선택지 2) — 브로커에 `cost_model`을 넘기는 길은
  **슬리피지를 못 잡는다**(브로커는 이중 반영을 피하려 체결가를 참조가 그대로 둔다) — 실측에서
  슬리피지가 비용의 **절반 이상**이다. 그래서 `PaperTradeRecorder`를 `build`(계산)/`persist`
  (저장)로 쪼개고, `ExecutionEngine.on_exit(settle=…)`(옵트인) 훅에 `live.executor.
  _LedgerSettlement`이 `net_pct/100 × 진입명목`을 넘긴다. **정산에 쓴 그 행을 그대로** 달러
  금액만 얹어 저장한다(다시 계산하면 갈라짐이 되살아난다).
- 📌 **두 항등식이 구조적으로 성립한다**(`CostBreakdown`의 분수가 전부 **진입 원 노셔널**
  대비이므로): `equity_after` 변화량 == `net_pct × notional / 100` · **`실현손익 ÷ (손절폭 ×
  수량) == r_multiple`**. 후자는 예전에 **1.8배 어긋나** PM이 한 대화에서 세 번 잘못 비교한
  자리다. 손절은 이제 −1.0R보다 **나쁘고** 익절은 +1.5R에 **못 미친다**(비용만큼).
- 🚨 **브로커는 여전히 수수료를 안 문다 — 일부러다.** 진입 수수료는 `on_entry`에서 빠지는데
  장부 `fee_pct`는 **왕복**이라 브로커가 물면 진입분이 두 번 빠진다. 회귀 테스트가 그 성질을
  **배선 줄이 아니라 지갑 잔고의 움직임으로** 고정한다(진입이 자본을 안 건드린다) —
  「라벨이 아니라 동작으로」(`tests/test_wan392_paper_wallet_costs.py`).
- 📌 **재시작 복원·서킷브레이커는 저절로 맞는다** — `_restore_equity`(WAN-238)가
  `초기자본 + Σrealized_pnl`로 복원하고 서킷브레이커가 `realized_pnl_between`을 읽는데 둘 다
  같은 열이라 net으로 바뀌면 함께 정확해진다. ⚠️ **화면·CLI 숫자는 전부 바뀐다**
  (`alphablock status` · 대시보드 성과 탭 · `paper.performance` · `live.trade_timeline` ·
  `live.same_minute_census`).
- 📌 **과거 장부 = (c) 리셋**(사용자 결정 2026-08-31 *"리셋해야지"*). 🚨 **순서가 결정의
  전부다 — 고치기 전에 리셋하면 새 장부도 똑같이 부풀려진다**: 머지 → **서버 배포**(WAN-365
  인과 취소 · WAN-370 비용 회계 · WAN-384 존폭 필터 폐지 · 이 수정) → **표본 CSV 보존**
  (`paper_trades_pre_wan392.csv`) → `scripts/paper-reset.sh` → 러너 재시작. 리셋과 배포를
  같은 시점에 하면 규칙 변경과 회계 수정이 **한 이음매**로 끝난다. ⚠️ 대신 **한 달 표본이
  0으로 돌아간다.** 그 뒤로 페이퍼와 백테스트가 처음으로 **같은 규칙 + 같은 자**가 되고,
  그때부터 `alphablock parity`(WAN-247)가 뜻을 갖는다.
- ⚠️ **WAN-371과 다른 이슈다** — 그쪽은 *진입을 시장가(테이커)로 계산한다*이고 이쪽은
  *계산한 비용을 지갑에서 안 뺀다*였다. 지금까지 WAN-371은 **잔고에 아무 영향도 못 줬는데**
  (어차피 안 뺐으니까) 이 수정 이후로 **실제로 돈을 먹기 시작한다** — 같이 볼 것.
- ⚠️ **백테스트는 하나도 안 건드렸다**(백테는 이미 맞다 — `book_cli`의 `net_r = 실현손익 ÷
  리스크금액`, CSV 실측 손절 −1.333R) · 전략·토대 불변(`ConfluenceParams()`·
  `LeverageBookParams()`) · 존폭 필터·손절폭 가드·익절 배수 **안 건드렸다** · 실거래 보류
  유지(`ALPHABLOCK_LIVE_TRADING=false`). 🚨 이 사고는 「라벨과 동작이 어긋남」(WAN-91/95/112/
  123/159/194)의 **회계 축 변종**이고, 잡을 도구(`alphablock parity`, WAN-247)가 **이미
  있었는데 그 창을 한 번도 안 돌려 봤다.**

🚨 **페이퍼 장부가 진입을 시장가로 계산했다 — 고쳤다(WAN-371, 2026-09-01): 판정은
**(나) 지갑 달러 손익까지 영향**이고, 이제 페이퍼가 백테스트와 **같은 유동성 구분**으로
비용을 문다.** [`docs/decisions/wan371.md`](docs/decisions/wan371.md).
`live/zone_limit_runner.py`가 `PaperTradeRecorder`에 유동성 구분을 **안 넘겨** 기본값
(진입·청산 모두 **테이커**)으로 갔다 — 러너는 존 근단 지정가로 도는데(WAN-45 배선 ·
WAN-256 틱 승격 · WAN-95/112 채택 진입) 그 진입을 **시장가 비용**으로 계산했고, WAN-370
이후로는 **익절**까지 갈렸다(백테 메이커 2bp·슬리피지 0 ↔ 페이퍼 테이커 4bp＋5bp).
- 📌 **판정 (나)의 근거는 WAN-392다** — 그전에는 지갑이 브로커 수수료(페이퍼 기본 **0**)만
  빼서 이 설정이 잔고에 **아무 영향도 못 줬다**(= (가) 표시 층). WAN-392가 「비용의 권위를
  장부 하나로」 옮긴 뒤로는 장부 행의 `net_pct`가 곧 지갑 정산이라 **자본곡선·MDD·다음
  베팅 크기까지** 이 값 위에 선다. 🚨 **브로커는 여전히 수수료를 안 문다 — 그게 맞다**
  (물면 진입분 이중 계상). 그래서 엔진의 `OrderType.MARKET`은 비용에 **아무것도 하지
  않고**, 이 수리는 주문 종류가 아니라 **장부**를 고친다.
- 📌 **크기(닫힌 식 · 산술이지 측정 아님)** — 옛 회계가 더 문 비용: 진입 **7bp 고정**
  (수수료 2bp ＋ 슬리피지 5bp) ＋ **익절 거래에 7bp 더**. R로는 손절폭에 반비례해
  0.458%(15m 중앙)에서 **0.238R** · 0.724%(`oos_warm` 중앙)에서 **0.151R** ·
  1.017%(1h 중앙)에서 **0.108R**(승률 55.33% 가중). ⚠️ **PM 코멘트의 「약 0.10R」은 진입만
  센 값**이라 과소평가다.
- 📌 **배선은 기본값이 아니라 호출부가 명시한다**(이슈 지시) — `ADOPTED_ENTRY_LIQUIDITY`
  (메이커) · `adopted_exit_liquidity(reason)`(**분기를 새로 쓰지 않고**
  `BacktestConfig.exit_liquidity`(WAN-370 단일 소스)에 위임) · 옛 회계는 `LEGACY_*`로
  명시 핀. `PaperTradeRecorder.exit_liquidity`는 이제 **고정값 또는 사유별 함수**를 받는다
  (고정값 하나를 주면 「청산」을 한 덩어리로 취급하게 된다 — WAN-370이 금지한 실패).
  러너는 신설 `live.zone_limit_runner.build_paper_recorder`를 부른다.
- 📌 **회귀 테스트는 라벨이 아니라 동작으로 건다** — 같은 진입가·청산가를 **백테스트 비용
  경로 그대로**(`zone_limit_backtest._to_trade`)와 페이퍼 장부에 먹여 **순손익률이 같은
  값**인지(청산 사유마다) · 옛 회계로 되돌리면 그 등식이 **깨지는지**(돌연변이) · 익절
  수수료 < 손절 수수료(사유별 분기가 **실제로** 도는지) · **AST 배선 가드**가 프로덕션
  호출부에서 `PaperTradeRecorder(cost_model=…)`에 유동성 미지정을 잡는지.
- 🚨 **파급: WAN-393 §1 교차 검증 표에 시점 배너가 붙는다**(아래 문단) — 그 표의 페이퍼
  열이 이 오차 위의 값이라 **「둘 다 음수이고 가깝다」가 성립하지 않게 된다**.
- 📌 **과거 장부 = 보존 ＋ 배너**(덮어쓰지 않는다, WAN-194/297/325). 덮어쓰기는 재계산이고
  `realized_pnl`·`equity_after`는 **정산 순서의 산물**이라 행 하나를 고쳐 쓸 수 없다.
  어차피 **WAN-392 §5의 장부 리셋**(사용자 결정)이 그 행들을 걷어낸다 — 📌 **배포 권고:
  이 수리를 그 리셋 이음매에 함께 태운다**(리셋 뒤에 배포하면 새 표본의 앞부분이 또 옛
  회계로 쌓인다). ⚠️ 이 변경 자체는 **DB에 아무것도 쓰지 않는다**.
- ⚠️ **페이퍼 성과가 좋아 보일 것이다 — 🚨 개선으로 인용 금지**(계산을 바로잡은 것이지
  매매가 나아진 게 아니다) · **체결 낙관은 그대로**(「닿으면 체결」 · 큐 우선순위 미모델링 ·
  WAN-98 Canceled 유지) · **백테스트 쪽 비용·요율·손절폭 가드·존폭 필터·전략 기본값 전부
  불변**(WAN-370/76/79/159 소관) · **「엣지 없음」(WAN-84/88/111/114/124/151/201/248/386)
  불변**(이 축은 *두 성적표가 같은 자를 쓰나*를 묻는다 — **다른 질문**) · 전부 페이퍼이고
  실거래 경로는 여전히 없다(`ALPHABLOCK_LIVE_TRADING=false` 유지).

🚨 **비용 분해가 붙지도 않은 진입 슬리피지 5bp를 계상했다 — 고쳤다(WAN-396, 2026-09-01):
판정 **(가) 분해만 틀렸다**(`net R`은 처음부터 맞다) — 그런데 그 위에 서 있던 WAN-370의
헤드라인 문장 셋이 흔들린다. 「비용을 0으로 만들어도 **천장이 +0.09R**」은 보정하면
**+0.007R**이고, 존폭 필터를 끈 오늘 좌표에서는 **+0.002R = 「시장에서도 본전」**이다.**
[`docs/decisions/wan396.md`](docs/decisions/wan396.md)(재현: `uv run python -m
backtest.wan396_entry_slippage_correction`). 엔진은 **후보**의 `entry_liquidity`(기본
**메이커**)로 진입을 체결하는데(`zone_limit_backtest._to_trade`) 비용 분해는
**`BacktestConfig.entry_liquidity`**(기본 **테이커**)를 읽어 5bp가 붙었다고 **역산**했다 —
`harness`가 그 필드를 아무 데도 안 넘겨 프로덕션 설정이 테이커를 그대로 들고 다녔다.
- 🚨 **`net R`로는 원리적으로 안 보인다** — 분해가 진입 참조가를 슬리피지만큼 밀어 되돌리므로
  그 몫이 `gross`에도 `slippage`에도 **똑같이** 더해져 **정확히 상쇄된다.** 「라벨과 동작이
  어긋남」(WAN-91/95/112/123/159/194)의 **회계 축 변종**이고, WAN-393이 *「R이라 불리는 자가
  셋」*으로 못 박은 것과 같은 부류다. 📌 **테스트가 못 잡은 이유도 여기 있다** — wan370 픽스처가
  `BacktestConfig(entry_liquidity=MAKER)`를 **명시**해 설정과 엔진을 우연히 맞춰 놨다(= 버그가
  없는 세계를 돌고 있었다).
- 📌 **고친 방향 = 단일 소스는 「거래」다** — `Trade.entry_liquidity`(신설 · 기본 메이커 · 순수
  기록 · 손익 미사용 · **비트 재현**)가 엔진이 실제로 쓴 값을 싣고, 분해가 그것을 읽는다(청산이
  `cfg.exit_liquidity` 한 곳에 물어보는 규칙의 진입 판). 🚨 **`BacktestConfig.entry_liquidity`의
  기본값은 안 뒤집었다** — A안 제거(WAN-200/215) 뒤 **읽는 코드가 하나도 없는 사문화 필드**이고,
  `config_json` 실행 지문(WAN-106)에 실려 있어 바꾸면 적재된 실행의 `run_id`가 통째로 달라진다.
  대신 **AST 배선 가드**가 「설정에서 진입 유동성을 읽는 모듈」을 매번 잡는다.
- 📌 **공개 표는 격자를 다시 안 돌리고 「열만」 되계산했다 — 보정이 근사가 아니라 항등식이다**:
  `보정 gross_r/slippage_r/cost_r = 각각 − K × entry_fee_r`, `K = (slip/(1+slip))/maker_rate =
  2.4987506…`(`entry_fee = 진입명목 × maker_rate`라 K가 거래에 무관한 상수). 대조표
  [`wan396_entry_slippage_correction.md`](backtest/reports/wan396_entry_slippage_correction.md)
  (346행 · 표 6개). **원본 여섯 CSV는 그때의 기록으로 보존한다**(WAN-194/297/325 관행).
  검산 셋 전부 기계 정밀도: (a) 슬리피지 항등식 잔차 **3.2e-14**(설명 안 되는 행이 있으면
  도구가 **죽는다**) · (b) `gross_r − slippage_r == mean_gross_r_after_slippage`(이 버그에 **안
  걸리는** 독립 자, 잔차 4.9e-14 → 보정이 신뢰할 수 있는 양을 안 건드렸다) · (c) `net_r` 불변.
- 🚨 **WAN-370 §1 재확인 — 판정 문자는 (나)로 남지만 폭이 무너지고, 오늘 좌표에서는 넘어간다**:
  채택 팔 gross **+0.0920 → +0.0071R**(노이즈선 ±0.005R의 18배 → **1.4배**) · 필터 끈 좌표
  (WAN-388/389/394) **+0.0765 → +0.0019R = (0 근처)**. **「최대 비용은 슬리피지 62%」도 틀렸다**
  (보정 후 **36.3%** · 손절 수수료 29.1% · 진입 수수료 24.6% · 익절 수수료 10.1%)이고, 남은
  슬리피지는 **전부 청산 쪽 몫**이다. 📌 **TF로 쪼개면 더 분명하다** — 보정 후 `oos_warm` gross는
  15m **+0.0221R** · 2h +0.0021 · 1h **−0.0179** · 4h **−0.0764**라 *「시장에서는 이기고 있었다」*의
  실체는 **15m 하나**이고, 하필 그 축이 낙관 체결 의존이 가장 깊다(WAN-124/330/348).
  ⚠️ **WAN-370 §2(익절 메이커 전환) 결정은 안 흔들린다** — 전환의 몫 **+0.0489R**은 두 팔의
  `net R` 차이이고 `net R`은 이 버그에 안 걸린다.
- ⚠️ **CLAUDE.md 「비용 가정」 절은 원래 맞았다** — *「진입(지정가)=메이커 2bp·슬리피지 0」*이
  **엔진이 실제로 한 것**이고, 그와 다르게 센 것은 분해였다. 정정한 것은 그 절이 아니라 그 위에
  쌓인 **인용 문장들**이다(위 여섯 자리에 🔁 표시를 달았다).
- ❌ **슬리피지 요율(5bp) 자체는 안 건드렸다** — WAN-397 소관이고 이 이슈는 *「5bp가 엉뚱한 데
  붙는다」*만 다룬다. 📌 그쪽에 주는 입력 하나: 보정 후 남은 슬리피지는 **전부 청산 쪽**이라
  요율 현실화는 진입이 아니라 **손절·만료 청산**에 걸린다.
- 🚨 **「gross가 사실 0이었다」를 「엣지 없음」의 새 근거로 인용 금지** — 고친 것은 **회계 한
  줄**이고 「엣지 없음」(WAN-84/88/111/114/124/151/201/248/386)은 *진입 규칙이 무작위와
  구분되는가*라는 **다른 질문**이다. 반대로 「그럼 비용만 줄이면 된다」도 아니다 — 보정은 천장을
  **낮췄지** 높이지 않았다.
- ⚠️ **캐시가 통째로 미스된다** — 엔진 소스(`backtest/models.py`·`zone_limit_backtest.py`)가
  바뀌어 후보 payload 캐시(WAN-394 §0)·야간 타임라인 캐시(WAN-297/325)의 리비전이 달라진다.
  **수치는 하나도 안 바뀌는데도 그렇고 그게 맞는 동작이다**(WAN-253). 되채우기는
  [`docs/ops/wan239-nightly-timeline-cache.md`](docs/ops/wan239-nightly-timeline-cache.md).
- **버그 수정 전용 · 엔진 동작·전략·기본값·토대 불변**(`ConfluenceParams()`·
  `LeverageBookParams()` 그대로 · 존폭 필터·손절폭 가드·익절 배수 **안 건드렸다** · 새 격자
  없음) · 실거래 보류 유지(`ALPHABLOCK_LIVE_TRADING=false`).

📌 **페이퍼 100거래가 백테스트를 처음으로 교차 검증했다 — 같은 자로 재면 페이퍼
**−0.1479R** ↔ 백테 **−0.1168R**로 **둘 다 음수이고 가깝다**(WAN-393 = 2026-08-31 조사
기록). 그리고 그 조사에서 **「R」이라 불리는 자가 셋**임이 드러났다 — 섞으면 부호가
뒤집힌다. 문서 전용 · 기본값·토대 불변.**
[`docs/decisions/wan393.md`](docs/decisions/wan393.md). 사용자 질문(*"페이퍼가 5% 벌고
있는데 왜?"*)에서 시작한 하루짜리 조사의 산출물이고 **새 격자도 재산출도 없다.**

- 🚨 **시점 배너(WAN-371, 2026-09-01) — §1 표의 페이퍼 열은 「옛 비용 회계」 위의
  값이다.** 그 창의 페이퍼 장부는 **지정가 진입을 시장가로 계산**했고(진입 테이커 4bp＋
  슬리피지 5bp) 익절도 테이커였다 — WAN-371이 그것을 백테와 같은 값(진입 메이커 · 익절
  메이커 · 손절 테이커)으로 맞췄다. 닫힌 식으로 재면 옛 회계가 더 문 비용이 손절폭에
  따라 **0.108R ~ 0.238R**(승률 55.33% 가중, `docs/decisions/wan371.md` §3)이라 **아래
  «차 −0.031R»보다 훨씬 크다** — 바로잡으면 페이퍼가 0 언저리로 올라가 백테를 **이기고**,
  즉 **「둘 다 음수이고 가깝다」가 성립하지 않게 된다.** ⚠️ **「그럼 페이퍼가 번다」로
  인용 금지**(계산을 바로잡은 것이지 매매가 나아진 게 아니다 · 그 크기는 **산술이지
  측정이 아니다** · 애초에 두 열은 같은 거래 집합이 아니다 — 아래 한계 셋). ⚠️ **§2
  「R이라 불리는 자가 셋」 규칙은 그대로 유효하다**(회계와 무관한 정의 문제).
- 📌 **§1 교차 검증(이 저장소 첫 실측)** — 좌표: 페이퍼 장부(서버 · 러너 실사용)
  **2026-07-31~08-31 · 100거래 · 12종목 · 존폭 필터 1.28 켬** vs 백테스트 채택 북(로컬 ·
  같은 창 · **필터 꺼짐**).

  | | 값 |
  | -- | -- |
  | 페이퍼 `r_multiple`(비용 반영 후) | **−0.1479** |
  | 백테스트 `net R`(같은 자) | **−0.1168** |
  | 페이퍼 gross R(비용 전) | +0.1351 |

  📌 **왜 중요한가** — 지금까지 이 저장소의 모든 판정은 **백테스트 하나**에 기대고 있었고
  **라이브가 그것을 확인해 준 적이 없었다.** 6년 백테스트가 예측한 거래당 실력이 실제
  페이퍼에서 그대로 나왔다. **「엣지 없음」(WAN-84/88/111/114/124/151/201/248/386)을 뒤집는
  게 아니라 라이브로 뒷받침한다.**
- ⚠️ **알고 받는 한계 셋 — 반드시 함께 인용한다**: (1) **표본 100거래 · 한 달 · 상승장 한
  구간**이라 6년을 대체하지 않는다. (2) **필터가 안 맞는다**(페이퍼 켬 · 백테 꺼짐) — 6년
  기준 그 차이는 **0.010R**이라(WAN-378) 이 비교의 **0.03R 격차**에 비하면 작지만 **0은
  아니다**. (3) 🚨 **페이퍼 쪽은 `r_multiple`만 유효하다** — 같은 장부의 `realized_pnl`·
  `equity_after`는 **WAN-392 수정 이전 표본**이라 gross다. `r_multiple`은 `net_pct /
  risk_pct`라 비용이 제대로 들어 있어 **이 비교는 무효가 아니다.**
  📌 **표본 보존**: 리셋 전에 내보낸 원본이 `~/paper_trades_pre_wan392.csv`다(WAN-392 §과거
  장부 = 리셋). **리셋 뒤로는 이 표를 재현할 수 없다.**
- 🚨 **§2 「R」이라 불리는 자가 셋이다 — 2026-08-31 조사에서 이것 때문에 결론이 세 번
  뒤집혔다.** 사람 주의력으로 막을 문제가 아니라 정의를 못 박는다:

  | 이름 | 정의 | 어디 | 비교 가능? |
  | -- | -- | -- | -- |
  | 백테스트 `net R` | 실현손익 ÷ 리스크금액, **리스크금액 = \|진입가−손절가\| × 수량** | `backtest/book_cli.py:613`(`net_r`) + `backtest/leverage_book.py:593` | ✅ |
  | 페이퍼 `r_multiple` | `net_pct ÷ risk_pct` | `paper/store.py:363` | ✅ |
  | ⚠️ 페이퍼 `realized_pnl ÷ risk_amount` | 손익 ÷ **걸려던 금액**(= `자본 × risk_per_trade`, **상한에 잘리기 전**) | `live/executor.py:227` | ❌ **다른 자** |

  📌 **규칙**: *「페이퍼와 백테스트의 거래당 성적을 비교할 때는 백테 `net R` ↔ 페이퍼
  `r_multiple`만 맞댄다. `realized_pnl ÷ risk_amount`는 다른 자다.」*
- 📌 **셋째가 다른 이유는 분모다 — 그리고 그 절반은 WAN-392가 고쳐도 남는다.** 페이퍼의
  `risk_amount`는 **「걸려던 금액」**이고 실제 리스크는 명목 천장·ADV 한도에 잘리므로 실측
  비율 중앙값이 **1.768**이다(백테스트의 `risk_amount`는 「손절폭 × 수량」이라 애초에 다른
  양이다). ⚠️ **WAN-392 이전에는 분자(`realized_pnl`)까지 gross여서 두 겹이었고**, 그
  수정으로 분자는 net이 됐지만 **분모는 그대로 다르다.** 실측에서 셋째 자는 첫 둘과 **부호가
  반대**로 나온다(**+0.137** vs −0.117 · −0.148).
- 🚨 **이 저장소가 다섯 번 겪은 「라벨과 동작이 어긋남」(WAN-91/95/112/123/159)의 자(metric)
  축 변종이다** — 이름이 비슷한 자가 셋인 한 다음 사람도 같은 실수를 한다. **코드로 막는 것은
  이 기록의 범위 밖**이고(필요하면 별도 이슈), 그전까지는 **이 표가 방어선**이다.
- **문서 전용 · 코드·엔진·기본값·토대 무변경**(`ConfluenceParams()`·`LeverageBookParams()`
  그대로 · 존폭 필터·손절폭 가드·익절 배수 **안 건드렸다** · 새 격자·재산출 없음) ·
  실거래 보류 유지(`ALPHABLOCK_LIVE_TRADING=false`).

## 실험 도구 — 새 리포트 스크립트를 짜기 전에 읽을 것 (WAN-101)

**"파라미터 바꿔서 다시 돌려보기"에는 새 스크립트를 만들지 않는다.** 범용 CLI가 있다:

```bash
uv run python -m backtest.run                                # 인자 없이 = 채택 북(cap_only 5배·band 재진입, WAN-213/273)
uv run python -m backtest.run --oos-warm                     # 정본 리포트 = 채택 북 warm/cold(WAN-166/213/273)
uv run python -m backtest.run --positions book --tf 15m,1h   # 채택 북을 명시(wan180 both 좌표)
uv run python -m backtest.run --reentry off --tf 15m,1h      # WAN-273 이전 재진입-off 북(옛 CSV 비트 재현, 미지정과 다름)
uv run python -m backtest.run --invalidation-cancel bar_open  # WAN-365 이전 소급 취소 북(옛 CSV 비트 재현, 미지정과 다름)
uv run python -m backtest.run --tf 1h --positions single --invalidation-cancel bar_close,bar_open --oos-warm  # 인과 vs 소급(per-cell)
uv run python -m backtest.run --tp-r 1.0,1.5,2.0,3.0          # 익절 R 스윕(전략 축 → per-cell 단일 자동)
uv run python -m backtest.run --tf 15m --fill baseline,pen_5bp,pen_5bp_drop_50
uv run python -m backtest.run --symbol BTCUSDT,ETHUSDT --oos --format csv --out x.csv
uv run python -m backtest.run --positions single --tf 1h --oos-warm       # per-cell 단일 warm/cold(WAN-166)
uv run python -m backtest.run --symbol BTCUSDT,ETHUSDT,SOLUSDT --jobs 6   # 병렬(WAN-121)
uv run python -m backtest.run --tf 15m,1h --positions single,3 --oos      # 단일 vs 다중(WAN-130)
uv run python -m backtest.run --tf 15m,1h --combine-obs true,false --oos  # 존 병합 전후(WAN-149)
uv run python -m backtest.run --tf 1h --max-zone-width-atr none,1.28 --oos # 존폭 필터 off/on(WAN-159 기본 1.28)
uv run python -m backtest.run --tf 1h --max-zone-width-atr none,1.28 --tp-r 1.0,1.5,2.0,3.0 --oos  # 문턱×배수(WAN-161)
uv run python -m backtest.run --tf 1h --limit-valid-bars 6,12,24,48,none --oos-warm  # 지정가 유효기간 스윕(WAN-222)
uv run python -m backtest.run --symbol BTCUSDT --tf 15m --trades t.csv --equity s.csv  # per-cell 거래별(WAN-106)
uv run python -m backtest.run --positions book --oos-warm --trades b.csv --trades-segment oos_warm  # 북 거래별(WAN-346)
uv run python -m backtest.run --symbol BTCUSDT --tf 15m --persist         # DB 적재 → 대시보드 조회(WAN-106)
```

📌 **거래별 내역·DB 적재(WAN-106)** — 요약 1행은 "언제 샀나 · 어디서 손절났나"에 답하지
못한다. `--trades`/`--equity`는 **단일 조합 전용** CSV(KST·UTC 병기 · 시드 변화 포함)이고
격자면 **거부한다**(조용히 마지막 조합만 내보낸 파일이 나중에 "채택 엔진의 거래"로 인용되는
것이 WAN-95의 교훈). `--persist`는 격자에서도 되며 **거래 · 미체결 셋업 · 시드곡선**을
`backtest/trade_store.py`의 테이블(`backtest_runs`/`_trades`/`_trade_exits`/`_setups`/
`_equity`)에 넣는다. 대시보드 **「저장된 거래」 탭**이 그걸 **계산 없이 조회**한다(손절/익절
필터 · 미체결 셋업 · 행 클릭 → 차트 점프).
- 📌 **실행 지문 없이는 적재도 조회도 되지 않는다** — 지문 = 심볼·TF·구간·창 + `ConfluenceParams`/
  `OrderBlockParams`/`BacktestConfig` 직렬화 + **엔진 버전 + 코드 리비전**(git 해시, 더러우면
  `-dirty`)이고 그 해시가 `run_id`다. **코드 리비전이 핵심**이다: 파라미터만으로 키를 만들면
  엔진 버그를 고쳐도 키가 같아 **옛 결과를 꺼내 준다**(WAN-91/95/112와 같은 부류의 조용한 실패).
  같은 지문 재적재는 **기본이 거부**(`--persist-replace`가 명시적 덮어쓰기).
- ⚠️ **지표의 정본은 적재된 요약이지 복원 결과가 아니다** — 종가(A안) 엔진의 자본곡선은 **봉
  단위**라 거래 단위로 다시 만든 곡선과 **MDD·Sharpe가 다르다**(최종 시드·거래 수·수익률은 같다).
  화면도 요약 지표를 쓴다.
- 📌 **북도 이제 거래별 내역을 낸다(WAN-346 §0)** — `--positions book --trades/--equity`.
  북은 칸을 가로지르는 한 지갑이라 per-cell 표에 없는 열이 붙는다(**칸(종목·TF) · 손절가 ·
  익절가 · 리스크금액 · net R · 재진입 · 같은 분 익절 · 탭 시각**). 값은 전부 시퀀서의 배치
  기록(`PlacedSetup`)에서 그대로 오고, 짝이 어긋나면 **시끄럽게 죽는다**(칸 라벨이 다른 거래에
  붙은 표가 조용히 나가면 안 된다).
  - ⚠️ **구간을 여럿 요청하면 거부한다** — 북은 구간마다 **다른 거래 집합**이라
    `--trades-segment {full,is,oos_warm,oos}`로 **고르게 한다**(조용히 마지막 것을 내보내지
    않는다 — WAN-106/95의 교훈). 구간이 하나면 생략 가능.
  - ⚠️ **`--positions`를 생략하면 예전처럼 per-cell로 접힌다** — 위 WAN-106 레시피가 말없이
    다른 회계의 파일을 내놓지 않게 한 것이다. **두 회계는 다른 경기다**(per-cell 거래별은 칸마다
    독립 자본 · 북 거래별은 한 지갑, WAN-213/341). 북의 거래를 보려면 `--positions book`을 콕 집는다.
- 컬럼은 **WAN-146이 만든 `backtest.report.trades_to_display_frame`을 그대로 재사용**한다
  (`include_utc=True`가 파일·DB용 옵트인) — 화면·CSV·DB가 두 벌로 갈라지면 같은 거래가 세 곳에서
  다른 숫자로 보인다.

값에 콤마를 주면 데카르트 곱 격자를 돌고 조합별 1행(`total_return`/승률/MDD/거래수/
체결률/평균 R/Sharpe)을 낸다. 축은 심볼·TF·진입 방식·익절 R·오프셋·재탭 정책·**포지션
정책**(`--positions`, WAN-130/213: **미지정 = 채택 북**(cap_only 5배) · `book` = 북 명시 ·
`single` = per-cell 동시 1포지션 · 숫자 = per-cell 다중 포지션 명목 상한 배수)·**존 병합**
(`--combine-obs`, WAN-149 — ⚠️ 이것만 **탐지** 파라미터라 값마다 오더블록을 다시 탐지한다)·
**존폭 필터**(`--max-zone-width-atr`, WAN-159 — 채택 기본값 `1.28`, `none`은 **끄기**이고 인자
미지정(1.28)과 다르다, 단위는 **ATR 배수**다)·**지정가 유효기간**(`--limit-valid-bars`, WAN-222 —
채택 기본값 `24`봉, `none`은 **무기한**이고 인자 미지정(24)과 다르다, 단위는 **봉 수**지 시간이
아니다)·**재진입**(`--reentry`, WAN-273 — 북 전용, **미지정 = 채택 band 켬**, `off`는 재진입
없는 옛 북이고 미지정과 다르다, `freeze`/`zone`/`band`로 규칙 명시)·**무효화 취소 시점**
(`--invalidation-cancel`, WAN-365 — **미지정 = 채택 `bar_close`(인과)**, `bar_open`은 WAN-365
이전의 소급 취소이고 미지정과 다르다. per-cell은 콤마 격자이고 **북은 단일값만** 받는다 —
한 실행이 한 지갑이라 두 취소 시점을 한 표에 못 넣는다)·체결 가정·시드이며,
`--oos`/`--walkforward`가 IS/OOS 분할을 기본 제공한다. **정본 리포트의 OOS는 `--oos-warm`**(WAN-166: `oos_warm` 주 수치
+ `oos` 스트레스 병기)이다.
📌 **포지션 기본값 = 채택 북(WAN-213)** — 인자 없는(또는 좌표만 준) 실행은 **레버리지 북**
(cap_only 5배 · 칸=(종목,TF) 공유 자본)을 돌아 **구간당 집계 행**을 낸다(`backtest/book_cli.py`).
⚠️ 전략·비용·거래별 축(`--tp-r`·`--fill`·`--combine-obs`·`--max-zone-width-atr`·
`--limit-valid-bars`·`--trades` 등)을 주면 북이 그 축을 표현하지 못하므로 **per-cell 단일 포지션으로 자동 접힌다** — 그 축
스윕은 예전 그대로 `RunRow`를 낸다(매번 `--positions single`을 붙일 필요 없다). 명시적
`--positions book`에 그 축을 함께 주면 **거부**한다(조용히 접지 않는다). per-cell 단일
포지션이 WAN-95/99 리포트 셀과 1e-9 이내로 일치함은 테스트가 고정한다(`tests/test_harness.py`
+ `tests/test_run_regression_real_data.py`, 후자는 `--positions single`). 북이 wan180 채택
셀과 구성상 비트 일치함은 `tests/test_book_cli.py`가 고정한다.
🔁 **인자 없는 실행의 데이터 좌표는 12종목 × 15m·1h·2h·4h × 못 박은 채택 창
(2020-09-15~2026-07-22)이다**(TF는 WAN-252가 2h를, 유니버스는 WAN-307이 12종목을 승격) —
`--years N`을 명시한 실행만 옛 미끄러지는 창을 쓴다(위 📌 WAN-182/WAN-252/WAN-307 문단).
**48셀**(12종목×4TF) × 6년이라 인자 없는 실행은 무겁다 — 탐색 중에는 심볼·TF를 좁혀 돌 것.

📌 **`--jobs N`은 (심볼, TF) 단위 병렬이고 숫자를 바꾸지 않는다(WAN-121/294)** —
`--jobs`는 **성능 노브이지 결과 축이 아니다**(직렬과 행·순서·stdout·stderr가 비트 단위로
같음을 합성·실데이터 양쪽 테스트가 고정한다). **미지정이면 설정 기본값
`ALPHABLOCK_BACKTEST_JOBS`(기본 4, WAN-294)** — M1 성능 코어 수다(효율 코어까지 세는
`auto`는 오버서브라 실효 상한을 기본으로 고정). 코어 수가 다른 서버(WAN-174)는 그
환경변수로 덮고, 명시적 `--jobs N`/`--jobs auto`(=`os.cpu_count()`, 캡 없음)는 이긴다.
리터럴 4가 아니라 설정값이므로 `harness.default_jobs()`를 CLI·리포트가 공유한다.
⚠️ **기대 배수를 코어 수로 잡지 말 것** — 실측은 **6심볼 1h 3년 격자에서 93.2s → 69.7s
(1.34배)** 수준이다(그 머신의 CPU 바운드 상한 자체가 2.36배였다). 두 가지가 천장을 만든다:
(1) **1분봉 로딩이 1조합 실행의 ~36%** 인데 이건 셀마다 필요해 병렬로 사라지지 않고
(WAN-91 이래 `--start`/`--end` 창은 캐시를 안 타 매번 SQL 전수 스캔이다),
(2) 애플 실리콘은 `cpu_count()`가 **E코어까지 세므로**(M1 = P4+E4) `auto`가 성능 코어 수보다
크게 잡힌다. 격자가 작으면 프로세스 기동 비용(spawn 재임포트)이 이득을 넘겨 **오히려
느리다**(600봉 합성 6심볼에서 2.3s → 4.5s). 조합이 많은 스윕일수록 유리하다.

🚨 **그 설정이 `alphablock` CLI에는 배선조차 안 돼 있었다(WAN-354)** —
[`docs/decisions/wan354.md`](docs/decisions/wan354.md). 백테를 부르는 네 하위 명령
(`trades`·`compare`·`stop-width`·`parity`)이 `--jobs` 기본값을 **리터럴 1로 박아** 둬서
`ALPHABLOCK_BACKTEST_JOBS`를 아무리 덮어도 그 경로에는 **닿지 않았다**. 함의 둘:
(1) **야간 크론(`trades --persist-cache`, 인자 없음)은 오버서브가 아니라 직렬로 돌았다** —
「2코어에서 4-way로 돈다」는 그 경로에 대해선 **사실이 아니었다**(WAN-322/324 서버 실측
6분 23초도 직렬 값이고, `user`가 2분 13초뿐이라 **병목은 CPU가 아니라 디스크**다).
(2) 서버 `.env`에 값을 넣어도 아무 일도 안 일어났을 것이다. 이제 네 명령이 미지정 시
`harness.default_jobs()`를 쓰고(명시 `--jobs N`이 이긴다) **그 값을 stderr 첫 줄에** 찍는다(`병렬 설정: 워커 N개 (…)`). 격자도 **요청값이 아니라 셀 수로 캡된 실제 값**을
찍는다. ⚠️ **대시보드 화면 버튼은 여전히 직렬이다**(워커마다 1분봉 사본 → `BrokenProcessPool`,
WAN-324) — 이 설정을 안 읽는다.
- 📌 **`.env`는 이제 CWD 기준 + 저장소 루트 기준 둘 다 읽는다**(뒤가 이기므로 CWD 판이
  여전히 최우선 · 순수 추가). 옛 코드는 CWD 상대 하나뿐이라 저장소 밖 실행이 조용히
  기본값으로 돌았다 — 서버가 맞았던 건 크론이 `cd`를 하고 유닛이 `WorkingDirectory`를
  두기 **때문**이지 설계 덕이 아니었다.
- 📌 **서버 설정은 `.env` 한 자리가 세 경로(크론·systemd·손 CLI)를 전부 덮는다** — 코어
  수에 맞춘다(서버 실측 **2코어** → `ALPHABLOCK_BACKTEST_JOBS=2`). 절차는
  [`docs/ops/server-migration.md`](docs/ops/server-migration.md) §2a, 확인은
  `./scripts/wan354-server-jobs-check.sh`(읽기 전용) · `python -m scripts.wan354_jobs_probe`
  (워커가 정말 그 수만큼 뜨는지 = **라벨이 아니라 동작**). 🚨 **크론 줄에만 숫자를 박지
  말 것** — 화면 버튼·`stop-width`·손으로 치는 실행이 각자 다른 값으로 돈다.
- ⚠️ **유닛 템플릿에 `EnvironmentFile=`을 일부러 넣지 않았다** — 유닛이 이미
  `WorkingDirectory=<저장소>`로 그 `.env`를 읽고, 두 자리에 같은 값을 두면 갈라진다
  (게다가 `.env`에는 API 키·텔레그램 토큰이 있어 `systemctl show`로 샌다).
- ⚠️ **코드 기본값 4는 안 바꿨다**(M1 성능 코어 수) · **측정값·재현성·캐시 지문 불변**
  (`--jobs`는 순수 성능 노브) — 옛 표 재산출·캐시 되채우기 **불필요**.

공통 골격은 `backtest/harness.py`(데이터 로딩·파라미터 조립·경로 스위치·구간 분할·렌더)다.
새 `wanNN_*.py` 리포트는 **CLI로 답이 안 나오는 것**(사후 분해·편향 진단·결론 문장이
필요한 분석)에만 만들고, 그때도 골격은 harness에서 가져다 쓴다. 일회성 스크립트
아카이브는 WAN-94 소관이다.

📌 **신규 리포트는 `baseline` 단독으로 낸다(WAN-128)** — `LENS_NAMES = ("baseline",
"pen_5bp", "pen_5bp_drop_50")` 같은 3렌즈 병기를 새로 짜지 말 것. 공식 렌즈는
`harness.BASELINE_FILL`(= `harness.fill_preset("baseline")`) 하나다. `pen_5bp`·
`pen_5bp_drop_50`은 **삭제가 아니라 옵트인 민감도**라 `FILL_PRESETS`·`--fill`에 그대로 있고,
`uv run python -m backtest.run --fill baseline,pen_5bp,pen_5bp_drop_50`처럼 **수동으로 언제든**
확인할 수 있다(위 예시 블록의 `--fill`). 기존 `wanNN_*.py`의 3렌즈 상수는 **당시 기록이라
손대지 않는다**(무수정 보존 — 그래야 CSV가 비트 단위로 재현된다). 배경·「잃는 것」은 위
📌 WAN-128 문단.

⚠️ `--entry-mode close`에 지정가 전용 노브(`--offset-bps`·`--fill`)를 주면 CLI가
거부한다 — 조용히 무시하면 아무 일도 하지 않은 결과에 라벨만 붙기 때문이다(WAN-95의 교훈).

## 하드 제로와 칼 같은 경계는 안심이 아니라 경보다 (WAN-367 = 사용자 지시 2026-08-23)

🚨 **하드 제로와 칼 같은 경계는 결과가 아니라 제약의 지문이다.**

수천 건 표본에서 무언가가 **정확히 0**이거나, 어떤 값이 **딱 떨어지는 상한/하한**에 붙어
있거나, 분포에 **자연스럽지 않게 깔끔한 경계**가 보이면 — 그건 관측이 아니라 **코드가 만든
벽**일 가능성이 높다. 시장은 딱 떨어지는 숫자를 만들지 않는다.

- **설명을 붙이기 전에 코드에서 찾는다.** 「그 값을 만들 수 있는 경로가 무엇인가」를 먼저
  묻고, 못 찾으면 **「안 쟀다」로 남기고 조사 이슈를 낸다.** 그럴듯한 해석을 붙여 넘어가지
  않는다.
- **「0건」을 판정 열로 쓰지 않는다.** 0은 *「안 일어났다」*와 *「일어날 수 없다」*를
  **구분하지 못한다.** 안심의 근거로 인용하려면 먼저 그 둘을 갈라야 한다.
- **가르는 가장 싼 방법은 반사실 팔이다** — 의심되는 규칙을 옵트인으로 끄고 그 0이 0이 아니게
  되는지 본다(WAN-364 §1이 실제로 한 것).

📎 **실제 사고 두 건(둘 다 이 저장소에서 났다)**

1. **「청산 0건」** — WAN-169/213/276/277이 판정 열로 써 왔고, **WAN-312가 「청산 0건인데
   MDD 100%」로 그 열이 아무것도 보증하지 않음을 밝혔다.** 그런데도 WAN-316/323/330/346과
   이후 PM 보고가 계속 인용했다. 사이징이 자본의 %라 연쇄 손실로는 청산 조건이 **구조적으로
   안 걸린다** — 즉 그 0은 처음부터 「일어날 수 없다」 쪽이었다.
2. **「진입 봉 최대 손실 이동 = 정확히 1.0000R, 그 위 0건」** — WAN-362가 6,336건 **전수**에서
   관측했다. 진입 봉 저가가 존 바닥 아래로 내려간 거래도 **0건**. 그 벽을 보고 *「라이브가 한
   봉 늦게 취소해서」* 로 **설명을 붙이고 넘어갔다.** 실제로는 소급 취소 룩어헤드의
   벽이었고(WAN-364), 그 위에 **6년치 채택 성과 전부**가 얹혀 있었다. **잡은 것은 코드도 PM도
   아니고 사용자였다** — *「손절을 해야지 뭘 취소한다는 거냐」*.

⚠️ **이 규칙은 기존 「조용한 실패」 규칙들과 다른 종류다.** WAN-91/95/112/123/159는 전부
**라벨과 동작이 어긋난** 부류이고, 그걸 막는 장치(핀·배너·시점 표시·오늘 엔진 확인)는
*「이 숫자가 어느 엔진에서 나왔나」*를 묻는다. 이 규칙이 묻는 것은 다른 질문이다 —
***「이 숫자가 세고 있는 게 맞나」***. 앞의 관문을 전부 통과해도 이 질문은 안 물어진다.

📌 **범위**: 문서 전용 규칙이다 — **코드·엔진·기본값·격자를 하나도 안 건드린다**
(`ConfluenceParams()`·`LeverageBookParams()` 그대로 · 실거래 보류 `ALPHABLOCK_LIVE_TRADING
=false` 유지). **옛 리포트의 「청산 0건」 문구도 지우지 않는다** — 당시 기록이고 지우면 이
사고의 증거도 사라진다. **읽는 법**만 바뀐다. 규칙을 **자동 검사로** 만드는 것(리포트가 0인
열을 자동 표시 등)은 **별건**이고, 만들기 전에 「이미 있나 · 첫 사건을 잡나 · 답이 나오면
행동이 달라지나」를 먼저 확인한다. ⚠️ **PM 러너 SKILL.md 교차 참조는 이 이슈 범위 밖이다**
(저장소 밖 파일 — 사용자가 직접 반영, [`docs/decisions/wan367.md`](docs/decisions/wan367.md) §4).

## 운영 출력 시각 = 한국시간(KST) 고정 (WAN-172)

📌 **사람이 읽는 출력의 시각은 전부 KST다** — `alphablock status` · 텔레그램 경고
(`live.health_watch`·`live.notifier`) · 러너 로그 · `live.fill_report` · 대시보드.
사용자 결정("무조건 한국시간", 2026-07-22): 폰으로 경고를 받는데 UTC면 볼 때마다
9시간을 더해야 한다. **시간대는 설정값이 아니라 상수다** — 설정으로 빼면 "로그는
KST인데 경고는 UTC" 같은 어긋남이 다시 생긴다.

- **포맷터는 `common/timefmt.py` 한 벌뿐이다** — `format_kst`(표기 없음, 열 이름이
  시간대를 밝히는 표 안) · `format_kst_zoned`(`… KST` 표기, 문장 속 단독 시각) ·
  `format_utc`(**데이터 열 전용**) · `use_kst_logging()`+`kst_log_format()`(로그
  `%(asctime)s`를 KST로). `backtest.report.format_time_kst`(WAN-146 대시보드 진입점)도
  이 구현에 위임한다. **새 출력에 `datetime.fromtimestamp(..., tz=UTC).strftime`을
  다시 쓰지 말 것** — 두 벌로 갈라지면 같은 사건이 화면과 로그에서 다른 시각으로 보인다.
- ⚠️ **표시 계층 전용이다 — 저장·계산·비교는 UTC(epoch ms) 그대로다.** 신선도 판정·
  워터마크·DB·백테스트 어디에도 로컬 시각을 넣지 않는다(서버 이전·서머타임에서 꼬이고
  백테스트 재현이 깨진다). **백테스트 CSV의 UTC 병기 열**(`진입시각(UTC)` 등, WAN-106)은
  **그대로 UTC**다 — KST 전환이 데이터 열을 바꾸지 않는다.
- ⚠️ **차트 축·기간 선택은 데이터 축이라 UTC 그대로다**(`dashboard.app._ms_to_datetime`).
  바뀐 것은 Health 탭 표·캡션 같은 **읽는 값**이다.
- 로그는 `use_kst_logging()`이 `logging.Formatter.converter`를 갈아끼워 **머신 시간대와
  무관하게** KST로 찍는다(WAN-174 리눅스 서버는 UTC라 노트북과 어긋났다).

## UI/대시보드 이슈 워크플로우 (WAN-259 = 사용자 결정 2026-08-06)

📌 **UI·대시보드 화면 이슈는 맨땅에서 짜지 않는다** — 「① 별도 HTML 디자인 도구로 목업
생성 → ② 사용자 승인 → ③ 저장소 개발자가 Streamlit(`dashboard/`)에 배선」 순서로 간다
(사용자가 이 워크플로우를 확정했다). 규칙이 이슈 코멘트 한 곳에만 있으면 다음 UI 이슈에
자동 적용되지 않으므로(PM 러너는 실행 간 기억이 없고 개발자도 매번 이슈 본문만 본다) 여기
명문화해 매 실행·개발 시 항상 참조되게 한다.

- **디자인 도구에는 기존 대시보드 파일을 반드시 import해 지금 화면의 시각 문법을 잇는다**
  (맨땅 디자인 금지) — 특히 `dashboard/lightweight_chart.py` +
  `dashboard/static/lightweight-charts.standalone.production.js`.
- ⚠️ **목업은 「시각·인터랙션 명세」이지 드롭인 코드가 아니다** — 지금 대시보드는 Streamlit
  + Lightweight Charts라 HTML 목업을 그대로 붙일 수 없다. "이렇게 보이고 이렇게 동작한다"의
  기준으로만 쓰고, 배선은 개발자가 Streamlit 위에서 다시 한다.

## 측정·대조 도구 기본값 원칙 — 백테스트는 항상 페이퍼와 같은 선상 (WAN-305 = 사용자 결정 2026-08-14)

📌 **"백테스트는 항상 페이퍼와 반드시 동일한 선상에서 진행되어야 한다."** 페이퍼 러너는 채택
규칙 그대로 뛰는데 그 성적을 재고 대조하는 백테스트가 다른 규칙으로 돌면, 페이퍼 성적표·괴리
화면·측정 리포트 전부가 "다른 경기의 기록"이 된다 — 어떤 결론도 지지하지 못한다. 그래서:

- **새 측정·대조 도구는 기본값이 채택 규칙(= 페이퍼가 실제로 뛰는 규칙)이어야 한다.** 다른
  규칙으로 재려면 **명시 옵트인**이고, 옛 결과 재현은 **명시 핀**(`LEGACY_*` 패턴)으로만 한다.
  "옛 CSV 비트 재현 보존"을 이유로 헬퍼 기본값을 옛 규칙에 두면 그 뒤의 새 리포트까지 조용히
  오염된다(WAN-273 파급 처리가 남긴 `include_reentry=False` 기본값이 wan288/291/293/300/301/302를
  전부 재진입 꺼진 북으로 돌게 한 사고 — WAN-91/95/112/123/159 부류의 기본값 방향 변종).
- 📌 **실험을 막는 게 아니라 실험의 출발점을 정하는 것이다**(사용자 부연) — 새 조건의 실험
  기준선은 항상 「페이퍼에서 돌고 있는 가장 최신 채택 규칙」이고 그 위에 조건 **하나**를 얹는다
  (실험 = 페이퍼 최신판 + 추가 조건 한 축). 옛 판에 조건을 얹은 표는 "그 조건의 효과"가 아니라
  "옛 판 + 조건"의 효과라 결론을 지지하지 못한다(WAN-300이 정확히 이 위반 — 조건 = 유니버스
  확대, 출발점 = 재진입 꺼진 옛 북. 재측정은 WAN-304).
- **규칙이 다른 것과 해상도가 다른 것을 구분한다** — 틱 vs 1분봉 체결(WAN-256, 명시된 차이)·
  워밍업 존 재고 근사(`--warmup-days` 노브)는 알려진·의도된 비대칭이라 이 원칙의 위반이 아니다.
  🔁 ⚠️ **단 「워밍업 존 재고」로 실제 파리티 차이를 설명하지는 말 것 — 실측이 기각했다**
  (WAN-337, 위 📌 문단): 라이브만 보는 셋업 60건 중 워밍업 창 **밖은 0건**이고 최고령 존도
  17일 전이라, 그 비대칭은 **존재하되 관측된 차이를 만들지 않았다**. 근사가 있다는 사실과
  그 근사가 이 차이의 원인이라는 주장은 다르다.
  🔁 📌 **「탐지 컨텍스트 창」도 같은 자리에 들어간다 — 실측으로 기각됐다**(WAN-343, 위 📌
  문단): 라이브(1500봉)와 백테(120일)의 창은 TF마다 크게 다르고 방향까지 반대인데, **두 창이
  함께 보는 구간에서는 존 정체성도 생애 필드도 같다**(불일치 0.90% · 전부 창 시작 +5~25봉).
  창 길이는 **알려진 비대칭이되 관측된 파리티 차이의 원인이 아니다**.
- 첫 적용(WAN-305): `wan169.run_cells`(`reentry`·`reentry_entry_rule`)·`_segment_cells`·
  `book_cli.build_book_rows`(`include_reentry`)의 기본값이 채택 재진입(band 켬)이 됐고, 옛 CSV를
  결론에 박아 둔 모듈들은 `reentry=False`/`include_reentry=False` 명시 핀으로 고정됐다. 대조
  도구(`alphablock compare`/`trades`·setup_compare)의 백테 쪽도 재진입을 포함한다 — 페이퍼가
  채택 규칙대로 한 재진입 매매가 🔴 「판정 갈림」으로 찍히지 않는다(같은 존 구제 조인 +
  재무장은 탭과 별도 단계로 계수). 장부(`live_limit_orders.origin`)가 재진입 주문을 표기한다.

## 판단은 북에서만 낸다 — per-cell은 탐색·귀속 진단용이다 (WAN-341 = 사용자 결정 2026-08-19)

📌 **채택 판단은 채택 회계(레버리지 북, WAN-213)에서만 낸다.** per-cell 단일 포지션
(`--positions single`)은 **탐색과 귀속 진단**을 위한 자이고, 그 표의 순위를 **결론으로 옮기지
않는다.** 이유는 단순하다 — **실제 매매는 북에서 일어난다.** 칸=(종목,TF)끼리 한 지갑을 나눠
쓰므로 A칸이 매매하면 B칸이 못 들어가는데, 그 간섭은 **잡음이 아니라 신호**다. 격리해서 이긴
규칙이 자본 경합에서 지면 **그건 지는 규칙이다**. WAN-305가 「백테는 페이퍼와 같은 선상」을
**규칙 축**에서 못 박았다면 이 문단은 같은 것을 **회계 축**에서 못 박는다(형제 규칙).

🚨 **전례 — 같은 팔인데 판정이 갈렸다(WAN-323 · 오늘 엔진 · 핀 없음).** 반익절 래더의
per-cell 판정은 **(c) TF에 갈린다**(수익 2h **+0.93%p** · 4h **−0.41%p** · 1h **−2.80%p**)
였는데 채택 북에서는 **세 축이 같은 방향**이었다(`oos_warm` MDD **19.58 → 14.73% = −4.84%p** ·
승률 **+10.5%p** · 수익/MDD **+92.2%** · 청산 0). 원인은 **per-cell에 원리적으로 없는 채널
둘**이다 — 부분 청산이 **공유 자본의 명목·리스크 자리를 비워 다른 칸이 쓰고**(거래 2,132 →
2,400 = **+12.6%**), 본절 청산이 **재진입을 앞당긴다**. 📌 **그때 알아차린 건 규칙이 있어서가
아니라 사람이 눈치채서**였다 — 이 문단이 그 자리를 메운다.

📌 **갈릴 부류를 미리 가르는 자**: **자본·슬롯 점유를 바꾸는 변경**(익절 구조 · 보유 시간 ·
부분 청산 · 재진입 · 거래 수를 크게 바꾸는 필터)은 북에서 다를 수 있다. **「어떤 셋업을
고를까」만 바꾸는 선별 변경**은 덜 그렇다. 확신이 안 서면 **재는 쪽이 싸다** — 판정이 뒤집히면
채택 결정 자체가 틀린다.

⚠️ **북 판정도 좌표가 맞아야 한다 — 「북에서 쟀다」로는 부족하다.** WAN-330이 같은 팔을 채택
좌표(4TF · 15m 포함)에서 다시 재자 **−4.84%p가 −2.67%p로 줄었고**(체결 보수화 `pen_5bp`까지
걸면 **−0.90%p** = 81% 소멸) **팔 순위까지 뒤집혔다**(4TF에서는 `A1_be_off` −3.81%p가
`A1_be_on` −2.67%p를 이긴다 — 3TF 판은 정반대였다). **채택 좌표의 북**이어야 한다(WAN-305
원칙의 북 축 적용). 🚨 **북은 이어붙일 수 없다**(WAN-316) — 지갑은 한 프로세스의 칸 집합이라
TF를 더하려면 **통째로 다시 돌린다**(`--append`는 단일 TF 행만 늘린다).

⚠️ **그렇다고 옛 per-cell 리포트를 전부 다시 돌리라는 뜻이 아니다.** `backtest/wan*.py`
**73개 중 52개가 per-cell · 21개가 북**인데(2026-08-20 실측, 아래 각주), 그 52개의 판정은 거의
전부 **음성**(「엣지 없음」·「채택 권고 없음」·「기본값 유지」)이다. **깨끗하게 격리해서 재도
무작위를 못 이긴 규칙**이 자본 경합을 얹어 살아나지는 않는다. 북 재산출은 **채택 후보로
올라온 팔에 대해서만** 한다.

⚠️ **이 규칙은 측정을 막지 않는다** — 비용 때문에 격자 스윕을 per-cell로 좁혀도 되고(북 한
팔이 **66분**이다 — WAN-330 실측, 48칸 · `--jobs 4` · M1 · 경합 없음), CLI가 전략·비용·거래별
축을 주면 per-cell로 **자동 접히는** 것도 그대로다(북이 그 축을 표현하지 못한다 — WAN-101
문단). **다만 그렇게 나온 표에는 「탐색용 · 판단 근거 아님」을 명시한다.**

📌 **per-cell이 여전히 더 나은 자리 하나 — 귀속**: 북에서 A팔이 B팔을 이기면 그게 **규칙
때문인지 자본이 재배치돼서인지 못 가른다**(WAN-330이 겪은 문제 — 그래서 WAN-323 §2가 채널
둘을 따로 짚어야 했다). 「왜 그런가」를 물을 때는 per-cell로 분해하는 것이 맞다.

📌 **대조 도구·야간 크론도 북이어야 한다** — WAN-340 소관(사용자 결정 2026-08-19). 지금은
`alphablock compare`/`trades`·setup_compare의 백테 쪽이 **per-cell 단일 포지션**이라 페이퍼
(북)와 다른 회계로 돈다 — 「자리가 없어서」 못 들어간 셋업을 대조 백테는 그냥 들어간다
(WAN-333이 그 사실을 화면·CLI에 적어 뒀다).

📎 **각주 — 52/21 카운트 재현**(모듈이 **북 러너를 실제로 부르는가**로 가른다):

```bash
cd backtest && grep -lE '\b(run_leverage_book|build_book_rows|run_book)\(|^from backtest\.(leverage_book|book_cli) import|^from backtest\.wan(169_leverage_book|180_leverage_book_nine) import' wan*.py | wc -l   # 북 = 21
```

⚠️ **느슨한 텍스트 grep(`LeverageBookParams` 문자열만)으로 세면 22~23이 나온다** — per-cell
모듈이 결론부에 「기본값·토대 불변(`ConfluenceParams()`·`LeverageBookParams()`)」을 적어 둔
**주의문**까지 세기 때문이다(wan228·wan328이 그 예). **라벨이 아니라 호출부로 가를 것** — 이
저장소가 반복해 경계한 「라벨과 동작이 어긋남」(WAN-91/95/112/123/159)의 카운트 축 변종이다.

📌 **범위**: 문서 전용 규칙이다 — **코드·엔진·기본값·토대를 하나도 안 건드린다**
(`ConfluenceParams()`·`LeverageBookParams()` 그대로 · 실거래 보류 `ALPHABLOCK_LIVE_TRADING
=false` 유지). 옛 리포트 재산출도, CLI 자동 접힘 동작 변경도 **하지 않는다**(라벨을 실제로
찍게 하는 것은 필요해지면 별도 이슈).

## 「선별 대 가격」은 판정 관문이 아니다 — 싸게 사는 것도 실력이다 (WAN-352 = 사용자 결정 2026-08-21)

📌 **어떤 규칙이 값을 더할 때 그것이 「좋은 셋업을 고른 것(선별)」인지 「그냥 더 좋은 가격에
산 것(가격)」인지 갈라 묻지 않는다.** 사용자 결정(*"앞으로 그 선별 vs 가격은 없애"*). 실매매에서
**더 좋은 가격에 사는 것도 실력**이고, 진입가를 낮춰 1R을 줄이는 것은 규칙이 하는 일 그
자체다. 「가격 효과일 뿐」이라는 문장이 결함처럼 읽혀 **살아남은 규칙을 깎아내리는 데** 쓰여
왔는데, 그 문장의 「가격」에는 이미 선별이 섞여 있었다(아래 🚨 근거 ①).

📌 **앞으로 안 하는 것 셋**: (1) 새 리포트·결정문에 「선별 대 가격」을 재는 **팔을 만들지
않는다**, (2) 그 갈래를 **채택 판정의 선행 조건으로 걸지 않는다**, (3) 「이 표는 선별과 가격을
못 가른다」류 **경고문을 새로 달지 않는다**.

📌 **옛 기록은 보존한다 — 이 결정은 「앞으로」다.** WAN-131(볼린저 축 판정 (b) 가격)·WAN-152
(존폭 축 판정 (a) 선별)의 판정문과 표는 **그때의 기록으로 유효**하고, 이 문서 위쪽 문단에 실린
「선별과 가격을 못 가른다」 경고문(WAN-124/131/145/151/164/201/203 계열)도 **원문 그대로 둔다**.
달라지는 것은 **후속 이슈가 그것을 「남은 관문」으로 인용하지 않는다**는 것뿐이다.

📌 **코드는 하나도 안 지운다** — 옵트인 파라미터 `DeviationFilterParams.select_only` ·
`backtest/wan131_selection_vs_price.py` · 그 테스트는 **전부 존치**한다(지우면 옛 CSV 재현이
깨진다). **WAN-128이 `pen_5bp`를 「삭제가 아니라 옵트인」으로 남긴 것과 같은 처리**이고, 기본
경로에서 안 쓸 뿐이다.

🚨 **근거 ① — 분해는 성립하는데 붙인 이름이 틀렸다.** 사용자 지적(*"아니 선별이 되었으니까
가격을 잘 들어간거지 — 말이 이상하지 않아?"*)이고, 그 근거를 **WAN-131 본문이 스스로 적어
두었다.** 두 팔은 주문이 걸린 셋업 수가 정확히 같아(`eligible_setups` 24셀 차이 0) 「C−B는
순수 가격」이라 불렀는데, **바로 다음 줄**이 *"재산정은 `num_filled`를 바꾼다(밴드가는 존
근단보다 닿기 어렵다 → **C가 24% 덜 체결**)"*이다. 📌 **즉 C는 B가 하는 거래의 76%만 한다** —
주문은 같은 수를 걸었지만 **실제로 체결된 거래가 다르다**. 「값이 2σ만큼 내려온 자리까지
왔다」는 그 자체가 조건이고 **그걸 고르는 것은 선별**인데, 분해는 그것을 「가격」 칸에 넣었다.
같은 문단이 이어서 *"지정가 엔진에서 「가격」은 진입가 개선과 체결률 감소를 묶는다(WAN-96/124의
체결 차원 「선별 대 가격 못 가름」과 같은 자리)"*라고 **자기 한계를 밝힌다** — 가르겠다고 만든
표가 자기 안에서 또 못 가른다고 고백한 셈이다. ⚠️ **분해 자체가 틀렸다는 뜻은 아니다**(스위치
하나만 움직였고 검산 72행 차이 0도 통과했다). 틀린 것은 **라벨**이다: 「그냥 싸게 사는 것」이
아니라 **「값이 그만큼 내려온 거래만 골라서, 그 가격에 사는 것」**이고, 둘은 분리된 게 아니라
**같은 동작의 두 얼굴**이다.

📌 **근거 ② — 대리 질문이 직접 측정으로 대체됐다.** 이 갈래를 쫓던 진짜 이유는 **「가격 효과가
체결 보수화에 가장 취약하다」**는 걱정이었다(WAN-131: 밴드가에 닿는 체결 = 큐 우선순위상 가장
안 될 체결). 즉 「선별인가 가격인가」는 **취약성의 대리 질문**이었는데, **WAN-346이 그 취약성을
대리가 아니라 직접 쟀다** — 팔 D(`pen_5bp` × 같은 분 익절 금지)에서 거래당 실력이 **0.1985R →
0.1089R로 절반이 되지만 부호는 남는다**. 대리 질문을 붙들 이유가 그만큼 줄었다.

⚠️ **이 결정으로 잃는 것 셋**(사용자가 알고 선택 — 나중에 「왜 안 보이지」를 막기 위한 기록):
1. **「왜 버는가」의 해상도가 낮아진다** — WAN-131은 볼린저 기여 +17.65%p를 **가격 84% · 선별
   16%**로 쪼갰는데(⚠️ 6종목·3년 **옛 좌표**), 앞으로 새 규칙이 값을 더해도 그 안을 쪼개 보지
   않는다.
2. **취약성 경고의 조기 신호가 사라진다** — 「가격 쪽 기여가 크다」가 곧 「체결 보수화에 약할
   것」의 힌트였다. 앞으로 그건 **체결 보수화를 직접 재서만** 알 수 있고 **비용이 더 크다**
   (북 한 팔 **66분** — WAN-330 실측).
3. **WAN-124 §7-2가 후속으로 남긴 질문 하나가 닫힌다** — 답이 나와서가 아니라 **안 묻기로
   해서**다.

⚠️ **「엣지 없음」(WAN-84/88/111/114/124/151/201/248)과는 무관하다 — 다른 질문이다.** 그쪽은
*진입 규칙이 무작위와 구분되는가*를 묻고, 이 결정은 *구분된다면 그 안을 더 쪼갤 것인가*를
묻는다. **이 결정은 어떤 옛 판정도 뒤집지 않는다.**

📌 **첫 적용 대상은 WAN-350**(타이밍 매칭 널 × 팔 D)이고 그 리포트가 「이 관문을 안 거는 첫
대상」임을 이미 본문에 밝혀 뒀다 — ⚠️ **쟀는데 문제없다는 뜻이 아니라 안 쟀고 결격으로 세지
않는다는 뜻**이다.

📌 **범위**: 문서 전용 결정이다 — **코드·엔진·기본값·토대를 하나도 안 건드린다**
(`ConfluenceParams()`·`LeverageBookParams()` 그대로 · 새 격자 없음 · 옛 md/csv 무수정 ·
실거래 보류 `ALPHABLOCK_LIVE_TRADING=false` 유지). 결정문은
[`docs/decisions/wan352.md`](docs/decisions/wan352.md)이고, WAN-305(「백테는 페이퍼와 같은
선상」)·WAN-341(「판단은 북에서만」)과 같은 **원칙 절의 이웃**이다.

## 롱온리 성과는 「그냥 사서 들고 있기」와 병기한다 (WAN-393 = 2026-08-31 조사)

📌 **우리 전략은 롱 온리다. 오르는 장에서는 진입 규칙에 실력이 없어도 계좌가 는다.**
그래서 **성과를 인용할 때는 같은 창·같은 유니버스의 buy&hold를 반드시 병기한다.**
총수익 %만으로는 **실력과 베타가 구분되지 않는다.**

🚨 **전례 — 이것 하나가 2026-08-31 조사의 방향을 정했다.** 페이퍼가 **+14%**(당시 표시값)라
「잘 번다」로 읽히고 있었는데, 같은 기간 12종목 buy&hold가 **+22.55%(12/12 상승)**였다.
**시장의 62%만 따라간 것**이고, 그 한 줄이 나오자 「장세 때문」 가설이 즉시 섰다. 그 뒤
WAN-392가 비용 버그를 잡자 그 +14%는 **−9.4%**가 됐다 — 즉 **시장이 +22.55% 오르는 동안
계좌는 잃고 있었다.**

📌 **백테스트에도 그대로 적용된다** — `buy_hold` 열은 **WAN-71 이래 이미 있는데 결론
문장에서 자주 빠진다.** 새 리포트·결정문의 성과 문장은 그 열을 함께 싣는다.

⚠️ **거래당 net R을 대체하는 규칙이 아니다** — 판정 자는 여전히 거래당 net R이고(총수익 %는
복리 착시이자 이 좌표에서 포화한다 — WAN-169/213/378), buy&hold 병기는 **총수익 %를 인용할
때 붙이는 대조군**이다. 두 축은 서로를 대신하지 않는다.

⚠️ **buy&hold가 진다고 「엣지 있음」이 되지도 않는다** — 「엣지 없음」(WAN-84/88/111/114/
124/151/201/248/386)이 묻는 것은 *「같은 존에 **무작위 시각**으로 들어가도 비슷한가」*이고
buy&hold 대조는 **다른 질문**이다. 이 원칙이 막는 것은 **한 방향의 오독**(오르는 장의 플러스를
실력으로 읽는 것)이다.

📌 **범위**: 문서 전용 원칙이다 — **코드·엔진·기본값·토대를 하나도 안 건드린다**
(`ConfluenceParams()`·`LeverageBookParams()` 그대로 · 새 격자 없음 · 옛 md/csv 무수정 ·
실거래 보류 `ALPHABLOCK_LIVE_TRADING=false` 유지). WAN-305(「백테는 페이퍼와 같은 선상」)·
WAN-341(「판단은 북에서만」)·WAN-352(「선별 대 가격은 관문이 아니다」)·WAN-367(「하드 제로는
경보다」)와 같은 **원칙 절의 이웃**이다.

## 기술 스택

Python 3.11+ · uv · ccxt · pandas · asyncio · pydantic-settings
